# crt-048 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `CoherenceWeights` struct contains exactly three fields: `graph_quality`, `embedding_consistency`, `contradiction_density`. No `confidence_freshness` field exists. | grep | `grep -r "confidence_freshness" crates/` returns zero matches | PENDING |
| AC-02 | `DEFAULT_WEIGHTS` values sum to 1.0 within f64 epsilon with exact literals `graph_quality: 0.46`, `contradiction_density: 0.31`, `embedding_consistency: 0.23`. No `confidence_freshness` field present. | test | `lambda_weight_sum_invariant` test passes using `(sum - 1.0_f64).abs() < f64::EPSILON` | PENDING |
| AC-03 | `confidence_freshness_score()` function does not exist anywhere in `infra/coherence.rs`. | grep | `grep -r "confidence_freshness_score" crates/` returns zero matches; `cargo build --workspace` emits no dead-code warning for any freshness function | PENDING |
| AC-04 | `oldest_stale_age()` function does not exist anywhere in `infra/coherence.rs`. | grep | `grep -r "oldest_stale_age" crates/` returns zero matches | PENDING |
| AC-05 | `compute_lambda()` signature contains no `freshness: f64` parameter. Accepts `(graph: f64, embedding: Option<f64>, contradiction: f64, weights: &CoherenceWeights)`. | shell | `cargo build --workspace` succeeds; all call sites compile without a freshness argument | PENDING |
| AC-06 | `StatusReport` contains no `confidence_freshness_score` field and no `stale_confidence_count` field. Text, markdown, and JSON output modes all omit these values. | grep | `grep -r "confidence_freshness" crates/unimatrix-server/src/mcp/` returns zero matches; integration tests updated and passing | PENDING |
| AC-07 | `context_status` with embedding enabled returns Lambda = 1.0 for all-one 3-dimension input. | test | Unit test: `compute_lambda(1.0, Some(1.0), 1.0, &DEFAULT_WEIGHTS) == 1.0` passes | PENDING |
| AC-08 | `context_status` with embedding absent (None) returns Lambda = 1.0 for all-one 2-dimension input (2-of-3 re-normalization sums to 1.0). | test | Unit test: `compute_lambda(1.0, None, 1.0, &DEFAULT_WEIGHTS) == 1.0` passes | PENDING |
| AC-09 | `generate_recommendations()` does not accept or reference `stale_confidence_count` or `oldest_stale_age_secs` parameters. Function signature shrinks accordingly. | shell | `cargo build --workspace` succeeds; function signature inspection confirms parameter removal | PENDING |
| AC-10 | All remaining tests in `infra/coherence.rs` pass. No new test failures outside the explicitly deleted freshness test set. | shell | `cargo test -p unimatrix-server` passes with zero unexpected failures | PENDING |
| AC-11 | `DEFAULT_STALENESS_THRESHOLD_SECS` constant is retained with comment: "Used by run_maintenance() confidence refresh targeting — not a Lambda input." | grep | `grep -n "DEFAULT_STALENESS_THRESHOLD_SECS" crates/unimatrix-server/src/infra/coherence.rs` returns exactly one definition; comment text present; `run_maintenance()` reference compiles without literal substitution | PENDING |
| AC-12 | ADR recording new 3-dimension Lambda weights stored in Unimatrix via `context_correct` superseding entry #179. ADR records: exact weight literals (0.46, 0.31, 0.23), original structural ratio (2:1.33:1 from 0.30:0.20:0.15), rationale (crt-036 cycle-based retention invalidates wall-clock freshness), reference to GH #520. | manual | `context_get` on new ADR entry returns all four data points; `context_get` on entry #179 shows status "deprecated" with superseded-by link | PENDING |
| AC-13 | `coherence_by_source` per-source Lambda uses updated 3-dimension `compute_lambda()` signature. Both call sites updated identically. | grep | `grep -n "compute_lambda(" crates/unimatrix-server/src/services/status.rs` returns exactly 2 matches, both with 4 arguments; code review confirms both call sites; per-source Lambda test passes | PENDING |
| AC-14 | `mcp/response/mod.rs` compiles with all 8 `StatusReport` default-construct fixture sites updated (16 field references removed). | shell | `cargo build --workspace` succeeds — compile errors (not test failures) indicate incomplete removal | PENDING |
