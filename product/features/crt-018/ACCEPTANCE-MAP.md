# crt-018 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Every active entry classified into exactly one of five categories: Effective, Settled, Unmatched, Ineffective, Noisy | test | Unit test in effectiveness.rs: provide entries spanning all five categories, verify each gets exactly one classification. Verify mutually exclusive assignment with priority order Noisy > Ineffective > Unmatched > Settled > Effective. |  PENDING |
| AC-02 | Classification uses injection_log + sessions.outcome join; entries with zero injections classified as Unmatched (if topic active) or Settled (if topic inactive) | test | Unit test: create entries with zero injections for both active and inactive topics, verify Unmatched vs Settled classification. |  PENDING |
| AC-03 | "Settled" requires no sessions for topic in available data window AND at least one historical injection with success outcome | test | Unit test: (a) inactive topic + success injection -> Settled; (b) inactive topic + no success injection -> NOT Settled; (c) inactive topic + only rework/abandoned injections -> NOT Settled. |  PENDING |
| AC-04 | "Ineffective" requires >= INEFFECTIVE_MIN_INJECTIONS (default: 3) distinct sessions AND success rate < 30% | test | Unit test: (a) 2 sessions + 0% success -> NOT Ineffective (below threshold); (b) 3 sessions + 0% success -> Ineffective; (c) 3 sessions + 35% success -> NOT Ineffective; (d) 3 sessions + exactly 30% success -> NOT Ineffective (< 30% required). |  PENDING |
| AC-05 | "Noisy" requires trust_source="auto" AND zero helpful_count AND at least one injection | test | Unit test: (a) auto + 0 helpful + injections -> Noisy; (b) auto + 1 helpful -> NOT Noisy; (c) agent + 0 helpful + injections -> NOT Noisy. |  PENDING |
| AC-06 | Aggregate effectiveness metrics per trust_source (auto, agent, system, human, neural) with counts per category and aggregate utility ratio | test | Unit test in aggregate_by_source: entries from multiple trust sources, verify per-source counts match expected. Verify aggregate_utility is 0.0 when no injections (R-13). |  PENDING |
| AC-07 | Confidence calibration produces 10 buckets (0.0-0.1 through 0.9-1.0) comparing confidence-at-injection-time to actual session success rate | test | Unit test in build_calibration_buckets: known confidence values and outcomes, verify bucket assignment. Boundary tests: 0.0, 0.1, 0.9, 1.0 land in correct bucket. Empty data returns 10 empty buckets. |  PENDING |
| AC-08 | context_status summary format includes one-line effectiveness summary with category counts and session count | test | Integration test: insert test data via TestDb, call compute_report, format as Summary, grep for "Effectiveness:" line with correct counts and "(N sessions over M days)". |  PENDING |
| AC-09 | context_status markdown format includes "### Effectiveness Analysis" section with category table, per-source table, calibration table, and top-10 ineffective entries | test | Integration test: insert test data, call compute_report, format as Markdown, verify section header and table contents present. |  PENDING |
| AC-10 | context_status JSON format includes `effectiveness` object with structured data matching EffectivenessReport schema | test | Integration test: insert test data, call compute_report, format as Json, deserialize effectiveness object, verify fields match expected structure. Verify skip_serializing_if omits field when None. |  PENDING |
| AC-11 | Effectiveness computation runs in spawn_blocking, does not block async runtime | grep | `grep -n "spawn_blocking" crates/unimatrix-server/src/services/status.rs` -- verify Phase 8 uses spawn_blocking for effectiveness SQL. |  PENDING |
| AC-12 | Top 10 ineffective entries and all noisy entries listed with entry_id and title | test | Unit test in build_report: create >10 ineffective entries, verify only top 10 returned sorted by injection_count descending. Verify all noisy entries listed (no cap). |  PENDING |
| AC-13 | Effectiveness computed on every context_status call; no opt-in flag; read-only with no writes; classifications transient | test | Integration test: call context_status twice with same data, verify identical results. Verify no table row count changes before/after via SQL COUNT checks. |  PENDING |
| AC-14 | Pure computation functions in effectiveness.rs have unit tests covering all five categories, boundary conditions, and empty-data handling | test | Unit tests: boundary at INEFFECTIVE_MIN_INJECTIONS (3), boundary at 30% success rate, empty input returns valid empty report, utility_score(0,0,0) returns 0.0. |  PENDING |
| AC-15 | Integration tests verify end-to-end: insert entries + injection_log + sessions, call status, verify effectiveness section | test | Integration test using TestDb: insert controlled data covering all five categories, invoke compute_report, verify effectiveness section in all three output formats. |  PENDING |
| AC-16 | Entries with NULL topic classified into "(unattributed)" bucket; sessions with NULL feature_cycle excluded from topic activity but outcomes still count | test | Unit test: entry with NULL/empty topic gets classified (not dropped), topic field is "(unattributed)". Store integration test: session with NULL feature_cycle excluded from active_topics but its injection outcomes count in entry stats. |  PENDING |
| AC-17 | Rework outcome weights (1.0/0.5/0.0) defined as named constants, not magic numbers | grep | `grep -n "OUTCOME_WEIGHT_" crates/unimatrix-engine/src/effectiveness.rs` -- verify OUTCOME_WEIGHT_SUCCESS, OUTCOME_WEIGHT_REWORK, OUTCOME_WEIGHT_ABANDONED constants exist and are used in utility_score computation. |  PENDING |
