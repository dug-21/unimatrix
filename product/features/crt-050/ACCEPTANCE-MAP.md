# crt-050 Acceptance Criteria Map

GH Issue: #542

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `PhaseFreqTable::rebuild()` sources data from `observations`-joined aggregates, not `query_log` | test | Two subtests: (a) populate `observations` only → `use_fallback = false` with non-empty table; (b) populate `query_log` only, empty `observations` → `use_fallback = true` | PENDING |
| AC-02 | Base aggregate query filters to all four tool name variants; excludes others (e.g., `context_search`) | test | Insert observations with each of the 4 tool names; assert all produce rows. Insert `context_search` observation; assert zero rows | PENDING |
| AC-03 | `CAST(json_extract(input, '$.id') AS INTEGER)` used in JOIN predicate; string-form IDs handled | test | Insert observation with `{"id": "42"}` (string-form ID); assert row is returned and matched to entry 42 | PENDING |
| AC-04 | Outcome weighting applied via two-query Rust post-process; `infer_gate_result()`-equivalent logic used | test | Populate Query A rows + Query B rows with known outcomes; assert weighted `freq` values applied correctly before rank ordering; confirm no SQL CASE expression used | PENDING |
| AC-05 | Graceful degradation when no `cycle_phase_end` history available — rebuild completes, all rows weighted 1.0, no error, no `use_fallback` escalation | test | Populate `observations` with no `cycle_phase_end` rows in `cycle_events`; assert `use_fallback = false`, non-empty result, all freqs unmodified | PENDING |
| AC-06 | All existing `PhaseFreqTable` contracts preserved: rank-based normalization (col-031 ADR-001), `phase_affinity_score()` returns 1.0 for cold-start / absent phase / absent entry, retain-on-error, poison recovery | test | Existing `PhaseFreqTable` contract tests pass without modification; add/confirm: cold-start returns 1.0, DB error leaves previous table unchanged | PENDING |
| AC-07 | `ts_millis` lookback boundary correct (ms-epoch, not s-epoch) | test | Insert observation at `now - lookback + 500ms` (inside window); insert observation at `now - lookback - 500ms` (outside window); assert only inside-window observation contributes | PENDING |
| AC-08 | `phase_category_weights()` returns correct probability distribution; empty map on cold-start | test | Unit tests: (a) `use_fallback = true` → empty map; (b) populated table → non-empty map where values per phase sum to 1.0 (f32); edge case: single-category phase → weight = 1.0 | PENDING |
| AC-09 | Old `query_phase_freq_table` deleted; no remaining call sites | grep | `grep -r 'query_phase_freq_table' crates/ --include='*.rs'` returns zero results after implementation | PENDING |
| AC-10 | `query_log_lookback_days` renamed to `phase_freq_lookback_days` with `#[serde(alias = "query_log_lookback_days")]`; serde alias works; struct literals updated | test | Deserialize `{"query_log_lookback_days": 30}` → assert `phase_freq_lookback_days == 30`; deserialize `{"phase_freq_lookback_days": 30}` → same; compilation succeeds with no struct literal using old name | PENDING |
| AC-11 | crt-036 tick-time diagnostic updated to reference `phase_freq_lookback_days`; new observations-coverage diagnostic added that emits `tracing::warn!` below threshold | test | Unit test: insert observations below threshold → warn! emitted; above threshold → no warn!; code review confirms old field name removed from diagnostic text | PENDING |
| AC-12 | MRR eval harness regression gate passes: MRR >= 0.2788 (post-PPR-expander baseline) | shell | Run eval harness: `product/research/ass-039/harness/scenarios.jsonl` (1,761 scenarios); MRR must be >= 0.2788; blocks merge on failure | PENDING |
| AC-13a | Empty observations → `use_fallback = true` | test | `#[test] fn empty_observations_cold_start()`: populate DB with no matching observations; call `rebuild()`; assert `table.use_fallback == true` | PENDING |
| AC-13b | Pass-outcome rows weighted 1.0 | test | `#[test] fn pass_outcome_weight_1_0()`: Query B row with outcome "PASS"; assert `apply_outcome_weights` produces `freq * 1.0` | PENDING |
| AC-13c | Rework-outcome rows weighted 0.5 | test | `#[test] fn rework_outcome_weight_0_5()`: Query B row with outcome "REWORK"; assert `apply_outcome_weights` produces `freq * 0.5` | PENDING |
| AC-13d | Fail-outcome rows weighted 0.5 | test | `#[test] fn fail_outcome_weight_0_5()`: Query B row with outcome "FAIL"; assert `apply_outcome_weights` produces `freq * 0.5` | PENDING |
| AC-13e | Missing outcome degrades to unweighted (weight 1.0), no error | test | `#[test] fn missing_outcome_defaults_to_1_0()`: no Query B rows for phase; assert all rows retain original freq (weight 1.0) | PENDING |
| AC-13f | Prefixed tool names `mcp__unimatrix__context_get` and `mcp__unimatrix__context_lookup` included | test | `#[test] fn prefixed_tool_names_included()`: observations with prefixed tool names produce rows in rebuild result | PENDING |
| AC-13g | Filter-based `context_lookup` (no `$.id` in input) excluded | test | `#[test] fn filter_lookup_excluded()`: observation with `context_lookup` tool and input `{"filter": "foo"}` (no `$.id`) produces zero rows | PENDING |
| AC-13h | `phase_category_weights()` returns empty map on cold-start; non-empty map with correct probability distribution on populated table | test | `#[test] fn phase_category_weights_cold_start()` + `#[test] fn phase_category_weights_populated()`: assert empty map when `use_fallback = true`; assert per-phase sums ≈ 1.0 when populated | PENDING |
| AC-14 | Minimum coverage threshold gate: below threshold → `use_fallback = true` + warning; at/above threshold → normal operation | test | Set `min_phase_session_pairs = N`; insert N-1 distinct `(phase, session_id)` pairs → assert `use_fallback = true` and `tracing::warn!`; insert N pairs → assert `use_fallback = false` (given non-empty rows) | PENDING |
| AC-15 | NULL `feature_cycle` sessions degrade to weight 1.0 without error | test | Integration test: sessions with `feature_cycle = NULL`; insert observations for those sessions; call `rebuild()`; assert non-empty table, all rows weighted 1.0, `use_fallback = false` if pair count meets threshold | PENDING |
| AC-SV-01 | Storage contract confirmed: `json_extract(input, '$.id')` returns non-NULL for hook-path observations | test | Unit test inserting a `PreToolUse` observation via the actual write path; assert `json_extract(input, '$.id')` returns a non-NULL value matching the expected entry ID (R-01 write-path contract test) | PENDING |
