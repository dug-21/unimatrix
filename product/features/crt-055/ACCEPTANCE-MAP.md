# crt-055 Acceptance Criteria Map

> Source: SPECIFICATION.md Acceptance Criteria (AC-01..AC-22), traced to SCOPE.md in-scope items 1–6 and folded issues (#556, #320, #593, #206-item-4). Every AC ships within crt-055; folded issues close as ACs, not separately. Status PENDING until verified in Session 2.
>
> **This revision (2026-06-16)** reflects three binding human decisions: AC-20 (basis-points-INTEGER `context_reload_pct`), AC-21 (coarse/directional behavioral-signal presentation), AC-22 (clock/unit seconds-normalization integration test). AC-14 no longer asserts `is_finite()` — the float footgun is designed out by integer storage.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Empty metric source class renders "unavailable", never "0" (per-metric, fail-loud) | test | Synthesize cycle per empty source class (zero cycle_events / zero compaction_events / empty fold / zero served-knowledge); assert each metric renders "unavailable" and never literal "0"; per-metric flags independent | PENDING |
| AC-02 | All new v5 columns exist on `cycle_review_index`, each NOT NULL DEFAULT 0 (TEXT '{}'), pragma-guarded; fresh-create and upgrade paths agree, idempotent | test | Migration test: `pragma_table_info` lists every new column with correct type/default on a fresh DB and an upgraded DB; re-run is a no-op | PENDING |
| AC-03 | `SUMMARY_SCHEMA_VERSION == 5`; pinned version assertion updated in same change; crt-054 does not bump it | test | Assert constant == 5; migration-version test reflects it; boundary check crt-054 leaves it untouched | PENDING |
| AC-04 (folded #556) | Phase with declared start and no close surfaces as a declared-but-never-closed hotspot feeding rank-1 | test | Seed `cycle_events` with an unclosed phase; assert it appears as a hotspot / `phase_unclosed_count` increments; matching-close phase does NOT | PENDING |
| AC-05 | Rank-1 phase durations/transitions/rework + rank-2 rework ratio persist and recompute from `cycle_events` / `SessionRecord.outcome`; closed-then-reopened phase counts as rework not new phase | test | Integration test with seeded phase events + session outcomes; assert persisted column values match expected aggregates | PENDING |
| AC-06 (folded #320) | Knowledge-reuse counts union of `query_log ∪ injection_log` (all served), not same-cycle-tagged only; entry served via both counts once | test | Seed served entries split across both logs incl. cross-cycle-tagged + a both-logs entry; assert count == size of union (dedup) | PENDING |
| AC-07 | `activity_snapshot()` landed into `transcript_bytes_total`, `_delta_count`, `_error_count` (class 0), `_refusal_count` (class 1), `signal_class_counts_json` | test | Integration test with a known fold; assert each column equals summed snapshot field; JSON map matches class catalog | PENDING |
| AC-08 | `activity_snapshot()` read BEFORE the crt-052 hold purge; reversing the order zeroes the columns | test | Ordering test: assert read site precedes `purge_cycle_transcripts`; inversion test (reversed order) fails — columns zeroed | PENDING |
| AC-09 (silent-zero regression guard) | Aggregation reads non-empty source for a representative TS-client cycle; fold columns non-zero (held-route #750 class cannot recur) | test | Regression test: representative TS-client cycle with held activity; assert fold source non-empty and columns non-zero; valid sessions not zeroed by an undeclared one | PENDING |
| AC-10 | No token estimate / token-named field on `CycleReviewRecord`/`RetrospectiveReport`; no `reread`/`compaction` regex class introduced by crt-055 | grep | Structural/AST guard asserting absence of any token-named field/column and any `reread`/`compaction` class | PENDING |
| AC-11 | `compaction_count` == count of `compaction_events` rows attributed via session→`feature_cycle` chain; undeclared/evicted sessions not mis-attributed | test | Seed `compaction_events` for declared and undeclared sessions; assert only declared-session rows count; evicted session (#4140 condition) → honest partial, never fabricated zero | PENDING |
| AC-12 | `compaction_reread_count` == PostToolUse file-overlap reads with `read_ts_secs > compacted_at` (seconds-aligned); earliest-boundary selection for multi-compaction sessions, counted once | test | Seed compaction_events + reads straddling `compacted_at` (before/after/exactly-at); assert only strictly-after overlapping reads count; multi-compaction gates on MIN(`compacted_at`); each read once | PENDING |
| AC-22 (clock/unit consistency — integration) | All timestamps entering the `compaction_reread` gate normalized to Unix seconds before comparison; a read shortly after a compaction counts, a unit mismatch (millis vs seconds) is caught — binding contract clause | test | **INTEGRATION test**: seed `compacted_at` (seconds) + a PostToolUse overlapping read whose `ts` (millis) is +500ms/+1s later → assert it counts; a read −500ms before → assert it does NOT; inject an unnormalized millis `ts` and assert seconds-normalization (`ts_millis ÷ 1000`) prevents the ~1000× mis-compare (never all-or-nothing). Cross-table (compaction_events × PostToolUse reads), not a comparator unit test | PENDING |
| AC-13 (dual reload not collapsed) | `context_reload_pct` (cross-session) and `compaction_reread_count` (within-cycle) are two distinct columns with distinct gates; neither derived from the other's window | test | Assert both persist independently from distinct overlap windows; cross-session-reload-only cycle → reload non-zero, reread "unavailable"; single-session compaction cycle → reverse | PENDING |
| AC-20 (basis-points encoding) | `context_reload_pct` is a NOT NULL DEFAULT 0 INTEGER column storing basis points (0–10000); persisted value == `compute_context_reload_pct` percentage × 100 rounded to nearest integer; no REAL/float column on `cycle_review_index` | test | 37.5% overlap persists 3750; rounding cases (0.005%→1, 99.995%→10000) round to nearest; out-of-range (>10000/negative) clamped/rejected before bind; `pragma_table_info` shows column type INTEGER not REAL; structural guard: no f64/REAL metric column added | PENDING |
| AC-21 (coarse-signal presentation honesty) | Behavioral signals (`transcript_error_count`, `transcript_refusal_count`, `signal_class_counts_json`) render with a coarse/directional qualifier, visually distinct from exactly-counted aggregates; no behavioral-signal value renders as an exact auditable count | test | Rendering test: assert the behavioral-signal fields carry the directional/coarse qualifier (e.g. "~"/"directional"/"approx") in the rendered report; assert an exactly-counted aggregate (compaction_count, rework ratio) does NOT carry it — the two presentations are distinguishable | PENDING |
| AC-14 | Producer widths convert to i64 without truncation/overflow (checked/saturating); basis-points range guard (0–10000) on `context_reload_pct` at the bind boundary | test | Near-`u64::MAX` / large `u32` fold values → persisted i64 correct or saturated, never wrapped; basis-points round-trip (ratio → i64 0–10000 → re-read equals stored); out-of-range clamped/rejected. **No `is_finite()` requirement** — the float footgun is designed out by integer storage (no float reaches the bind) | PENDING |
| AC-15 (folded #593) | `context_cycle_review` accepts `auto_close: bool` default `false`; when `true` and no `cycle_stop`, writes stop synchronously before pipeline; else no stop | test | Handler test: (a) auto_close=true, no prior stop → stop written at top before rank-1; (b) auto_close=true, stop exists → idempotent no-op; (c) auto_close=false → no stop, open final phase surfaces never-closed; stop via event writer not a second cycle_review writer | PENDING |
| AC-16 (folded #206-4) | Retrospective surfaces the entries that helped this cycle (response-time enrichment, no durable column) | test | Assert retrospective output includes the cycle's contributing entries; assert no new column added for it (ADR-009) | PENDING |
| AC-17 (single writer / no-clobber) | New columns written only by the one `store_cycle_review()`; memo-hit, purged-retain, force+purged returns do NOT write them | test | The three #5022 assertions: (a) data-present recompute writes columns; (b) purged-retain returns byte-identical, no write; (c) force+purged does not clobber with zeros; static assert exactly one writer site | PENDING |
| AC-18 (guarded recompute coexistence) | Pre-v5 stale row with source present auto-refreshes via clear-memo-and-fall-through (no second writer); purged stale row retains stored columns | test | Stale+source-present → recompute populates new columns at v5; stale+purged → retain (no recompute, no "use force=true" advisory) | PENDING |
| AC-19 (structural leak gate) | `RetrospectiveReport`/`CycleReviewRecord` carry no content field; only integers/aggregates persist | test | `test_candidates_structurally_absent_from_memoized_report` holds; structural test asserts no content field; consumed surfaces metadata-only (no Display/content serialization on persist path) | PENDING |
