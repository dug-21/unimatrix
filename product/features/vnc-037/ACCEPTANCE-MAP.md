# vnc-037 Acceptance Criteria Map

Every AC from SCOPE.md (AC-01..AC-14). Verification detail traced from the SPECIFICATION.md AC
matrix and the RISK-TEST-STRATEGY.md discriminating scenarios.

> **REGENERATED for the next-hop reframe (2026-06-15).** Cap is **3** (was 10); ranking (D-09)
> is the acceptance surface; symmetric canonicalization (D-10) is tested **independently on
> display AND totals**; AC-12 latency is new.
>
> **Updated 2026-06-16** — added **AC-13** (display cap is a single named constant
> `GET_EDGE_DISPLAY_LIMIT`; FR-18 / C-12 / ADR-006 #5054) and the **display-cap-is-a-constant**
> case in the AC-10 inventory. Additive maintainability refinement; cap value stays **3**.
>
> **Updated 2026-06-16 — OQ-A RESOLVED = FAIL LOUD (human-directed).** Added **AC-14**
> (fail-loud edge error contract; FR-19 / C-13) and the **edge-query-failure-fails-loud**
> RED case to the AC-10 inventory. On a post-primary-read edge/count/title failure the whole
> `context_get` fails via the primary-read error mapping — never degrade-with-note, never
> silent edge omission; a successful zero-edge get (AC-06) is structurally distinct from a
> failed get and the two are never conflated.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `context_get` surfaces depth-1 edges, both directions, **by default**, reading live `graph_edges` (D-01). | test | Integration: `context_get` with no `include_edges` on an entry with known edges → edges present; write a new edge then get again → it appears immediately (no tick wait). | PENDING |
| AC-02 | Each surfaced edge carries `{edge_type, direction, target_id, target_title, authored}`; titles resolve in one batched join; unresolved target → `target_title: null`, edge retained (D-02). | test | Assert exact 5-field set per edge (no extra field); dangling-target test asserts `null` title + edge retained; query-count assertion proves a single batched `IN (…)` join over the ≤3 displayed targets (no N+1). | PENDING |
| AC-03 | `authored` true iff `source == 'agent'`; all current inferred sources → false; `source` added to read query with **no schema migration** (D-03). | test + file-check | Seed `source='agent'` and `co_access`/`cosine`/`behavioral`/`S8` edges → assert `authored` true/false; near-miss (`'Agent'`,`' agent'`) stays false (exact match); assert no migration file added (file-check). | PENDING |
| AC-04 | `Supersedes` never appears in surfaced edges; `supersedes`/`superseded_by` remain sole supersession representation (D-04). | test | Seed a `Supersedes` edge alongside typed edges → assert absent from surfaced edges **and** totals (filter inherited via empty `edge_types`, re-verified against the rewritten ranked/count SQL); supersession reflected only via existing fields. | PENDING |
| AC-05 | Output renders **≤3** edges by the D-09 rule (authored first; inferred fill only when authored < 3; inferred ranked by target-entry `entries.confidence`); always reports exact **uncapped** counts split inbound/outbound with symmetric counted **once** (post-canonicalization); >3-edge entry shows the "…N more — use context_graph" affordance (D-05, D-09, D-10). | test | (a) **authored-priority-under-cap**: >3 edges, authored ≥3 → only authored show, no inferred. (b) **inferred-fill-only-when-authored<3**: <3 authored + inferred → inferred tops up to exactly 3 by target confidence. (c) **ranking-by-target-confidence (discriminating, #3886)**: ≥4 inferred whose confidences straddle the cap; higher-confidence target in top-3, lower excluded; seed the high-confidence target with a **lower** `graph_edges.weight` so weight- and confidence-ordering disagree — assert weight does NOT decide. (d) totals exact, uncapped, split, symmetric-once. (e) >3 edges → affordance present. Each scenario carries an explicit per-edge rank trace (#3645). | PENDING |
| AC-06 | A zero-edge entry renders an explicit empty state in all three formats (D-06). | test | Get an edge-free entry → assert summary `edges: none`, markdown `### Related` + `No related entries.`, json `"edges": []` + `edge_totals: {inbound:0,outbound:0}`. | PENDING |
| AC-07 | `context_get` JSON gains `edges` + direction-split totals; `context_search`/`lookup`/`store`/`correct` payloads **byte-identical** to pre-vnc-037 (no `edges` key) (D-07, SR-01). | test | **Byte-identity golden** across the 4 list-view tools × 3 formats produced via the **real producer path** (#1268) — assert no `edges` key, no `### Related`, no `edges:` digest, byte-equality vs pre-vnc-037 baseline; assert `entry_to_json` signature unchanged; `context_get` payload asserts `edges` + `edge_totals` present. | PENDING |
| AC-08 | summary, markdown, json each render per D-08 — cap-3 ranked set, `↔` glyph for canonicalized symmetric edges, honest symmetric-once split totals, and the `…and N more — use context_graph` pointer when capped (D-08, D-10). | test | Format snapshots: summary digest with `↔` split (OQ-02 form); markdown `### Related` **flat ranked ≤3** list with line `- {edge_type} {→|←|↔} #{id} "{title}"`, `↔` on symmetric lines, single capped pointer — **assert the Author-asserted/Inferred sub-split is ABSENT** (dropped); json shape + nested `edge_totals` symmetric-once. | PENDING |
| AC-09 | The get edge shape is a documented projection of `EdgeRecord` (same `relation_type`/`target_id`/`direction`); `context_graph` neighbors contract **unchanged** (additive only) (FR-15/FR-16, SR-02/SR-06). | test + file-check | Run existing `context_graph` neighbors suite **unedited** after `RawEdgeRow`/SELECT extension → green (empirical, #4876); assert `↔`/canonicalization absent from neighbors output (no leak); `EdgeRecord` wire bytes unchanged; projection documented in ADR-002 (file-check). | PENDING |
| AC-10 | `cargo build --workspace` and `cargo test --workspace` pass; new tests cover the named cases below (FR-1..FR-18). | shell + test | `cargo build --workspace` and `cargo test --workspace` green; the named cases in the AC-10 inventory below all present and asserting (discriminating, not smoke). | PENDING |
| AC-11 | `GetParams` gains `include_edges: Option<bool>`; `None`/`Some(true)` surface, `Some(false)` suppress (no `edges` key, ranked select + count + title join **skipped**); additive + backward-compatible (D-01). | test | Test all three resolutions: edges present (None, Some(true)); absent + no `edges` key (Some(false)); assert the ranked/count/title queries are **skipped** on opt-out via query-count/instrumentation. | PENDING |
| AC-12 | The default-on edge path adds two bounded SQL queries (ranked `LIMIT 3` confidence-join select + split `COUNT(*)`) and stays within a **stated per-call latency budget** added over the edge-free baseline — proposed **≤5 ms p50 / ≤15 ms p95** on a representative store **including a high-degree node**; numbers **confirmed against a measured baseline** before locking (NFR-1/NFR-2, FR-11, SR-12, C-9). | test + manual | Benchmark: measure edge-free baseline (opt-out path) on a representative store **with a high-degree node**, record it; measure default-on on the same store/node; assert added delta within the **confirmed** budget; assert read-pool + indexed JOIN (NFR-3). **manual**: if the baseline shows the budget unattainable, escalate to the human (relax / mandate OQ-03 opt-out / revisit default-on). | PENDING |
| AC-13 | The display cap is a **single named constant** `GET_EDGE_DISPLAY_LIMIT` (value **3**, unchanged) referenced by the SQL `LIMIT` and the `…and N more` rendering — no magic literal `3` at any cap-application site; changing the constant changes **only** the number of edges rendered — totals stay exact/uncapped (FR-10) and `↔` canonicalization is unaffected (FR-8) (FR-18 / C-12 / D-05 / ADR-006 #5054). | grep + test | (a) **cap single-source**: grep/test asserts **no literal `3`** at the SQL `LIMIT` site or the `…N more` threshold/arithmetic; the ranked query binds `LIMIT ?` to `GET_EDGE_DISPLAY_LIMIT` and the render derives `N = total − GET_EDGE_DISPLAY_LIMIT`; tests reference the constant, not `3`. (b) **cap-isolation**: parametrize/override the constant (e.g. cap=2) → assert the rendered set shrinks to that count **while** the inbound/outbound totals and the `↔`-once canonicalization are **byte-unchanged**; restore. Confirms a one-line value edit is the only change needed to retune. | PENDING |
| AC-14 | On the default-on path, an edge/ranked-query, split-`COUNT(*)`, or title-join failure that occurs **after** the primary entry read succeeds **FAILS the whole `context_get`** via the same error mapping as the primary-read failure path — never degrade-with-note, never silent edge omission. A **successful get with zero edges** (AC-06 / FR-12 empty state) is **DISTINCT** from a **failed get** and the two are **never conflated**. The opt-out path (FR-3, `Some(false)`) skips the edge queries entirely and **cannot reach this failure** (FR-19 / OQ-A / C-13 / R-16 / SR-13). | test + grep | (a) **edge-query-failure-fails-loud (named, RED)**: inject a failure into the edge/ranked query *after* a successful primary read → assert `context_get` **returns an error** (mapped `ServerError`, run **RED** per #4876), not a success payload with omitted edges; repeat the injection separately for the split `COUNT(*)` and the title join → same mapped failure. (b) **zero-edges-is-not-failure**: assert a genuine zero-edge entry returns a **success** with the explicit empty state (`edges: []` / "No related entries" / `edges: none`) and that this success is **structurally distinguishable** from the (a) error result — no success payload is ever produced by a failed edge path. (c) **static (grep)**: no `.unwrap()`/`expect()` on the edge path in non-test code. | PENDING |

## AC-10 test inventory (named, discriminating — not smoke; SR-13)

| Case | Maps | Verification | Status |
|------|------|--------------|--------|
| **symmetric-canonicalization — display** | R-01, D-10, SR-08 | A `Contradicts` pair stored as both reciprocal rows → **one `↔` edge** in the displayed set, not two; extend to `CoAccess`/`Informs`. | PENDING |
| **symmetric-canonicalization — totals (separate assertion)** | R-01/R-03, D-10, SR-08 | The same pair contributes **once** to the inbound/outbound split totals; asserted on the COUNT output directly, independent of the displayed set. | PENDING |
| **canonicalization-before-cap-and-count (order-of-ops)** | R-01, D-10 | Seed >3 symmetric pairs + an authored edge → the authored edge still wins a slot (proves symmetric rows collapsed *before* the cap consumed slots and before the count). | PENDING |
| **asymmetric-untouched** | R-01/R-10, D-10 | A `Prerequisite`/`Supports` single-row edge keeps its `→`/`←` and is NOT collapsed. | PENDING |
| **authored-priority-under-cap** | R-02, D-09 | >3 edges, authored ≥3 → only authored show, no inferred (the `(source='agent') DESC` term). | PENDING |
| **inferred-fill-only-when-authored<3** | R-02, D-09 | <3 authored + several inferred → inferred top up to exactly 3, ordered by target confidence. | PENDING |
| **ranking-by-target-confidence (proof OUTSIDE cap)** | R-02/R-06, D-09, #3886 | ≥4 inferred whose target confidences straddle the cap; higher-confidence target in top-3, lower excluded; high-confidence target seeded with a **lower** `graph_edges.weight` so weight- and confidence-ordering disagree → assert weight does NOT decide. Per-edge rank trace (#3645). | PENDING |
| **deterministic tie-break** | R-02/R-06, D-09 | Equal-confidence inferred targets resolve by `target_id ASC`, stable across runs; cold-start uniform 0.0 confidence resolves by the tiebreak, not arbitrary row order. | PENDING |
| **carried-forward classifies authored (named, FR-17)** | R-05, SR-10 | Carry an authored edge forward via the vnc-035 path; `context_get` the corrected entry → edge has `source='agent'`, `authored=true`, **wins a slot ahead of inferred**. | PENDING |
| **context_edge classifies authored** | R-05, SR-10 | Edge written via `context_edge` → `source='agent'`, authored, slot priority. | PENDING |
| **opt-out skips queries** | R-14, AC-11 | `include_edges:Some(false)` issues **zero** ranked/count/title queries (query-count/instrumentation) and emits **no `edges` key** — byte-indistinguishable from a list-view payload. | PENDING |
| **internal-caller opt-out (OQ-03)** | R-14, OQ-03 | Enumerated internal call sites (hook path, briefing by-ID fetches, by-ID loop fetches) pass `Some(false)`; assert **each** as a test (per ARCH OQ-03). | PENDING |
| **high-degree node hits SQL LIMIT, not memory** | R-04, SR-14, FR-11 | Node with many edges (≥50) → ranked query returns **exactly `GET_EDGE_DISPLAY_LIMIT` rows**, count returns **two scalars**; full neighbor set **never** materialized — proven at the **store boundary** (returned-row-count / allocation), not just rendered output. | PENDING |
| **display-cap-is-a-constant** | AC-13, FR-18, C-12, D-05 | (a) **single-source**: no literal `3` at the SQL `LIMIT` / `…N more` sites (grep/test); the SQL binds `LIMIT ?` to `GET_EDGE_DISPLAY_LIMIT`, the render uses `total > GET_EDGE_DISPLAY_LIMIT` and `N = total − GET_EDGE_DISPLAY_LIMIT`; tests reference the constant. (b) **cap-isolation**: override the constant (e.g. cap=2) → rendered set shrinks to that count while inbound/outbound totals and `↔`-once canonicalization are **byte-unchanged**; restore. Confirms one-line modifiability. | PENDING |
| **edge-query-failure-fails-loud (named, RED)** | AC-14, FR-19, OQ-A, R-16, SR-13 | Inject a failure into the edge/ranked query (and, **separately**, the split `COUNT(*)` and the title join) **after** a successful primary read → assert `context_get` **FAILS** (returns the mapped `ServerError`, run **RED** per #4876), never a success payload with omitted edges. Cross-assert that a genuine **zero-edge** success (AC-06) is **structurally distinct** from this failure result — the two are never conflated (zero-vs-failure distinction). No `.unwrap()`/`expect()` on the edge path. | PENDING |
| **dangling-title retained + LEFT JOIN** | R-06/R-15, DNB-1 | Inferred edge whose `target_id` has no `entries` row → `target_title: null`, edge **retained** across all three formats, ranks deterministically last (`NULLS LAST`), no panic. Asserts the JOIN is LEFT not INNER. | PENDING |
| **zero-edge** | R-17, AC-06, DNB-3 | Explicit empty state in all three formats. | PENDING |
| **byte-identity (4 list-view tools)** | R-07, AC-07, SR-01 | Golden/snapshot of the 4 list-view payloads via the real producer: no `edges` key, unchanged bytes vs pre-vnc-037. | PENDING |

## Additional verifiable assertions (promoted from scope risks)

| Ref | Assertion | Verification Method | Verification Detail | Status |
|-----|-----------|--------------------|--------------------|--------|
| SR-08 / C-6 | Symmetric "counted once" is an invariant on **display AND totals**, with canonicalization proven to precede both the cap and the count. | test | Display-dedup and count-dedup asserted **separately**; a render-dedup fix that misses count-dedup must fail the totals test. | PENDING |
| SR-09 / C-8 | Locked `ORDER BY (source='agent') DESC, t.confidence DESC NULLS LAST, target_id ASC LIMIT 3`. | test | Ranking discriminating test with proof value **outside the cap** (#3886); weight does NOT decide; per-edge rank trace present (#3645). | PENDING |
| SR-14 / C-7 | Rank-and-limit and split count execute **in SQL**, not Rust slicing. | test | Store-boundary assertion: high-degree fixture allocates ≤`GET_EDGE_DISPLAY_LIMIT` `RawEdgeRow`; executed statement carries `LIMIT ?` (bound to the constant) + `COUNT(*)`, not `SELECT *` then Rust truncation. | PENDING |
| FR-18 / C-12 / AC-13 | Display cap is **one named constant** `GET_EDGE_DISPLAY_LIMIT` (=3); no literal `3` at any cap site; decoupled from uncapped totals + canonicalization. | grep + test | grep the SQL `LIMIT` and `…N more` sites for a literal `3` → none (bind/threshold reference the constant); cap-isolation test (override → only rendered set shrinks; totals + `↔`-once unchanged) per the AC-10 display-cap-is-a-constant case. | PENDING |
| SR-01 | List-view byte-identity is real-path, not hand-crafted. | test | Byte-identity baseline produced by invoking the genuine tool handler/serializer path (#1268). | PENDING |
| SR-02 | Additive `source` does not break `context_graph` neighbors. | test | Existing neighbors suite green **unedited**; `map_edge_row` `source` populates across all 4 SELECT branches (#4166 — audit ALL passes). | PENDING |
| Security | All edge queries (ranked select, split `COUNT(*)`, confidence JOIN, title `IN (…)`) use **positional binds**; `LIMIT`/`ORDER BY`/canonicalization `CASE` are static SQL. | grep | Assert parameterized binds at each query site; no string-interpolated ids or string-built IN-list. | PENDING |
| C-11 | No `.unwrap()`/`expect()` on the edge path in non-test code. | grep | grep the edge path (`graph_queries_ranked`, `get_edges`, edge assembly, `map_edge_row` change) for `.unwrap()`/`.expect()` → none. | PENDING |
| OQ-A (RESOLVED = FAIL LOUD) / FR-19 / AC-14 / C-13 | Edge-query / split-count / title-join failure path **fails loud**, exercised **red**; zero-vs-failure distinction held. | test | Inject ranked-query, count-query, and title-join failures *after* a successful primary read → assert the whole `context_get` **fails** with the mapped `ServerError` (same mapping as the primary read), **not** degrade-with-note and **not** silent omission; run **red** (#4876). Cross-assert a genuine zero-edge success (AC-06) is structurally distinct from this failure. No silent-omit path exists (C-13: any future degrade requires an explicit "edges unavailable" marker distinct from "no edges"). | PENDING |
| OQ-B / R-18 | All touched/new files ≤500 lines; any split lands on the pre-authorized sibling module. | file-check | Line-count `graph_queries*.rs`, `tools.rs`, `response/entries.rs`, and new `graph_queries_ranked.rs` / `mcp/get_edges.rs` / `response/edges.rs` ≤500. | PENDING |
| C-10 / SR-05 | `authored` NLI-dark precondition documented at code site; `source` retained underneath. | grep | Comment/doc precondition at the `authored` computation site names NLI revival as the revisit trigger; `RawEdgeRow.source` preserved beneath the boolean. | PENDING |
| DNB-2 / R-19 | Corrected-entry transient asserted as expected behavior. | test | `context_correct` an entry with authored + inferred edges, then `context_get` → authored (carried) fill slots first, inferred sparse by design; encoded as expected, not a bug. | PENDING |
