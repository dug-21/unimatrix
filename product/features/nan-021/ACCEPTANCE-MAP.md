# nan-021 Acceptance Criteria Map

Every AC from SCOPE.md traced to its SPECIFICATION FR/NFR, governing ADR(s) and risk(s), and the concrete
verification. Pure test-infrastructure feature — verification is itself the running fixture plus static
change-set audits. Status set during Session 2 delivery.

| AC-ID | Description | Spec FR/NFR · ADR · Risk | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------------|--------------------|--------------------|--------|
| AC-01 | Cloud path stood up cumulatively: HTTPS / multi-project server (`serve` HTTP-on, registered `[[projects]]` slug — NOT `serve --stdio`), self-signed TLS with leaf-fingerprint pinning, vnc-039 bridge, live hook → `POST /v1/{slug}/observe`, `/v1/{slug}/...` routing | FR-1 · D-1 · ADR-001 · SR-04/R-10 | test + file-check | The release-gate smoke (`scripts/docker-http-posture-smoke.sh`) brings up the HTTPS image, registers a slug pre-serve, terminates TLS with a leaf-fp pin, and routes `/v1/{slug}/...`; assert NOT `serve --stdio`. Static: the path extends `docker-http-posture-smoke.sh` Gates 1–7 + Python harness clients — no third server-spawn/cert/bundle path exists. | PENDING |
| AC-02 | Full cycle through the bridge over pinned HTTPS: `context_cycle(start) → tool calls incl. real Bash → stop` flows through `mcp-bridge.js` (stdio JSON-RPC) over pinned HTTPS; hooks over pinned HTTPS `/observe`; NOT UDS/stdio-direct/direct-`mcp_url`-POST; bridge carried it (`Mcp-Session-Id` replay + SSE parsed, not just 200) | FR-2/FR-9/NFR-7 · ADR-002 · R-04/R-05/SR-02 | test | C2 gate spawns `node mcp-bridge.js <projectHash>` and drives the cycle over its stdio JSON-RPC; assert zero direct cycle-`mcp_url` POSTs; assert `Mcp-Session-Id` captured on `initialize` and replayed byte-stable + `text/event-stream` parsed (observable in bridge stderr/log). Negative control: JSON-only `Accept` must FAIL framing (#5129). First tool call follows session-id capture with no interposed wait; mid-cycle eviction survived by shipped self-heal (#830) or hard-fails with captured bridge stderr. | PENDING |
| AC-03 | Every driven observation has `topic_signal == feature`, **derived** by `extract_topic_signal → enrich_topic_signal_with_source` from observed content over the wire; **no seed** (`_seed_observation_sql_lifecycle` SQL nor `make_stamped_event` struct) anywhere in setup/assertion path | FR-3/FR-6 · ADR-004 · SR-06/R-07/R-09 | test + grep | Assert exact string `topic_signal == feature` for every driven observation (a derived `unattributed` near-miss HARD-fails). Pin the workload `feature_cycle` to a VALID registry feature so `enrich_topic_signal_with_source` resolves the `declared` branch; assert the slug/feature is registered before driving. Static reachability audit (grep/import): neither seed site is reachable from this test. Assert the Bash content carries the parseable feature-ID token (FR-3 load-bearing). | PENDING |
| AC-04 | Live-vs-live `MetricVector` parity: non-empty (`total_tool_calls>0`, `session_count>0`, `phases` populated) and equal field-for-field MODULO the named D-5 exclusion set across all `UniversalMetrics`, the `phases` key set + `tool_call_count`, and `domain_metrics`, to the UDS run of the identical workload in the same test execution (NOT a captured golden). **D-5 completeness is a load-bearing ASSUMPTION pending a first-live-run field-by-field validation gate.** | FR-5/FR-7/FR-10/**NFR-8** · ADR-003(#5293)/ADR-006/ADR-001 · D-5/D-6 · R-01/R-02/R-03/R-06 | test + manual (first-run gate, product disposition) | The symmetric durability barrier (FR-10) passes on BOTH legs first — expected observe count provably durable at per-slug DIR granularity (incl. `-wal`) before either review; barrier timeout HARD-fails "observes not durable", never compares an empty vector. Then `context_cycle_review(feature)` HTTPS `MetricVector` == UDS `MetricVector` field-for-field modulo D-5 (`computed_at`, `total_duration_secs`, `phases[*].duration_secs`). **FIRST-LIVE-RUN GATE (ADR-003 #5293): the gate is NOT trusted until the first live dual-transport run is examined field-by-field across all 18 non-excluded `UniversalMetrics` fields (+ `phases` key set / `tool_call_count`) and confirmed equal — this must pass ONCE.** Any non-wall-clock divergence is dispositioned per **NFR-8** by **product/human**: (a) real defect → file a GitHub bug (gate stays RED), or (b) transport-inherent → exclusion-set amendment with product sign-off + rationale recorded in ADR-003 via `context_correct`; **never silently widened** by implementer/tester. At-risk session-lifecycle fields to examine first: `cold_restart_events`, `coordinator_respawn_count`, `context_load_before_first_write_kb`, `total_context_loaded_kb`, `permission_friction_events`. Comparator mutation test: force a non-excluded structural field (e.g. drop one HTTPS observe) → comparator MUST fail. ≥20x repeat-run burst: zero out-of-D-5 divergence. HTTPS vector carries a run-correlation token; stale/missing vector → pytest ERRORS. | PENDING |
| AC-05 | Standing false-green-proof gate in the release-gate Docker lane via `workflow_dispatch`/tag (NOT per-PR), mirroring nan-019 with `release-gate-lib.sh`; skip-when-Docker-absent HARD-fails by distinct exit code; positive anchored whole-line terminal run-marker asserted; image acquisition reuses nan-019's `pull \|\| inspect \|\| exit-<code>` verbatim | FR-8 · ADR-005 · D-3 · R-08/R-12/SR-03 | shell + file-check | `run_smoke_gate IMAGE SMOKE_CMD...` returns 0 iff rc==0 AND run-marker captured. Assert distinct exit codes: Docker-absent → 3 (treated as HARD failure), image-unacquirable → 4 (distinct), passed → 0. Assert anchored whole-line marker via `grep -qxE '\[[a-z0-9-]+-smoke\] ALL GATES PASSED.*'` — exit-0-without-marker FAILS. CI lane is `workflow_dispatch`/tag in the release workflow, NOT `pull_request`. Pre-merge `SMOKE_*_CMD` stub-drive unit-tests the gate spine before the live tag run (R-12). | PENDING |
| AC-06 | Zero production-code change — diff is test infrastructure only (harness/fixtures/scripts/CI lane); image, routes, bridge, TLS provisioning, attribution chain exercised as-is | NFR-1 · all ADRs | shell (git diff) | `git diff` touches only `product/test/infra-001/**`, the release-gate CI lane, and `product/features/nan-021/**` docs — assert NO `crates/**` or `lib/**` runtime modification. | PENDING |
| AC-07 | Extends infra-001, no parallel scaffolding — new helpers map to existing assets (Docker smoke + gate lib AND/OR Python harness clients/fixtures); no net-new duplicate of server spawn, cert-pinning, credstore, or bundle handling | NFR-2 · ADR-001 · SR-04/R-10/R-11 | manual + grep | Diff review: every net-new helper names the existing asset it extends; assert C4 (`parity_workload.py` + comparator) is the ONLY substantial net-new module. Assert `projectHash` is READ BACK from `init --bundle` output (or by listing the single dir under `$SANDBOX/home/.unimatrix/`), NOT recomputed — no hashing primitive imported/invoked in the bridge-spawn path (R-11). Assert no net-new server-spawn/cert-pin/credstore/bundle path. | PENDING |
| **NFR-8** (process) | Exclusion-set amendment disposition authority: any non-wall-clock field diverging on the first-live-run gate is a HUMAN/PRODUCT call — (a) defect → GitHub bug, or (b) transport-inherent → exclusion-set amendment with product sign-off + recorded rationale; implementer/tester MUST NOT silently widen the set | NFR-8 · ADR-003(#5293) · **R-01/R-02** · D-5 | manual (delivery gate) + grep | DELIVERY-TIME process gate enforced by tester + leader, NOT auto-verifiable by the fixture alone. On any non-wall-clock divergence: confirm a GitHub bug was filed (disposition a) OR an exclusion-set amendment with product approver + rationale was appended to ADR-003 via `context_correct` (disposition b). Static guard: grep the comparator diff — the exclusion-set literal changes ONLY alongside a recorded product-signed rationale; a reactive widen with no GitHub bug / no ADR amendment is a hard REWORK. The fixture itself doubles as the #830 self-heal regression guard (ADR-002 #5294) — a keep_alive flake here SIGNALS a #830 regression, not a fixture bug. | PENDING |

## Coverage Notes

- **AC ↔ FR closure:** AC-01↔FR-1; AC-02↔FR-2/FR-9/NFR-7; AC-03↔FR-3/FR-6; AC-04↔FR-5/FR-7/FR-10;
  AC-05↔FR-8; AC-06↔NFR-1; AC-07↔NFR-2. FR-4 (stable session identity) and NFR-4 (dual-transport workload
  identity) are cross-cutting through AC-02/AC-03/AC-04 (the single workload driver, #832 regression class
  R-09). NFR-3 (CI determinism), NFR-5 (pinned-HTTPS fidelity), NFR-6 (workspace rules) are enforced across
  AC-02/AC-04/AC-05/AC-07.
- **The two WARN-accepted mechanisms** (ADR-006/FR-10, ADR-002/NFR-7) are verified within AC-04 (barrier
  symmetry + durability) and AC-02 (idle-window minimization + shipped self-heal reliance) respectively —
  pure test orchestration, preserving NFR-1.
- **First-live-run delivery gate (ADR-003 #5293 / NFR-8):** AC-04 carries a one-time field-by-field
  validation gate over the 18 non-excluded `UniversalMetrics` fields; the 3-field D-5 exclusion set is an
  unproven assumption until it passes. Divergence disposition (defect vs. signed amendment) is a
  product/human call (NFR-8 row) — the implementer/tester must never silently widen. This is the one place
  the fixture touches the operational DEFINITION of C0 parity, so it is surfaced as a DELIVERY GATE in the
  IMPLEMENTATION-BRIEF for the tester/leader to honor.
- **Highest-leverage risks for verdict integrity:** R-01 (incomplete D-5 set → flake) and R-02 (over-broad
  set → vacuous green) are both load-bearing on AC-04/NFR-8 and require the mutation harness + closed-set
  classification + the first-live-run gate. R-03/R-09 (single-execution + stable identity) underpin
  live-vs-live validity.
