# col-006 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Risks Mitigated | Components Validated | Status |
|-------|-------------|--------------------|--------------------|-----------------|---------------------|--------|
| AC-01 | UDS listener starts on `~/.unimatrix/{project_hash}/unimatrix.sock` alongside stdio transport, with socket permissions 0o600 | integration test | Start server; verify socket file exists at expected path. `stat` confirms mode 0o600. Server accepts UDS connections while simultaneously handling MCP tool calls on stdio. | R-03 (lifecycle ordering), R-05 (SocketGuard) | uds-listener, engine-extraction (ProjectPaths.socket_path) | PENDING |
| AC-02 | UDS listener handles concurrent connections without blocking stdio MCP transport | integration test | Two UDS clients connect simultaneously, send Ping requests, both receive Pong responses. A stdio MCP tool call (e.g., context_search) executes concurrently without delay. Verify no mutual blocking between transports. | R-13 (concurrent connections), R-19 (listener crash isolation) | uds-listener | PENDING |
| AC-03 | `unimatrix-server hook <EVENT>` reads Claude Code hook JSON from stdin, connects to UDS, dispatches event | integration test | Pipe synthetic `{"hook_event_name":"SessionStart","session_id":"test-123","cwd":"/tmp/project"}` to `unimatrix-server hook SessionStart`. Verify hook process connects to UDS and server receives a `SessionRegister` request. Exit code is 0. Verify stderr contains no errors. | R-12 (stdin parsing), R-18 (no tokio init) | hook-subcommand, wire-protocol, transport | PENDING |
| AC-04 | Ping/Pong round-trip under 50ms end-to-end (process start to exit) | benchmark test | Spawn `unimatrix-server hook Ping` with timing instrumentation (wall-clock from fork to exit). Run 10 iterations. Assert p95 < 50ms. Verify individual latency components: process startup < 5ms, socket connect < 1ms, round-trip < 10ms. | R-06 (latency budget), R-18 (heavy init), SR-03 | hook-subcommand, transport, wire-protocol | PENDING |
| AC-05 | `Transport` trait defined with 5 methods; `LocalTransport` implements it over UDS | unit test | `LocalTransport::connect()` succeeds against a running server. `request(Ping, 50ms)` returns `Pong`. `fire_and_forget(RecordEvent(...))` returns `Ok(())`. `is_connected()` returns true after connect, false after disconnect. `disconnect()` closes cleanly. Verify trait is `Send + Sync`. | R-07 (framing), R-14 (resource leak) | transport, wire-protocol | PENDING |
| AC-06 | `unimatrix-engine` crate exists with `confidence`, `coaccess`, `project` modules; all 174+ integration tests pass without modification | shell | `cargo test --workspace` passes. `unimatrix-engine` is in workspace members. `confidence.rs`, `coaccess.rs`, `project.rs` are importable from `unimatrix_engine`. `grep -r "pub mod confidence" crates/unimatrix-server/src/` returns no local module declarations (only re-exports). | R-01 (extraction breaks tools), R-02 (re-export divergence), SR-01 | engine-extraction | PENDING |
| AC-07 | UDS handler authenticates via UID verification; rejects connections from different users | unit test | On Linux: mock `SO_PEERCRED` with a different UID; verify connection rejected. On both platforms: connect from same user succeeds. Synthetic connection with mismatched UID is rejected with close (no response sent). Verify auth failure logged to stderr with peer UID. Error code -32001. | R-10 (UID bypass), R-11 (lineage false negative), SR-04 | authentication, uds-listener | PENDING |
| AC-08 | Hook subcommand exits 0 when server unavailable; queues fire-and-forget events; skips synchronous queries | unit test + file-check | Run `unimatrix-server hook SessionStart` with synthetic stdin but no running server (no socket file). Verify exit code 0. Verify queue file exists at `~/.unimatrix/{hash}/event-queue/pending-*.jsonl`. Parse queue file and verify it contains the serialized SessionRegister event. | R-15 (queue corruption), R-16 (size limits), SR-07 | event-queue, hook-subcommand | PENDING |
| AC-09 | Stale socket files detected and cleaned up on server startup | integration test | Create a socket file at the bind path (simulating crashed server). Start server. Verify stale socket is removed and new socket is bound. Server starts successfully and accepts UDS connections. | R-04 (stale socket blocks restart), SR-09 | uds-listener, hook-subcommand | PENDING |
| AC-10 | Socket cleanup on graceful shutdown; socket file does not persist after clean exit | integration test | Start server, verify socket exists. Send shutdown signal (SIGTERM). Verify socket file is removed before process exits. Verify PID file is also cleaned up. Verify ordering: socket removed before compaction begins. | R-03 (lifecycle ordering), R-05 (SocketGuard drop), R-23 (drain timeout) | uds-listener, engine-extraction | PENDING |
| AC-11 | `cortical-implant` agent pre-enrolled as Internal trust in `bootstrap_defaults()` | unit test | After `bootstrap_defaults()`, call `resolve_or_enroll("cortical-implant")`. Verify trust_level is `Internal`. Verify capabilities include `Read` and `Search` but not `Write` or `Admin`. Call `bootstrap_defaults()` again; verify enrolled_at unchanged (idempotent). | R-20 (bootstrap idempotency) | authentication (agent identity) | PENDING |
| AC-12 | Event queue respects size limits: 1000 events/file, 10 files max, 7-day pruning | unit test | Write 1001 events: verify file rotation (first file 1000 events, second file 1 event). Write events across 11 files: verify oldest file deleted when 11th created. Create queue files with timestamps >7 days old: verify pruned on next hook invocation. Verify max bounded at ~10,000 events. | R-16 (size limits not enforced), R-17 (pruning error), SR-07 | event-queue | PENDING |
| AC-13 | SessionStart and Stop hooks round-trip through full chain | integration test (end-to-end) | Start server. Pipe SessionStart JSON `{"hook_event_name":"SessionStart","session_id":"e2e-test","cwd":"/tmp"}` to `unimatrix-server hook SessionStart`. Verify server log confirms session registration. Pipe Stop JSON to `unimatrix-server hook Stop`. Verify server log confirms session close. Both hook processes exit 0. Full chain validated: hook stdin -> parse -> UDS connect -> authenticate -> server dispatch -> response -> hook exit. | R-01 (extraction), R-07 (framing), R-10 (auth), R-12 (parsing), R-22 (dropped events) | All components (end-to-end) | PENDING |
