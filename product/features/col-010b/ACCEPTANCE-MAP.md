# col-010b Acceptance Criteria Map

| AC-ID | Description | Component | Verification Method | Verification Detail | Status |
|-------|-------------|-----------|--------------------|--------------------|--------|
| AC-01 | `context_retrospective` with default `evidence_limit` returns <= 3 evidence items per hotspot; total JSON payload <= 10KB for a 13-hotspot report | 1 | test | Integration test: build synthetic report with 13 hotspots (each with 10+ evidence items), call context_retrospective without evidence_limit (defaults to 3), serialize JSON response -- assert each hotspot.evidence.len() <= 3; assert JSON byte length <= 10240. | PENDING |
| AC-02 | `context_retrospective` with `evidence_limit = 0` returns complete evidence arrays -- output is field-for-field identical to pre-col-010b output | 1 | test | Integration test: snapshot a context_retrospective response before col-010b changes; after col-010b, call with evidence_limit=0 -- field-by-field parity on hotspot evidence arrays. All pre-col-010b integration tests pass with evidence_limit=0. | PENDING |
| AC-03 | `context_retrospective` with default `evidence_limit` returns `narratives: Some(...)` alongside capped evidence when the structured-events path is used; `narratives` is `None` on the JSONL path | 2 | test | Integration test: (a) populate SESSIONS + INJECTION_LOG for a feature cycle with hotspots, call context_retrospective -- report.narratives is Some, each narrative.summary is non-empty, each hotspot.evidence.len() <= 3. (b) JSONL fallback path -- narratives is None. | PENDING |
| AC-04 | A `sleep_workarounds` hotspot with monotone-increasing sleep intervals (30s, 60s, 90s, 120s) produces `HotspotNarrative.sequence_pattern = Some("30s->60s->90s->120s")`. Non-monotone pattern returns `None`. | 2 | test | Unit test: construct HotspotFinding with sleep events at 30/60/90/120 monotone-increasing, call synthesize_narratives -- assert narrative.sequence_pattern == Some("30s->60s->90s->120s"). Construct non-monotone [30,60,30,120] -- assert sequence_pattern is None. | PENDING |
| AC-05 | A report with a `permission_retries` hotspot includes a `Recommendation` with non-empty `action`. A report with no recognised hotspot types produces an empty `recommendations` list. All 4 template types verified. | 2 | test | Unit test: recommendations_for_hotspots with each of 4 recognized types -- each produces a Recommendation with non-empty action and rationale. With compile_cycles at measured=5.0 (<10) -- no recommendation. With unrecognized type only -- empty Vec. | PENDING |
| AC-06 | After `context_retrospective` with >= 1 hotspot or recommendation, a `category:lesson-learned` entry exists in Unimatrix with `topic = "retrospective/{feature_cycle}"`, non-empty content, `trust_source = "system"`, and `embedding_dim > 0` (after background task completes). | 3 | test | Integration test: context_retrospective with >= 1 hotspot, wait for fire-and-forget embed task, context_lookup(category:"lesson-learned", topic:"retrospective/{fc}") -- returns entry with trust_source="system", embedding_dim>0, non-empty content, tags contain "source:retrospective". | PENDING |
| AC-07 | Calling `context_retrospective` twice for the same feature_cycle produces exactly one active lesson-learned entry -- the second supersedes the first (`status=Deprecated`, `superseded_by` set on prior entry). | 3 | test | Integration test: call context_retrospective twice for same fc, wait for both embed tasks -- exactly 1 active entry with topic "retrospective/{fc}"; prior entry has status=Deprecated and superseded_by set. | PENDING |
| AC-08 | `context_search` with a query related to a prior retrospective finding (e.g., "permission retry patterns") returns the lesson-learned entry within the top 5 results. | 3 | test | Integration test: context_retrospective produces lesson-learned entry with "permission retry" in content, wait for embedding, context_search("permission retry patterns") -- lesson-learned entry appears in results. | PENDING |
| AC-09 | A lesson-learned entry and a generic convention entry with identical similarity and confidence scores: the lesson-learned entry ranks higher by exactly `PROVENANCE_BOOST = 0.02`. Verified on both the UDS ContextSearch path and the MCP `context_search` tool path. | 4 | test | Unit test: compute rerank_score(sim=0.8, conf=0.6) for category="lesson-learned" vs category="convention" -- difference is exactly 0.02. Integration test via both MCP context_search and ContextSearch hook path -- lesson-learned ranks first when all other factors are equal. | PENDING |
| AC-10 | All existing tests pass without modification after evidence_limit addition, RetrospectiveReport type additions, and provenance boost. Existing `context_retrospective` tests that assert on exact evidence array lengths are updated to pass `evidence_limit = 0`. | All | shell | `cargo test --workspace` -- all test suites pass, zero failures. R-09 gate audit documented. | PENDING |
