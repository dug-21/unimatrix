# nan-014 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `docker build -t unimatrix .` succeeds from repo root on both x86_64 and ARM64, producing a runnable image under 350 MB | shell | `docker build -t unimatrix . && docker images unimatrix --format '{{.Size}}'` -- verify build succeeds and size < 350 MB | PENDING |
| AC-02 | `docker run -v unimatrix-data:/data ghcr.io/dug-21/unimatrix` starts daemon in foreground mode, creates /data directory structure, ONNX embedding model available without internet | shell | `docker run --network=none -v unimatrix-data:/data unimatrix` -- verify process starts, `/data` structure created, logs show embedding model loaded | PENDING |
| AC-03 | `docker compose up` starts service with named volume `unimatrix-data` mounted at `/data` | shell | `docker compose up -d && docker compose ps` -- verify container is running, volume mounted at `/data` | PENDING |
| AC-04 | Container runs as non-root (UID 65534). No process runs as UID 0 | shell | `docker top <container>` -- verify UID column shows 65534, not 0 | PENDING |
| AC-05 | ORT installed in builder stage with SHA-256 hash verification. Tampered hash causes build failure | shell | Modify SHA-256 constant in Dockerfile, run `docker build .` -- build must fail at `sha256sum -c` step with checksum error | PENDING |
| AC-06 | `unimatrix serve --foreground` runs full daemon as PID 1 without forking. SIGTERM triggers graceful shutdown | shell | Run `unimatrix serve --foreground`, verify PID 1 in container, send SIGTERM, verify clean exit with vector dump and DB compaction logs | PENDING |
| AC-07 | `unimatrix health` exits 0 when daemon running, exits 1 otherwise. Dockerfile HEALTHCHECK uses this command | shell | Start daemon, run `unimatrix health` (exit 0). Stop daemon, run `unimatrix health` (exit 1). `docker inspect --format='{{.State.Health.Status}}'` shows `healthy` | PENDING |
| AC-08 | `release.yml` includes container build jobs for x86_64 and ARM64, producing multi-arch manifest at `ghcr.io/dug-21/unimatrix:v{version}` | grep | Verify `release.yml` contains `build-container-x64`, `build-container-arm64`, `create-container-manifest` jobs with correct platform and GHCR push config | PENDING |
| AC-09 | `.dockerignore` excludes `target/`, `.git/`, `product/`, `packages/`, `.claude/`, test fixtures. Build context under 5 MB | shell | `docker build .` output shows context size. Verify excluded paths not accessible in build via `COPY` test | PENDING |
| AC-10 | Image contains zero enterprise/commercial code. Only 9 MIT-licensed workspace crates compiled | file-check | Inspect Dockerfile -- only this repo workspace compiled. No `unimatrix-collective` dependency, no enterprise feature flags | PENDING |
| AC-11 | NLI model (quantized) baked into image alongside embedding model. Both available at container startup without downloading | shell | `docker run --network=none -v unimatrix-data:/data unimatrix` -- logs show both embedding and NLI models loaded successfully | PENDING |
| AC-12 | `docker-compose.override.yml` debug pattern documented in comments showing swap to `debian:12-slim` for shell access | grep | `grep -A5 'override' docker-compose.yml` -- verify pattern documented and syntactically valid YAML | PENDING |
