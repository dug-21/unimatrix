# crt-036 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01a | `DELETE FROM observations WHERE ts_millis` pattern absent from `status.rs` | grep | `grep -r "DELETE FROM observations WHERE ts_millis" crates/unimatrix-server/src/services/status.rs` — must return no matches | PENDING |
| AC-01b | `DELETE FROM observations WHERE ts_millis` pattern absent from `tools.rs` | grep | `grep -r "DELETE FROM observations WHERE ts_millis" crates/unimatrix-server/src/mcp/tools.rs` — must return no matches | PENDING |
| AC-02 | Cycle-based pruning correctness: N > K cycles inserted, GC with K, oldest (N-K) reviewed cycles pruned, newest K retained — observations, query_log, injection_log, sessions all checked | test | `cargo test -p unimatrix-server test_gc_cycle_based_pruning_correctness` — asserts per-table row counts match exactly | PENDING |
| AC-03 | Regression: after GC, `entries`, `GRAPH_EDGES`, `co_access`, `cycle_events`, `cycle_review_index`, and `observation_phase_metrics` row counts are identical to pre-GC snapshot | test | `cargo test -p unimatrix-server test_gc_protected_tables_regression` — compares COUNT(*) per table before and after GC | PENDING |
| AC-04 | crt-033 gate: cycle with no `cycle_review_index` row is never pruned regardless of age | test | `cargo test -p unimatrix-server test_gc_gate_no_review_row` — inserts cycle without cycle_review_index row, runs GC, asserts sessions and observations still present | PENDING |
| AC-05 | After GC prunes a cycle, `cycle_review_index.raw_signals_available = 0` for pruned cycle; `summary_json` is byte-for-byte identical to pre-GC value; retained cycles still have `raw_signals_available = 1` | test | `cargo test -p unimatrix-server test_gc_raw_signals_flag_and_summary_json_preserved` | PENDING |
| AC-06 | Unattributed pruning: closed unattributed sessions (feature_cycle IS NULL, status != Active) and their observations/query_log pruned; Active unattributed sessions and their observations are NOT pruned | test | `cargo test -p unimatrix-server test_gc_unattributed_active_guard` — tests both Active (survives) and Closed (pruned) cases | PENDING |
| AC-07 | `query_log` rows for sessions in purgeable cycles deleted; `query_log` rows for retained cycles preserved | test | `cargo test -p unimatrix-server test_gc_query_log_pruned_with_cycle` — asserts COUNT(*) per cycle after GC | PENDING |
| AC-08 | `injection_log` deleted before `sessions` in per-cycle transaction; no orphaned `injection_log` rows after GC; mutation test (invert order) confirms test fails with orphans | test | `cargo test -p unimatrix-server test_gc_cascade_delete_order` — includes order-inversion mutation assertion | PENDING |
| AC-09 | `audit_log` rows older than `audit_log_retention_days` (180) deleted; rows within window preserved; both sides of boundary tested | test | `cargo test -p unimatrix-server test_gc_audit_log_retention_boundary` — inserts 200-day-old rows (deleted) and 100-day-old rows (kept) | PENDING |
| AC-10 | `[retention]` config block parses correctly; absent block yields defaults (activity_detail_retention_cycles=50, audit_log_retention_days=180, max_cycles_per_tick=10); explicit values override defaults | test | `cargo test -p unimatrix-server test_retention_config_defaults_and_override` — unit + TOML parse tests | PENDING |
| AC-11 | `validate()` rejects `activity_detail_retention_cycles = 0`; error message contains `"activity_detail_retention_cycles"` | test | `cargo test -p unimatrix-server test_retention_config_validate_rejects_zero_retention_cycles` | PENDING |
| AC-12 | `validate()` rejects `audit_log_retention_days = 0`; error message contains `"audit_log_retention_days"` | test | `cargo test -p unimatrix-server test_retention_config_validate_rejects_zero_audit_days` | PENDING |
| AC-12b | `validate()` rejects `max_cycles_per_tick = 0`; error message contains `"max_cycles_per_tick"`; also rejects `max_cycles_per_tick = 1001` (upper bound) | test | `cargo test -p unimatrix-server test_retention_config_validate_rejects_invalid_max_cycles` | PENDING |
| AC-13 | `activity_detail_retention_cycles` field declaration has `///` doc comment containing both "PhaseFreqTable lookback" and "GNN training window" | manual | PR reviewer confirms triple-slash doc comment on `RetentionConfig::activity_detail_retention_cycles` contains both required strings | PENDING |
| AC-14 | Protected tables (`cycle_events`, `cycle_review_index`, `observation_phase_metrics`, `entries`, `GRAPH_EDGES`) each have a row inserted before GC; each row survives GC unchanged | test | `cargo test -p unimatrix-server test_gc_protected_tables_row_level` — targeted row-existence check per protected table | PENDING |
| AC-15 | Structured tracing output: `info` with `purgeable_count` on pass start; `info` with `observations_deleted` + `cycle_id` per pruned cycle; `info` with `cycles_pruned = 2` on completion; `warn` with cycle ID on gate skip | test | `cargo test -p unimatrix-server test_gc_tracing_output` — uses tracing-test to assert log events for a 2-cycle prune run including one gate skip | PENDING |
| AC-16 | `max_cycles_per_tick` cap: 20 purgeable cycles with cap=5 — exactly 5 pruned per tick; oldest cycles processed first; all 20 pruned after 4 ticks | test | `cargo test -p unimatrix-server test_gc_max_cycles_per_tick_cap` — multi-tick drain test with ordering assertion | PENDING |
| AC-17 | PhaseFreqTable mismatch warning: `query_log_lookback_days=365` with 5 cycles reviewed in past 7 days emits `warn` containing `"query_log_lookback_days"` and `"retention window"`; no warn when lookback is within coverage | test | `cargo test -p unimatrix-server test_gc_phase_freq_table_mismatch_warning` — tests both warning-fires and warning-suppressed conditions | PENDING |
