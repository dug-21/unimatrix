# crt-033 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Schema version is 18 in fresh databases | test | `test_current_schema_version_is_18` asserts `CURRENT_SCHEMA_VERSION == 18`; `test_fresh_db_creates_schema_v18` asserts `schema_version` counter = 18 after opening a new database | PENDING |
| AC-02 | Existing v17 databases migrate to v18 without data loss; `cycle_review_index` table is created | test | `test_v17_to_v18_migration_creates_table` in `tests/migration_v17_to_v18.rs`: build v17-shaped DB, open with `SqlxStore`, assert table exists, all pre-existing rows are readable | PENDING |
| AC-02b | All seven schema cascade touchpoints are updated (authoritative: architecture 7-touchpoint list, supersedes spec's 5-touchpoint list) | manual | Code review gate: (1) `migration.rs` `CURRENT_SCHEMA_VERSION = 18`; (2) `migration.rs` `if current_version < 18` block with DDL; (3) `db.rs` `create_tables_if_needed()` includes `cycle_review_index` DDL; (4) `sqlite_parity.rs` table-count / named-table assertions updated; (5) `server.rs` schema version assertions updated to 18; (6) previous migration test renamed to `test_current_schema_version_is_at_least_17` with `>= 17`; (7) all migration test files checked for column-count assertions referencing old count | PENDING |
| AC-03 | First call for a cycle computes full report and writes a row with `raw_signals_available = 1` | test | Integration test: seed observations for a cycle; call handler once; `SELECT raw_signals_available FROM cycle_review_index WHERE feature_cycle = ?`; assert value = 1. Also assert `schema_version = 1` (covers AC-11) | PENDING |
| AC-04 | Second call with `force` absent or `false` returns stored record without re-running computation | test | Integration test: call handler twice for same cycle; assert second call does NOT execute observation-load path (mock/spy on store reads); returned `feature_cycle` and `metrics` are identical to first call | PENDING |
| AC-04b | When stored `schema_version` differs from `SUMMARY_SCHEMA_VERSION`, response includes advisory and handler does NOT silently recompute | test | Integration test: insert `cycle_review_index` row with `schema_version = 0`; call handler with `force=false`; assert response text contains `"use force=true to recompute"` with both version numbers; assert no observation-load DB reads occurred | PENDING |
| AC-05 | `force=true` with live signals recomputes and overwrites stored row | test | Integration test: write initial row; call handler with `force=true` and fresh signals; assert `computed_at` in stored row is updated (greater than initial); returned report reflects fresh computation | PENDING |
| AC-06 | `force=true` with purged signals but existing stored record returns stored record with explanatory note | test | Integration test: insert `cycle_review_index` row directly (no live observations); call handler with `force=true`; assert response is `Ok`; response contains `"Raw signals have been purged"`; `raw_signals_available` reported as false in response | PENDING |
| AC-07 | `force=true` with purged signals and no stored record returns `ERROR_NO_OBSERVATION_DATA` | test | Integration test: empty observations, no `cycle_review_index` row; call handler with `force=true`; assert MCP error code matches `ERROR_NO_OBSERVATION_DATA` | PENDING |
| AC-08 | Stored `summary_json` preserves full hotspot evidence; `evidence_limit` truncation applied only at render time | test | Integration test: store cycle with 10 hotspots × 5 evidence items; call handler with `evidence_limit=2`; read raw `summary_json` from table, deserialize, assert each hotspot has 5 evidence items; assert MCP response hotspots have 2 evidence items | PENDING |
| AC-09 | `context_status` includes `pending_cycle_reviews` listing K-window cycles with `cycle_events` rows (cycle_start) but no `cycle_review_index` row | test | Integration test: seed two cycles in `cycle_events` (cycle_start, within K-window); write `cycle_review_index` row for one; call `context_status`; assert `pending_cycle_reviews` contains exactly the un-reviewed cycle ID | PENDING |
| AC-10 | `pending_cycle_reviews` returns empty list when all K-window cycles have review rows | test | Same setup as AC-09 but write review rows for both cycles; assert `pending_cycle_reviews` is empty | PENDING |
| AC-11 | `cycle_review_index.schema_version` populated with `SUMMARY_SCHEMA_VERSION` at write time | test | Covered by AC-03 test: assert `schema_version = 1` in the written row | PENDING |
| AC-12 | `force: Option<bool>` field accepted in `RetrospectiveParams` JSON; absent equivalent to `false` | test | Unit test: deserialize `{"feature_cycle": "x"}` into `RetrospectiveParams`, assert `force.is_none()`; deserialize `{"feature_cycle": "x", "force": true}`, assert `force == Some(true)` | PENDING |
| AC-13 | Migration integration test confirms `cycle_review_index` table exists after migrating a v17 database to v18 | test | Covered by AC-02: `tests/migration_v17_to_v18.rs` following pattern of `tests/migration_v16_to_v17.rs` | PENDING |
| AC-14 | Unit test for memoization hit path (stored record returned without DB read of observations) | test | Test with mock or patched store: insert `cycle_review_index` row; call handler with `force=false`; assert observation tables not queried (no calls to `get_observations` or equivalent); assert `computed_at` in stored row unchanged | PENDING |
| AC-15 | Unit test for `force=true` with purged signals (stored record returned with note) | test | Test: seed `cycle_review_index` row, no observations; call handler with `force=true`; assert (a) return is `Ok` not `ERROR_NO_OBSERVATION_DATA`; (b) response text contains explanatory note; (c) `raw_signals_available` reported as false | PENDING |
| AC-16 | `RetrospectiveReport` (or dedicated DTO) is fully `Serialize + Deserialize` — compile-time verification | test | `cargo build --workspace` is the compile-time gate (write/read call sites fail to compile if bound is missing); additionally: unit test round-trip — `serde_json::from_str::<RetrospectiveReport>(&serde_json::to_string(&sample_report).unwrap()).unwrap()` on a fully-populated instance with all nested fields | PENDING |
| AC-17 | `SUMMARY_SCHEMA_VERSION` const defined in `cycle_review_index.rs`, not in `tools.rs` or `unimatrix-observe` | grep | `grep -r 'SUMMARY_SCHEMA_VERSION' crates/` — assert single definition in `crates/unimatrix-store/src/cycle_review_index.rs`; `grep -r 'SUMMARY_SCHEMA_VERSION.*=.*[0-9]' crates/unimatrix-server/` returns zero matches | PENDING |
