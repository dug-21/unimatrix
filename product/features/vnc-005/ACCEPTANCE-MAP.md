# vnc-005 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Risk Coverage | Status |
|-------|-------------|--------------------|--------------------|---------------|--------|
| AC-01 | `unimatrix serve --daemon` starts the server as a background process, creates `unimatrix-mcp.sock`, and the launcher process exits 0 within 5 seconds | shell | `unimatrix serve --daemon`; assert exit code 0 within 5s; `stat ~/.unimatrix/{hash}/unimatrix-mcp.sock` succeeds; `kill -0 $(cat ~/.unimatrix/{hash}/unimatrix.pid)` exits 0 | R-04, R-09, R-16 | PENDING |
| AC-02 | `unimatrix-mcp.sock` is created with `0600` permissions before the first connection is accepted | shell | After daemon start: `stat -c '%a' ~/.unimatrix/{hash}/unimatrix-mcp.sock` returns `600`; group and other bits are zero | R-06 | PENDING |
| AC-03 | `unimatrix` (no subcommand, daemon already running) connects stdin/stdout to the daemon's MCP socket and passes MCP JSON-RPC traffic bidirectionally | test | Start daemon; pipe MCP `initialize` JSON to `unimatrix` (no subcommand); assert a valid MCP `initialize` response received from the daemon | — | PENDING |
| AC-04 | When the bridge client terminates (stdin EOF), the daemon process continues running with all background state intact | test | Start daemon; connect bridge; send MCP `initialize`; close bridge stdin; sleep 1s; assert daemon PID still alive; open second bridge; assert `initialize` succeeds; assert tick counter or uptime shows continuity | R-03 | PENDING |
| AC-05 | `unimatrix` (no subcommand, no daemon running) spawns `unimatrix serve --daemon`, waits up to 5 seconds for `unimatrix-mcp.sock`, then bridges | test | Ensure no daemon is running; invoke `unimatrix` with piped MCP `initialize`; assert valid response received within 8s total; assert `unimatrix-mcp.sock` was created during the invocation | R-08 | PENDING |
| AC-06 | The bridge calls `is_unimatrix_process(pid)` on the PID file before spawning a new daemon; a healthy running daemon is never duplicated | test | Start daemon (record PID); invoke `unimatrix` (bridge); assert only one daemon process exists after bridge connects; no duplicate spawn visible in process table | R-04 | PENDING |
| AC-07 | `unimatrix serve --daemon` exits non-zero when a healthy daemon is already running for this project | shell | Start daemon; invoke `unimatrix serve --daemon` a second time; assert exit code non-zero; assert only one daemon process remains; assert stderr contains "already running" or equivalent | R-04 | PENDING |
| AC-08 | SIGTERM on the daemon triggers graceful shutdown: vector dump, adapt save, DB compaction complete before the process exits | test | Start daemon; call `context_store` for one entry, await ack; send SIGTERM; assert daemon exits within 30s; start new daemon; call `context_get` for stored entry; assert entry is retrievable | R-01, R-03 | PENDING |
| AC-09 | SIGINT on the daemon triggers the same graceful shutdown sequence as SIGTERM | test | Same as AC-08 with SIGINT substituted for SIGTERM | R-01 | PENDING |
| AC-10 | `unimatrix stop` sends SIGTERM to the daemon and exits 0 after the daemon process is gone | test | Start daemon; invoke `unimatrix stop`; assert exit code 0; assert daemon PID no longer alive within 10s | — | PENDING |
| AC-11 | `unimatrix stop` exits non-zero with a clear message when no daemon is running | shell | Ensure no daemon is running; invoke `unimatrix stop`; assert non-zero exit code; assert stderr contains message indicating no daemon was found | — | PENDING |
| AC-12 | `unimatrix serve --stdio` works as a drop-in replacement for the pre-vnc-005 default `unimatrix` invocation — exits when stdin closes, no MCP socket created | test | Invoke `unimatrix serve --stdio` with piped MCP `initialize`; assert valid response; close stdin; assert process exits within 5s; assert no `unimatrix-mcp.sock` created; assert exit code 0 | R-12 | PENDING |
| AC-13 | `unimatrix hook SessionStart` works correctly against a running daemon (hook IPC unaffected) | shell | Start daemon; invoke `unimatrix hook SessionStart --feature test-001`; assert exit code 0; assert `unimatrix.sock` present and served by daemon | R-13 | PENDING |
| AC-14 | Multiple simultaneous MCP sessions can store and retrieve entries without data corruption | test | Start daemon; open 4 concurrent bridge connections; each calls `context_store` with a unique key then `context_get` for that key; assert all 4 retrievals return the correct entry; assert no SQLite error or panic in daemon logs | R-01, R-05 | PENDING |
| AC-15 | If the daemon socket does not appear within 5 seconds of auto-start spawn, the bridge exits 1 and prints a diagnostic message to stderr that includes the log file path | test | Simulate a daemon that never creates `unimatrix-mcp.sock` (stub binary that sleeps); invoke `unimatrix`; assert exit code 1 within 7s; assert stderr contains the log file path string | R-08 | PENDING |
| AC-16 | If `unimatrix-mcp.sock` exists from a crashed prior daemon (no healthy process), `unimatrix serve --daemon` unlinks it and binds fresh | test | Create stale `unimatrix-mcp.sock` file with no listening process; invoke `unimatrix serve --daemon`; assert launcher exits 0; assert a new socket is bound and accepts connections | R-09 | PENDING |
| AC-17 | Entries stored across multiple bridge sessions under the same `feature_cycle` value accumulate in the same bucket and are all returned by a single `context_retrospective` drain call | test | Start daemon; open session A, store 2 entries with `feature_cycle=fnc-test-001`; close session A; open session B, store 1 entry with `feature_cycle=fnc-test-001`; call `context_retrospective` with `topic=fnc-test-001`; assert all 3 entries present in result | R-05 | PENDING |
| AC-18 | After `context_retrospective` drains a `feature_cycle` bucket, a subsequent call for the same feature cycle returns an empty accumulator | test | Drain bucket as in AC-17; call `context_retrospective` again with the same topic; assert the accumulator section is empty (no duplicate entries) | R-05, R-18 | PENDING |
| AC-19 | The `fork`/`setsid` daemonization sequence executes before any Tokio runtime is initialized in the daemon process | grep | In `infra/daemon.rs` and `main.rs`: confirm no `tokio::runtime::Runtime`, `#[tokio::main]`, or `Runtime::new()` appears before `nix::unistd::setsid()` call in the daemon child startup path | SR-02 (C-01) | PENDING |
| AC-20 | A 33rd concurrent MCP connection receives connection-close; the 32 existing sessions are unaffected | test | Open 32 concurrent sessions against the daemon; attempt a 33rd connection; assert the 33rd connection is immediately closed; assert all 32 existing sessions remain active and responsive | R-11 | PENDING |

---

## Additional Verification Requirements (from RISK-TEST-STRATEGY.md)

The following items are not formal ACs from SCOPE.md or SPECIFICATION.md but are required
test coverage per the risk strategy. Delivery agents must include these in the test plan.

| Req-ID | Description | Verification Method | Risk |
|--------|-------------|--------------------|----- |
| RV-01 | `Arc::try_unwrap(store)` strong count is 1 immediately before `graceful_shutdown` call | test | Assert `Arc::strong_count(&store) == 1` in a test harness after all session handles joined; N=1 and N=4 concurrent sessions | R-01 |
| RV-02 | `retain(|h| !h.is_finished())` sweep runs on each accept loop iteration (not only at shutdown) | test | After 100 sequential connect/disconnect cycles, `session_handles.len()` is bounded (≤ 1, not 100) | R-10 |
| RV-03 | `--daemon-child` does not appear in `unimatrix --help` or `unimatrix serve --help` output | shell | `unimatrix --help` and `unimatrix serve --help`; grep for `daemon-child`; assert no match | R-17 |
| RV-04 | `unimatrix hook SessionStart` wall-clock time under 50ms against a running daemon | shell | `time unimatrix hook SessionStart --feature test-001`; assert wall clock under 50ms | R-13 |
| RV-05 | Socket path validation rejects paths exceeding 103 bytes; accepts paths of 103 bytes or fewer | test | Unit test `validate_socket_path_length` with inputs: 103 bytes (pass), 104 bytes (pass Linux / borderline macOS), 107 bytes (fail both) | R-14 |
| RV-06 | `PendingEntriesAnalysis::upsert` + `drain_for` under concurrent load produces correct entry counts with no data loss | test | Spawn 4 tasks each calling `upsert` 250 times on same `feature_cycle`; fifth task calls `drain_for` repeatedly; assert total entries seen = total inserted, no duplicates | R-05, R-15 |
| RV-07 | Both `unimatrix-mcp.sock` and `unimatrix.sock` are absent after SIGTERM graceful shutdown | shell | Start daemon; send SIGTERM; wait for exit; assert neither socket file exists on disk | R-16 |
| RV-08 | After SIGKILL (skips graceful shutdown), both socket files exist; next daemon start cleans them up | test | Start daemon; kill -9; assert both socket files exist; start new daemon; assert startup succeeds and new sockets are bound | R-09, R-16 |
| RV-09 | `graceful_shutdown` has exactly one call site after refactor, reachable only from daemon token cancellation path | grep | `grep -rn 'graceful_shutdown' crates/unimatrix-server/src/`; assert exactly one call site; assert it is NOT reachable from `QuitReason::Closed` in session task | R-03 |
| RV-10 | `unimatrix serve --stdio` exits when SIGTERM is sent (stdio mode still respects signals) | test | Start `unimatrix serve --stdio`; send SIGTERM; assert process exits with graceful shutdown within 30s | R-12 |
| RV-11 | `CallerId::UdsSession` rate-limit exemption site carries code comment referencing C-07 and W2-2 | grep | `grep -n 'UdsSession' crates/unimatrix-server/src/`; assert comment containing "C-07" or "W2-2" is present at exemption site | R-07 |
| RV-12 | Eviction path (`evict_stale` and `drain_for`) holds Mutex for full duration; no eviction runs outside the lock | test | Unit test: acquire Mutex in test; confirm neither `evict_stale` nor `drain_for` can be called without the lock held | R-18 |
