# crt-013 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Episodic augmentation stub (`episodic.rs`) removed from `unimatrix-adapt`. Module declaration removed from `lib.rs`. All references cleaned up. | grep | `grep -r "episodic" --include="*.rs" crates/` returns zero hits. `cargo build --workspace` succeeds. | PENDING |
| AC-02 | `co_access_affinity()` function removed from `confidence.rs`. `W_COAC` constant removed. Tests for removed function removed. | grep | `grep -r "co_access_affinity\|W_COAC" --include="*.rs" crates/` returns zero hits. `cargo build --workspace` succeeds. | PENDING |
| AC-03 | ADR documenting the two-mechanism co-access architecture (MicroLoRA + boost) stored in Unimatrix. ADR notes transitional status. | shell | ADR retrievable via `context_search` for "co-access architecture". ADR references MicroLoRA, scalar boost, and Graph Enablement transitional status. | PENDING |
| AC-04 | Integration test proves deprecated entry ranks below active entry at comparable similarity in Flexible mode. Test asserts relative ranking, not score constants. | test | `cargo test -p unimatrix-server status_penalty` — T-SP-01 passes. Test contains no assertions on constants 0.7 or 0.5. | PENDING |
| AC-05 | Integration test proves deprecated and superseded entries excluded in Strict mode. | test | `cargo test -p unimatrix-server status_penalty` — T-SP-03 passes. Results contain zero entries with status Deprecated or Superseded. | PENDING |
| AC-06 | Integration test proves deprecated entries excluded from co-access boost computation. | test | `cargo test -p unimatrix-server status_penalty` — T-SP-04 passes. Deprecated entry receives no boost. | PENDING |
| AC-07 | Integration test proves deprecated-only query still returns results in Flexible mode. | test | `cargo test -p unimatrix-server status_penalty` — T-SP-05 passes. Results non-empty when only deprecated entries match. | PENDING |
| AC-08 | Briefing `semantic_k` parameter configurable with default 3. Existing behavior unchanged when unconfigured. | test | `cargo test -p unimatrix-server briefing_k` — tests pass for default k=3, configured k=5, clamp to [1, 20], invalid env var fallback. | PENDING |
| AC-09 | Status scan no longer performs `SELECT * FROM entries` for aggregate metrics. Correction chain metrics, trust source distribution, and security metrics computed via SQL aggregation. | grep | `grep -n "SELECT.*FROM entries" crates/unimatrix-server/src/services/status.rs` shows no unqualified full scan. New code calls `compute_status_aggregates()`. | PENDING |
| AC-10 | `context_status` returns equivalent results before and after optimization. Comparison test runs both paths on same dataset and diffs output field-by-field. | test | `cargo test -p unimatrix-server status_aggregates_comparison` — test creates entries with edge cases (NULL supersedes, empty trust_source, empty created_by, high correction_count), runs both paths, asserts field-by-field equality. | PENDING |
| AC-11 | All existing tests pass with no regressions. | test | `cargo test --workspace` — all tests pass (zero failures). | PENDING |
