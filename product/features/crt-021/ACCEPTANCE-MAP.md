# crt-021 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `TypedRelationGraph` (`StableGraph<u64, RelationEdge>`) replaces `SupersessionGraph` (`StableGraph<u64, ()>`) in `unimatrix-engine/src/graph.rs`. All existing graph.rs unit tests pass without modification to test expectations. | test | `cargo test -p unimatrix-engine` passes; `grep -r "SupersessionGraph" crates/unimatrix-engine/src/` returns zero matches outside comments | PENDING |
| AC-02 | `RelationType` enum defines exactly five variants: `Supersedes`, `Contradicts`, `Supports`, `CoAccess`, `Prerequisite`. Each serializes to its variant name as a string. Round-trip `RelationType → String → RelationType` succeeds for all five variants. | test | Unit test constructs each variant, calls `as_str()`, calls `from_str()`, asserts round-trip equality for all five variants | PENDING |
| AC-03 | `RelationEdge` struct carries `relation_type: String`, `weight: f32`, `created_at: i64`, `created_by: String`, `source: String`, `bootstrap_only: bool`. `weight.is_finite()` is checked before any persist path accepts it. Non-finite weights are rejected with a logged ERROR and not written. | test | Unit test passes `f32::NAN`, `f32::INFINITY`, `f32::NEG_INFINITY` to the weight validation guard and asserts rejection; valid weights `0.0`, `0.5`, `1.0` pass through | PENDING |
| AC-04 | `GRAPH_EDGES` table exists in the database schema after `create_tables_if_needed` on a fresh database. DDL matches specification exactly including `metadata TEXT DEFAULT NULL` and `UNIQUE(source_id, target_id, relation_type)`. Three indexes exist: `idx_graph_edges_source_id`, `idx_graph_edges_target_id`, `idx_graph_edges_relation_type`. | test | Integration test opens a fresh database, queries `sqlite_master` for `graph_edges`, asserts all columns exist with correct types/constraints including `metadata` column | PENDING |
| AC-05 | A v12→v13 migration runs on an existing v12 database. After migration: `CURRENT_SCHEMA_VERSION` is 13, `GRAPH_EDGES` exists, and bootstrap Supersedes edges from `entries.supersedes` are present in `GRAPH_EDGES`. | test | Migration integration test creates a synthetic v12 database with at least one entry where `supersedes IS NOT NULL`, runs `migrate_if_needed`, queries `schema_version` counter (asserts 13), queries `graph_edges` for `relation_type='Supersedes'` rows (asserts count matches input) | PENDING |
| AC-06 | Bootstrap inserts from `entries.supersedes` produce one `relation_type="Supersedes"` edge per supersession link. `bootstrap_only=0` and `source="bootstrap"` for all Supersedes edges. Edge direction: `source_id = entry.supersedes` (old/replaced), `target_id = entry.id` (new/correcting). | test | After v12→v13 migration on synthetic data, assert all `relation_type='Supersedes'` rows in `graph_edges` have `bootstrap_only=0`, `source='bootstrap'`, and correct `source_id`/`target_id` direction | PENDING |
| AC-07 | Bootstrap inserts from `co_access` produce `relation_type="CoAccess"` edges for pairs where `count >= 3`. Pairs with `count < 3` produce no edge. `bootstrap_only=0`. Edge weights normalized: `COALESCE(CAST(count AS REAL) / NULLIF(MAX(count) OVER (), 0), 1.0)`. Fresh-database migration (empty `co_access`) succeeds without error. | test | Migration integration test (a) populates `co_access` with count=2, count=3, count=5 pairs, runs migration, asserts only count>=3 pairs appear as CoAccess edges with `bootstrap_only=0`, asserts count=5 weight > count=3 weight, asserts all weights in `(0.0, 1.0]`; (b) runs migration on empty `co_access` and asserts zero errors and zero CoAccess rows | PENDING |
| AC-08 | No `Contradicts` edges are written during the v12→v13 bootstrap migration. `bootstrap_only` column (`INTEGER NOT NULL DEFAULT 0`) and `source` column (`TEXT NOT NULL DEFAULT ''`) exist in `GRAPH_EDGES` schema for W1-2 NLI use. | test | After migration on any synthetic v12 database, assert zero rows with `relation_type='Contradicts'` in `graph_edges`; assert `bootstrap_only` and `source` columns exist in `sqlite_master` schema for `graph_edges` | PENDING |
| AC-09 | `AnalyticsWrite::GraphEdge { source_id, target_id, relation_type, weight, created_by, source, bootstrap_only }` variant exists. Drain task arm executes `INSERT OR IGNORE INTO graph_edges`. `variant_name()` returns `"GraphEdge"`. | test | Unit test constructs variant, calls `variant_name()`, asserts `"GraphEdge"`; integration test enqueues a `GraphEdge` event with valid weight, drains, queries `graph_edges`, asserts row present | PENDING |
| AC-10 | `graph_penalty` and `find_terminal_active` produce identical results on a `TypedRelationGraph` containing only Supersedes edges as they did on the old `SupersessionGraph`. All 25+ existing graph.rs unit tests pass without modification to test expectations. | test | `cargo test -p unimatrix-engine -- graph` — all 25+ existing tests pass; no test expectations modified | PENDING |
| AC-11 | `graph_penalty` does NOT apply any penalty derived from Contradicts, Supports, CoAccess, or Prerequisite edges. Only Supersedes edge topology drives penalty computation. | test | Unit test builds `TypedRelationGraph` with mixed edge types (Supersedes + Contradicts on same node), asserts `graph_penalty` output equals result from a graph containing only the Supersedes edges | PENDING |
| AC-12 | `bootstrap_only=1` edges are excluded from `TypedRelationGraph` structurally. `build_typed_relation_graph` skips `bootstrap_only=true` rows during edge insertion; they are never added to `inner`. | test | Unit test calls `build_typed_relation_graph` with a `GraphEdgeRow` list where all edges have `bootstrap_only=true`; asserts resulting graph has zero edges. Second test: one `bootstrap_only=false` + one `bootstrap_only=true` edge for the same source; asserts only confirmed edge present | PENDING |
| AC-13 | Background tick rebuilds `TypedRelationGraph` from `GRAPH_EDGES` after `maintenance_tick` completes and after GRAPH_EDGES orphaned-edge compaction. In-memory graph updated under `Arc<RwLock<_>>` write lock. | test | Integration test seeds `GRAPH_EDGES`, triggers tick, acquires read lock on `TypedGraphStateHandle`, asserts graph contains expected edges | PENDING |
| AC-14 | Orphaned edge compaction deletes `GRAPH_EDGES` rows where `source_id` or `target_id` no longer exists in `entries`. Runs before the in-memory graph rebuild in the tick sequence. | test | Integration test inserts `GRAPH_EDGES` rows referencing deleted entry IDs, triggers tick, asserts orphaned rows are absent from `graph_edges` after tick completes | PENDING |
| AC-15 | On cold start (before first tick), search path applies `FALLBACK_PENALTY`. `TypedGraphState::new()` sets `use_fallback=true` with empty `typed_graph` and empty `all_entries`. | test | Unit test: `TypedGraphState::new_handle()` returns state with `use_fallback=true`; unit test: search path given `use_fallback=true` returns `FALLBACK_PENALTY` without calling `graph_penalty` | PENDING |
| AC-16 | New ADR (Unimatrix entry #2417, superseding entry #1604) is stored in Unimatrix before the feature ships. ADR documents typed edge weights, Supersedes-only penalty filter rationale, `bootstrap_only` exclusion policy, and tick-rebuild pattern. | manual | `context_lookup` for entry #2417 returns an active entry tagged `[crt-021, adr]`; `context_lookup` for entry #1604 shows status deprecated | PENDING |
| AC-17 | `weight: f32` values are validated finite (not NaN, not ±Inf) before any `GraphEdge` analytics write is enqueued. Invalid weights are rejected with a logged ERROR; event is not written. | test | Unit test calls weight validation guard with `f32::NAN`, `f32::INFINITY`, `f32::NEG_INFINITY` — all rejected; valid weights `0.0`, `0.5`, `1.0` pass; integration test: enqueue `GraphEdge` with `weight=f32::NAN`, drain, assert zero rows written to `graph_edges` | PENDING |
| AC-18 | Migration integration test covers v12→v13 migration on a synthetic v12 database. Asserts: `GRAPH_EDGES` table exists, `schema_version` counter is 13, at least one Supersedes edge present (if test data includes `entries.supersedes`). | test | Test in `migration.rs` test module following existing migration integration test pattern; `cargo test -p unimatrix-store -- migration` | PENDING |
| AC-19 | `sqlx-data.json` is regenerated via `cargo sqlx prepare` and committed. CI compile-time SQL validation is not silently disabled after the schema change. | shell | `cargo build --workspace` with `SQLX_OFFLINE=true` succeeds after `sqlx-data.json` update | PENDING |
| AC-20 | `RelationType::Prerequisite` exists in the enum. No code path in crt-021 creates a Prerequisite edge (no bootstrap, no analytics write, no migration). Variant is reserved for W3-1. | grep | `grep -r "Prerequisite" crates/` shows enum definition and round-trip test only — no INSERT or write path references; round-trip test for the variant passes (covered by AC-02) | PENDING |
| AC-21 | Schema and analytics write path support W1-2 NLI promoting a `bootstrap_only=1` edge to confirmed via DELETE + INSERT (idempotent via UNIQUE constraint). No promotion logic implemented in crt-021; mechanism documented. | test | Integration test demonstrates promotion pattern: insert edge with `bootstrap_only=1`, DELETE it, INSERT with `bootstrap_only=0`, assert final row has `bootstrap_only=0`; verify idempotency by repeating INSERT (no duplicate, no error) | PENDING |
