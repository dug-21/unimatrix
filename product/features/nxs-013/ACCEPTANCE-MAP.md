# nxs-013 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Dockerfile runtime ENV block does not set `UNIMATRIX_CONFIG`. The env var mechanism remains in `load_config` code for explicit operator use. | grep | `grep -c 'UNIMATRIX_CONFIG' Dockerfile` returns 0; `grep 'HOME=/data' Dockerfile` returns match | PENDING |
| AC-02 | docker-compose.yml does not contain a commented-out config bind mount to `/etc/unimatrix/config.toml`. Instead, comments explain that per-project config lives in the data volume. | grep | `grep -c '/etc/unimatrix/' docker-compose.yml` returns 0; `grep -c 'per-project config' docker-compose.yml` returns >= 1 | PENDING |
| AC-03 | `log_config_provenance` labels per-project config as "primary" and global config as "defaults" in log messages. | grep | `grep '"primary config' crates/unimatrix-server/src/main.rs` matches per-project branch; `grep '"defaults config' crates/unimatrix-server/src/main.rs` matches global branch | PENDING |
| AC-04 | README Configuration section presents per-project `~/.unimatrix/{hash}/config.toml` as the canonical config location, with global as the optional defaults layer. | file-check | README.md Configuration section leads with per-project path; no reference to `/etc/unimatrix/config.toml` as primary container config pattern | PENDING |
| AC-05 | PRODUCT-VISION.md W2-1 section describes a single `unimatrix-data` volume containing databases, vector indexes, config, and logs. No reference to `unimatrix-shared` for config. | grep | `grep -c 'unimatrix-shared' product/PRODUCT-VISION.md` in W2-1 section returns 0 for config references; `grep 'unimatrix-data' product/PRODUCT-VISION.md` matches single-volume description | PENDING |
| AC-06 | WAVE2-ROADMAP.md W2-1 section matches the updated single-volume model. | grep | `grep 'unimatrix-data' product/WAVE2-ROADMAP.md` matches single-volume description; `grep -c 'unimatrix-shared' product/WAVE2-ROADMAP.md` in W2-1 returns 0 for config references | PENDING |
| AC-07 | `DEFAULT_CONFIG_TOML` header comment emphasizes per-project as canonical and global as defaults. | grep | `grep -c 'canonical' crates/unimatrix-server/src/infra/config.rs` returns >= 1 within DEFAULT_CONFIG_TOML; `grep -c 'defaults' crates/unimatrix-server/src/infra/config.rs` returns >= 1 within header | PENDING |
| AC-08 | Backup documentation in docker-compose.yml volume comment states that backup = snapshot `unimatrix-data` volume (includes config). | grep | `grep -c 'backup' docker-compose.yml` returns >= 1; comment references snapshot of `unimatrix-data` volume | PENDING |
| AC-09 | Merge semantics unchanged: global defaults -> per-project overrides -> UNIMATRIX_CONFIG env override. Verified by existing tests passing without modification. | test | `cargo test --workspace` -- all pass; `git diff --name-only` shows zero test file changes in PR | PENDING |
| AC-10 | Container starts correctly without `UNIMATRIX_CONFIG` set, loading config from the per-project path inside the data volume. | shell | Build image: `docker build -t unimatrix-test .`; inspect ENV: `docker inspect --format '{{.Config.Env}}' unimatrix-test` confirms no UNIMATRIX_CONFIG; run container and check logs for "primary config" messages | PENDING |
