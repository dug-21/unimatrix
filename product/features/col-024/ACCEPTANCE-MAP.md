# col-024 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | When `cycle_events` contains `(cycle_start, cycle_stop)` rows for a `cycle_id`, `load_cycle_observations` returns all observations from sessions that have `topic_signal = cycle_id` AND `ts_millis` within any `(start_ts * 1000, stop_ts * 1000)` window. | test | `cargo test -p unimatrix-server load_cycle_observations_single_window` — insert cycle_events + matching observations, assert returned records equal those within the window. | PENDING |
| AC-02 | When a feature has multiple `(cycle_start, cycle_stop)` windows (e.g., resumed session), observations from all windows are included and observations between/outside windows are excluded. | test | `cargo test -p unimatrix-server load_cycle_observations_multiple_windows` — two disjoint windows, observations in window 1, in gap, and in window 2; assert only in-window observations returned, exact count verified. | PENDING |
| AC-03 | When `cycle_events` has no rows for `cycle_id`, `load_cycle_observations` returns `Ok(vec![])`. | test | `cargo test -p unimatrix-server load_cycle_observations_no_cycle_events` — assert `Ok(vec![])` return shape, not `Err(...)`. | PENDING |
| AC-04 | `context_cycle_review` invokes `load_cycle_observations` first; if non-empty it is used without calling `load_feature_observations`; if empty it falls back to `load_feature_observations`; if that is also empty it falls back to `load_unattributed_sessions` + `attribute_sessions`. | test | `cargo test -p unimatrix-server` — integration or mock test exercising each of the three branches independently; verify `load_feature_observations` is NOT called when primary path returns non-empty. | PENDING |
| AC-05 | Observations written via the `RecordEvent` path receive `topic_signal` from the session registry's in-memory `feature` when `event.topic_signal` is `None` and the session has a registered feature. | test | `cargo test -p unimatrix-server enrich_record_event_path` — register session with feature, send RecordEvent with no explicit `topic_signal`; assert stored observation has `topic_signal = registered_feature`. | PENDING |
| AC-06 | Observations written via the `ContextSearch` path receive `topic_signal` from the session registry's in-memory `feature` when `extract_topic_signal(query)` returns `None` and the session has a registered feature. | test | `cargo test -p unimatrix-server enrich_context_search_path` — ContextSearch with non-feature-ID query, session with registered feature; assert stored observation `topic_signal`. | PENDING |
| AC-07 | Observations written via the rework candidate path and the `RecordEvents` batch path receive `topic_signal` from the session registry's in-memory `feature` when `event.topic_signal` is `None` and the session has a registered feature. | test | `cargo test -p unimatrix-server enrich_rework_path` and `cargo test -p unimatrix-server enrich_record_events_batch_path` — each path tested independently; batch test uses 3-event batch, asserts all three observations carry `topic_signal`. | PENDING |
| AC-08 | When `extract_topic_signal(input)` returns `Some(signal)`, the explicit signal is stored unchanged; the registry feature is not used. When `signal` differs from the registry feature, a `tracing::debug!` is emitted with both values. | test | `cargo test -p unimatrix-server enrich_explicit_signal_unchanged` — assert return is `"bugfix-342"` not `"col-024"`; assert debug log fires with both signal values when they differ. | PENDING |
| AC-15 | `load_cycle_observations` performs a count-only pre-check before loading; returns `Ok(vec![])` for both "no cycle_events rows" and "rows exist but no topic_signal match," with the caller able to distinguish which case occurred via the structured fallback log. | test | `cargo test -p unimatrix-server load_cycle_observations_no_cycle_events_count_check` and `load_cycle_observations_rows_exist_no_signal_match` — assert `Ok(vec![])` in both cases; verify count pre-check fires before the window query. | PENDING |
| AC-09 | Features predating `cycle_events` (no rows in the table for that cycle_id) continue to work via the existing `load_feature_observations` + `load_unattributed_sessions` path without behavioral change. | test | `cargo test -p unimatrix-server` — existing `context_cycle_review` tests pass without modification; no test deletions or changes permitted. | PENDING |
| AC-10 | `load_cycle_observations` is declared on the `ObservationSource` trait in `unimatrix-observe/src/source.rs` and implemented on `SqlObservationSource`; all existing trait tests remain green. | test | `cargo test -p unimatrix-observe && cargo test -p unimatrix-server` — full test suite passes with no compilation errors from the new trait method. | PENDING |
| AC-11 | Unit tests in `services/observation.rs` cover AC-01, AC-02, and AC-03 using `insert_cycle_event` for cycle_events rows. | test | `cargo test -p unimatrix-server` — new tests in `services/observation.rs` appear in output; must use `SqlxStore::insert_cycle_event` (not raw SQL) for cycle_events fixtures. | PENDING |
| AC-12 | All existing `context_cycle_review` tests pass unmodified, confirming backward compatibility. | test | `cargo test -p unimatrix-server` — no test deletions or modifications to existing `context_cycle_review` tests; full suite green. | PENDING |
| AC-13 | The timestamp unit conversion uses a named helper or constant — no raw `* 1000` literal appears in query-construction code for window boundary binding. | grep | `grep -n '\* 1000' crates/unimatrix-server/src/services/observation.rs` — must return zero matches within the `load_cycle_observations` implementation block. | PENDING |
| AC-14 | When `load_cycle_observations` returns empty and the fallback to `load_feature_observations` activates, a `tracing::debug!` log line is emitted naming the `feature_cycle` value. | test | `cargo test -p unimatrix-server` — test using `tracing_test` or log-capture fixture: call `context_cycle_review` for a feature with no `cycle_events` rows; assert DEBUG-level log line contains `feature_cycle` value and message substring `"primary path empty"`. | PENDING |
