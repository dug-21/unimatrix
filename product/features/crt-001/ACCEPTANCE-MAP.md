# crt-001 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | FEATURE_ENTRIES multimap table exists and is created on Store::open() | test | Unit test: open store, verify FEATURE_ENTRIES table is accessible via begin_read().open_multimap_table(FEATURE_ENTRIES) | PENDING |
| AC-02 | helpful_count: u32 and unhelpful_count: u32 fields added to EntryRecord with serde(default), appended after trust_source | test | Unit test: serialize/deserialize roundtrip with helpful_count=42, unhelpful_count=7; verify field position after trust_source | PENDING |
| AC-03 | Schema migration v1->v2 runs on Store::open(), backfilling helpful_count=0 and unhelpful_count=0 | test | Integration test: create v1 database (24-field entries), open with crt-001 code, verify helpful_count=0 and unhelpful_count=0 on all entries; verify schema_version=2 | PENDING |
| AC-04 | access_count incremented at most once per (agent_id, entry_id) per session | test | Integration test: call filter_access twice with same (agent, entry), verify second call returns empty; verify Store::record_usage increments access_count only for non-deduped IDs | PENDING |
| AC-05 | last_accessed_at set to current unix timestamp on every retrieval (no dedup) | test | Integration test: call record_usage twice for same entry, verify last_accessed_at updated both times (or same second); verify all_ids always updates last_accessed_at regardless of access_ids | PENDING |
| AC-06 | helpful=true increments helpful_count (once per session per agent per entry) | test | Integration test: call check_votes then record_usage with helpful_ids=[42], verify helpful_count=1; second call with same agent+entry returns NoOp, verify helpful_count still 1 | PENDING |
| AC-07 | helpful=false increments unhelpful_count; helpful=None changes neither | test | Integration test: three cases -- helpful=false (unhelpful_count=1), helpful=None (both=0), helpful=true then helpful=false same entry (vote corrected: helpful_count=0, unhelpful_count=1) | PENDING |
| AC-08 | FEATURE_ENTRIES populated when feature param provided AND agent trust level >= Internal | test | Integration test: Internal agent calls record_feature_entries("crt-001", [1,2,3]), read multimap, verify all 3 entry IDs present under "crt-001"; Restricted agent's feature param does not write | PENDING |
| AC-09 | FEATURE_ENTRIES does not create duplicate pairs (multimap idempotency) | test | Integration test: call record_feature_entries("crt-001", [1]) twice, verify only one entry in multimap for key "crt-001" with value 1 | PENDING |
| AC-10 | context_briefing updates usage once per entry in final result | test | Integration test: create entry matching both role lookup and task search, call context_briefing, verify access_count=1 (not 2) | PENDING |
| AC-11 | EntryStore trait extended with record_access method (object-safe, batch) | test | Compile test: `fn _check(_: &dyn EntryStore) {}` and `fn _check(_: Arc<dyn EntryStore>) {}` must compile; runtime test: call through async wrapper | PENDING |
| AC-12 | AuditLog::write_count_since returns count of write operations by agent_id since timestamp | test | Unit test: log mix of write and read audit events, call write_count_since, verify only write operations counted; verify agent_id filtering; verify timestamp boundary | PENDING |
| AC-13 | Existing retrieval behavior unchanged | test | Regression: run all existing tool tests from vnc-002/vnc-003 without modification; verify same results, same formats | PENDING |
| AC-14 | All usage updates atomic in single write transaction | test | Unit test: call record_usage with multiple entries including decrement IDs, verify all are updated atomically (batch atomicity); verify partial batch with deleted entries handles gracefully | PENDING |
| AC-15 | UsageDedup tracks (agent_id, entry_id) per session, not persisted | test | Unit test: verify filter_access returns correct subsets; verify check_votes returns correct VoteActions (NewVote, CorrectedVote, NoOp); verify vote_recorded uses HashMap; verify no dedup-related tables in redb after operations | PENDING |
| AC-16 | Vote correction: when an agent changes its vote on an entry within a session, the old counter is decremented (saturating at 0) and the new counter is incremented. Net effect is a vote flip, not inflation. | test | Integration test: (1) vote helpful=false then helpful=true on same entry, verify helpful_count=1, unhelpful_count=0; (2) vote helpful=true twice, verify helpful_count=1 (no change); (3) vote helpful=true then helpful=false, verify helpful_count=0, unhelpful_count=1; (4) saturating subtraction when counter already 0 | PENDING |
| AC-17 | Restricted agents' feature parameters are silently ignored. Only Internal or higher trust levels write to FEATURE_ENTRIES. | test | Integration test: (1) Restricted agent retrieves with feature="test-feature", verify FEATURE_ENTRIES empty for "test-feature"; (2) Internal agent retrieves with feature="test-feature", verify FEATURE_ENTRIES has entries; (3) Privileged agent retrieves, verify FEATURE_ENTRIES has entries; (4) Restricted agent's retrieval results unchanged despite feature param being ignored | PENDING |
| AC-18 | All new code has unit tests; integration tests verify end-to-end retrieval-to-usage-update flow including dedup and vote correction behavior | test | Test count verification: new tests > 0 for each component; end-to-end test: retrieve entry via tool, verify access_count incremented, helpful_count incremented, FEATURE_ENTRIES populated (for Internal+ agent), vote correction works end-to-end | PENDING |
