# nxs-010 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `topic_deliveries` table exists in schema v11 with 9 columns (topic PK, created_at, completed_at, status, github_issue, total_sessions, total_tool_calls, total_duration_secs, phases_completed) | test | Unit test: open v11 db, `pragma_table_info('topic_deliveries')` returns 9 columns with correct names and types | PENDING |
| AC-02 | `query_log` table exists in schema v11 with 9 columns (query_id PK AUTOINCREMENT, session_id, query_text, ts, result_count, result_entry_ids, similarity_scores, retrieval_mode, source) | test | Unit test: open v11 db, `pragma_table_info('query_log')` returns 9 columns with correct names and types | PENDING |
| AC-03 | `query_log` has indexes on session_id and ts | test | Unit test: `pragma_index_list('query_log')` returns indexes including idx_query_log_session and idx_query_log_ts | PENDING |
| AC-04 | Migration from v10 to v11 creates both tables and backfills `topic_deliveries` from existing attributed sessions | test | Integration test: create v10 db with 3 topics (varying session counts/durations), run migration, verify topic_deliveries rows match expected aggregates (total_sessions, total_duration_secs, created_at = MIN(started_at)) | PENDING |
| AC-05 | Migration is idempotent -- running on a v11 database is a no-op | test | Integration test: run migration twice on same db, verify no errors and no duplicate rows in topic_deliveries | PENDING |
| AC-06 | Migration handles empty sessions table (no attributed sessions) without error | test | Integration test: create v10 db with no sessions (or no attributed sessions), run migration, verify 0 topic_deliveries rows and schema_version = 11 | PENDING |
| AC-07 | `Store::upsert_topic_delivery` inserts a new row or replaces an existing row | test | Unit test: insert topic, verify; upsert same topic with changed fields, verify update applied | PENDING |
| AC-08 | `Store::get_topic_delivery` returns None for nonexistent topics | test | Unit test: query nonexistent topic, assert returns None | PENDING |
| AC-09 | `Store::update_topic_delivery_counters` atomically increments counters on an existing row | test | Unit test: insert row with known values, call update with deltas (+3, +5, +100), verify new values equal original + delta. Also verify error returned for nonexistent topic. | PENDING |
| AC-10 | `Store::insert_query_log` writes a row with auto-allocated query_id | test | Unit test: insert row with query_id=0, read back via scan, assert query_id > 0 and monotonically increasing across inserts | PENDING |
| AC-11 | `Store::scan_query_log_by_session` returns all query_log rows for a given session_id, ordered by ts ASC | test | Unit test: insert 3 rows for same session with ts=300,100,200, verify returned in order ts=100,200,300 | PENDING |
| AC-12 | UDS `handle_context_search` writes a `query_log` row after every successful search (fire-and-forget; search latency unaffected) | test | Integration test: invoke UDS search, allow async write to complete, query query_log table, verify row exists with source="uds" and retrieval_mode="strict" | PENDING |
| AC-13 | MCP `SearchService::search` caller writes a `query_log` row after every successful search (fire-and-forget) | test | Integration test: invoke MCP search tool, allow async write to complete, query query_log table, verify row exists with source="mcp" and retrieval_mode="flexible" | PENDING |
| AC-14 | `query_log.result_entry_ids` is a JSON array of entry ID integers | test | Unit test: insert row with result_entry_ids from serde_json::to_string(&vec![1u64, 2, 3]), read back, deserialize as Vec<u64>, verify [1, 2, 3] | PENDING |
| AC-15 | `query_log.similarity_scores` is a JSON array of f64 values, parallel to result_entry_ids | test | Unit test: insert row with 3 entry IDs and 3 scores, read back, verify lengths match and values round-trip correctly | PENDING |
| AC-16 | `query_log.source` is "uds" for hook-triggered searches and "mcp" for tool-invoked searches | test | Covered by AC-12 (source="uds") and AC-13 (source="mcp") integration tests | PENDING |
| AC-17 | `query_log.retrieval_mode` is "strict" or "flexible" matching the search path | test | Covered by AC-12 (retrieval_mode="strict") and AC-13 (retrieval_mode="flexible") integration tests | PENDING |
| AC-18 | Backfill computes `total_sessions` and `total_duration_secs` from existing session data | test | Covered by AC-04 integration test which verifies aggregate values match expected computation | PENDING |
| AC-19 | Backfill sets `status = 'completed'` for historically attributed topics | test | Covered by AC-04 integration test which asserts status = 'completed' on all backfilled rows | PENDING |
| AC-20 | All existing unit and integration tests continue to pass (no regressions) | shell | `cargo test --workspace` passes with 0 failures | PENDING |
