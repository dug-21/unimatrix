# vnc-038 Acceptance Criteria Map

> Every AC from SCOPE.md (AC-01..AC-13, including folded-in #735 carry-items AC-11/12/13), pinned to an existing test surface per SPECIFICATION.md and the Risk/Test Strategy. Status PENDING until verified in Session 2.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | No request resolves under a default/no-slug identity; the `/v1/tools/...` alias and no-slug `/v1` target no longer resolve a servable project; attempt fails loud. | test | Seam/funnel test (`http/router/seam.rs`): assert `parse_project_key` no longer maps `/v1/tools/...` or the `_` arm to a servable `Default`; assert the unified resolver returns an error (no servable store) for a no-slug path. (R-07, R-10) | PENDING |
| AC-02 | First project comes up via the single `register <slug>` — creates per-slug data dir AND writes `[[projects]]`, no hand-edit; restart applies. | test | Project-lifecycle fixture: `register <slug>` from clean state → assert data dir + genesis store created AND `[[projects]]` written; assert no instruction string printed; assert routable after a boot-time `[[projects]]` re-read. (R-06) | PENDING |
| AC-03 | `register <slug>` writes routing intent rather than printing instructions; routable after restart. | test | Same fixture as AC-02: assert the `projects.rs:334-335` instruction print is gone, replaced by an atomic `[[projects]]` write; assert boot reads it and the slug resolves. (R-06) | PENDING |
| AC-04 | Onboarding the Nth project is the IDENTICAL `register <slug>` command, no config edit, restart-to-apply. | test | Project-lifecycle fixture at N=2: register a second slug via the same command path; assert second `[[projects]]` entry written and second store created with no manual edit; both slugs routable after restart. | PENDING |
| AC-05 | Bundle carries server-composed MCP and observe URLs; decoded bundle yields finished URLs posted verbatim; client appends no slug / composes no path; `v:2` strict-schema rejects on both Rust and JS sides; parity corpus updated. | test | Bundle codec parity corpus (extended for `v:2`): Rust-encode → JS-decode equality (byte-for-byte); strict-reject matrix (missing/extra/wrong-type key, malformed URL, unknown major version) on both sides; invariant test asserting the client posts the bundle's URLs byte-for-byte. (R-01, R-03, R-04) | PENDING |
| AC-06 | With two registered projects, an MCP request and an observe request each resolve to exactly one project's store; no path lets B reach A. **N=2 proof required.** | test | Seam/funnel test at N=2 (not N=1, #4974): counting/recording resolver asserts each MCP and each observe request consults the resolver once with the transport-derived `ProjectKey` and resolves the matching store; assert no boot-bound or parallel observe path exists. (R-02, R-09; C-11 / GATE-4) | PENDING |
| AC-07 | End-to-end: bundle → `init --bundle` → init-time Ping/observe returns 200 over the real per-slug route (the #766 repro passes). | test | The #766 repro: drive `init --bundle <v:2 bundle>`; assert the init-time Ping posts to the bundle's observe URL and the real `/v1/{slug}/observe` route returns 200 (was 404). (R-12) | PENDING |
| AC-08 | Runtime hook telemetry posts to a reachable per-slug observe route and is accepted (the #766 wider blast radius). | test | Hook-client transport test: a runtime hook event posts to the bundle's observe URL; assert the per-slug observe route accepts it (200) and resolves to the bundle's project store. (R-12) | PENDING |
| AC-09 | Hard cut (CLOUD/CONTAINER-HTTP ONLY): a no-slug cloud/container HTTP deployment is no longer valid; first boot serves nothing until a project is registered, failing loud with an actionable "register a project to begin" message; no silent default; no path-hash adoption logic. Applies ONLY to cloud/container HTTP (currently zero deployments); local STDIO/UDS unaffected (see AC-10). | test | First-boot test on the served-project model: from empty state assert no servable store exists, every request fails loud with the actionable message, no adopt/derive code path runs; assert no local-transport code path (STDIO `main.rs:1158` / UDS `main.rs:859`) is touched by the cutover. (R-10; GATE-2 residual = human check: confirm zero existing cloud/container-HTTP served deployments before cut.) | PENDING |
| AC-10 | Local single-project UDS/STDIO / path-hash install (ADR-004) continues unchanged — keeps its **direct path-hash store binding, untouched by the HTTP resolver**, not required to take a manual slug; cutover does not apply. **GATE-2 confirmed.** Delivery must NOT route local through the unified resolver. | test | Local-UDS/STDIO fixture (existing): assert the path-hash store still resolves directly over UDS/STDIO with no slug supplied and NOT through the unified resolver, behavior unchanged from ADR-004; assert delivery did not add a resolver-key path for local. (R-13, the load-bearing C-13 / GATE-2 guard) | PENDING |
| AC-11 | First-boot bearer token is NOT emitted to stdout or logs (`http/token.rs:101` redacted/gated); the remote client obtains the token via the validated `v:2` client bundle — cloud HTTPS posture (#735 CI-1, NFR-06). | test | First-boot token-surface test on the HTTP/cloud first-boot path: capture stdout AND `tracing` output, assert no token substring appears; assert the emitted `v:2` bundle carries the token; assert local token affordance unchanged (deployment-context gating). (R-14) | PENDING |
| AC-12 | After the route-grammar rewrite, `crates/unimatrix-server/src/http/router.rs` is at or under the 500-line guideline (#735 CI-2, NFR-09). | file-check | Line-count check on `crates/unimatrix-server/src/http/router.rs` post-rewrite: assert file length ≤ 500 lines. (R-15) | PENDING |
| AC-13 | The stale module-level `#![allow(dead_code)]` and "until wiring lands" comment at `crates/unimatrix-server/src/http/public_url.rs:19` are removed (#735 CI-3). | grep | Absence check on `crates/unimatrix-server/src/http/public_url.rs`: assert no `#![allow(dead_code)]` and no "until wiring lands" comment remain. (R-15) | PENDING |
