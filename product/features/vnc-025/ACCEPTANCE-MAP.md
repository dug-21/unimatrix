# vnc-025 Acceptance Criteria Map

AC-IDs verbatim from SCOPE.md; verification bindings from SPECIFICATION.md and RISK-TEST-STRATEGY.md.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | UDS direct-dispatch `transcript_delta` for a registered session merges `{offset, bytes}` into that session's buffer and returns `Ack`; buffer content equals streamed bytes | test | Integration test: direct dispatch to the single-event arm; assert `Ack` + buffer bytes via `contiguous_tail`/test accessor | PENDING |
| AC-02 | Out-of-order/duplicate/overlapping deltas converge to identical buffer content regardless of arrival order; `transcript_high_water` = max(offset+len) seen. Under overflow (per ADR-002 / Variance 1): tail-window equivalence, below-floor deltas are defined no-ops | test | Unit/property permutation tests on `apply_delta` (R-01 harness, ass-069 PoC fixtures): below-cap → full-content equality across orders; cap-crossing → final `contiguous_tail` identical across orders (R-03) | PENDING |
| AC-03 | Delta for an unregistered session is a silent no-op: `Ack`, no registry slot created, no other session's buffer affected | test | Integration test: dispatch to unknown session; assert registry size and other buffers unchanged; assert no allocation before registry check | PENDING |
| AC-04 | Malformed `transcript_delta` payload still returns `Ack`; no `Error` reaches the client; no payload content in logs | test | Integration test with log capture: malformed + well-formed deltas carrying a sentinel string; assert `Ack` and sentinel absent from captured tracing output (R-05.2) | PENDING |
| AC-05 | No delta — single/batch, UDS/HTTP — ever produces a durable observation row or disk write; mixed batch persists only non-delta events | test | vnc-024 AC-12 zero-rows test runs **unmodified** with buffer active + new mixed-batch test asserting row counts AND row content (delta bytes in no persisted column) on both transports (R-04) | PENDING |
| AC-06 | HTTP `/observe` merges deltas into the `http-`prefixed session's buffer; `prefix_session_id` preserves `event_type` (single + batch); `SessionWrite` still enforced | test | Per-transport pre-dispatch transform tests + one shared-arm direct-dispatch proof (pattern #4725); bearer/capability rejection test before dispatch (R-12) | PENDING |
| AC-07 | Accumulated buffer never exceeds `transcript_buffer_max_bytes`; overflow keeps tail (ring-tail), records dropped-byte count as metadata, `high_water` monotonic | test | Unit test: drive past cap; assert size bound, tail content, `elided_bytes` counter, `high_water` monotonicity; assert no marker bytes in content (R-03) | PENDING |
| AC-08 | `drain_and_signal_session` + `sweep_stale_sessions` free the buffer and emit content-free `transcript_session_purged` (session_id, byte count, timestamp) when non-empty | test | Integration tests at both purge points; assert audit row fields, sentinel-free content, no emission for empty buffers; mandatory silently-evicted-session case (empty `injection_history`, non-empty buffer → purge record + audit row, R-08.1) | PENDING |
| AC-09 | `context_cycle_review` clears buffers of sessions attributed to the reviewed `feature_cycle` (empty afterward, still registered), emits purge audit, gated on `PurgeOnCycleClose` read from config; review output otherwise unchanged | test | Integration test through the tool handler with mixed registry (matching / other / `None` feature); snapshot-compare review output pre/post (R-10) | PENDING |
| AC-10 | `get_state()` and hot-path registry reads do not deep-copy transcript bytes | file-check | Structural review: `SessionState.transcript: Arc<Mutex<TranscriptBuffer>>` in `infra/session.rs` (ADR-001 — clone is 8 bytes + refcount); optionally a clone-cost guard test | PENDING |
| AC-11 | Non-empty buffer: `CompactPayload` returns `BriefingContent` with server-built transcript-tail block prepended; empty buffer: byte-identical to pre-vnc-025 (no double-prepend) | test | Golden parity test (ADR-005, #3426): expected = `extract_transcript_block(path)` on fixture JSONL; actual = same bytes streamed as shuffled+duplicated deltas → `extract_transcript_block_from_bytes(contiguous_tail(...))`; byte-for-byte equality. Plus empty-buffer byte-identity snapshot (R-09.4) | PENDING |
| AC-12 | Raw transcript bytes never reach disk: no SQL write, no file write, no log line carries buffer content | grep | Static gate: grep `tracing` calls in `infra/session_transcript.rs` + `uds/transcript_block.rs` for content-bearing values; verify no `Display` impl, no content-bearing `Result`, manual metadata-only `Debug`; plus sentinel dynamic tests (R-05 — both, not either). Code-review hard gate | PENDING |
| AC-13 | `cargo audit` passes; no new runtime dependency | shell | `cargo audit` run + `Cargo.toml` diff review (no new runtime deps) | PENDING |

## Supplementary verification (NFR-09, ADR-008 — no AC-ID in SCOPE but a hard spec requirement)

| Item | Verification Method | Verification Detail | Status |
|------|--------------------|--------------------|--------|
| Never-panics contract | test | Fuzz-ish randomized (offset, len) no-panic test incl. near-`u64::MAX` offsets (R-02); `apply_delta(u64::MAX - 10, ...)` = silent drop-whole, no state change | PENDING |
| Poison recovery | test | Explicit poisoned-mutex test: poison via panicking closure helper; assert merge-resumes-empty with `Ack`, PreCompact degrades to empty-buffer path (R-06.2) | PENDING |
| Arithmetic review gate | grep | No bare `unwrap()` on the buffer mutex; no unchecked offset arithmetic; no raw `offset as usize` in the new module | PENDING |
