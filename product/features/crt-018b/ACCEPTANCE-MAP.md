# crt-018b Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `EffectivenessState` is written by the background tick loop in `background.rs` after each `compute_report()` call using the `EffectivenessReport` already present in `StatusReport.effectiveness`. `context_status` MCP calls do NOT write to `EffectivenessState`. | test | Integration test: call `context_status` N times with a known Ineffective-producing fixture; assert `EffectivenessState.consecutive_bad_cycles` remains 0 for all entries; assert `categories` map remains empty until background tick fires; after tick fires, confirm map is populated | PENDING |
| AC-02 | `SearchService::search()` snapshots `EffectivenessState.categories` under a short read lock at the top of the search pipeline, before embedding computation or SQL execution, using the same pattern as `confidence_weight` in crt-019. | test | Code review of `search.rs` snapshot placement; unit test confirming utility delta is applied to all four `rerank_score` call sites | PENDING |
| AC-03 | `UTILITY_BOOST`, `SETTLED_BOOST`, and `UTILITY_PENALTY` are defined as public `f64` constants in `unimatrix-engine::effectiveness`. Default values: `UTILITY_BOOST = 0.05`, `SETTLED_BOOST = 0.01`, `UTILITY_PENALTY = 0.05`. | test | Unit test asserts constant values; assertion in test suite verifies `SETTLED_BOOST < 0.03` (co-access max invariant) | PENDING |
| AC-04 | All four `rerank_score` call sites in `search.rs` (Steps 7 and 8) apply the utility delta: `+UTILITY_BOOST` for Effective, `+SETTLED_BOOST` for Settled, `-UTILITY_PENALTY` for Ineffective or Noisy, `0.0` for Unmatched or absent classification. | test | Unit tests exercise each category value against the scoring function and assert correct delta; Deprecated and Superseded entries with non-zero utility delta have numeric assertions verifying the delta is inside the penalty multiplication | PENDING |
| AC-05 | An Effective entry with sim=0.75, conf=0.60 ranks above an Ineffective entry with sim=0.76, conf=0.60, all else equal (same `confidence_weight`, no co-access or provenance adjustment). | test | Unit test with fixed inputs (`confidence_weight` at both 0.15 and 0.25) asserts result ordering | PENDING |
| AC-06 | An entry with no record in `EffectivenessState.categories` (e.g., newly inserted, not yet seen by background tick) receives `utility_delta = 0.0`. No panic, no default-to-penalty. | test | Unit test calling `utility_delta(None)` asserts exactly `0.0`; unit test running `search()` against empty `EffectivenessState` confirms result ordering identical to pre-crt-018b scoring | PENDING |
| AC-07 | `BriefingService` accepts `EffectivenessStateHandle` as a required constructor parameter (not `Option<EffectivenessStateHandle>`). Injection history sort uses effectiveness category as tiebreaker: same confidence → Effective ranks above Ineffective/Noisy. | test | Unit test with entry A (confidence=0.90, Ineffective) and entry B (confidence=0.40, Effective): assert A ranks first (primary key respected); unit test with A (confidence=0.60, Ineffective) and B (confidence=0.60, Effective): assert B ranks first (tiebreaker activated) | PENDING |
| AC-08 | Convention lookup sort in briefing uses effectiveness as tiebreaker when feature-sort does not differentiate entries: same confidence, non-feature-tagged → Effective before Ineffective. | test | Unit test asserts ordering of convention entries with identical confidence and no feature tag; same two-key sort check repeated for convention lookup path | PENDING |
| AC-09 | `EffectivenessState.consecutive_bad_cycles` increments for entries classified Ineffective or Noisy on a background tick write; resets to 0 for entries reclassified to any other category; is not incremented by `context_status` calls; is not incremented when `compute_report()` returns an error (hold-on-error semantics). | test | Unit test simulates three tick writes with known category sequences (Ineffective, Effective, Ineffective) and asserts counter values after each write: counter=1, counter=0, counter=1 | PENDING |
| AC-10 | When `consecutive_bad_cycles[entry_id] >= AUTO_QUARANTINE_CYCLES` (and `AUTO_QUARANTINE_CYCLES > 0`), `maintenance_tick()` calls the store quarantine path for that entry with `agent_id = "system"` and a reason string that includes the cycle count. | test | Integration test: seed entry as Ineffective-yielding, simulate N background ticks (N = configured threshold), confirm entry status becomes Quarantined after tick N | PENDING |
| AC-11 | `AUTO_QUARANTINE_CYCLES` is read from `UNIMATRIX_AUTO_QUARANTINE_CYCLES` env var at startup, defaulting to 3. Setting to any positive integer N requires exactly N consecutive bad ticks before triggering. | test | Integration test with `UNIMATRIX_AUTO_QUARANTINE_CYCLES=2`: confirm quarantine on second consecutive bad tick, not first | PENDING |
| AC-12 | Setting `UNIMATRIX_AUTO_QUARANTINE_CYCLES=0` disables auto-quarantine entirely. No entries are quarantined regardless of classification or cycle count. | test | Unit test with counter at 100 and threshold 0 confirms no quarantine call is issued; entry with Settled/Unmatched category at `consecutive_bad_cycles = 10` also confirms no quarantine | PENDING |
| AC-13 | Auto-quarantine writes an audit event containing `operation = "auto_quarantine"`, `agent_id = "system"`, `entry_title`, `entry_category`, `classification`, `consecutive_cycles`, `threshold`, and `reason` string. | test | Integration test reads the audit log after auto-quarantine fires and asserts all required fields are present with correct values; also asserts `auto_quarantined_this_cycle` in `context_status` response contains the quarantined entry's ID | PENDING |
| AC-14 | Auto-quarantine only fires for entries classified Ineffective or Noisy. Settled and Unmatched entries are never auto-quarantined regardless of counter value. | test | Unit test with Settled entry at `consecutive_bad_cycles = 10` confirms no quarantine triggered; unit test with Unmatched entry at `consecutive_bad_cycles = 10` confirms no quarantine; integration test: entry transitions Ineffective (2 cycles) → Settled (1 cycle): counter resets, no quarantine | PENDING |
| AC-15 | If an entry is already Quarantined when a background tick runs, it does not appear in the active entry set fed to `load_entry_classification_meta`. Its counter is not incremented and is removed from `consecutive_bad_cycles` on the next tick write that omits it. | test | Unit test with pre-quarantined entry confirms its ID is absent from the counter map after one tick | PENDING |
| AC-16 | Unit tests cover: delta for all five `EffectivenessCategory` values, delta for absent entry (0.0), Effective-vs-Ineffective ordering at close similarity values, `SETTLED_BOOST < 0.03` constant invariant. | test | Tests in `unimatrix-engine::effectiveness` module or `search.rs` test module; `cargo test` on `unimatrix-engine` must show all passing | PENDING |
| AC-17 | Integration tests cover: (1) background tick with known injection/session data produces correct utility deltas in search result ordering; (2) briefing injection history orders Effective above Ineffective at same confidence; (3) auto-quarantine fires after N consecutive background ticks with bad classification; (4) crt-019 confidence spread is non-zero in the test fixture (prerequisite check confirming adaptive weight dependency is exercised). | test | Tests in existing integration test infrastructure using `TestDb` helper; at least one test must run with fixture where `observed_spread >= 0.20` | PENDING |
| AC-18 | All existing calibration, effectiveness classification, and re-ranking pipeline tests pass without modification. | shell | `cargo test -p unimatrix-engine -p unimatrix-store -p unimatrix-server` exits 0 with no previously-passing tests failing | PENDING |
