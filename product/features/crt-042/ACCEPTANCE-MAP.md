# crt-042 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-00 | Prerequisite gate: before any Phase 0 implementation code is written, confirm S1/S2 Informs edge directionality in deployed GRAPH_EDGES (bidirectional or single-direction). If single-direction, file back-fill issue before crt-042 ships. | shell | `SELECT source_id, target_id FROM GRAPH_EDGES WHERE relation_type = 'Informs' LIMIT 100` — check for symmetric reverse pairs; document result in PR | PENDING |
| AC-01 | When `ppr_expander_enabled = false` (default), all existing search integration tests pass with no output differences — bit-for-bit identical to pre-crt-042 behavior. | test | `cargo test --package unimatrix-server -- search` — full suite must pass with no diff vs. baseline | PENDING |
| AC-02 | When `ppr_expander_enabled = true` and HNSW returns seed set containing entry B, `graph_expand` is called with B's entry ID before the PPR personalization vector is computed. | test | Unit/integration test asserting Phase 1 receives `results_with_scores` slice containing entries sourced from graph traversal | PENDING |
| AC-03 | Given seed entry B and a graph edge B→A of type CoAccess, Supports, Informs, or Prerequisite — entry A appears in the `graph_expand` return set. Must hold for each of the four positive edge types independently. | test | Four unit tests, one per positive edge type, each constructing a minimal two-node graph with one seed and asserting the neighbor appears in the result | PENDING |
| AC-04 | Given seed entry B and a graph edge C→B (B is the target, C is the source, no reverse edge) — entry C does NOT appear in the `graph_expand` return set unless C is reachable via a forward edge from another seed. | test | Unit test with seed {B}, edge C→B only; assert `graph_expand` returns empty (no backward traversal) | PENDING |
| AC-05 | Given seeds {B} and graph path B→A→D (two hops, positive edges) with `expansion_depth = 2` — both A and D appear in the `graph_expand` return set. | test | Unit test with two-hop chain B→A→D, depth=2, seed={B}; assert result == {A, D} | PENDING |
| AC-06 | Given seeds {B} and graph path B→A→D with `expansion_depth = 1` — A appears in the return set, D does not. | test | Unit test with same two-hop chain, depth=1, seed={B}; assert result == {A} | PENDING |
| AC-07 | Given seed entry B and a graph edge B→X of type Supersedes or Contradicts — entry X does NOT appear in the `graph_expand` return set. | test | Two unit tests (one Supersedes, one Contradicts); assert result is empty for each excluded edge type | PENDING |
| AC-08 | No seed entry ID appears in the `graph_expand` return set, even if the graph contains an edge that creates a path from one seed back to another seed. | test | Unit test with seeds {A, B} and edge A→B; assert neither A nor B appears in result | PENDING |
| AC-09 | When BFS traversal reaches exactly `max_candidates` entries, `graph_expand` returns a set of that size and stops. | test | Unit test with a graph containing more than `max_candidates` reachable entries; assert `result.len() == max_candidates` | PENDING |
| AC-10 | `graph_expand` returns an empty set when `seed_ids` is empty. | test | Unit test: `graph_expand(&graph, &[], 2, 200)` — assert result is empty | PENDING |
| AC-11 | `graph_expand` returns an empty set when the graph has no nodes. | test | Unit test: call `graph_expand` with an empty `TypedRelationGraph`; assert result is empty | PENDING |
| AC-12 | `graph_expand` returns an empty set when `depth = 0`. | test | Unit test: `graph_expand(&graph, &[seed_id], 0, 200)` — assert result is empty | PENDING |
| AC-13 | When Phase 0 expansion surfaces entry Q whose status is quarantined, Q does not appear in `results_with_scores`. No warning or error is logged for the skip. | test | Integration test constructing a graph-reachable entry with quarantined status; assert Q is absent from final result set with expander enabled | PENDING |
| AC-14 | A unit/integration test constructs a scenario where a graph-reachable entry has quarantined status. The test asserts Q is absent from the final result set with the expander enabled. | test | Fixture with seed B → quarantined Q; `ppr_expander_enabled = true`; assert Q not in `results_with_scores` | PENDING |
| AC-15 | When Phase 0 expansion surfaces an entry with no stored embedding (vector lookup returns None), that entry is silently skipped and does not appear in `results_with_scores`. | test | Unit test where `vector_store.get_embedding()` returns None for an expanded entry; assert entry absent from results | PENDING |
| AC-16 | No call to `.edges_directed()` or `.neighbors_directed()` appears in `graph_expand.rs`. All traversal is performed via `edges_of_type()`. | grep | `grep -n 'edges_directed\|neighbors_directed' crates/unimatrix-engine/src/graph_expand.rs` — assert zero matches | PENDING |
| AC-17 | A TOML file that omits `ppr_expander_enabled`, `expansion_depth`, and `max_expansion_candidates` entirely loads without error and produces default values (`false`, `2`, `200`). | test | Config unit test: parse minimal TOML without expander fields; assert `InferenceConfig::default()` values for all three fields | PENDING |
| AC-18 | `InferenceConfig::validate()` returns an error for `expansion_depth = 0` regardless of the value of `ppr_expander_enabled`. | test | Config validation unit test: `expansion_depth = 0`, `ppr_expander_enabled = false`; assert validate() returns Err | PENDING |
| AC-19 | `InferenceConfig::validate()` returns an error for `expansion_depth = 11` regardless of the value of `ppr_expander_enabled`. | test | Config validation unit test: `expansion_depth = 11`, `ppr_expander_enabled = false`; assert validate() returns Err | PENDING |
| AC-20 | `InferenceConfig::validate()` returns an error for `max_expansion_candidates = 0` regardless of the value of `ppr_expander_enabled`. | test | Config validation unit test: `max_expansion_candidates = 0`, `ppr_expander_enabled = false`; assert validate() returns Err | PENDING |
| AC-21 | `InferenceConfig::validate()` returns an error for `max_expansion_candidates = 1001` regardless of the value of `ppr_expander_enabled`. | test | Config validation unit test: `max_expansion_candidates = 1001`, `ppr_expander_enabled = false`; assert validate() returns Err | PENDING |
| AC-22 | `product/research/ass-037/harness/profiles/ppr-expander-enabled.toml` exists and `run_eval.py --profile ppr-expander-enabled.toml` executes to completion without error. | file-check | File exists at `product/research/ass-037/harness/profiles/ppr-expander-enabled.toml`; then `python run_eval.py --profile ppr-expander-enabled.toml` exits 0 | PENDING |
| AC-23 | With `ppr_expander_enabled = true`, eval run produces MRR >= 0.2856 (no regression from baseline). P@5 is measured and recorded; any value above 0.1115 is evidence the expander functions. Eval result recorded as feature completion evidence. | shell | Run eval harness with `ppr-expander-enabled.toml`; extract MRR and P@5 from aggregated output; assert MRR >= 0.2856; record P@5 in PR description | PENDING |
| AC-24 | When `ppr_expander_enabled = true`, every search invocation emits a `debug!` log line from Phase 0 containing: seed count, raw expanded count, final added count, and elapsed milliseconds. | test | Test with a `tracing` test subscriber capturing debug events; assert one event with fields `expanded_count`, `fetched_count`, `elapsed_ms`, `expansion_depth`, `max_expansion_candidates` is emitted per search invocation | PENDING |
| AC-25 | Integration test: with `ppr_expander_enabled = true`, an entry whose embedding is dissimilar to the query (would not appear in HNSW k=20) but is connected by a positive graph edge to an HNSW seed appears in the final result set. With `ppr_expander_enabled = false`, the same entry is absent from results. | test | Integration test constructing a controlled graph fixture; run search with flag on and off; assert cross-category entry present only when flag is on | PENDING |
