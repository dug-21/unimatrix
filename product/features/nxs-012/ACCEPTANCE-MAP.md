# nxs-012 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `export` emits `graph_edges` rows with 9 columns (excluding `id`) as JSONL lines with `_table: "graph_edges"` | test | Unit test: export populated graph_edges, parse output, verify 9 data fields and `_table` discriminator; confirm no `id` field | PENDING |
| AC-02 | `export` emits `observations` rows with all 10 columns (including `id`) as JSONL lines with `_table: "observations"` | test | Unit test: export populated observations, parse output, verify 10 fields including `id` | PENDING |
| AC-03 | `export` emits `cycle_events` rows with 9 columns (excluding `goal_embedding`) as JSONL lines with `_table: "cycle_events"` | test | Unit test: export populated cycle_events with goal_embedding data, parse output, verify 9 fields and goal_embedding absent or null | PENDING |
| AC-04 | Export header `format_version` is 2 | test | Unit test: parse JSONL header line, assert `format_version == 2` | PENDING |
| AC-05 | `import` accepts `format_version` 1 (legacy) without error — new table types simply absent | test | Integration test: import v1 file with 8 table types, assert success, verify 0 counts for graph_edges/observations/cycle_events | PENDING |
| AC-06 | `import` accepts `format_version` 2 and ingests all 11 table types | test | Integration test: import v2 file with all 11 table types populated, verify non-zero counts for all tables | PENDING |
| AC-07 | `import` rejects `format_version` 3+ with a clear error message | test | Unit test: craft file with format_version 3 header, assert error containing version number and supported range | PENDING |
| AC-08 | `graph_edges` rows exported in `ORDER BY source_id, target_id, relation_type` for determinism | test | Unit test: insert graph_edges in non-sorted order, export, verify output sorted by (source_id, target_id, relation_type) | PENDING |
| AC-09 | `observations` rows exported in `ORDER BY id` for determinism | test | Unit test: insert observations with non-sequential ids, export, verify output sorted by id | PENDING |
| AC-10 | `cycle_events` rows exported in `ORDER BY id` for determinism | test | Unit test: insert cycle_events with non-sequential ids, export, verify output sorted by id | PENDING |
| AC-11 | `graph_edges.weight` uses `Number::from_f64` with NaN fallback | test | Unit test: insert edge with weight=f64::NAN, export, verify JSON contains 1.0 fallback (ADR-003); also test f64::INFINITY and normal precision | PENDING |
| AC-12 | `graph_edges.metadata` nullable TEXT emitted as JSON null when SQL NULL | test | Unit test: insert edge with metadata=NULL, export, verify JSON null; also test non-null metadata round-trip | PENDING |
| AC-13 | `--force` import DELETEs from `graph_edges`, `observations`, `cycle_events` (and derived metric tables) before importing | test | Integration test: populate all 5 tables (including observation_metrics, observation_phase_metrics), import with --force, verify all empty before ingestion | PENDING |
| AC-14 | New tables appear after existing 8 tables in export output (dependency order preserved) | test | Unit test: export all 11 tables populated, verify line ordering: all graph_edges after audit_log, all observations after graph_edges, all cycle_events after observations | PENDING |
| AC-15 | Round-trip: export DB with all 11 tables populated, import into fresh DB, re-export, compare (excluding `exported_at` and `goal_embedding`) | test | Integration test: full round-trip export-import-export with byte-level comparison excluding exported_at timestamp | PENDING |
| AC-16 | `observations.id` preserved through export/import | test | Integration test: insert observations with known ids, export, import into fresh DB, query SELECT id FROM observations, confirm exact id match | PENDING |
| AC-17 | `cycle_events.id` preserved through export/import | test | Integration test: insert cycle_events with known ids, export, import into fresh DB, query SELECT id FROM cycle_events, confirm exact id match | PENDING |
| AC-18 | `graph_edges` import uses plain INSERT (not INSERT OR IGNORE) to surface duplicate/corruption errors | test | Integration test: craft v2 file with duplicate (source_id, target_id, relation_type) graph_edges rows, import, assert UNIQUE constraint violation error | PENDING |
| AC-19 | `cycle_events.goal_embedding` not present in export output (column excluded or emitted as null) | test | Unit test: insert cycle_event with non-null goal_embedding BLOB, export, verify field absent from JSONL line | PENDING |
| AC-20 | Import summary (`print_summary`) reports counts for all 3 new tables | test | Integration test: import v2 file, capture stderr, verify graph_edges/observations/cycle_events count lines present with correct values | PENDING |
| AC-21 | All new export functions produce empty output for empty tables (no spurious rows) | test | Unit test: export DB with empty graph_edges/observations/cycle_events, verify zero JSONL lines for those _table types | PENDING |
| AC-22 | `--skip-quarantined` CLI flag accepted by the **export** subcommand (default: off) | test | Unit test: parse CLI args with and without --skip-quarantined; verify flag parsed correctly with default false | PENDING |
| AC-23 | With `--skip-quarantined`, entries with `status = 3` are not emitted to the export file | test | Integration test: populate DB with 3 active + 2 quarantined entries, export with --skip-quarantined --confirm, parse JSONL, verify only 3 entries rows present | PENDING |
| AC-24 | With `--skip-quarantined`, `entry_tags` rows referencing skipped entry IDs are not emitted | test | Integration test: entry_tags for quarantined entries absent from export; entry_tags for active entries present | PENDING |
| AC-25 | With `--skip-quarantined`, `feature_entries` rows referencing skipped entry IDs are not emitted | test | Integration test: feature_entries for quarantined entries absent from export; feature_entries for active entries present | PENDING |
| AC-26 | With `--skip-quarantined`, `co_access` rows where either `entry_a` or `entry_b` references a skipped entry ID are not emitted | test | Integration test: verify all 4 combinations — (a) neither quarantined: present, (b) entry_a quarantined: absent, (c) entry_b quarantined: absent, (d) both quarantined: absent | PENDING |
| AC-27 | With `--skip-quarantined`, `graph_edges` rows where `source_id` or `target_id` references a skipped entry ID are not emitted | test | Integration test: verify all 4 combinations — (a) neither quarantined: present, (b) source_id quarantined: absent, (c) target_id quarantined: absent, (d) both quarantined: absent | PENDING |
| AC-28 | Export summary reports the count of skipped entries and skipped dependent rows when `--skip-quarantined` is active | test | Integration test: export with --skip-quarantined --confirm, capture stderr, verify skip entry count and per-table skipped dependent row counts present | PENDING |
| AC-29 | Without `--skip-quarantined`, all entries (including quarantined) are exported as before — no behavioral change to default path | test | Integration test: export DB with quarantined entries without --skip-quarantined, verify all entries (including status=3) and all dependents present; row counts match SELECT COUNT(*) | PENDING |
| AC-30 | `--skip-quarantined` requires `--confirm` flag to proceed — export aborts with clear message if confirmation missing | test | Unit test: export with --skip-quarantined but no --confirm, verify non-zero exit and error message mentioning --confirm; verify no output file created | PENDING |
| AC-31 | Export file produced with `--skip-quarantined` has valid hash integrity — import with hash validation succeeds without `--skip-hash-validation` | test | Integration test: export with --skip-quarantined --confirm, import resulting file without --skip-hash-validation, verify import succeeds with hash validation passing | PENDING |
