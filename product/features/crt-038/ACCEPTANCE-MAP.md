# crt-038 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Default weights in `InferenceConfig::default()`: `w_sim=0.50`, `w_conf=0.35`, `w_nli=0.00`, `w_util=0.00`, `w_prov=0.00` | test | `test_inference_config_weight_defaults_when_absent` asserts new values; `test_inference_config_default_weights_sum_within_headroom` still holds (0.85 ≤ 0.95) | PENDING |
| AC-02 | Default `nli_enabled=false`. `FusionWeights::effective()` short-circuits when `w_nli == 0.0`: returns weights unchanged regardless of `nli_available`. Three unit tests required: `test_effective_short_circuit_w_nli_zero_nli_available_false`, `test_effective_short_circuit_w_nli_zero_nli_available_true`, `test_effective_renormalization_still_fires_when_w_nli_positive` | test | `cargo test -p unimatrix-server test_effective_short_circuit` and `test_effective_renormalization_still_fires_when_w_nli_positive` all pass | PENDING |
| AC-03 | `run_post_store_nli` function deleted from `nli_detection.rs` | grep | `grep -r "run_post_store_nli" crates/` returns zero matches | PENDING |
| AC-04 | `store_ops.rs` no longer imports or calls `run_post_store_nli`; `tokio::spawn` NLI block removed | grep | `grep -r "tokio::spawn.*nli\|run_post_store_nli" crates/unimatrix-server/src/services/store_ops.rs` returns zero matches | PENDING |
| AC-05 | `maybe_run_bootstrap_promotion` and `run_bootstrap_promotion` deleted from `nli_detection.rs` | grep | `grep -r "maybe_run_bootstrap_promotion\|run_bootstrap_promotion" crates/` returns zero matches | PENDING |
| AC-06 | `background.rs` no longer imports or calls `maybe_run_bootstrap_promotion`; stale sequencing comment removed | grep | `grep -n "maybe_run_bootstrap_promotion" crates/unimatrix-server/src/background.rs` returns zero matches | PENDING |
| AC-07 | `nli_auto_quarantine_allowed` function and `NliQuarantineCheck` enum deleted from `background.rs` | grep | `grep -r "nli_auto_quarantine_allowed\|NliQuarantineCheck" crates/` returns zero matches | PENDING |
| AC-08 | `process_auto_quarantine` signature no longer includes `nli_enabled: bool` or `nli_auto_quarantine_threshold: f32`; call site in `maintenance_tick` updated | shell | `cargo build --workspace` succeeds with updated signature; inspect `maintenance_tick` call site argument count matches updated signature | PENDING |
| AC-09 | All tests for removed code paths deleted; all tests for retained paths pass without modification (except required default value updates) | grep | Grep-verify each deleted test symbol listed in the Implementation Brief returns zero matches in `crates/`; `cargo test --workspace` passes | PENDING |
| AC-10 | `cargo test --workspace` passes with zero failures | shell | `cargo test --workspace` exits 0 | PENDING |
| AC-11 | `cargo clippy --workspace -- -D warnings` passes with zero warnings | shell | `cargo clippy --workspace -- -D warnings` exits 0 (note: requires `write_edges_with_cap` deletion — see WARN in Implementation Brief) | PENDING |
| AC-12 | Eval run on `product/research/ass-039/harness/scenarios.jsonl` produces MRR ≥ 0.2913. Blocking pre-merge gate. PR description must include: exact eval command, full terminal output with MRR value, git commit hash confirming run was post-AC-02, confirmation of production server with new defaults active. | manual | Human reviewer verifies PR description contains all four required eval items before approving merge; MRR value in output is ≥ 0.2913 | PENDING |
| AC-13 | `nli_detection.rs` retains `write_nli_edge`, `format_nli_metadata`, `current_timestamp_secs` as `pub(crate)`; no cross-module compilation breakage | shell | `cargo build --workspace` succeeds after all removals; `grep -r "write_nli_edge\|format_nli_metadata\|current_timestamp_secs" crates/unimatrix-server/src/services/nli_detection.rs` returns three matches (pub(crate) definitions) | PENDING |
| AC-14 | `NliStoreConfig` struct deleted entirely from `store_ops.rs`; import removed from `mod.rs`; construction site removed from `mod.rs` | grep | `grep -r "NliStoreConfig" crates/` returns zero matches; `grep -r "nli_store_cfg" crates/` returns zero matches | PENDING |
