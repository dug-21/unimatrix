# crt-029 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `InferenceConfig` has four new fields: `supports_candidate_threshold: f32` (default 0.5), `supports_edge_threshold: f32` (default 0.7), `max_graph_inference_per_tick: usize` (default 100), `graph_inference_k: usize` (default 10) | test | `cargo test -- test_inference_config_defaults`; assert each default value matches spec | PENDING |
| AC-02 | `InferenceConfig::validate()` rejects configs where `supports_candidate_threshold >= supports_edge_threshold` (strict `>=`; equal values such as both 0.7 are rejected) | test | Unit test with equal values (0.7, 0.7) must return `Err`; unit test with (0.69, 0.7) must return `Ok`; unit test with (0.71, 0.7) must return `Err` | PENDING |
| AC-03 | `InferenceConfig::validate()` rejects `supports_candidate_threshold` or `supports_edge_threshold` outside `(0.0, 1.0)` exclusive (0.0 and 1.0 are each invalid) | test | Unit tests with values 0.0 and 1.0 for each field independently must return `Err` | PENDING |
| AC-04 | `InferenceConfig::validate()` rejects `max_graph_inference_per_tick` outside `[1, 1000]` (0 and 1001 are each invalid) | test | Unit tests with values 0 and 1001 must return `Err`; value 1 and 1000 must return `Ok` | PENDING |
| AC-04b | `InferenceConfig::validate()` rejects `graph_inference_k` outside `[1, 100]` (0 and 101 are each invalid) | test | Unit tests with values 0 and 101 must return `Err`; value 1 and 100 must return `Ok` | PENDING |
| AC-05 | `run_graph_inference_tick` returns immediately (no-op) when `nli_handle.get_provider()` returns `Err`; no store queries, no embeddings fetched | test | Unit test with a stub `NliServiceHandle` that always returns `Err`; assert zero DB calls made | PENDING |
| AC-06 | `run_graph_inference_tick` queries only `status = Active` entries; uses HNSW similarity > `supports_candidate_threshold`; uses `graph_inference_k` as the HNSW neighbour count independent of `nli_post_store_k` | test | Integration test verifying a `Deprecated` entry is never a candidate source or target; config with `graph_inference_k != nli_post_store_k` uses the tick-specific value | PENDING |
| AC-06b | Pairs where a `Supports` edge already exists in GRAPH_EDGES are skipped before NLI scoring; existing confirmed pairs consume zero NLI budget | test | Unit test seeds an existing `Supports` edge; assert NLI scorer is not called for that pair | PENDING |
| AC-06c | `get_embedding` is called only for source candidates as they are selected — not for all active entries upfront; source candidate count is bounded to `max_graph_inference_per_tick` before any embedding lookup | test | Unit test with N=50 active entries and `max_graph_inference_per_tick=5`; mock `get_embedding` with call counter; assert counter never exceeds 5 per tick | PENDING |
| AC-07 | Candidate pairs are processed in priority order: cross-category pairs first, isolated entries second (no existing non-bootstrap edges), remaining pairs by similarity descending; lower-priority pairs are dropped when cap is hit | test | Unit test with 3 cross-category pairs, 3 isolated-entry pairs, 4 same-category pairs, cap=3; assert only cross-category pairs are written | PENDING |
| AC-08 | For each tick, all NLI inference is dispatched as a single `rayon_pool.spawn()` call (W1-2 contract); no `spawn_blocking`, no inline async NLI calls | shell | `grep -n 'spawn_blocking' crates/unimatrix-server/src/services/nli_detection_tick.rs` must return empty; code review confirms single dispatch pattern | PENDING |
| AC-09 | A `Supports` edge `(A, B, "Supports")` is written when `score(A→B).entailment > supports_edge_threshold` (strict `>`); at-threshold does not produce an edge; uses `INSERT OR IGNORE` | test | Unit test with mock scores at and above threshold; at-threshold pair must not produce an edge | PENDING |
| AC-10 | A `Contradicts` edge is written when `score(A→B).contradiction > nli_contradiction_threshold` (strict `>`; existing threshold reused, not lowered); at-threshold does not produce an edge; uses `INSERT OR IGNORE` | test | Unit test with mock scores at and above `nli_contradiction_threshold`; at-threshold pair must not produce a `Contradicts` edge | PENDING |
| AC-11 | Total edges written per tick is bounded by `max_graph_inference_per_tick`; cap counts `Supports` + `Contradicts` edges combined | test | Unit test with cap=3 and 10 high-scoring pairs; assert exactly 3 edges written and function returns 3 | PENDING |
| AC-13 | All written edges carry `source = EDGE_SOURCE_NLI` ("nli") | test | Integration test queries GRAPH_EDGES after tick; assert `source = 'nli'` on all rows inserted by the tick | PENDING |
| AC-14 | `run_graph_inference_tick` is called from `background_tick_loop` after `maybe_run_bootstrap_promotion`, gated on `inference_config.nli_enabled` | test | Code review of `background.rs` call ordering; integration test with `nli_enabled = false` asserts tick is not invoked | PENDING |
| AC-15 | `Store::query_entries_without_edges()` returns the IDs of all active entries with no non-bootstrap edge on either endpoint (`source_id` or `target_id`) | test | Unit test seeds entries with and without edges; assert only edge-free entry IDs are returned; test with bootstrap-only edges confirms those entries still returned as isolated | PENDING |
| AC-16 | Unit tests cover: no-NLI no-op, cross-category priority ordering, isolated-entry priority, edge cap enforcement, pre-filter skips pairs with existing Supports edges, and idempotency (duplicate pair write → INSERT OR IGNORE guard, no duplicate rows) | test | `cargo test -- nli_detection_tick` passes with all six named test cases present in `nli_detection_tick.rs` | PENDING |
| AC-17 | TOML deserialization of `InferenceConfig` with the four new fields works correctly; fields absent from TOML use the specified defaults | test | Unit test parses a minimal TOML string without the four new fields; assert each default value matches spec (0.5, 0.7, 100, 10) | PENDING |
| AC-18† | All existing `InferenceConfig { ... }` struct literal constructions in `crates/unimatrix-server/src/` are updated to include the four new fields or to use `..InferenceConfig::default()` tail; no compile failure from missed struct literal updates | shell | `grep -rn 'InferenceConfig {' crates/unimatrix-server/src/` before merge; every occurrence updated; `cargo check -p unimatrix-server` passes with no struct-field errors | PENDING |
| AC-19† | Contradiction edges written by `run_graph_inference_tick` use `nli_contradiction_threshold` exactly — the same threshold as `run_post_store_nli`; no alternative, softer contradiction threshold is introduced for the tick path | grep | `grep -n 'contradiction_threshold\|nli_contradiction' crates/unimatrix-server/src/services/nli_detection_tick.rs` — no literal float values for contradiction threshold; all calls sourced from `config.nli_contradiction_threshold`; unit test with at-threshold mock score asserts no Contradicts edge written | PENDING |
