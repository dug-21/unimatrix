# vnc-018 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `context_graph(mode="chain", id=X)` returns all entries in the supersession chain containing X, ordered oldest to newest, including both ancestors and descendants | test | Integration test: 5-entry chain A→B→C→D→E; call chain with id=C; assert all 5 entries returned in order A, B, C, D, E | PENDING |
| AC-02 | `context_graph(mode="chain", id=X, direction="forward")` returns only X and descendants; `direction="backward"` returns only X and ancestors | test | Integration test using same 5-entry chain: forward from C → assert C,D,E; backward from C → assert A,B,C | PENDING |
| AC-03 | `context_graph(mode="chain", id=X)` on a chain > 50 hops returns at most 50 entries per direction; `truncated.forward` and/or `truncated.backward` is true when cap fires | test | Unit test: synthetic 60-entry chain; assert result length <= 50 per direction and `truncated.forward=true` | PENDING |
| AC-03b | `context_graph(mode="chain", id=X, direction="both")` where only forward branch hits the 50-hop cap returns `truncated.forward=true` and `truncated.backward=false`; backward branch returned in full; raw JSON wire shape of `truncated` field inspected (not just deserialized fields) | test | Unit test: 55 forward hops + 3 backward hops from seed; assert `truncated.forward=true`, `truncated.backward=false`, backward branch has 3 ancestors; confirm JSON shape is `{"forward": true, "backward": false}` not a flat bool | PENDING |
| AC-04 | `context_graph(mode="chain", id=X)` where X does not exist returns empty result, not an error | test | Integration test: call with id=999999 (absent); assert empty list, no error code | PENDING |
| AC-05 | `context_graph(mode="current", id=X)` where X is an active entry with no `superseded_by` returns X | test | Integration test: store a new entry; call current with its id; assert same entry returned | PENDING |
| AC-05a | `context_graph(mode="current", id=999999)` where 999999 does not exist returns an informative error ("No active terminal found for entry 999999" or equivalent) — it does NOT return an empty result. Intentionally asymmetric with chain mode (AC-04 returns empty). Both AC-04 and AC-05a must be present as a matched pair; test comment must state asymmetry is intentional design | test | Integration test: call current with id=999999 (absent); assert error response (not empty); assert error message identifies the entry ID. Paired with AC-04 test on same ID. | PENDING |
| AC-06 | `context_graph(mode="current", id=X)` where X is deprecated returns the terminal active entry at the end of the `superseded_by` chain — only if `status = 'Active'`; an orphaned deprecated entry (`superseded_by IS NULL`, `status = 'Deprecated'`) is NOT a valid terminal and must not be returned | test | Integration test: chain A(deprecated)→B(deprecated)→C(active); call current with id=A; assert entry C returned and C.status is Active | PENDING |
| AC-06b | `context_graph(mode="current", id=X)` where X's supersession chain terminates at an orphaned deprecated entry (`superseded_by IS NULL`, `status = 'Deprecated'`) returns an error ("No active terminal found" or equivalent) — it does NOT return the deprecated entry | test | Integration test: create entry D; call context_deprecate on D with no successor (superseded_by stays NULL); call current with id=D; assert error response; assert D is not returned as entry | PENDING |
| AC-07 | `context_graph(mode="current", id=X)` where `superseded_by` chain exceeds 50 hops returns an error response indicating chain too long | test | Unit test: mock 55-hop superseded_by chain; assert error response contains "50-hop safety cap" | PENDING |
| AC-08 | `context_graph(mode="neighbors", id=X, edge_types=["Prerequisite"], direction="outgoing", depth=1)` returns all entries with a Prerequisite edge from X | test | Integration test: write Prerequisite edges X→Y and X→Z; call neighbors; assert Y and Z appear with `direction="outgoing"` | PENDING |
| AC-09 | `context_graph(mode="neighbors", id=X, edge_types=["Supports"], direction="incoming", depth=1)` returns all entries with a Supports edge pointing at X | test | Integration test: write Supports edges Y→X and Z→X; call neighbors with direction="incoming"; assert Y and Z appear | PENDING |
| AC-10 | `context_graph(mode="neighbors", id=X, edge_types=[], direction="both", depth=1)` returns all neighbors across all edge types in both directions; Supersedes edges excluded without warning | test | Integration test: write edges of type Supports, Informs, and Supersedes from X to different targets; call with edge_types=[]; assert Supports and Informs neighbors returned; assert Supersedes target absent | PENDING |
| AC-10a | When `edge_types` is absent or empty and the graph contains Supersedes edges from the anchor, the response payload includes no warning, no `excluded_types` field, and no indication of exclusion | test | Inspect raw JSON response from AC-10 test; assert no top-level `excluded_types`, `warnings`, or similar field present | PENDING |
| AC-11 | `context_graph(mode="neighbors", id=X, depth=2)` returns both direct neighbors (depth=1) and their neighbors (depth=2); each EdgeRecord carries correct `depth` value | test | Integration test: X→Y→Z via typed edges; call neighbors depth=2; assert Y has depth=1 and Z has depth=2 in returned records | PENDING |
| AC-11a | A node reachable at both depth=1 and depth=2 (via two different paths) appears exactly once in the result, at depth=1. BFS visited set is keyed by `node_id` only — not by `(node_id, depth)` | test | Unit test: graph where X→Y direct (depth=1) AND X→Z→Y two-hop (Y also reachable at depth=2); call neighbors depth=2; assert Y appears exactly once with depth=1; assert no duplicate Y record at depth=2 | PENDING |
| AC-12 | `context_graph(mode="neighbors", ..., resolve_supersessions=true)` substitutes deprecated neighbor entries with their terminal active successors | test | Integration test: write edge X→Y; correct Y→Z; call neighbors with resolve_supersessions=true; assert Z appears, not Y | PENDING |
| AC-13 | `context_graph(mode="neighbors", ..., resolve_supersessions=false)` returns edges as stored, including deprecated endpoints | test | Integration test: same setup as AC-12; call with resolve_supersessions=false; assert Y (deprecated) appears in result | PENDING |
| AC-14 | `context_graph` with unrecognized `mode` value returns error listing supported modes | test | Call with mode="walk"; assert error response contains "chain, current, neighbors" | PENDING |
| AC-15 | `context_graph(mode="neighbors", edge_types=["UnknownType"])` returns an error before any traversal | test | Call with edge_types=["BogusEdge"]; assert error response; assert no edges retrieved | PENDING |
| AC-15a | `context_graph(mode="neighbors", edge_types=["Supersedes"])` returns exact error: "Supersedes edges are not traversable via neighbors mode — use chain or current modes for supersession navigation" | test | Call with edge_types=["Supersedes"]; assert exact error message string | PENDING |
| AC-15b | Passing `seed_ids`, `from_id`, `to_id`, or `max_nodes` to `chain`, `current`, or `neighbors` mode returns a descriptive error before traversal identifying the field and correct future mode | test | Four unit tests (one per field) passed to `neighbors` mode; assert each returns error containing field name and future mode name; also test unrecognized mode with seed_ids present and assert "unrecognized mode" error fires, not "seed_ids not supported" | PENDING |
| AC-15c | `context_graph(mode="chain", ..., resolve_supersessions=true)` returns exact error: "resolve_supersessions is not applicable to chain mode — chain IS the supersession audit". Rejection must occur in `validate_no_unsupported_params`, not inside `handle_chain` | test | Unit test `validate_no_unsupported_params` directly with mode="chain" and resolve_supersessions=Some(true); assert error string matches exactly. Also test via MCP integration path. | PENDING |
| AC-16 | `test_protocol.py` P-03 asserts exactly 14 `context_*` tools (updated from 13) | test | P-03 passes in the infra-001 test suite after `context_graph` registration | PENDING |
| AC-17 | `Advances` and `Motivates` participate in PPR expansion (`personalized_pagerank` and `positive_out_degree_weight` in `graph_ppr.rs`) | test | Unit test enumerates positive types; asserts `RelationType::Advances` and `RelationType::Motivates` are present in set used by both functions | PENDING |
| AC-18 | `Advances` and `Motivates` participate in positive BFS graph expansion (`graph_expand.rs`) | test | Unit test: entries connected via `Advances` and `Motivates` edges; run BFS expansion; assert both types are followed | PENDING |
| AC-19 | After migration, all four indexes exist: `idx_entries_supersedes`, `idx_entries_superseded_by`, `idx_graph_edges_source_type`, `idx_graph_edges_target_type` | test | Migration test `migration_v26_to_v27.rs` queries `sqlite_master` and asserts all four index names present | PENDING |
| AC-20 | All three modes (`chain`, `current`, `neighbors`) covered by at least one integration test in the infra-001 Python suite | test | `product/test/infra-001/` suite passes with test functions exercising each mode | PENDING |
