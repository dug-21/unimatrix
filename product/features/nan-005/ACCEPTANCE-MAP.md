# nan-005 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | README.md rewritten with all 11 sections (hero, why, capabilities, getting started, tips, MCP tools, skills, categories, CLI, architecture, security) | grep | `grep -c '^## ' README.md` returns at least 11; each section header from FR-01a present and non-empty | PENDING |
| AC-02 | MCP tool reference documents all tools with name, purpose, key parameters, when-to-use note; count matches `tools.rs` | shell | `grep -c '#\[tool(' crates/unimatrix-server/src/mcp/tools.rs` matches row count in README MCP Tool Reference table | PENDING |
| AC-03 | Skills reference documents all 14 skills with name, purpose, trigger condition | shell | `ls .claude/skills/ \| wc -l` returns 14; each skill name appears in README Skills Reference table | PENDING |
| AC-04 | All factual errors corrected: no redb references, no .redb extension, correct crate count, correct test count | grep | `grep -ri 'redb\|\.redb' README.md` returns no matches; crate count matches `ls crates/ \| wc -l` | PENDING |
| AC-05 | Operational guidance documents all 7 constraints (session boundaries, feature cycle naming, commit format, category discipline, hook latency, cold start, near-duplicate threshold) | grep | Each keyword findable: `grep -c 'session\|feature cycle\|commit\|category\|hook\|cold start\|duplicate' README.md` returns matches for all 7 | PENDING |
| AC-06 | uni-docs agent definition exists at `.claude/agents/uni/uni-docs.md` with artifact-reading, fallback, scope boundary, no-source-code constraints | file-check | File exists; `grep -l 'SCOPE.md\|SPECIFICATION.md' .claude/agents/uni/uni-docs.md` matches; `grep -l 'README.md only' .claude/agents/uni/uni-docs.md` matches | PENDING |
| AC-07 | Delivery protocol contains documentation step in Phase 4 after `gh pr create` and before `/review-pr` | grep | `grep -n 'uni-docs\|documentation' .claude/protocols/uni/uni-delivery-protocol.md` shows step between PR creation and review-pr lines | PENDING |
| AC-08 | Documentation step specifies mandatory vs optional criteria (mandatory: tool/skill/CLI/category/schema; skip: refactor/test-only/docs-only) | grep | `grep -A 10 'mandatory\|MANDATORY' .claude/protocols/uni/uni-delivery-protocol.md` shows trigger criteria decision table | PENDING |
| AC-09 | Getting started includes npm install path (`npm install @dug-21/unimatrix`) and build-from-source path (`cargo build`) with prerequisites | grep | `grep -n 'npm install' README.md` and `grep -n 'cargo build' README.md` both return matches with surrounding prerequisite content | PENDING |
| AC-10 | Architecture section describes SQLite storage, 9 crates with accurate descriptions, correct data layout (`.db`, `.pid`, `.sock`) | grep | `grep -c 'SQLite\|unimatrix.db' README.md` returns matches; `grep 'redb' README.md` returns no matches; crate list has 9 entries | PENDING |
| AC-11 | Knowledge categories section explains all 8 categories (outcome, lesson-learned, decision, convention, pattern, procedure, duties, reference) with description and example | grep | Each of the 8 category names appears in the README categories section: `grep -c 'outcome\|lesson-learned\|decision\|convention\|pattern\|procedure\|duties\|reference' README.md` returns >= 8 | PENDING |
| AC-12 | README content factually accurate: no aspirational features, no stale references, no placeholder content | grep | `grep -in 'TODO\|TBD\|placeholder\|will be added\|coming soon' README.md` returns no matches; `grep -in 'redb\|oauth\|_meta' README.md` returns no content-bearing matches | PENDING |
