# Fix Report — batch-337-345-346-378-379-380-agent-1-fix

Agent ID: batch-337-345-346-378-379-380-agent-1-fix
Branch: bugfix/batch-small-fixes

## Files Modified

- `crates/unimatrix-server/src/infra/config.rs`
- `crates/unimatrix-server/src/infra/session.rs`
- `crates/unimatrix-server/src/mcp/response/retrospective.rs`
- `crates/unimatrix-server/src/mcp/tools.rs`
- `crates/unimatrix-server/src/services/search.rs`
- `crates/unimatrix-server/src/uds/listener.rs`

## Changes by Issue

### GH #337 — merge_configs missing post-merge validation
- Inserted `validate_config(&merged, &global_path)?;` in `load_config()` after `merge_configs()` call, before `return Ok(merged)`
- Added regression test `test_merge_configs_post_merge_fusion_weight_sum_exceeded`

### GH #345 — saturating_add for session histogram counters
- `session.rs`: `*count += 1` → `*count = count.saturating_add(1)` (category histogram counter)
- `session.rs`: `tally.count += 1` → `tally.count = tally.count.saturating_add(1)` (TopicTally counter)
- `session.rs`: topic_signals sum → `.fold(0u32, |acc, v| acc.saturating_add(v))`
- `search.rs`: histogram_total sum → `.fold(0u32, |acc, v| acc.saturating_add(v))`
- Types confirmed u32 on all 4 sites before change
- Added test `test_category_counter_saturates_at_u32_max`

### GH #346 — sanitize_session_id on CompactPayload dispatch
- Added `sanitize_session_id` guard in `HookRequest::CompactPayload` match arm before `handle_compact_payload` call
- Pattern mirrors SessionRegister and SessionClose paths exactly
- Added test `dispatch_compact_payload_invalid_session_id_returns_error`

### GH #378 + #379 — Full markdown escaping sweep in retrospective.rs
- Added `escape_md_cell(s: &str) -> String` helper (table cells: strip newlines, escape `|`)
- Added `escape_md_text(s: &str) -> String` helper (free text: strip newlines, escape `|`, escape leading `#` only when string starts with `#`)
- Applied at 15 call sites across `render_goal_section`, `render_phase_timeline`, `render_sessions`, `render_phase_outliers`, `render_baseline_outliers`, `render_cross_cycle_table`, `render_knowledge_reuse`
- Added tests: `test_escape_md_cell_escapes_pipe_and_newlines`, `test_escape_md_text_heading_embedded_reference_and_pipe`, `test_knowledge_reuse_section_pipe_in_category_and_feature_cycle`, `test_render_goal_section_heading_goal_is_escaped`

### GH #380 — obs.ts as i64 behavior fix
- Replaced `obs.ts as i64` with `i64::try_from(obs.ts).unwrap_or(i64::MAX)` in `compute_phase_stats`
- Updated comment to accurately describe as type cast (not unit conversion), consistent with `cycle_ts_to_obs_millis` saturation philosophy
- Added test `test_compute_phase_stats_obs_ts_u64_max_included_via_saturation`

## New Tests

- `test_merge_configs_post_merge_fusion_weight_sum_exceeded`
- `test_category_counter_saturates_at_u32_max`
- `dispatch_compact_payload_invalid_session_id_returns_error`
- `test_escape_md_cell_escapes_pipe_and_newlines`
- `test_escape_md_text_heading_embedded_reference_and_pipe`
- `test_knowledge_reuse_section_pipe_in_category_and_feature_cycle`
- `test_render_goal_section_heading_goal_is_escaped`
- `test_compute_phase_stats_obs_ts_u64_max_included_via_saturation`

## Tests

2526 passed, 0 failed (full workspace)

## Issues

None.

## Knowledge Stewardship

**Queried:**
- `mcp__unimatrix__context_briefing` — entry #3372 (col-026 ADR-002 on `cycle_ts_to_obs_millis`) relevant to GH #380 saturation pattern context; entries #3901–#3905 (lessons stored by investigators) consulted for fix approach confirmation

**Stored:** Declined — all patterns applied are standard Rust idioms (saturating_add, try_from) and project conventions already codified in Unimatrix lesson entries. No new reusable knowledge surfaces from this implementation pass.
