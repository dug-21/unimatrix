# alc-003 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | When `UNIMATRIX_SESSION_AGENT=my-agent` is set, server enrolls `my-agent` with `TrustLevel::Internal` and `[Read, Write, Search]` before any tool call is processed | test | Integration test: query `AGENT_REGISTRY` for `agent_id = "my-agent"` immediately after server construction; assert `trust_level = "internal"`, capabilities = `[Read, Write, Search]`, row present before first tool call | PENDING |
| AC-02 | When `UNIMATRIX_SESSION_AGENT` is set and a tool call omits `agent_id`, audit log records the session agent name (not `"anonymous"`) | test | Integration test: issue a tool call with no `agent_id` param; inspect `AUDIT_LOG`; assert stored `agent_id = "my-agent"`; assert string `"anonymous"` does not appear | PENDING |
| AC-03 | Per-call `agent_id` is attribution only — capabilities come from the session; no registry lookup occurs; unenrolled swarm specialist can call Write tools without being enrolled | test | Integration test with `alc-003-researcher` not in `AGENT_REGISTRY`: (1) `context_store` call succeeds; (2) audit log records `"alc-003-researcher"`; (3) no new row for `alc-003-researcher` in `AGENT_REGISTRY`; (4) no registry lookup during call (verify by mocking or instrumentation) | PENDING |
| AC-04 | When `UNIMATRIX_SESSION_AGENT` is absent, server exits with non-zero exit code; stderr contains `"UNIMATRIX_SESSION_AGENT"`; no tool calls served | test | Integration test spawning server as subprocess without env var; assert non-zero exit code; assert stderr contains `"UNIMATRIX_SESSION_AGENT"` and a description of required action; assert no MCP tool call is served | PENDING |
| AC-05 | Swarm specialist `agent_id` passes through to audit log without enrollment; `AGENT_REGISTRY` row count is unchanged | test | Integration test: `context_store` with `agent_id: "alc-003-spec-writer"` (not enrolled); assert (1) call succeeds; (2) audit log records `"alc-003-spec-writer"`; (3) `AGENT_REGISTRY` row count before == row count after | PENDING |
| AC-06 | `PERMISSIVE_AUTO_ENROLL` no longer exists anywhere in the codebase | grep | `grep -r "PERMISSIVE_AUTO_ENROLL" crates/` returns no results; no env var by that name is read; no test references the constant or passes `permissive=true` as a boolean flag | PENDING |
| AC-07 | When `UNIMATRIX_SESSION_AGENT` is set to an invalid value, server exits with non-zero exit code and a message naming the validation failure | test | Parameterized integration test (subprocess) covering 6 cases: `""` (empty), 65-char string (too long), `"my agent"` (space), `"system"` (protected), `"human"` (protected), `"HUMAN"` (protected uppercase); each must produce non-zero exit and stderr naming the specific failure reason | PENDING |
| AC-08 | Session agent enrollment is idempotent — second server start with same env var produces exactly one `AGENT_REGISTRY` row | test | Integration test: construct `UnimatrixServer` with `UNIMATRIX_SESSION_AGENT=my-agent`, tear down, construct again; assert `AGENT_REGISTRY` row count for `"my-agent"` = 1 after both cycles | PENDING |
| AC-09 | Bootstrap agents (`"system"`, `"human"`) retain their original capabilities and trust levels after session agent enrollment | test | Integration test: start server with valid `UNIMATRIX_SESSION_AGENT`; query `AGENT_REGISTRY` for `"system"` and `"human"`; assert each has its `bootstrap_defaults()` trust level and capabilities unchanged | PENDING |
| AC-10 | All existing infra integration tests continue to pass; tests previously relying on permissive behavior are updated; no test is deleted | shell | `cargo test --workspace` passes in CI with `UNIMATRIX_SESSION_AGENT` set to a valid test value and `PERMISSIVE_AUTO_ENROLL` absent from the environment; pre-flight Phase 1 fixture updates are a prerequisite | PENDING |
