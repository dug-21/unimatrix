# vnc-024 Acceptance Criteria Map

> Every AC traces to SCOPE.md (AC-01..AC-15). **AC-12 is a GATE prerequisite** (SR-07 / R-03 /
> #4711 / #4311): it must be green on BOTH transports + the batch arm before any downstream AC is
> trusted. Verification anchors: SPECIFICATION.md (AC table), RISK-TEST-STRATEGY.md (R-01..R-14).
>
> **Design-review rework applied (2026-06-05):** AC-11 is now **dual-sided** (Rust↔TS) over the typed
> `TranscriptDeltaPayload` (a 6th ts-rs export); AC-01/AC-02/AC-15 cover six exported types; AC-13 now
> asserts OSS `validate()` **rejects** `RetainDays` as enterprise-only (no range check). ADR-001/002/004/005 revised.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | ts-rs is a dev-dependency of `unimatrix-engine`; the **six** exported types (5 wire types + `TranscriptDeltaPayload`) derive `TS` + `#[ts(export)]`. | file-check + shell | Inspect `crates/unimatrix-engine/Cargo.toml` `[dev-dependencies]` for `ts-rs`; grep `wire.rs` for `#[derive(...TS...)]` + `#[ts(export` on `HookInput`/`HookRequest`/`HookResponse`/`ImplantEvent`/`EntryPayload`/`TranscriptDeltaPayload`; `cargo metadata` shows ts-rs under dev-deps only. | PENDING |
| AC-02 | `cargo test` generates committed `.ts` bindings for all **six** exported types; output is checked in. | shell + file-check | Clean checkout → `cargo test -p unimatrix-engine`; assert each `crates/unimatrix-engine/bindings/{HookInput,HookRequest,HookResponse,ImplantEvent,EntryPayload,TranscriptDeltaPayload}.ts` exists and is non-empty. | PENDING |
| AC-03 | CI gate fails when committed bindings differ from fresh `cargo test` output (drift cannot merge). | shell | Mutate a `wire.rs` field without regenerating → CI step `cargo test && git diff --exit-code crates/unimatrix-engine/bindings/` exits non-zero; restore → exits zero. (R-14 meta-gate.) | PENDING |
| AC-04 | Generated `HookRequest`/`HookResponse` discriminated unions carry a literal `type` field per variant. | grep + test | Inspect generated `.ts`: each variant has its literal `type`; behaviorally asserted by per-variant fixtures in `node --test` (AC-05), the fixture being the authority (ADR-002). | PENDING |
| AC-05 | JSON round-trip fixtures exist for every `HookRequest`/`HookResponse` variant; a Rust test AND a standalone `node --test` harness both deserialize the same fixtures and agree on structure. Ships in F1. | test + shell | Run Rust round-trip test (extended `wire.rs` `#[cfg(test)]` suite) + `node --test crates/unimatrix-engine/bindings/contract.test.mjs`; both exit 0; a deliberately malformed fixture makes `node --test` exit non-zero. | PENDING |
| AC-06 | Fixtures cover `None`-vs-omission for `skip_serializing_if = "Option::is_none"` fields: `ImplantEvent.topic_signal`, `ImplantEvent.provider`, `ContextSearch.source`, `CompactPayload.transcript_excerpt`. | test | Dual-direction assertion per field in BOTH Rust test and `node --test`: (a) emitted-as-absent (not `null`) when `None`; (b) deserialized-to-default when key absent; plus a non-trivial-value round-trip per field (#3557). | PENDING |
| AC-07 | `POST /observe` with `Accept: text/plain` for an `Entries` response returns `Content-Type: text/plain` with body byte-identical to `format_injection` output for the same entries. | test | Integration test: post an `Entries`-producing request with `Accept: text/plain`; assert `Content-Type: text/plain` and body bytes == direct `format_injection(&items, max_bytes)` call with the PRODUCTION budget, including an over-budget/truncation case (R-05). | PENDING |
| AC-08 | `POST /observe` with `Accept: application/json` or no header returns the current JSON envelope unchanged (`Ack`→204, `Entries`/`BriefingContent`/`Pong`→200 JSON, `Error`→400 JSON). | test | Integration tests over each response type with/without the header assert status + JSON body identical to pre-change behavior; negotiated content-type asserted at the HTTP boundary (R-07). | PENDING |
| AC-09 | `BriefingContent` honors `Accept: text/plain` (formatted text body) consistently with `Entries`. Text applies ONLY to these two; `Pong`/`Ack`/`Error` stay JSON regardless of `Accept`. | test | Integration tests: `BriefingContent` under `text/plain` → text body; `Pong`/`Ack`/`Error` under `text/plain` → JSON envelope (not text), `Pong.server_version` parseable (R-06). | PENDING |
| AC-10 | UDS hook path output is identical before and after this change. | test | UDS round-trip parity / golden comparison test; assert no behavior change on the local transport (additive HTTP-only change). | PENDING |
| AC-11 | The typed `TranscriptDeltaPayload { offset: u64, bytes: String }` ships as the **6th** exported binding; a cross-language `{ "offset": u64, "bytes": "<text>" }` fixture round-trips **DUAL-SIDED (Rust↔TS)**, parsing into `TranscriptDeltaPayload` on **both** sides — not Rust-emit-only. The carrier (`RecordEvent`/`ImplantEvent`, free-form `event_type` + `Value` payload) is unchanged and appears in the bindings' wire surface; no new wire variant. | test + grep | `node --test`: the `{offset,bytes}` fixture deserializes into `TranscriptDeltaPayload` (TS→Rust direction included), AND the Rust round-trip test parses the same fixture into the Rust struct (Rust→TS); inspect bindings confirm `TranscriptDeltaPayload.ts` is emitted and `RecordEvent`/`ImplantEvent` carry arbitrary `event_type`/`payload`. A Rust-emit-only check does NOT satisfy this (R-01 scenario 4 — the TS→Rust direction catches client-side `offset`/`bytes` drift). | PENDING |
| **AC-12 (GATE)** | A `transcript_delta` sent to `/observe` AND over UDS is handled by an explicit accept-and-drop branch: returns `Ack`, raw `bytes` persisted nowhere durable (no `insert_observation` at `listener.rs:849`, no other disk write). The branch parses the payload into the typed `TranscriptDeltaPayload` (same shape as AC-11). Zero observation rows on both transports; a delta inside a `RecordEvents` batch is dropped while the rest persist. | test | Negative integration test run TWICE (HTTP `POST /observe` via `router.rs:234`, and direct UDS dispatch): send a delta → assert `Ack` AND observation-row count unchanged (zero rows for the delta). Batch test: `RecordEvents` with one delta + normal events → delta dropped, rest persisted. Structure check: `return Ack` sits after `SessionWrite` check + `sanitize_session_id`, before feature-extraction (`:793`) / `insert_observation` (`:849`); guard parses into `TranscriptDeltaPayload`, not raw `serde_json::Value`; not the col-022 fall-through (#1266). **Must be green before downstream ACs are trusted.** | PENDING |
| AC-13 | `RetentionConfig` gains `transcript_retention` typed as the enum (`PurgeOnCycleClose` default \| `RetainDays(u32)`); compiled default is `PurgeOnCycleClose`; absent `[retention]` loads to that default. In the OSS build, `validate()` **REJECTS** `RetainDays(_)` with a clear enterprise-only error and **ACCEPTS** `PurgeOnCycleClose` (always valid). The enum shape (not a bare `u32`) is retained as the enterprise seam. | test | Unit tests: default value == `PurgeOnCycleClose`; absent-config load → default; `validate()` rejects `RetainDays(N)` for ANY `N` (incl. `0`) with an enterprise-only error naming `RetainDays` (NOT a generic range error); `validate()` accepts `PurgeOnCycleClose`; `"PurgeOnCycleClose"` TOML deserializes; bare-`u32` (`transcript_retention = 30`) rejected (R-10); reviewer confirms enum (not `u32`) + `PartialEq` derive (R-11). | PENDING |
| AC-14 | `transcript_retention` participates in the per-field project-wins config merge (`config.rs:3307` pattern). | test | Merge test with distinct project + global `transcript_retention` values → project wins; assert merged result is re-validated (#3905) so a merged `RetainDays` is still rejected. | PENDING |
| AC-15 | No new runtime dependency on any shipped crate (ts-rs dev-only); `cargo audit` passes; ts-rs absent from `cargo tree --edges normal`. | shell | `cargo tree --edges normal` → ts-rs absent from runtime edge set; `cargo metadata` → ts-rs under `[dev-dependencies]` only; `cargo audit` exits clean. | PENDING |
