# crt-024 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `InferenceConfig` in `infra/config.rs` adds six f64 fields (`w_sim`, `w_nli`, `w_conf`, `w_coac`, `w_util`, `w_prov`), all with `#[serde(default)]`; defaults sum to ≤ 0.95 (at least 0.05 WA-2 headroom) | test | Unit test: parse a `config.toml` without the new fields; assert all six fields equal their defaults; assert `w_sim + w_nli + w_conf + w_coac + w_util + w_prov <= 0.95` | PENDING |
| AC-02 | `InferenceConfig::validate()` rejects configs where the sum of all six weights exceeds 1.0; error message names all six fields and the computed sum | test | Unit test: construct config with sum = 1.05; assert `validate()` returns `Err` with message containing all six field names and the computed sum | PENDING |
| AC-03 | Each weight must individually be in [0.0, 1.0]; `validate()` rejects any weight < 0.0 or > 1.0 with a structured error naming the offending field | test | Twelve unit tests: for each of the six fields, one test sets value to -0.01 (assert `Err`) and one sets it to 1.01 (assert `Err`) | PENDING |
| AC-04 | Search pipeline applies a single unified scoring pass — no secondary sort after the fused score computation; pipeline order matches SPECIFICATION.md FR-08 | test + grep | Code review: no sort/re-rank call after the fused scorer returns; integration test: 10 candidates scored, assert final order is unchanged after Step 7 | PENDING |
| AC-05 | Fused score formula contains all six signal terms: `fused = w_sim*sim + w_nli*nli + w_conf*conf + w_coac*coac_norm + w_util*util_norm + w_prov*prov_norm`; result correct to f64 epsilon | test | Unit test with controlled inputs: sim=0.8, nli=0.7, conf=0.6, coac_raw=0.015, util_norm=0.5, prov_norm=1.0, weights=(0.30,0.30,0.15,0.10,0.05,0.05), penalty=1.0; assert `|result - 0.665| < 1e-9` | PENDING |
| AC-06 | When NLI is absent, `w_nli` term is 0.0 and the remaining five weights are re-normalized by dividing each by `(w_sim + w_conf + w_coac + w_util + w_prov)`; re-normalized weights sum to 1.0 | test | Unit test: set `w_nli = 0.0`, five non-zero remaining weights; assert `sum(effective weights) == 1.0` within f64 epsilon; assert two candidates ordered correctly by non-NLI signals | PENDING |
| AC-07 | Co-access normalization: `coac_norm = raw_boost / MAX_CO_ACCESS_BOOST`; `MAX_CO_ACCESS_BOOST` referenced from `unimatrix_engine::coaccess`, not redefined in `search.rs` | grep + test | `grep` for `MAX_CO_ACCESS_BOOST` in `search.rs` must show only an imported reference, not a `const` definition; unit test: raw=0.03→norm=1.0, raw=0.015→norm=0.5, raw=0.0→norm=0.0 | PENDING |
| AC-08 | All tests that previously asserted specific `final_score` values are updated to new formula expected values; no test is deleted; total test count after crt-024 ≥ total test count before crt-024 | shell | `git diff --stat` shows no deleted test files; `cargo test` passes with zero failures; test count assertion | PENDING |
| AC-09 | `status_penalty` preserved as multiplier: `final_score = fused_score * status_penalty`; penalty constants ORPHAN_PENALTY, CLEAN_REPLACEMENT_PENALTY, DEPRECATED_PENALTY, SUPERSEDED_PENALTY unchanged | test | Unit test: fused_score=0.8, DEPRECATED_PENALTY=0.7 → assert final_score=0.56; assert penalty constant values match pre-crt-024 values | PENDING |
| AC-10 | `utility_delta` and `PROVENANCE_BOOST` are included as `w_util * util_norm` and `w_prov * prov_norm` terms in the fused formula — not additive afterthoughts outside the formula | grep + test | Code review: no `+ utility_delta` or `+ prov_boost` outside `compute_fused_score`; unit test: entry with `util_norm=1.0, w_util=0.05` scores exactly 0.05 higher than identical entry with `util_norm=0.0` | PENDING |
| AC-11 | Regression test: entry with NLI entailment=0.9 and zero co-access ranks above entry with NLI entailment=0.3 and maximum co-access (raw=0.03), given equal similarity=0.8 and confidence=0.65, using default weights | test | Unit test: Entry A (nli=0.9, coac=0.0, sim=0.8, conf=0.65, util=0.0, prov=0.0) vs Entry B (nli=0.3, coac=0.03, sim=0.8, conf=0.65, util=0.0, prov=0.0); assert `score(A) > score(B)`; test marked with AC-11 reference comment | PENDING |
| AC-12 | All weight config validation errors from `InferenceConfig::validate()` use the same structured error pattern as existing NLI config validation (named fields, `Result<(), ConfigError>`, no panics) | test + grep | Code review: validation returns `Result`, no `unwrap()`/`expect()` on validation results; integration test: server fails to start and logs structured error when weights are invalid | PENDING |
| AC-13 | When weight sum is in (0.0, 1.0] (valid with headroom), the formula is applied as configured — no re-normalization on the NLI-enabled path | test | Unit test: weights sum to 0.90, NLI enabled, NLI score=0.5; assert `w_nli_eff == w_nli` (0.35 unchanged); assert fused score equals hand-computed value using configured weights directly | PENDING |
| AC-14 | `BriefingService` code path is not modified; `MAX_BRIEFING_CO_ACCESS_BOOST = 0.01` remains in the briefing pipeline; existing briefing integration tests pass without modification | shell + grep | `git diff` for briefing service source files shows no changes; `cargo test` briefing integration tests pass | PENDING |
| AC-15 | `EvalServiceLayer` passes `InferenceConfig` (including fusion weights) from the eval profile TOML to `SearchService`; a profile with `w_sim=1.0` and all other weights=0.0 produces `final_score == similarity_score * status_penalty`; a default-weight profile produces a different score on the same candidate | test | Unit test 1: `EvalServiceLayer` constructed with `w_sim=1.0` profile; assert `final_score == similarity_score * status_penalty` within f64 epsilon. Unit test 2: default weights; assert score differs from test 1 score when NLI or confidence is non-zero | PENDING |
| AC-16 | D1–D4 eval harness run completed on pre-crt024 snapshot before merge; human reviews report; zero true regressions; intentional NLI-override corrections identified and accepted | human review + artifact | Eval report exists at `/tmp/eval/crt024-report.json`; baseline log updated; PR description includes report review summary distinguishing intentional corrections from true regressions | PENDING |
