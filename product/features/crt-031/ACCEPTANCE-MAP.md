# crt-031 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `KnowledgeConfig` has `adaptive_categories: Vec<String>` field with `#[serde(default = "default_adaptive_categories")]` defaulting to `["lesson-learned"]`. Serialization round-trips correctly. | test | `cargo test -p unimatrix-server -- config` — test serializes `KnowledgeConfig { adaptive_categories: vec!["custom-a", "custom-b"], .. }`, deserializes via `toml::to_string` / `toml::from_str`, asserts round-trip identity | PENDING |
| AC-02 | A config file omitting `adaptive_categories` produces a `KnowledgeConfig` with `adaptive_categories == ["lesson-learned"]` after deserialization. | test | `cargo test -p unimatrix-server -- config` — parse minimal TOML string (no `adaptive_categories` key) into `UnimatrixConfig` and assert `knowledge.adaptive_categories == ["lesson-learned"]` | PENDING |
| AC-03 | A config file specifying `adaptive_categories = ["lesson-learned", "convention"]` produces a `KnowledgeConfig` with both values present. | test | `cargo test -p unimatrix-server -- config` — parse TOML with two-value `adaptive_categories`, assert both entries present | PENDING |
| AC-04 | `validate_config` rejects a config where any entry in `adaptive_categories` is absent from `categories`, returning `ConfigError::AdaptiveCategoryNotInAllowlist` with the offending category name and config file path. | test | `cargo test -p unimatrix-server -- config` — construct `KnowledgeConfig { categories: vec!["lesson-learned"], boosted_categories: vec![], adaptive_categories: vec!["nonexistent"] }`, assert `matches!(result, Err(ConfigError::AdaptiveCategoryNotInAllowlist { category, .. }) if category == "nonexistent")` | PENDING |
| AC-05 | `CategoryAllowlist::is_adaptive("lesson-learned")` returns `true` when constructed with the default policy. | test | `cargo test -p unimatrix-server -- categories` — construct via `from_categories_with_policy(INITIAL_CATEGORIES.to_vec(), vec!["lesson-learned"])`, assert `is_adaptive("lesson-learned") == true` | PENDING |
| AC-06 | `CategoryAllowlist::is_adaptive("decision")` returns `false` when constructed with the default policy. | test | `cargo test -p unimatrix-server -- categories` — same construction as AC-05, assert `is_adaptive("decision") == false` | PENDING |
| AC-07 | `CategoryAllowlist::is_adaptive` returns `false` for any category not in the allowlist. | test | `cargo test -p unimatrix-server -- categories` — construct with default policy, assert `is_adaptive("nonexistent-category") == false` | PENDING |
| AC-08 | Poison recovery on the adaptive lock follows `.unwrap_or_else(|e| e.into_inner())` — `is_adaptive` does not panic on a poisoned lock. | test | `cargo test -p unimatrix-server -- categories` — poison the `adaptive` `RwLock` using the existing `poison_allowlist` helper pattern; assert `is_adaptive` returns a value without panicking | PENDING |
| AC-09 | `context_status` output includes a per-category lifecycle section. Summary text lists only adaptive categories. JSON output includes all categories with lifecycle labels. | test | `cargo test -p unimatrix-server -- status` — call `compute_report()` on a `StatusService` with known `adaptive_categories`, assert `category_lifecycle` contains all 5 default categories each labeled `"adaptive"` or `"pinned"`; assert summary text contains only the adaptive category name; assert JSON contains full labeled list | PENDING |
| AC-10 | `maintenance_tick` logs a `tracing::debug!` message listing the adaptive categories at each tick. The log does NOT fire if `adaptive_categories` is empty. | test | `cargo test -p unimatrix-server -- background` — invoke the tick stub with a `CategoryAllowlist` containing `["lesson-learned"]` adaptive; assert debug log fires. Repeat with empty adaptive list; assert no log. Use `tracing_test` or equivalent subscriber capture. | PENDING |
| AC-11 | The lifecycle guard stub in `maintenance_tick` calls `is_adaptive()` indirectly (via `list_adaptive()`) and is annotated with a `// TODO(#409)` comment. The stub is a no-op — no entries are modified. | grep | `grep -n "TODO(#409)" crates/unimatrix-server/src/background.rs` returns at least one hit inside the Step 10b block | PENDING |
| AC-12 | All existing `CategoryAllowlist` tests continue to pass without modification. | test | `cargo test -p unimatrix-server -- categories` — all pre-existing test names pass; no tests renamed or removed | PENDING |
| AC-13 | `CategoryAllowlist::new()` is equivalent to constructing with default `adaptive_categories = ["lesson-learned"]`. | test | `cargo test -p unimatrix-server -- categories` — assert `CategoryAllowlist::new().is_adaptive("lesson-learned") == true` and `CategoryAllowlist::new().is_adaptive("decision") == false` | PENDING |
| AC-14 | `validate_config` accepts a config where `adaptive_categories` is an empty list `[]`. | test | `cargo test -p unimatrix-server -- config` — construct `KnowledgeConfig` with default categories and `adaptive_categories: vec![]`, assert `validate_config` returns `Ok(())` | PENDING |
| AC-15 | `validate_config` accepts a config where `adaptive_categories` is a proper subset of `categories` with multiple entries. | test | `cargo test -p unimatrix-server -- config` — construct `KnowledgeConfig` with `categories: INITIAL_CATEGORIES` and `adaptive_categories: vec!["lesson-learned", "convention"]`, assert `validate_config` returns `Ok(())` | PENDING |
| AC-16 | `merge_configs` handles `adaptive_categories` with project-overrides-global semantics. | test | `cargo test -p unimatrix-server -- config` — call `merge_configs` with project having `adaptive_categories = ["pattern"]` and global having `adaptive_categories = ["lesson-learned"]`; assert merged result is `["pattern"]`. Second test: project default, global non-default; assert global value wins. | PENDING |
| AC-17 | `KnowledgeConfig::Default` impl returns `boosted_categories: vec![]`. A unit test asserts this. | test | `cargo test -p unimatrix-server -- config` — test named `test_knowledge_config_default_boosted_is_empty`; assert `KnowledgeConfig::default().boosted_categories.is_empty()` | PENDING |
| AC-18 | Deserializing an empty TOML string (`""`) into `UnimatrixConfig` produces `knowledge.boosted_categories == ["lesson-learned"]`. The existing test `test_default_config_boosted_categories_is_lesson_learned` is updated to cover this serde path, not `Default`. | test | `cargo test -p unimatrix-server -- main_tests` — updated test parses `""` into `UnimatrixConfig` via `toml::from_str`, asserts `knowledge.boosted_categories == ["lesson-learned"]`; test name and comment updated | PENDING |
| AC-19 | `eval/profile/layer.rs` contains no `HashSet::from(["lesson-learned"...])` literal. | grep | `grep -n 'lesson-learned' crates/unimatrix-server/src/eval/profile/layer.rs` returns zero hits | PENDING |
| AC-20 | `server.rs`, `shutdown.rs` (both occurrences), `test_support.rs`, `services/index_briefing.rs`, and `uds/listener.rs` contain no `HashSet::from(["lesson-learned"...])` literals. | grep | `grep -rn 'HashSet::from.*lesson-learned' crates/unimatrix-server/src/server.rs crates/unimatrix-server/src/infra/shutdown.rs crates/unimatrix-server/src/test_support.rs crates/unimatrix-server/src/services/index_briefing.rs crates/unimatrix-server/src/uds/listener.rs` returns zero hits | PENDING |
| AC-21 | `config.rs` test `test_empty_categories_documented_behavior` does not explicitly set `boosted_categories: vec![]`. The `Default` impl produces `[]` naturally. | grep | `grep -A 20 'test_empty_categories_documented_behavior' crates/unimatrix-server/src/infra/config.rs` — confirm no `boosted_categories: vec![]` line and no workaround comment | PENDING |
| AC-22 | `README.md` `[knowledge]` example block includes both `adaptive_categories` and `boosted_categories` entries, each with an inline comment explaining its purpose. | manual | Review `README.md` diff — confirm `adaptive_categories = ["lesson-learned"]` with comment and updated `boosted_categories` comment are present in the `[knowledge]` example block | PENDING |
| AC-23 | No test regressions — `cargo test --workspace` passes with no new failures. | shell | `cargo test --workspace` exits 0 | PENDING |
| AC-24 | Every `validate_config` test fixture using a custom `categories` list explicitly zeros both `boosted_categories: vec![]` AND `adaptive_categories: vec![]`. Test helpers `config_with_custom_weights` and `config_with_categories` (if present) are updated. | grep | `grep -n 'KnowledgeConfig {' crates/` — manual audit confirms all fixtures with custom `categories` zero both parallel lists; `cargo test --workspace` green | PENDING |
| AC-25 | A dedicated `validate_config` test for `AdaptiveCategoryNotInAllowlist` uses `boosted_categories: vec![]` and `adaptive_categories: vec!["nonexistent"]`. The test does not trigger `BoostedCategoryNotInAllowlist`. | test | `cargo test -p unimatrix-server -- config` — test asserts error is exactly `AdaptiveCategoryNotInAllowlist { category: "nonexistent" }`, not `BoostedCategoryNotInAllowlist` | PENDING |
| AC-26 | PR description documents the output of `grep -rn "KnowledgeConfig::default()" crates/` and confirms every hit has been reviewed and updated if it relied on `boosted_categories == ["lesson-learned"]`. | manual | PR description contains the grep output and a checklist item confirming each hit was inspected | PENDING |
| AC-27 | A unit test named `test_knowledge_config_default_adaptive_is_empty` asserts `KnowledgeConfig::default().adaptive_categories.is_empty()`. | test | `cargo test -p unimatrix-server -- config` — test named `test_knowledge_config_default_adaptive_is_empty` present and passing | PENDING |
