# crt-031 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `KnowledgeConfig` has `adaptive_categories: Vec<String>` field with `#[serde(default = "default_adaptive_categories")]` defaulting to `["lesson-learned"]`. Serialization round-trips correctly. | test | `cargo test -p unimatrix-server -- config` — test serializes `KnowledgeConfig { adaptive_categories: vec!["custom-a", "custom-b"], .. }`, deserializes, asserts round-trip identity | PENDING |
| AC-02 | A config omitting `adaptive_categories` produces `KnowledgeConfig` with `adaptive_categories == ["lesson-learned"]` after deserialization. | test | `cargo test -p unimatrix-server -- config` — test deserializes minimal TOML with no `adaptive_categories` key; asserts field equals `["lesson-learned"]` | PENDING |
| AC-03 | A config specifying `adaptive_categories = ["lesson-learned", "convention"]` produces a `KnowledgeConfig` with both values. | test | `cargo test -p unimatrix-server -- config` — test deserializes TOML with two values; asserts both are present | PENDING |
| AC-04 | `validate_config` rejects a config where any entry in `adaptive_categories` is absent from `categories`, returning `ConfigError::AdaptiveCategoryNotInAllowlist` with the offending category name and config file path. | test | `cargo test -p unimatrix-server -- validate_config` — test passes `categories = ["lesson-learned"]`, `adaptive_categories = ["lesson-learned", "unknown-cat"]`; asserts error variant and `category == "unknown-cat"` | PENDING |
| AC-05 | `CategoryAllowlist::is_adaptive("lesson-learned")` returns `true` when constructed with default policy. | test | `cargo test -p unimatrix-server -- categories` — test calls `CategoryAllowlist::new()`; asserts `al.is_adaptive("lesson-learned") == true` | PENDING |
| AC-06 | `CategoryAllowlist::is_adaptive("decision")` returns `false` when constructed with default policy. | test | `cargo test -p unimatrix-server -- categories` — test calls `CategoryAllowlist::new()`; asserts `al.is_adaptive("decision") == false` | PENDING |
| AC-07 | `CategoryAllowlist::is_adaptive` returns `false` for a category not in the allowlist (unknown category is not adaptive). | test | `cargo test -p unimatrix-server -- categories` — test calls `al.is_adaptive("no-such-category")` on default allowlist; asserts `false` | PENDING |
| AC-08 | Poison recovery on the adaptive set follows `.unwrap_or_else(|e| e.into_inner())` — `is_adaptive` does not panic on a poisoned lock. | test | `cargo test -p unimatrix-server -- categories` — test poisons the `adaptive` `RwLock` using existing helper pattern; calls `is_adaptive`; asserts no panic and valid `bool` returned | PENDING |
| AC-09 | `context_status` output includes a per-category lifecycle section in both summary and JSON formats. Summary lists only adaptive categories; JSON lists all categories with labels. | test | `cargo test -p unimatrix-server -- status` — two tests: (1) `ResponseFormat::Summary` asserts "Adaptive categories" line present; (2) `ResponseFormat::Json` asserts `category_lifecycle` key with all five default categories labeled | PENDING |
| AC-10 | `maintenance_tick` logs a `tracing::debug!` listing adaptive categories at each tick. Log does NOT fire when `adaptive_categories` is empty. | test | `cargo test -p unimatrix-server -- background` — test with `["lesson-learned"]` adaptive set confirms debug path is reachable; test with empty adaptive set confirms guard `if !adaptive_cats.is_empty()` is skipped | PENDING |
| AC-11 | The lifecycle guard stub in `maintenance_tick` calls `is_adaptive()` and is annotated with `// TODO(#409)`. The stub is a no-op. | grep | `grep -n "TODO(#409)" crates/unimatrix-server/src/background.rs` returns at least one match; code review confirms stub body is no-op | PENDING |
| AC-12 | All existing `CategoryAllowlist` tests continue to pass without modification. | test | `cargo test -p unimatrix-server -- categories` passes with zero failures and zero test renames | PENDING |
| AC-13 | `CategoryAllowlist::new()` is equivalent to constructing with `adaptive_categories = ["lesson-learned"]` — no behavior regression. | test | `cargo test -p unimatrix-server -- categories` — test constructs `new()` and `from_categories_with_policy(INITIAL_CATEGORIES, ["lesson-learned"])`; asserts identical `is_adaptive` results for all 5 initial categories | PENDING |
| AC-14 | `validate_config` accepts a config where `adaptive_categories` is `[]` (disabling adaptive management is valid). | test | `cargo test -p unimatrix-server -- validate_config` — test passes `adaptive_categories = []`; asserts `Ok(())` | PENDING |
| AC-15 | `validate_config` accepts a config where `adaptive_categories` is a proper subset of `categories` with multiple entries. | test | `cargo test -p unimatrix-server -- validate_config` — test passes `categories = ["a", "b", "c"]`, `adaptive_categories = ["a", "b"]`; asserts `Ok(())` | PENDING |
| AC-16 | Every `KnowledgeConfig` test fixture with custom `categories` explicitly sets both `boosted_categories: vec![]` and `adaptive_categories: vec![]` to prevent cross-check false failures. | test | `cargo test -p unimatrix-server -- validate_config` passes with zero failures — any missing `vec![]` will cause a cross-check error that surfaces as a test failure | PENDING |
| AC-17 | `CategoryAllowlist` constructed via `new()` and passed through `Arc` to `maintenance_tick` carries correct policy: `is_adaptive("lesson-learned") == true`, `is_adaptive("decision") == false`. | test | `cargo test -p unimatrix-server -- background` — wiring test creates `CategoryAllowlist::new()`, wraps in `Arc`, asserts both `is_adaptive` results | PENDING |
