# bugfix-523 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | When `inference_config.nli_enabled = false`, `run_graph_inference_tick` returns after Path C without calling `nli_handle.get_provider()` or spawning any rayon task. | test | `test_nli_gate_path_b_skipped_nli_disabled` in `nli_detection_tick.rs` — run tick with `nli_enabled=false` and mock-ready provider; assert no NLI Supports edges written and no rayon dispatch. | PENDING |
| AC-02 | When `inference_config.nli_enabled = false`, Path A (Informs writes) and Path C (cosine Supports writes) are NOT affected — they continue to run and write edges as normal. | test | `test_nli_gate_path_a_informs_edges_still_written_nli_disabled` and `test_nli_gate_path_c_cosine_supports_edges_still_written_nli_disabled` in `nli_detection_tick.rs` — assert Informs edges and cosine Supports edges both present after tick. Both tests required. | PENDING |
| AC-03 | When `inference_config.nli_enabled = true` and a provider is available, Phases 6/7/8 continue to execute (no regression in NLI-enabled path). | test | `test_nli_gate_nli_enabled_path_not_regressed` in `nli_detection_tick.rs` — run tick with `nli_enabled=true` and mock provider; assert `get_provider()` is called and NLI Supports edges are written. | PENDING |
| AC-04 | `run_cosine_supports_path` with a candidate pair whose source or target ID is absent from `category_map` emits `tracing::debug!` (not `warn!`) and skips the pair. | test | `test_cosine_supports_path_skips_missing_category_map_src` and `test_cosine_supports_path_skips_missing_category_map_tgt` in `nli_detection_tick.rs` — behavioral proxy: assert pair skipped, no panic, function returns. Log level verified by code review only per ADR-001(c) (entry #4143). Gate report must state: "AC-04 log-level assertion is behavioral-only per ADR-001(c). Log level verified by code review." | PENDING |
| AC-05 | `run_cosine_supports_path` with a non-finite cosine value continues to emit `tracing::warn!` (the non-finite cosine guard at line 766 is unchanged). | test | `test_cosine_supports_path_nonfinite_cosine_handled` in `nli_detection_tick.rs` — assert pair skipped, no panic. Log level (must remain `warn!`) verified by code review only per ADR-001(c) (entry #4143). Gate report must state: "AC-05 warn! retention verified by code review." | PENDING |
| AC-06 | `InferenceConfig::validate()` returns `Err(ConfigError::NliFieldOutOfRange { field: "nli_entailment_threshold", ... })` when `nli_entailment_threshold = f32::NAN`. | test | `test_nan_guard_nli_entailment_threshold` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "nli_entailment_threshold")`. | PENDING |
| AC-07 | `InferenceConfig::validate()` returns `Err(ConfigError::NliFieldOutOfRange { field: "nli_contradiction_threshold", ... })` when `nli_contradiction_threshold = f32::NAN`. | test | `test_nan_guard_nli_contradiction_threshold` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "nli_contradiction_threshold")`. | PENDING |
| AC-08 | `InferenceConfig::validate()` returns `Err(ConfigError::NliFieldOutOfRange { field: "nli_auto_quarantine_threshold", ... })` when `nli_auto_quarantine_threshold = f32::NAN`. | test | `test_nan_guard_nli_auto_quarantine_threshold` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "nli_auto_quarantine_threshold")`. | PENDING |
| AC-09 | `InferenceConfig::validate()` returns `Err` when `supports_candidate_threshold = f32::NAN`. | test | `test_nan_guard_supports_candidate_threshold` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "supports_candidate_threshold")`. | PENDING |
| AC-10 | `InferenceConfig::validate()` returns `Err` when `supports_edge_threshold = f32::NAN`. | test | `test_nan_guard_supports_edge_threshold` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "supports_edge_threshold")`. | PENDING |
| AC-11 | `InferenceConfig::validate()` returns `Err` when `ppr_alpha = f64::NAN`. | test | `test_nan_guard_ppr_alpha` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "ppr_alpha")`. | PENDING |
| AC-12 | `InferenceConfig::validate()` returns `Err` when `ppr_inclusion_threshold = f64::NAN`. | test | `test_nan_guard_ppr_inclusion_threshold` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "ppr_inclusion_threshold")`. | PENDING |
| AC-13 | `InferenceConfig::validate()` returns `Err` when `ppr_blend_weight = f64::NAN`. | test | `test_nan_guard_ppr_blend_weight` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "ppr_blend_weight")`. | PENDING |
| AC-14 | `InferenceConfig::validate()` returns `Err` when `nli_informs_cosine_floor = f32::NAN`. | test | `test_nan_guard_nli_informs_cosine_floor` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "nli_informs_cosine_floor")`. | PENDING |
| AC-15 | `InferenceConfig::validate()` returns `Err` when `nli_informs_ppr_weight = f64::NAN`. | test | `test_nan_guard_nli_informs_ppr_weight` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "nli_informs_ppr_weight")`. | PENDING |
| AC-16 | `InferenceConfig::validate()` returns `Err` when `supports_cosine_threshold = f32::NAN`. | test | `test_nan_guard_supports_cosine_threshold` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "supports_cosine_threshold")`. | PENDING |
| AC-17 | `InferenceConfig::validate()` returns `Err` when `w_sim = f64::NAN`. | test | `test_nan_guard_w_sim` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_sim")`. Field name must match the `&'static str` in the `fusion_weight_checks` array exactly. | PENDING |
| AC-18 | `InferenceConfig::validate()` returns `Err` when `w_nli = f64::NAN`. | test | `test_nan_guard_w_nli` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_nli")`. | PENDING |
| AC-19 | `InferenceConfig::validate()` returns `Err` when `w_conf = f64::NAN`. | test | `test_nan_guard_w_conf` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_conf")`. | PENDING |
| AC-20 | `InferenceConfig::validate()` returns `Err` when `w_coac = f64::NAN`. | test | `test_nan_guard_w_coac` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_coac")`. | PENDING |
| AC-21 | `InferenceConfig::validate()` returns `Err` when `w_util = f64::NAN`. | test | `test_nan_guard_w_util` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_util")`. | PENDING |
| AC-22 | `InferenceConfig::validate()` returns `Err` when `w_prov = f64::NAN`. | test | `test_nan_guard_w_prov` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_prov")`. | PENDING |
| AC-23 | `InferenceConfig::validate()` returns `Err` when `w_phase_histogram = f64::NAN`. | test | `test_nan_guard_w_phase_histogram` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_phase_histogram")`. Field name must match the `&'static str` in `phase_weight_checks` array exactly. | PENDING |
| AC-24 | `InferenceConfig::validate()` returns `Err` when `w_phase_explicit = f64::NAN`. | test | `test_nan_guard_w_phase_explicit` in `config.rs` `#[cfg(test)]` — `assert_validate_fails_with_field(&c, "w_phase_explicit")`. | PENDING |
| AC-25 | `InferenceConfig::validate()` returns `Err` when `nli_entailment_threshold = f32::INFINITY` (representative f32 Inf test). | test | `test_inf_guard_nli_entailment_threshold_f32` in `config.rs` `#[cfg(test)]` — set `c.nli_entailment_threshold = f32::INFINITY`; `assert_validate_fails_with_field(&c, "nli_entailment_threshold")`. | PENDING |
| AC-26 | `InferenceConfig::validate()` returns `Err` when `ppr_alpha = f64::INFINITY` (representative f64 Inf test). | test | `test_inf_guard_ppr_alpha_f64` in `config.rs` `#[cfg(test)]` — set `c.ppr_alpha = f64::INFINITY`; `assert_validate_fails_with_field(&c, "ppr_alpha")`. | PENDING |
| AC-27 | All existing `InferenceConfig::validate()` tests continue to pass — no regression on boundary values. | shell | `cargo test -p unimatrix-server -- infra::config` — all pre-existing tests pass. Specifically verify boundary tests for `w_sim` (valid: 0.0, 0.5; invalid: -0.1, 1.1) pass. | PENDING |
| AC-28 | `dispatch_request` with `post_tool_use_rework_candidate` event and invalid `event.session_id` returns `HookResponse::Error { code: ERR_INVALID_PAYLOAD }` without calling `record_rework_event`. | test | `test_dispatch_rework_candidate_invalid_session_id_rejected` in `listener.rs` `#[cfg(test)]` — dispatch event with `session_id = "../../etc/passwd"`; assert `HookResponse::Error { code: ERR_INVALID_PAYLOAD }` and verify `record_rework_event` not called. Also verify by code inspection that guard appears before first use of `event.session_id`. | PENDING |
| AC-29 | `dispatch_request` with a valid `post_tool_use_rework_candidate` event continues to call `session_registry.record_rework_event` (no regression on the normal path). | test | `test_dispatch_rework_candidate_valid_path_not_regressed` in `listener.rs` `#[cfg(test)]` — dispatch event with valid `session_id = "session-abc123"`; assert success response and `record_rework_event` called. | PENDING |
