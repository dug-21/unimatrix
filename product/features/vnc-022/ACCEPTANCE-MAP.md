# vnc-022 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | POST /observe with valid bearer token and SessionRegister HookRequest JSON body returns 204 and registers the session in SessionRegistry | test | Integration test sends SessionRegister via HTTP, asserts 204 status, queries SessionRegistry to verify session exists with "http-" prefixed session_id |
| AC-02 | POST /observe with RecordEvent (fire-and-forget type) returns 204 and persists the observation to the database | test | Integration test sends RecordEvent (PreToolUse) via HTTP, asserts 204 status, queries database for persisted observation row |
| AC-03 | POST /observe with UserPromptSubmit (ContextSearch) returns 200 with HookResponse::Entries JSON containing ranked entries identical in structure to UDS path | test | Integration test sends ContextSearch via HTTP, asserts 200 status, deserializes response as Entries JSON, verifies "type":"Entries" and items array present |
| AC-04 | POST /observe with CompactPayload (PreCompact) returns 200 with HookResponse::BriefingContent JSON | test | Integration test sends CompactPayload via HTTP, asserts 200 status, deserializes response as BriefingContent JSON, verifies "type":"BriefingContent" and content field present |
| AC-05 | POST /observe with SubagentStart (ContextSearch with source) returns 200 with entries JSON | test | Integration test sends ContextSearch with source="SubagentStart" via HTTP, asserts 200 status, deserializes response as Entries JSON |
| AC-06 | POST /observe without valid bearer token returns 401 | test | Integration test sends request without Authorization header, asserts 401 status; second test with invalid token, asserts 401 status |
| AC-07 | POST /observe with malformed JSON body returns 400 with error JSON | test | Integration test sends `{"type":"Bogus"}` via HTTP, asserts 400 status, verifies response body contains "error" field |
| AC-08 | POST /observe with oversized body returns 413 | test | Integration test sends body exceeding 1MB via HTTP, asserts 413 status, verifies response body contains error message about maximum size |
| AC-09 | CompactPayload wire type accepts optional transcript_excerpt: Option<String> field | test | Unit test on wire.rs: round-trip deserialize CompactPayload JSON with transcript_excerpt present (asserts Some), without field (asserts None), serialize with None (asserts field absent from JSON output) |
| AC-10 | PreCompact returns briefing content only (no transcript block) -- Day 1 degradation | test | Integration test sends CompactPayload via HTTP, asserts BriefingContent response does not contain transcript-specific markers; response contains briefing content derived from pipeline |
| AC-14 | Multiple concurrent sessions from same bearer token with different session_id values are correctly isolated in SessionRegistry | test | Integration test registers two sessions (different session_ids, same token), sends events to each, verifies independent session state in SessionRegistry -- events from session A do not appear in session B |
| AC-15 | All 6 critical and 4 important event types are handled by /observe endpoint | test | Integration tests cover at least one event per wire type variant: SessionRegister (204), SessionClose (204), RecordEvent (204), ContextSearch (200+Entries), ContextSearch+source (200+Entries), CompactPayload (200+BriefingContent) |
| AC-16 | /observe request/response JSON schema documented | grep | `grep -l "Wire Contract" product/features/vnc-022/specification/SPECIFICATION.md` returns the specification file; code review confirms handler has doc comments describing the contract |
| AC-17 | Integration tests cover HTTP observation path: fire-and-forget (204), sync with injection (200+JSON), auth rejection (401), malformed body (400) | test | `cargo test` passes with test cases covering all four scenarios; test function names grep-able for "observe" |
| AC-18 | Existing UDS hook path continues to work unchanged (zero regression) | test | `cargo test` -- all pre-existing UDS hook integration tests pass without modification after dispatch_request refactor |
| AC-19 | dispatch_request shared between UDS and HTTP paths with no logic duplication | grep | `grep -rn "pub(crate) async fn dispatch_request" crates/unimatrix-server/src/uds/listener.rs` returns exactly one result; `grep -c "uds_has_capability" crates/unimatrix-server/src/uds/listener.rs` inside dispatch_request body returns 0 after refactor |
