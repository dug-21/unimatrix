# crt-040 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | When `candidate_pairs` contains a pair with `cosine >= supports_cosine_threshold` AND `[source_category, target_category]` in `informs_category_pairs`, a `Supports` edge is written with `source = EDGE_SOURCE_COSINE_SUPPORTS = "cosine_supports"` | test | Unit test with synthetic `candidate_pairs` containing a qualifying pair (e.g., `lesson-learned → decision` at cosine 0.70); assert `graph_edges` row exists with `relation_type = 'Supports'` and `source = 'cosine_supports'` | PENDING |
| AC-02 | When `cosine < supports_cosine_threshold`, no `Supports` edge is written for that pair by Path C | test | Unit test with pair at cosine 0.64 (below default 0.65 threshold); assert no `Supports` edge in `graph_edges` | PENDING |
| AC-03 | When the category pair is NOT in `informs_category_pairs`, no `Supports` edge is written even if `cosine >= threshold` | test | Unit test with pair at cosine 0.80 with disallowed category combination (e.g., `decision → decision`); assert no edge written. Category resolution must use the `HashMap<u64, String>` pre-built from `all_active` — per-pair DB lookup is prohibited | PENDING |
| AC-04 | Pairs already in `existing_supports_pairs` are skipped (pre-filter). `INSERT OR IGNORE` is the authoritative dedup backstop | test | Unit test pre-populating `existing_supports_pairs` with a qualifying pair; run Path C; assert DB row count = 1 (no duplicate) | PENDING |
| AC-05 | Cosine Supports detection runs unconditionally — NOT gated by `nli_enabled` or `get_provider()` | test | Integration test with `nli_enabled = false`; assert Path C still writes edges when candidates qualify | PENDING |
| AC-06 | Informs path (Path A) behavior is unchanged — `informs_category_pairs` usage in Path A and all guards (temporal ordering, cross-feature, cosine floor) are not modified | test | All existing Path A tests pass without modification after delivery | PENDING |
| AC-07 | NLI Supports path (Path B) behavior is unchanged — Phase 6/7/8 and `supports_edge_threshold` are not modified | test | All existing Path B tests pass without modification after delivery | PENDING |
| AC-08 | `EDGE_SOURCE_COSINE_SUPPORTS = "cosine_supports"` is defined as a named constant in `unimatrix-store::read` and re-exported from the crate root (`lib.rs`) | test | Unit test asserts `EDGE_SOURCE_COSINE_SUPPORTS == "cosine_supports"`; constant is accessible via `unimatrix_store::EDGE_SOURCE_COSINE_SUPPORTS` import | PENDING |
| AC-09 | `supports_cosine_threshold` config field added to `InferenceConfig` with default 0.65, range `(0.0, 1.0)` exclusive. `InferenceConfig::validate()` rejects out-of-range values with a structured error naming the field `"supports_cosine_threshold"` | test | Unit tests: `validate()` with 0.0 (fail), 1.0 (fail), 0.65 (pass), 0.001 (pass), 0.999 (pass) | PENDING |
| AC-10 | `InferenceConfig::default()` returns `supports_cosine_threshold = 0.65` | test | `assert_eq!(InferenceConfig::default().supports_cosine_threshold, 0.65_f32)` | PENDING |
| AC-11 | The Path C write path uses `write_graph_edge` with `source = EDGE_SOURCE_COSINE_SUPPORTS` — NOT the hardcoded `'nli'` source | test | Inspect written `graph_edges.source`; assert value is `"cosine_supports"`, not `"nli"`. Also assert `created_by = 'cosine_supports'` | PENDING |
| AC-12 | Path C is capped at `MAX_COSINE_SUPPORTS_PER_TICK = 50` edges per tick; budget is independent of `max_graph_inference_per_tick` and `MAX_INFORMS_PER_TICK` | test | Unit test with 60 qualifying candidate pairs; assert exactly 50 `Supports` edges written in `graph_edges` | PENDING |
| AC-13 | Tick ordering invariant is preserved. Path C runs inside `run_graph_inference_tick`, which always runs last in the tick sequence | manual | Code inspection: tick ordering enforced by caller in `background.rs`; verify Path C placement is after Path A log and before Path B entry gate | PENDING |
| AC-14 | Eval gate passes: MRR >= 0.2875 on `product/research/ass-039/harness/scenarios.jsonl` after delivery, with Path C active and at least one tick completed | shell | `python product/research/ass-039/harness/run_eval.py`; assert reported MRR >= 0.2875 | PENDING |
| AC-15 | `inferred_edge_count` in `GraphCohesionMetrics` continues to count only `source = 'nli'` edges. Path C edges (`source = 'cosine_supports'`) do not appear in this metric | test | Unit test: write a `source = 'cosine_supports'` edge; compute `GraphCohesionMetrics`; assert `inferred_edge_count` is unchanged (same as before the write) | PENDING |
| AC-16 | `default_supports_cosine_threshold()` serde backing function and `impl Default for InferenceConfig` struct literal agree: both return 0.65. Three independent assertions required | test | (1) `assert_eq!(default_supports_cosine_threshold(), 0.65_f32)` (2) `assert_eq!(InferenceConfig::default().supports_cosine_threshold, 0.65_f32)` (3) `assert_eq!(toml::from_str::<InferenceConfig>("").unwrap().supports_cosine_threshold, 0.65_f32)` — all three tests must be independent | PENDING |
| AC-17 | `nli_post_store_k` field is absent from `InferenceConfig`: no struct field, no backing function, no `impl Default` entry, no `validate()` block, no config merge branch, no test assertions | grep | `grep -n "nli_post_store_k" crates/unimatrix-server/src/infra/config.rs` returns zero results | PENDING |
| AC-18 | Deserializing a TOML string containing `nli_post_store_k = 5` into `InferenceConfig` succeeds without error (serde silently discards unknown field) | test | Unit test: `toml::from_str::<InferenceConfig>("nli_post_store_k = 5").unwrap()` returns `Ok(_)` without panic. Confirms `deny_unknown_fields` is NOT active on `InferenceConfig` | PENDING |
| AC-19 | After the Path C write loop, an unconditional `debug!` log fires with fields `cosine_supports_candidates: usize` and `cosine_supports_edges_written: usize`. Fires even when both values are zero | test | Unit test or integration test: run Path C with empty `candidate_pairs`; assert log event fires with `cosine_supports_candidates = 0` and `cosine_supports_edges_written = 0` | PENDING |
