# crt-027 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `build_request` with SubagentStart + non-empty `prompt_snippet` returns `HookRequest::ContextSearch` with correct `query`, `source: Some("SubagentStart")`, and `session_id` from parent input | test | `hook.rs` unit test `build_request_subagentstart_with_prompt_snippet` asserting variant and field values |  PENDING |
| AC-02 | `build_request` with SubagentStart + absent `prompt_snippet` key returns `HookRequest::RecordEvent` | test | `hook.rs` unit test `build_request_subagentstart_empty_prompt_snippet` covering (a) key absent, (b) key present with `""` | PENDING |
| AC-02b | `hook.rs` defines `MIN_QUERY_WORDS: usize = 5`; UserPromptSubmit with < 5 words → `RecordEvent`; UserPromptSubmit with >= 5 words → `ContextSearch`; SubagentStart with 1-word non-empty `prompt_snippet` → `ContextSearch` (guard inapplicable) | test | Five `hook.rs` unit tests on `build_request`: 3-word query → RecordEvent, 1-word query → RecordEvent, 5-word query → ContextSearch, 6-word query → ContextSearch, SubagentStart `prompt_snippet="ok"` → ContextSearch | PENDING |
| AC-03 | SubagentStart `ContextSearch` carries `session_id = input.session_id` (parent session, not ppid fallback); when a registered session exists, WA-2 histogram boost applies | test | Unit test asserting `session_id` field taken from `input.session_id`; integration test confirming histogram lookup fires via `ServiceSearchParams.category_histogram` | PENDING |
| AC-04 | SubagentStart is synchronous — `is_fire_and_forget` returns `false` for `HookRequest::ContextSearch`; response written to stdout via `transport.request()` | test | Unit test asserting `ContextSearch` is NOT matched by `is_fire_and_forget` pattern | PENDING |
| AC-05 | `HookRequest::ContextSearch` carries `source: Option<String>` with `#[serde(default)]`; SubagentStart sets `source = Some("SubagentStart")`; UserPromptSubmit sets `source = None`; `dispatch_request` uses `source.as_deref().unwrap_or("UserPromptSubmit")` for observation `hook` column | test | (a) `hook.rs` unit test: SubagentStart builds `source = Some("SubagentStart")`; (b) `listener.rs` integration test with `source: Some("SubagentStart")` writes `hook = "SubagentStart"` to observations table; (c) existing UserPromptSubmit test writes `hook = "UserPromptSubmit"` | PENDING |
| AC-SR01 | SubagentStart stdout injection confirmed: hook writes `HookResponse::Entries` to stdout and Claude Code injects content into subagent before first token | manual | Manual smoke test: spawn subagent with `prompt_snippet` matching a known Unimatrix entry; verify injection text appears in subagent initial context. Must be marked OPEN or CONFIRMED before Gate 3c | PENDING |
| AC-06 | `context_briefing` returns only `status = Active` entries; deprecated entries do not appear | test | `tools.rs` integration test: database with one Active + one Deprecated entry sharing same topic; briefing response includes Active, excludes Deprecated | PENDING |
| AC-07 | `context_briefing` default k is 20; `UNIMATRIX_BRIEFING_K` env var not read by `IndexBriefingService`; `k=5` parameter returns at most 5 entries | test | `tools.rs` unit/integration test asserting max result count with no k param = 20; with `k=5` = 5; code inspection confirms `UNIMATRIX_BRIEFING_K` not referenced in `IndexBriefingService` | PENDING |
| AC-08 | Both `context_briefing` and `handle_compact_payload` output flat indexed table with columns: row number, id, topic, category, confidence (2 decimal places), 150-char snippet; no section headers | test | `tools.rs` test asserting flat table presence and absence of `"## Decisions"` / `"## Injections"` / `"## Conventions"` strings | PENDING |
| AC-09 | Query derivation priority: (1) explicit `task` used when provided; (2) when `task` absent and `session_id` present, synthesized from `feature_cycle` + top 3 `topic_signals` by vote count; (3) `topic` param fallback; both call sites use same shared helper | test | Unit tests covering all three steps against `derive_briefing_query`; code inspection confirms single shared function | PENDING |
| AC-10 | `handle_compact_payload` query derivation uses already-held session state for step 2 (no `SessionRegistry` lookup by ID); `context_briefing` MCP tool uses `SessionRegistry` for step 2; both delegate to same shared helper | test | Code inspection and unit test for UDS path confirming no registry lookup when session state is held directly | PENDING |
| AC-11 | `context_briefing` `session_id` parameter, when provided, is passed as `ServiceSearchParams.session_id`, triggering category histogram lookup for WA-2 boost | test | Integration test: register session, accumulate category histogram, call `context_briefing` with `session_id`; higher-histogram-category entries rank before a call without `session_id` | PENDING |
| AC-12 | `handle_compact_payload` no longer calls `BriefingService::assemble()`; calls `IndexBriefingService` and produces `HookResponse::BriefingContent` with flat indexed table | test | `BriefingService` import absent from `listener.rs`; `listener.rs` integration test covering `HookRequest::CompactPayload` asserts flat table format in response | PENDING |
| AC-13 | `BriefingService`, `BriefingParams`, `BriefingResult`, `InjectionSections`, `InjectionEntry`, all `BriefingService` methods, all tests in `briefing.rs`, and all re-exports from `services/mod.rs` are deleted; no dead code remains | grep | `grep -r "BriefingService" crates/` returns no results; `cargo build --release` emits no `dead_code` warnings for removed types | PENDING |
| AC-14 | `uni-delivery-protocol.md` includes `context_briefing(topic="{feature-id}", session_id: "{session-id}", max_tokens: 1000)` immediately after `context_cycle(type: "start")` (1 site) and after each `context_cycle(type: "phase-end")` (5 sites) | grep | `grep -c "context_briefing" .claude/protocols/uni/uni-delivery-protocol.md` returns >= 6; diff shows all six insertion points; each call includes `max_tokens: 1000` | PENDING |
| AC-15 | All existing tests in `listener.rs`, `hook.rs`, and `tools.rs` (briefing path) pass; test count is non-decreasing from pre-feature baseline | test | `cargo test` CI run; test count comparison before and after merge | PENDING |
| AC-16 | Byte budget enforcement: `format_compaction_payload` with large entries and small `max_bytes` cap produces output where `output.len() <= max_bytes`; low-ranked rows dropped first | test | Unit test `format_payload_budget_enforcement` with large content entries exceeding `MAX_COMPACTION_BYTES` and small `max_bytes` override | PENDING |
| AC-17 | UTF-8 boundary truncation: snippet truncation at 150 chars lands on valid UTF-8 char boundary; CJK characters not split | test | Unit test `format_payload_multibyte_utf8` using `"\u{4e16}\u{754c}".repeat(200)` as entry content; assert `snippet.is_char_boundary(snippet.len())` and `snippet.len() <= 450` (150 chars * 3 bytes max) | PENDING |
| AC-18 | Empty result handling: `format_compaction_payload` with zero entries and empty histogram returns `None`; with non-empty histogram and zero entries returns `Some(...)` containing histogram block | test | Unit tests `format_payload_empty_entries_returns_none` (empty entries + empty histogram → None) and `test_compact_payload_histogram_block_present` (non-empty histogram + empty entries → Some with histogram) | PENDING |
| AC-19 | Confidence sort order: flat table sorted by fused score descending; two-entry result with scores 0.9 and 0.3 lists 0.9 entry at row 1, 0.3 entry at row 2 | test | Unit test `format_payload_sorted_by_confidence` with two `IndexEntry` values in low-first order; assert row 1 has confidence 0.90, row 2 has 0.30 | PENDING |
| AC-20 | Token limit override: `token_limit = 100` produces output of at most 400 bytes | test | Unit test `format_payload_token_limit_override` on `format_compaction_payload` with `max_bytes = 400` and large content; assert `output.len() <= 400` | PENDING |
| AC-21 | Histogram block: non-empty histogram produces `"Recent session activity:"` block; empty histogram produces no histogram block; top-5 cap and count-descending sort apply | test | Unit tests `test_compact_payload_histogram_block_present` and `test_compact_payload_histogram_block_absent` | PENDING |
| AC-22 | `MIN_QUERY_WORDS = 5` boundary: 4-word UserPromptSubmit → `RecordEvent`; 5-word UserPromptSubmit → `ContextSearch` | test | Unit tests `build_request_userpromptsub_four_words_record_event` and `build_request_userpromptsub_five_words_context_search` with exact 4-word and 5-word inputs | PENDING |
| AC-23 | `MIN_QUERY_WORDS` inapplicable to SubagentStart: 1-word non-empty `prompt_snippet` returns `HookRequest::ContextSearch` | test | `hook.rs` unit test `build_request_subagentstart_one_word_prompt` with `event = "SubagentStart"` and `prompt_snippet = "implement"` asserting `ContextSearch` returned | PENDING |
| AC-24 | `IndexBriefingService` compiles and `handle_compact_payload` tests pass regardless of `mcp-briefing` feature flag | shell | `cargo test` (without `--features mcp-briefing`) passes all `handle_compact_payload` tests | PENDING |
| AC-25 | All tests constructing `HookRequest::ContextSearch` via struct literal compile after `source` field addition (uses `source: None` or `..` spread) | shell | `cargo build --release` and `cargo test` both pass without `dead_code`/`non-exhaustive` compile errors | PENDING |
| AC-R01 | `SNIPPET_CHARS` constant exists in `mcp/response/briefing.rs` and equals 150; `format_index_table` produces correct column layout for documented example row | test | Unit test asserting `SNIPPET_CHARS == 150`; format contract test asserting literal output for entry `{id=2, topic="product-vision", category="decision", confidence=0.60, snippet="Unimatrix is..."}` matches FR-12 column layout | PENDING |
| AC-R02 | `UNIMATRIX_BRIEFING_K=3` set in test environment does not cap `IndexBriefingService` results to 3 entries; `parse_semantic_k()` no longer exists | shell | Set env var; call `IndexBriefingService::index()` with default k; assert up to 20 entries returned; `grep -r "parse_semantic_k" crates/` returns no results | PENDING |
| AC-R03 | SQL metacharacters in `prompt_snippet` do not cause server panic or error; `SecurityGateway` wraps search calls for SubagentStart-sourced queries | test | Integration test: SubagentStart event with `prompt_snippet = "'; DROP TABLE ENTRIES; --"` returns normal result (possibly empty), not error or panic | PENDING |
