# crt-027 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `build_request` with SubagentStart + non-empty `prompt_snippet` returns `HookRequest::ContextSearch` with `query` = prompt_snippet, `source: Some("SubagentStart")`, and `session_id` from `input.session_id` | test | `hook.rs` unit test `build_request_subagentstart_with_prompt_snippet` asserting variant and all specified field values | PENDING |
| AC-02 | `build_request` with SubagentStart + absent or empty `prompt_snippet` returns `HookRequest::RecordEvent` (not `ContextSearch`) | test | `hook.rs` unit test `build_request_subagentstart_empty_prompt_snippet` covering (a) key absent, (b) key present with `""` | PENDING |
| AC-02b | `hook.rs` defines `MIN_QUERY_WORDS: usize = 5`; UserPromptSubmit with < 5 trimmed words falls through to `generic_record_event` (RecordEvent, no injection); UserPromptSubmit with >= 5 words routes to ContextSearch; SubagentStart with 1-word non-empty `prompt_snippet` routes to ContextSearch (MIN_QUERY_WORDS inapplicable) | test | Five `hook.rs` unit tests on `build_request`: 3-word query → RecordEvent, 1-word query → RecordEvent, 5-word query → ContextSearch, 6-word query → ContextSearch, SubagentStart `prompt_snippet="ok"` (1 word) → ContextSearch | PENDING |
| AC-03 | SubagentStart `ContextSearch` carries `session_id = input.session_id` (parent session, not ppid fallback); when a registered session exists, WA-2 histogram boost applies via `ServiceSearchParams.category_histogram` | test | Unit test asserting `session_id` field taken from `input.session_id`; integration test confirming histogram lookup fires | PENDING |
| AC-04 | SubagentStart is synchronous: `is_fire_and_forget` returns `false` for `HookRequest::ContextSearch`; response written to stdout via `transport.request()` | test | Unit test asserting `ContextSearch` is NOT matched by `is_fire_and_forget` pattern; no code change needed to `is_fire_and_forget` | PENDING |
| AC-05 | `HookRequest::ContextSearch` carries `source: Option<String>` with `#[serde(default)]`; SubagentStart sets `source = Some("SubagentStart")`; UserPromptSubmit sets `source = None`; `dispatch_request` uses `source.as_deref().unwrap_or("UserPromptSubmit")` for observation `hook` column | test | (a) `hook.rs` unit test: SubagentStart builds `source = Some("SubagentStart")`; (b) `listener.rs` integration test: `source: Some("SubagentStart")` writes `hook = "SubagentStart"` to observations table; (c) existing UserPromptSubmit path writes `hook = "UserPromptSubmit"` | PENDING |
| AC-SR01 | CONFIRMED: SubagentStart stdout injection works in Claude Code via `hookSpecificOutput` JSON envelope. The hook process writes `{"hookSpecificOutput": {"hookEventName": "SubagentStart", "additionalContext": "<content>"}}` to stdout; Claude Code injects `additionalContext` into the subagent before its first token. Confirmed via Claude Code hooks documentation. | test | Unit test on `write_stdout_subagent_inject` with SubagentStart source confirms valid JSON output with `hookSpecificOutput.additionalContext` containing formatted entries (see AC-SR02) | CONFIRMED |
| AC-SR02 | When SubagentStart routes to `ContextSearch` and receives a non-empty response, `hook.rs` writes the `hookSpecificOutput` JSON envelope to stdout; output MUST parse as valid JSON with `hookSpecificOutput.hookEventName == "SubagentStart"` and `hookSpecificOutput.additionalContext` containing non-empty formatted entry string | test | Unit test on `write_stdout_subagent_inject` (stdout-writing path): assert output is valid JSON, `hookEventName = "SubagentStart"`, `additionalContext` non-empty | PENDING |
| AC-SR03 | When `UserPromptSubmit` routes to `ContextSearch`, `hook.rs` writes plain text to stdout (NOT the `hookSpecificOutput` JSON envelope); stdout from a UserPromptSubmit response MUST NOT contain the string `"hookSpecificOutput"` | test | Unit test with `source = None` (UserPromptSubmit): assert stdout does not start with `{` and does not contain `"hookSpecificOutput"`; paired with AC-SR02 to confirm structural divergence | PENDING |
| AC-06 | `context_briefing` returns only `status = Active` entries; a test database with one Active and one Deprecated entry sharing the same topic includes only the Active entry in the response | test | `tools.rs` integration test with mixed-status test database asserting Deprecated entry absent from result | PENDING |
| AC-07 | `context_briefing` default `k` is 20; `UNIMATRIX_BRIEFING_K` env var is not read by `IndexBriefingService`; a call with no `k` parameter returns up to 20 entries; a call with `k=5` returns up to 5 entries | test | `tools.rs` unit/integration test asserting max result count = 20 by default; code inspection confirms `UNIMATRIX_BRIEFING_K` is not referenced in `IndexBriefingService`; runtime test with env var set to 3 confirms 20-entry behavior | PENDING |
| AC-08 | Both `context_briefing` and `handle_compact_payload` output a flat indexed table with columns: row number, id, topic, category, confidence (2 decimal places), 150-char snippet; no section headers (`## Decisions`, `## Injections`, `## Conventions`) appear | test | `tools.rs` test asserting flat table column format and absence of `"## Decisions"` / `"## Injections"` / `"## Conventions"` strings; `listener.rs` integration test asserting same for CompactPayload response | PENDING |
| AC-09 | Query derivation priority order is correct at both call sites: (1) explicit `task` param used as query when provided and non-empty; (2) when `task` absent/empty and `session_id` present, query synthesized from `feature_cycle` + top 3 `topic_signals` by vote count; (3) when no session state or empty signals, `topic` param used as query; both call sites use the same shared helper function | test | Unit tests covering all three steps against `derive_briefing_query` directly; code inspection confirms a single shared function call site in both MCP handler and `handle_compact_payload` | PENDING |
| AC-10 | `handle_compact_payload` query derivation uses the already-held session state for step 2 (no `SessionRegistry` lookup by ID); `context_briefing` MCP tool uses `SessionRegistry` for step 2; both delegate to the same shared helper | test | Code inspection and unit test for UDS path confirming no registry lookup when session state is held directly | PENDING |
| AC-11 | `context_briefing` `session_id` parameter, when provided, is passed as `ServiceSearchParams.session_id`, causing category histogram lookup for WA-2 boost | test | Integration test: register a session, accumulate a category histogram entry, call `context_briefing` with that `session_id`; result ranks higher-histogram-category entries first relative to a call without `session_id` | PENDING |
| AC-12 | `handle_compact_payload` no longer calls `BriefingService::assemble()`; it calls `IndexBriefingService::index()` and produces `HookResponse::BriefingContent` with flat indexed table format | test | `BriefingService` import absent from `listener.rs` after migration; `listener.rs` integration test covering `HookRequest::CompactPayload` asserts flat table format in response | PENDING |
| AC-13 | `BriefingService` struct, `BriefingParams`, `BriefingResult`, `InjectionSections`, `InjectionEntry`, all `BriefingService` methods, all tests in `briefing.rs`, and all re-exports from `services/mod.rs` are deleted; no dead code remains; `cargo build --release` emits no `dead_code` warnings for these types | grep | `grep -r "BriefingService" crates/` returns no results (excluding docs/specs); `briefing.rs` original content confirmed deleted | PENDING |
| AC-14 | `uni-delivery-protocol.md` includes `context_briefing(topic="{feature-id}", session_id: "{session-id}", max_tokens: 1000)` immediately after `context_cycle(type: "start", ...)` (1 instance) and after each `context_cycle(type: "phase-end", ...)` (5 instances = 6 total); each call specifies `max_tokens: 1000` | grep | `grep -c "context_briefing" .claude/protocols/uni/uni-delivery-protocol.md` returns >= 6; visual diff showing all six insertion points; each call has `max_tokens: 1000` | PENDING |
| AC-15 | `cargo test` passes without any pre-existing tests being silently deleted; test count at module level for `listener.rs`, `hook.rs`, and `tools.rs` (briefing path) is non-decreasing from pre-feature baseline | shell | CI run; test count comparison before/after using `cargo test -- --list 2>&1 \| wc -l` | PENDING |
| AC-16 | Byte budget enforcement: when total serialized flat table exceeds `max_bytes`, output is truncated; output byte length MUST be <= `max_bytes`; low-ranked rows dropped first | test | Unit test `format_payload_budget_enforcement`: large content entries + small `max_bytes` override; assert `output.len() <= max_bytes` | PENDING |
| AC-17 | UTF-8 boundary truncation: snippet truncation at 150 chars MUST land on a valid UTF-8 character boundary; CJK characters (3 bytes each) MUST NOT be split | test | Unit test `format_payload_multibyte_utf8`: `"\u{4e16}\u{754c}".repeat(200)` as entry content; assert `snippet.is_char_boundary(snippet.len())` and `snippet.len() <= 450` (150 chars * 3 bytes) | PENDING |
| AC-18 | Empty result handling: when `IndexBriefingService` returns zero entries AND histogram is empty, `format_compaction_payload` returns `None`; when histogram is non-empty but entries are empty, function returns `Some(...)` containing the histogram block | test | Unit tests `format_payload_empty_entries_returns_none` (both empty → None) and `test_compact_payload_histogram_only_categories_empty` (histogram non-empty, entries empty → Some with histogram) | PENDING |
| AC-19 | Confidence sort order: flat table MUST be sorted by fused score descending; a two-entry result with scores 0.9 and 0.3 lists the 0.9 entry as row 1 and 0.3 entry as row 2 | test | Unit test `format_payload_sorted_by_confidence`: two `IndexEntry` values in low-first order; assert row 1 shows `0.90` and row 2 shows `0.30` in rendered output | PENDING |
| AC-20 | Token limit override: when `token_limit` supplied to `handle_compact_payload`, byte budget is `min(token_limit * 4, MAX_COMPACTION_BYTES)`; a call with `token_limit = 100` produces output of at most 400 bytes | test | Unit test `format_payload_token_limit_override`: `max_bytes = 400`, large content; assert `output.len() <= 400` | PENDING |
| AC-21 | Histogram block: when category histogram is non-empty, output MUST include the histogram block (`"Recent session activity:"`); when empty, histogram block MUST be absent; top-5 cap and count-descending sort apply | test | Unit tests `test_compact_payload_histogram_block_present` and `test_compact_payload_histogram_block_absent` on rewritten `format_compaction_payload` | PENDING |
| AC-22 | Boundary test for `MIN_QUERY_WORDS = 5` in UserPromptSubmit: exactly 4-word prompt → `generic_record_event` (RecordEvent); exactly 5-word prompt → `ContextSearch` | test | Two `hook.rs` unit tests `build_request_userpromptsub_four_words_record_event` and `build_request_userpromptsub_five_words_context_search` on `build_request` | PENDING |
| AC-23 | `MIN_QUERY_WORDS` is inapplicable to SubagentStart; SubagentStart with a 1-word non-empty `prompt_snippet` returns `HookRequest::ContextSearch` (not `RecordEvent`) | test | `hook.rs` unit test with `event = "SubagentStart"` and `prompt_snippet = "implement"` (1 word) asserting `ContextSearch` is returned | PENDING |
| AC-23b | SubagentStart with whitespace-only `prompt_snippet` (e.g., `"   "`) falls through to `generic_record_event` and returns `HookRequest::RecordEvent`; the guard uses `.trim().is_empty()`, NOT `.is_empty()`, so `"   "` is treated as absent | test | `hook.rs` unit test with `event = "SubagentStart"` and `prompt_snippet = "   "` asserting `RecordEvent` (not `ContextSearch`) is returned | PENDING |
| AC-23c | UserPromptSubmit word count uses `.trim().split_whitespace().count()`; a prompt of `"  approve  "` has word count 1 (< `MIN_QUERY_WORDS`), falls through to `generic_record_event`; leading/trailing whitespace MUST NOT inflate word count | test | `hook.rs` unit test with `event = "UserPromptSubmit"` and `query = "  approve  "` asserting `RecordEvent` is returned (not `ContextSearch`) | PENDING |
| AC-24 | `IndexBriefingService` compiles and is exercised by `handle_compact_payload` tests regardless of the `mcp-briefing` feature flag; `cargo test` (without `--features mcp-briefing`) passes all `handle_compact_payload` tests | shell | CI run without `--features mcp-briefing`; confirm `handle_compact_payload` tests pass | PENDING |
| AC-25 | All tests constructing `HookRequest::ContextSearch` via struct literal compile after the `source` field addition; existing struct literal construction uses `..` spread or explicitly adds `source: None` | shell | `cargo build --release` and `cargo test` pass without `dead_code`/`non_exhaustive` compile errors | PENDING |
