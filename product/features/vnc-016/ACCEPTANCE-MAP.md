# vnc-016 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `test_dependency_on_deprecated_e2e` exists in `test_tools.py` and passes against the live MCP server | test | `pytest product/test/infra-001/suites/test_tools.py -k test_dependency_on_deprecated_e2e` exits 0 | PENDING |
| AC-02 | Test executes the 9-step scenario (enroll Restricted+Write agent, store A with `feature_cycle` using that agent, store B, add Prerequisite A→B, deprecate A, seed observations with same cycle_id, call `context_cycle_review(force=True)`, assert finding) in stated order | manual | Code inspection of `test_dependency_on_deprecated_e2e` body; all 9 steps present and in order; step 2 (enroll) precedes step 3 (store A) precedes step 4 (store B) | PENDING |
| AC-03 | Assertion checks `rule_name == "dependency_on_deprecated"` in the `hotspots` array of the JSON response (exact string from `scope.rs:286`) | manual | Code inspection: `any(h["rule_name"] == "dependency_on_deprecated" for h in data["hotspots"])` present in test body | PENDING |
| AC-04 | `query_stale_prerequisite_edges_for_cycle` in `read.rs` uses `fe.feature_id` in the WHERE clause, not `fe.feature_cycle` | grep | `grep -n 'fe\.feature_id' crates/unimatrix-store/src/read.rs` contains line ~1618; `grep -n 'fe\.feature_cycle' crates/unimatrix-store/src/read.rs` returns no results | PENDING |
| AC-05 | `harness/client.py` `context_store()` accepts optional `feature_cycle` keyword argument; absent by default; forwarded to args dict only when non-None | manual | Code inspection: `feature_cycle: str \| None = None` in signature; `if feature_cycle is not None: args["feature_cycle"] = feature_cycle` guard present; all existing call sites unmodified | PENDING |
| AC-06 | All existing integration tests and Rust unit tests pass without regression | test | `cargo test --workspace` exits 0; full `pytest product/test/infra-001/` run exits 0 | PENDING |
| AC-07 | Both integration tests call `context_cycle_review` with `force=True` | manual | Code inspection of both `test_dependency_on_deprecated_e2e` and `test_dependency_on_deprecated_no_finding_without_stale_edge` bodies; `force=True` present in every `context_cycle_review` call | PENDING |
| AC-08 | `test_dependency_on_deprecated_no_finding_without_stale_edge` passes: `dependency_on_deprecated` not in hotspots when no stale Prerequisite edge exists for the cycle | test | `pytest product/test/infra-001/suites/test_tools.py -k test_dependency_on_deprecated_no_finding_without_stale_edge` exits 0 | PENDING |
| AC-09 | Rust unit test in `unimatrix-store` calls `query_stale_prerequisite_edges_for_cycle` directly; asserts `result.is_ok()`, `result.unwrap().len() == 1`, and `result.unwrap()[0] == (A.id, B.id)` | test | `cargo test -p unimatrix-store test_query_stale_prerequisite_edges_for_cycle_returns_pair` exits 0; test body contains all three sub-assertions | PENDING |
| AC-10 | `UsageContext` in `usage.rs` has a `write_capable: bool` field with no `#[serde(default)]` or `Default` derivation | grep | `grep -n 'write_capable' crates/unimatrix-server/src/services/usage.rs` shows field declaration; `cargo build --workspace` exits 0; `grep -n 'serde(default)' crates/unimatrix-server/src/services/usage.rs` does not appear adjacent to `write_capable` | PENDING |
| AC-11 | The `feature_recording` gate in both `record_mcp_usage` and `record_hook_injection` checks `ctx.write_capable` (not trust level); `TrustLevel` is not referenced in either gate block | grep | `grep -n 'TrustLevel' crates/unimatrix-server/src/services/usage.rs` shows no matches within the gate blocks at lines 207-218 and 272-283; `grep -n 'write_capable' crates/unimatrix-server/src/services/usage.rs` shows matches in both gate locations | PENDING |
| AC-12 | Integration test enrolls a Restricted-trust agent with Write capability and uses that agent (not `agent_id="human"`) for the `context_store` call that tags entry A to the feature cycle | manual | Code inspection of `test_dependency_on_deprecated_e2e`: `context_enroll` call with `trust_level="restricted"` and `capabilities=["write", "read"]` appears before `context_store` for entry A; `context_store` for entry A uses the enrolled `test_agent_id`, not `"human"` | PENDING |
| AC-13 | A regression test confirms that a Restricted-trust agent WITHOUT Write capability is rejected by `require_cap` from reaching `context_store` at all; the `write_capable: false` default is never exercised on a rejected path | test | `cargo test -p unimatrix-server` exits 0; test in `usage.rs` mod tests for the `write_capable: false` branch is present and passing | PENDING |
