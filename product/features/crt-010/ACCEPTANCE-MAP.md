# crt-010 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | UDS search returns zero deprecated entries and zero superseded entries regardless of similarity scores | test | Integration test: insert Active + Deprecated + Superseded entries with Deprecated having higher similarity; UDS search returns only Active |
| AC-02 | MCP search returns deprecated entries ranked below Active entries at comparable similarity (within 5% similarity delta) | test | Integration test: Active entry sim=0.88, Deprecated entry sim=0.90; Active ranks higher after 0.7x penalty |
| AC-03 | Superseded entries in MCP search receive harsher penalty than plain deprecated entries | test | Integration test: Deprecated (no successor) score > Superseded (with successor) score at equal base similarity; verify 0.5x vs 0.7x |
| AC-04 | Supersession injection: when deprecated entry X has `superseded_by: Y` (Active), Y appears in results even if Y was not in HNSW top-k | test | Integration test: query matches X's embedding closely but not Y's; Y appears in results via injection with its own cosine similarity score |
| AC-05 | Injected successor similarity is computed via cosine similarity against query embedding, not inherited from predecessor | test | Unit test: injected successor's score reflects its own embedding distance from query, not the deprecated entry's similarity |
| AC-06 | Single-hop limit enforced: if successor Y is itself superseded by Z, Z is not injected | test | Integration test: chain A→B→C; search matching A injects B but not C; B skipped if it has `superseded_by` set |
| AC-07 | Dangling `superseded_by` reference (non-existent entry) does not cause error or panic | test | Unit test: entry with `superseded_by: 99999`; search completes successfully with no injection, no error |
| AC-08 | Co-access boost excludes deprecated entries: deprecated anchor produces no boost; deprecated partner produces no boost | test | Unit test: co-access pair (Active, Deprecated) returns zero boost for both directions when deprecated ID in `deprecated_ids` set |
| AC-09 | Co-access pairs involving deprecated entries continue to be stored (write path unchanged) | test | Unit test: record co-access between Active and Deprecated entries; verify pair exists in storage |
| AC-10 | UDS strict mode returning zero results yields empty set, not fallback | test | Integration test: knowledge base with only deprecated entries for a query topic; UDS returns empty results with `total_tokens: 0` |
| AC-11 | BriefingService injection history excludes deprecated entries | test | Integration test: briefing payload contains no deprecated entries even if they were previously injected |
| AC-12 | Vector compaction excludes deprecated and quarantined entries from rebuilt HNSW (ALREADY SATISFIED — verification of existing col-013 behavior) | test | Integration test: after background tick compaction, deprecated entry IDs absent from VECTOR_MAP; Active entries present. This is a verification test of existing behavior, not new logic. |
| AC-13 | MCP `context_search` without filters applies flexible mode (deprecated penalized, not excluded) | test | Integration test: unfiltered MCP search returns deprecated entry with reduced score (0.7x), not excluded |
| AC-14 | MCP `context_search` with explicit `status: Deprecated` returns deprecated entries at full score (no penalty) | test | Integration test: explicit status filter bypasses penalty multipliers; deprecated entries returned at full computed score |
| AC-14b | MCP `context_search` with explicit `status: Deprecated` disables supersession injection — no Active successors injected | test | Integration test: deprecated entry with `superseded_by` set; explicit `status: Deprecated` search returns the deprecated entry but does NOT inject its Active successor |
| AC-15 | No new MCP tools, no new tool parameters, no schema changes | grep | `grep -r "Tool::new\|#\[tool\]" crates/unimatrix-server/src/mcp/` shows no new tool registrations; diff shows no new parameters added to existing tools; no migration files added |
| AC-16 | Search latency p95 regression under 15% on 200-entry knowledge base with 50% deprecation ratio | manual | Benchmark test or manual measurement: baseline p95 latency vs post-implementation p95 latency on synthetic 200-entry KB |
