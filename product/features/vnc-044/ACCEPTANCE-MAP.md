# vnc-044 Acceptance Criteria Map

> Every AC from SCOPE.md (AC-01..AC-09, incl. AC-03b) mapped to a verification method. AC-01 is a Phase-2a companion deliverable (the suite-wide ADR) owned by uni-architect — verified by artifact review, not by the `context_graph` implementation. AC-02..AC-09 are the implementation criteria. Test cases trace to RISK-TEST-STRATEGY.md (R-01..R-13).

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Suite-wide ADR defines the two-axis contract (`format`=serialization `markdown\|json`; `detail` `summary\|full`; default `summary`; legacy `format=summary`→`detail=summary`), the summary field set, the shared 256-byte `CONTENT_PREVIEW_BYTES` constant, and records crt-057 (#894) reconciliation. | file-check | `product/features/vnc-044/architecture/ADR-001-two-axis-format-verbosity-contract.md` exists and is Unimatrix entry #5509; §2 (axis), §5 (field set), §6 (256 constant), §Reconciliation (crt-057) present. | PENDING |
| AC-02 | `context_graph` exposes both axes and honors them end-to-end; resolved values reach serialization (parse-and-drop at `graph_read.rs:251` fixed); `format` no longer selects verbosity. | test | Integration test: same query with `detail=summary` vs `detail=full` yields different payloads; code inspection confirms `handle_graph` reads resolved `(Detail, GraphSerialization)` from `resolve_graph_output`. (R-01 integration seam.) | PENDING |
| AC-03 | With `detail=summary`, a node serializes to exactly `{id,title,category,tags,status,confidence,content_preview,content_truncated}` and edges to exactly `{source_id,target_id,relation_type,depth}`; `content`, hashes, `embedding_dim`, timestamps, `created_by`/`modified_by`, counts omitted; `status` is lifecycle `EntryRecord.status`. | test | Serialization test asserting the exact JSON key set — present keys AND absent keys — for a summary node and edge; `status` value via `status_str(entry.status)`. (R-07.) | PENDING |
| AC-03b | `content_preview` = first ≤256 bytes of `content` floored to a UTF-8 char boundary (always valid UTF-8, never splits a multibyte char). `content_truncated` = `true` iff `content` exceeded 256 bytes, `false` otherwise. No ellipsis. | test | Table-driven unit test on `content_preview(&str)`: (a) empty→`("",false)`; (b) <256B→whole,`false`; (c) exactly-256B→whole,`false`; (d) 257B ASCII→256B,`true`; (e) multibyte straddling byte 256→floored below 256 on a boundary, valid UTF-8, `true`. Assert `content_truncated == content.len() > 256`; assert no `…`. (R-01, R-02 — Critical.) | PENDING |
| AC-04 | With `detail=full`, `context_graph` returns the current full-`EntryRecord` payload byte-for-byte (no regression for existing full consumers). | test | Golden/byte-equality test comparing `detail=full` output to the pre-vnc-044 captured payload for a fixed query; `subgraph` + ≥1 other node-bearing mode; key order and field presence asserted. (R-04 — Critical.) | PENDING |
| AC-05 | Default verbosity is `summary`: a `context_graph` call with no verbosity axis returns the lean projection (accepted behavior change). | test | Test per node-bearing mode: call without `detail` → output equals `detail=summary` output and differs from `detail=full`. (R-03 — Critical, all five modes.) | PENDING |
| AC-06 | The #913 reproduction (vision-root subgraph, 75 nodes / 82 edges) drops from ~135KB to a few KB by default and is valid parseable JSON. Result carries **lifecycle** `status` only — a capability subgraph shows `active` for every node and does NOT deliver a delivery-status tally; this limitation is documented. | test + manual | Reproduction test: payload size < agreed KB threshold, output is valid JSON. Manual/doc review: tool description + AC-06 notes state the lifecycle-vs-delivery caveat. (R-06 size, R-11 doc gate — do NOT treat delivery-status absence as a defect.) | PENDING |
| AC-07 | Legacy `format=summary` remains accepted and maps to `detail=summary` (serialization `json`); no working caller breaks. | test | Test: `format=summary` (no `detail`) → byte-identical to `detail=summary` output, no error. `format=summary` + explicit `detail` → `ERROR_INVALID_PARAMS` (conflict, pinned). (R-08.) | PENDING |
| AC-08 | `format=markdown` on any graph mode is rejected with `ERROR_INVALID_PARAMS` naming the reason and pointing to `format=json`; no silent JSON fallback. `detail` on `neighbors`/`path` is accepted and ignored. | test | Test: `format=markdown` on **all seven** modes → `ERROR_INVALID_PARAMS`, reason substring (`"markdown"`, `"format=json"`) asserted, no JSON body. `detail=summary`/`detail=full`/absent on `neighbors`/`path` → identical non-erroring output; `detail=bogus` → `ERROR_INVALID_PARAMS`. (R-05, R-09, R-13.) | PENDING |
| AC-09 | `GraphParams` field layout invariant holds — new axis additive, existing fields unmoved (ADR-003 preserved); tool description documents both axes and states `status` is lifecycle status. | test + manual | Existing `GraphParams` layout test passes with additive `Option<String> detail`. Tool-description review: both axes, default-summary + per-tool divergence, `format=markdown` rejection, lifecycle-vs-delivery caveat present. (R-11, NFR-3.) | PENDING |
