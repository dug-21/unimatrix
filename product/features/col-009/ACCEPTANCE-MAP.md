# col-009 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Schema v4 migration: SIGNAL_QUEUE table created, next_signal_id=0 in COUNTERS, schema_version=4; existing entries intact | test | `test_migration_v3_to_v4`: open v3 test db with 10 entries, call Store::open(), assert schema_version==4, SIGNAL_QUEUE exists, next_signal_id==0, all 10 entries readable | PENDING |
| AC-02 | For session with 3 distinct injected entries and "success" outcome, exactly one Helpful SignalRecord with entry_ids=[id1,id2,id3] is written | test | `test_signal_generation_success_session`: populate SessionState with 3 entries, call drain_and_signal_session("success"), assert signal_queue_len()==1, drain returns 1 Helpful record with 3 entry_ids | PENDING |
| AC-03 | Session-scoped dedup: if drain_and_signal_session called twice, second call returns None | test | `test_drain_and_signal_idempotent`: call method twice with same session_id, assert second returns None, SIGNAL_QUEUE has exactly 1 record | PENDING |
| AC-04 | Confidence consumer increments helpful_count exactly once per entry_id in Helpful SignalRecord | test | `test_confidence_consumer_helpful_count`: insert Helpful signal for entry_ids [1,2], run_confidence_consumer(), fetch entries 1 and 2, assert helpful_count incremented by 1 each | PENDING |
| AC-05 | Abandoned sessions (empty or None outcome) produce no signals | test | `test_abandoned_session_no_signals`: session with 3 injections, call drain_and_signal_session(""), assert returns None, signal_queue_len()==0 | PENDING |
| AC-06 | Rework outcome produces Flagged signals; helpful_count and unhelpful_count NOT modified | test | `test_rework_signals_flagged_only`: populate rework_events crossing threshold, call drain_and_signal_session("success"), assert signal_type==Flagged; run both consumers, assert helpful_count and unhelpful_count unchanged for all entries | PENDING |
| AC-07 | Flagged signals appear in entries_analysis in context_retrospective response | test | `test_entries_analysis_in_retrospective`: generate rework session signals, run retrospective consumer, call context_retrospective MCP tool, assert response.entries_analysis is non-empty with correct entry_ids | PENDING |
| AC-08 | After 3 edit-fail-edit cycles on same file, rework threshold crossed | test | `test_rework_threshold_three_cycles`: populate ReworkEvents: Edit(foo.rs,no_fail), Bash(fail), Edit(foo.rs,no_fail), Bash(fail), Edit(foo.rs,no_fail), Bash(fail), Edit(foo.rs,no_fail); assert has_crossed_rework_threshold()==true | PENDING |
| AC-09 | Stale session sweep evicts sessions with last_activity_at < now - 4h | test | `test_stale_session_sweep`: register session, set last_activity_at = now - (4*3600+1), call sweep_stale_sessions(), assert session removed from registry and returned in sweep output | PENDING |
| AC-10 | SIGNAL_QUEUE capped at 10,000 records; oldest dropped when exceeded | test | `test_signal_queue_cap`: insert 10,001 signals sequentially, call signal_queue_len(), assert ==10,000; drain all, assert record with lowest signal_id (first inserted) is absent | PENDING |
| AC-11 | All existing 1,025 unit + 174 integration tests pass | shell | `cargo test --workspace` exits 0 with no test failures | PENDING |
| AC-12 | Signal consumer processes 50 entries in < 100ms | test | `test_confidence_consumer_performance`: insert Helpful signal for 50 entry_ids, time run_confidence_consumer(), assert duration < 100ms | PENDING |
| AC-13 | entries_analysis absent from JSON (not null) when None | test | `test_entries_analysis_absent_when_none`: serialize RetrospectiveReport with entries_analysis=None, assert JSON string does not contain "entries_analysis" key | PENDING |
