# nan-022 Acceptance Criteria Map

GH Issue #837. TEST-ONLY parity suite (C0 proof artifact). Every AC-01..AC-12 from SCOPE.md
appears below with its verification method. Most proving load is OFF-Docker (comparator
teeth, classifier order, tolerance policy, drift guard, bundle contract, no-seed audit); the
live Docker matrix proves #5298 frame byte-identity, WAL-flush barrier ordering,
bridge-in-path carriage, and cross-language bundle emit.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | One canonical workload drives BOTH transports in ONE pytest invocation, under one stable session identity + one run-correlation token; zero seeded attribution (static guard extended to all new modules + seed loader). | test + shell | Off-Docker: assert single `ParityWorkload` object, `run_token == workload.session_id`, one barrier helper after augmentation; manifest `to_json`/`from_json` round-trip stable. `assert_no_seed_reachable` covers every net-new module + seed-corpus loader (`FORBIDDEN_SEED_SITES` unreachable). Live: both legs replay the same augmented manifest. (FR-1..FR-4; R-13, R-15) | PENDING |
| AC-02 | Retrieval parity: identical query set + identically-seeded store → same result ids in same ranked order, MODULO closed tie/score tolerance (minimized). | test | Off-Docker `ranking_parity`: deep-prefix-match → matched; in-prefix divergence → not matched; tie-class permutation → match (membership, not position); tie-class member loss → not matched; scores-absent → documented membership-only fallback; prefix floor N>1 asserted. Live: stable-prefix ordered-set compare cross-leg; double-capture-diff classifies HNSW/tie flip as INTRA-TRANSPORT-NONDETERMINISM. (D1; R-01, R-06, R-07) | PENDING |
| AC-03 | Behavioral-signal parity: every driven observation `topic_signal == feature` (derived, never seeded) identically on both legs; cross-leg compared. | test | Live: symmetric HTTPS+UDS `topic_signal` read from per-slug `observations` (DB read gated behind durability barrier); string-exact cross-leg compare; `unattributed`/NULL HARD fail. Off-Docker: `AttributionComparator` exclusion set is empty (no wall-clock field). (D2; R-04) | PENDING |
| AC-04 | Analytics/learning parity: `MetricVector` (nan-021 comparator verbatim) AND `Informs` edge set + phase signal equal field-for-field modulo closed set. | test | Off-Docker: `MetricVectorComparator` adapter delegates to unchanged `compare_metric_vectors`/`EXCLUDED` — golden nan-021 pair produces identical diff list through the adapter (logic NOT altered). Live: net-new `Informs`-edge SET compare (IDs exact, edge wall-clock fields justified-excluded) + exact phase compare, barrier-gated. (D3; R-11, R-14) | PENDING |
| AC-05 | Proactive-delivery parity: `context_briefing` ranked index + injection set identical across legs modulo closed tolerance. | test | Live: stable-prefix briefing compare + injection-set equality. Off-Docker: `BriefingComparator` imports the SAME `ranking_parity` policy as retrieval (single-sourced, no second tie policy). Double-capture-diff for intra-stability. (D4; R-01, R-07) | PENDING |
| AC-06 | PreCompact-restoration parity: restored compact-context payload identical across legs modulo closed wall-clock/ordering set. | test + manual | Live: symmetric `/observe` PreCompact (`CompactContext` `wire.rs:171`/#670) capture; byte-equal over restored entry ids/content modulo exclusion set. Measurability determined at first live drive: if a host-side (CC) component is undrivable, `measurable=False` + named `host_side_gap` is a DOCUMENTED measurability call-out in the evidence table (manual human review), never a vacuous pass and never a silent drop. (D5; R-08) | PENDING |
| AC-07 | Per-slug isolation parity: under BOTH transports, slug-A write not visible to slug B and lands only in slug A's store. | test | Live: isolation probe per transport (write slug A `/observe`, read-back slug B MCP, on-disk landing in `/data/.unimatrix/<slug>/` not hash dir, DB read barrier-gated); cross-transport equality of the isolation boolean — compared EXACTLY (no tolerance, NFR-6). Builds on posture-smoke per-slug Gates 1–4. (D6; R-04) | PENDING |
| AC-08 | CI-runnable parity matrix in the release-gate Docker lane (`workflow_dispatch`/tag), false-green-proof (Docker-absent HARD-fails by distinct exit code; anchored run-marker asserted), per-dimension PASS/FAIL table keyed by run token. | test + shell | Off-Docker: `rollup` exit-code truth table; roll-up unit (GREEN iff all PARITY_PASS; any PARITY_FAIL → RED; INFRA_ERROR → distinct ERROR exit; INTRA-NONDET does not redden). Live: matrix emits per-dimension evidence table keyed by run token; skip-when-Docker-absent HARD-fails by distinct exit code; anchored run-marker tied to run token asserted present. (FR-9/17/18; R-02, R-10, R-12) | PENDING |
| AC-09 | Every per-dimension exclusion set is closed, enumerated, individually justified in code; none silently widened; amendments need product sign-off. | test + grep | Off-Docker `assert_comparator_contract(DIMENSIONS)`: every `Dimension.comparator` is a `DimensionComparator` subclass; each `EXCLUDED` key appears in `EXCLUSION_JUSTIFICATIONS` (no unjustified exclusion); `FORBIDDEN_SEED_SITES` referenced from the ONE definition (import-graph/grep — no private copy); `capture_key`s unique and match the bundle schema. Negative: unjustified entry → guard FAILS off-Docker. (FR-8/16, NFR-8; R-05) | PENDING |
| AC-10 | A real parity defect surfaced by any dimension is filed as a new GH bug; gate stays RED; fix not absorbed. | manual | A two-intra-stable-legs cross-leg divergence classifies PARITY-FAIL → gate RED + first-live-run field-by-field evidence record emitted; disposition is "file a NEW GH bug, do NOT fix here" (PRODUCT/HUMAN call). Verified by review of the failure-handling path + no fix code in the diff. (FR-14) | PENDING |
| AC-11 | Zero production-code change; diff is test infra only, cumulative on `infra-001`, no fork. | shell + file-check | `git diff` confined to `product/test/infra-001/`; no `crates/**`, shipped `lib/`, or production-script change (SCOPE-FAIL guard). Review-flag any net-new transport/cert/spawn/framing code in the HTTPS leg (bridge-in-path reuse verified). (NFR-1/NFR-2; R-16) | PENDING |
| AC-12 | Passing the full matrix is C0's proof artifact (evidence to flip C0 #5304 → proven). This feature does NOT itself flip C0. | manual + file-check | The per-dimension PASS table keyed by run token IS the behavioral evidence an authorized session uses to flip C0. Verified: no C0-flip action in this feature's diff; `Dimension.blocks_c0_proof` (default all six True) encodes the flip-bar disposition — RESOLVED (human-confirmed 2026-06-25): all six block, the corrected C0 (#5304) `done_when` makes parity the total bar, any unreachable dimension a human-signed documented exception. Flipping C0 is explicitly out-of-scope (SPEC §8). | PENDING |
