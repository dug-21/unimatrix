# crt-052 Acceptance Criteria Map

GH Issue: #689. Source: product/features/crt-052/SCOPE.md (AC-01..AC-13) + specification/SPECIFICATION.md
supplementary verification (AC-V-SEAM, AC-V-FUZZ). Every SCOPE AC appears. "Merge gate" ACs (per
RISK-TEST-STRATEGY §Merge Gates) are marked in the Description.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Snapshot-and-release: bytes of every session attributed to the reviewed cycle snapshotted before purge; Arc clones under single registry lock, byte copies under per-buffer locks, all parsing/matching after every lock release. **[MERGE GATE]** | test + grep | (a) source/grep assertion no parse or marker-match call occurs inside a lock-guard scope; (b) concurrency/stress test streaming `transcript_delta` during a cycle review — assert no deadlock, no torn read, consistent snapshot | PENDING |
| AC-02 | Selection contract + dual cap: keep only user/assistant text blocks (tool_use/tool_result/thinking/command-noise dropped); four families matched; blocks whole; deduped; per-session cap (24 KB) AND per-cycle aggregate cap enforced; ordered with session_id+byte_offset+ts+family_hints. | test | unit tests on pure selection module over fixtures: dropped block types absent; whole-block preservation; dedup; per-session AND per-cycle truncation at configured limits (two independent knobs); deterministic aggregate-cap truncation; ordering + provenance fields populated | PENDING |
| AC-03 | Independent fixture recall/volume: on the committed labeled corpus authored independently of the ported regex set, ≥0.90 block-level recall of in-block labeled items; selected volume ≤10% of raw bytes. | test + manual | committed fixture carries provenance header (anchors-before-port OR different-author); test computes block-level recall (≥0.90) and volume (≤10%); review check the independence header is present and asserts a mode | PENDING |
| AC-04 | Additive, absent-when-empty: response carries `transcript_candidates` as an additive section, absent (not null/empty) when no session yields candidates; pre-existing fields + col-024/crt-033 behaviors byte-unchanged when no transcripts exist. | test | serde round-trip test: field omitted from JSON when None; golden-output diff of a no-transcript cycle review vs pre-crt-052 output (byte-identical for existing fields) | PENDING |
| AC-05 | Four-return ordering: distill-before-purge at all four success returns (`tools.rs:2110/2236/2925/3027`); candidates reflect call-time content; purge fires after; error paths keep transcripts, no candidates; memoization-hit distills fresh (may differ from cached). **[MERGE GATE]** | test | per-path test for each of the four returns asserting (distill → purge) order; exhaustiveness regression test that fails if a fifth success return is added unwired; memoization-hit fresh-candidate test; error-path retained-no-candidates test | PENDING |
| AC-06 | Response-transient, no leak: memoized `cycle_review_index` record has no candidate/transcript content; no SQL/file/log line carries candidate or buffer content. **[MERGE GATE]** | test + grep | (a) structural/compile-level: `RetrospectiveReport` has no candidate field; (b) re-review-of-stored-record test returns report without stale candidates; (c) content-leak grep/log/SQL gate over all new paths (extends vnc-025 AC-12); (d) audit `detail` content-free; (e) metadata-only `Debug` on snapshot types | PENDING |
| AC-07 | Labeled reconstruction fallback: attributed session with empty/hole-ridden buffer contributes input from stored `ObservationRecord`s, labeled `Reconstructed`; never writes byte buffer, never produces observation rows; trigger whole-session either/or; `topic_source` only reorders never drops. | test | tests: (i) empty buffer + observations → reconstructed labeled degraded; (ii) hole-ridden ≥ threshold → whole-session fallback; (iii) assert no buffer write + no observation-row insert on fallback path; (iv) topic_source reorders only, drops no feature-matched session | PENDING |
| AC-08 | Loss never silent: per-session `elided_bytes`, hole indication, primary-vs-reconstructed provenance, and cap-forced candidate truncation (per-session volume cap AND per-cycle aggregate cap — AC-02) appear in the candidates section whenever non-zero/active. | test | tests: metadata block populated for elided/holed/reconstructed sessions; omitted when zero/inactive; provenance label matches source path; per-session AND per-cycle aggregate-cap candidate truncation surfaces dropped-count/affected-session (no silent aggregate-cap drop); cap-eviction / poison-recovery surfaces loss (not silently absent) | PENDING |
| AC-09 | Two-pipe boundary preserved: with distillation active, 23 detection rules' inputs bit-identical to pre-crt-052; batch filter `listener.rs:1238` unchanged; distillation reaches KB only via agent `context_store`. | test | extend `detection_isolation` tests to run with distillation active and assert rule inputs byte-identical; assert no new path feeds buffer bytes into `insert_observations_batch` | PENDING |
| AC-10 | Exhaustive retention match: distill+purge gated on exhaustive `TranscriptRetention` match (`PurgeOnCycleClose` `server.rs:543`, `RetainDays(_)` `:551`); `RetainDays` neither distills nor purges in OSS (rejected at `validate()`). | test | compile-level: no wildcard arm (exhaustive match); test asserting `RetainDays` config rejected at `validate()`; constructed-`RetainDays` test asserting helper returns None (no distill, no purge) | PENDING |
| AC-11 | Transcript continuity (Option B), simulated real lifecycle — the ONLY pre-merge primary-path proof. ≥3 simulated drain cycles with inter-drain deltas; re-adoption rebinds same cycle / fails loud on mismatch; held-count bounded + eviction observable; stale sweep reclaims; audit fires exactly once per held session. **[MERGE GATE]** | test | named test `continuity_simulated_lifecycle`: register → deltas → drain → deltas → drain → deltas → drain → re-register → cycle review (≥3 drains, inter-drain deltas). Assert: (a) snapshot has cross-turn content not just last; (b) re-adopt same-cycle / fail-loud mismatch (cite #981); (c) held-count within cap + observable eviction; (d) stale sweep reclaims without review; (e) `transcript_session_purged` exactly once per held session. A single-turn happy path does NOT satisfy. | PENDING |
| AC-12 | Throughput: server rule pass over a 4 MiB buffer < 50 ms off-lock; cycle-review latency class unchanged. | test | benchmark/timed test over a 4 MiB fixture asserting < 50 ms for the off-lock rule pass | PENDING |
| AC-13 | Consumer guidance + dependency posture: uni-retro skill / protocol cycle-review step updated (four families, Q8 folds, call-time-vs-cached note, feature-attributed `context_store`); `cargo audit` passes; no new heavyweight runtime dependency (regex-class only). | manual + shell | documentation review checklist (four families, Q8 folds, call-time-vs-cached note, feature-attributed `context_store` present); `cargo audit` pass; dependency-diff review confirming regex-class only | PENDING |
| AC-V-SEAM | Single-reader / #700 reuse (Constraint 4, SR-04): `take_transcripts_for_feature` returns owned bytes + per-session `elided_bytes`/`hole_info`; a #700-style marker-recovery caller parses the snapshot without `contiguous_tail` or any second buffer reader. **[MERGE GATE]** | test + grep | source/grep assertion that only two production buffer-content readers exist (PreCompact `contiguous_tail`, seam `snapshot()`) — fails if a third is added; #700-shaped reuse test parsing `TranscriptSnapshot.bytes` with its own patterns; assert snapshot exposes all four metadata fields (base_offset, high_water, elided_bytes, holes) | PENDING |
| AC-V-FUZZ | Untrusted-input no-panic (Constraint 7, SR-09): malformed/adversarial JSONL (truncated JSON, non-UTF-8, oversized line, unknown record type, embedded NUL) degrades to skip-with-count; parser and handler never panic. **[MERGE GATE]** | test | AC-V-FUZZ malformed/adversarial corpus at module + handler level asserting skip-with-count, no `Err`, no panic; truncated-final-line tolerated; bounded handling of nested/gigantic fields (no resource exhaustion); fully-corrupt snapshot → normal review response with candidates absent | PENDING |

## Prerequisite Delivery Gate (not an AC — blocks Wave B audit move)

| Gate | Description | Verification | Status |
|------|-------------|--------------|--------|
| ADR-009 no-consumer audit survey | Confirm no downstream consumer keys on per-close `transcript_session_purged` cadence before moving the audit points to review/sweep/evict (SR-03, RISK Coverage Gap 1). | survey `gc_audit_log` (crt-036), retention/analytics readers, per-close-emission tests; record result clean before Wave B merges | PENDING |
