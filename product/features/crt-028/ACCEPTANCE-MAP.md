# crt-028 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | When `input.transcript_path` is present and readable, PreCompact hook stdout begins with a transcript restoration block before the Unimatrix briefing content | test | `test_precompact_stdout_begins_with_transcript_block`: run `write_stdout` with a valid JSONL fixture and a non-empty `BriefingContent`; assert stdout starts with `"=== Recent conversation"` before any briefing content |  PENDING |
| AC-02 | Transcript restoration block includes user text turns and assistant text turns from the last k exchanges, most-recent first | test | `test_exchange_order_most_recent_first`: build JSONL with exchanges A, B, C; call `build_exchange_pairs`; assert C precedes B precedes A in the returned `Vec<ExchangeTurn>` |  PENDING |
| AC-03 | Tool use entries from assistant turns are included as compact `[tool: name(key_param) → snippet]` pairs truncated to ~300 bytes | test | `test_tool_pair_format_and_truncation`: construct assistant turn with `Read` tool_use and a 2000-byte tool_result; assert formatted pair matches `[tool: Read(/path) → ...]` with snippet ≤ 300 bytes |  PENDING |
| AC-04 | `type: "tool_result"` blocks in user turns are skipped (not included in restoration block) | test | `test_user_tool_result_skipped`: build user message with only `type:"tool_result"` content; assert `build_exchange_pairs` emits no `UserText` for that turn | PENDING |
| AC-05 | The combined transcript + briefing output does not exceed `MAX_PRECOMPACT_BYTES` for the transcript portion | test | `test_transcript_block_within_budget`: create JSONL with many exchanges; call `extract_transcript_block`; assert returned `String` byte length ≤ `MAX_PRECOMPACT_BYTES` (3000) | PENDING |
| AC-06 | When `input.transcript_path` is `None`, the hook behaves as if the transcript block is absent — no error, briefing content written normally | test | `test_transcript_path_none_writes_briefing`: call `prepend_transcript(None, "briefing_content")`; assert output equals `"briefing_content"` with no header prepended | PENDING |
| AC-07 | When the transcript file does not exist or cannot be opened, the hook silently skips transcript restoration and writes only the briefing content; exit code is 0 | test | `test_missing_transcript_file_writes_briefing`: call `extract_transcript_block("/nonexistent/path.jsonl")`; assert `None` returned; verify `prepend_transcript` with `None` + non-empty briefing produces non-empty stdout (SR-07 invariant, R-01) | PENDING |
| AC-08 | When the transcript file contains malformed JSONL, those lines are skipped silently; any parseable lines are used; exit code is 0 | test | `test_malformed_jsonl_skipped`: write JSONL file with mix of valid and invalid lines; call `extract_transcript_block`; assert valid lines produce exchange pairs and no panic/error | PENDING |
| AC-09 | When the transcript file contains no user/assistant pairs (e.g., post-compaction empty file), the transcript block is omitted and only the briefing content is written | test | `test_no_user_assistant_pairs_returns_none`: write JSONL file with only `type:"system"` lines; call `extract_transcript_block`; assert `None` returned | PENDING |
| AC-10 | `MAX_PRECOMPACT_BYTES` is a named compile-time constant in `hook.rs`, distinct from `MAX_INJECTION_BYTES` | grep | `grep -n "MAX_PRECOMPACT_BYTES" crates/unimatrix-server/src/uds/hook.rs` — assert constant defined with value 3000 and not aliased to `MAX_INJECTION_BYTES` | PENDING |
| AC-11 | The `source` field in `HookRequest::ContextSearch` is validated against allowlist `{"UserPromptSubmit", "SubagentStart"}` before writing to the observations `hook` column; unrecognized values fall back to `"UserPromptSubmit"` | test | `test_sanitize_observation_source_allowlist`: unit test `sanitize_observation_source` for all six cases: `Some("UserPromptSubmit")`→`"UserPromptSubmit"`, `Some("SubagentStart")`→`"SubagentStart"`, `None`→`"UserPromptSubmit"`, `Some("unknown")`→`"UserPromptSubmit"`, `Some("")`→`"UserPromptSubmit"`, `Some("UserPromptSubmitXXXXX")`→`"UserPromptSubmit"` (ADR-004, GH #354) | PENDING |
| AC-12 | A regression test in `index_briefing.rs` verifies that a `ScoredEntry` with `status: Quarantined` is excluded by the `index()` post-filter | test | `test index_briefing_excludes_quarantined_entry` in `services/index_briefing.rs`: store quarantined entry, run `index()` with matching query, assert entry ID absent from result (GH #355, mirrors deleted T-BS-08) | PENDING |
| AC-13 | `IndexBriefingService::index()` has a doc comment stating that query validation is delegated to `SearchService.search()` → `gateway.validate_search_query()` | grep | `grep -A5 "pub.*async fn index" crates/unimatrix-server/src/services/index_briefing.rs` — assert doc comment contains "delegated to" and "validate_search_query" (GH #355) | PENDING |
| AC-14 | All existing hook.rs tests continue to pass; no `write_stdout` behavior changes for non-PreCompact events | test | `cargo test -p unimatrix-server hook` — all pre-existing hook tests pass; no regressions in UserPromptSubmit, SubagentStart, PostToolUse paths | PENDING |
| AC-15 | Hook always exits 0 regardless of transcript read outcome (FR-03.7 invariant) | test | `test_exit_zero_on_transcript_failure`: run hook with `transcript_path` pointing to unreadable file; assert process exits 0 and `BriefingContent` is written to stdout (R-01, ADR-003 structural enforcement) | PENDING |
