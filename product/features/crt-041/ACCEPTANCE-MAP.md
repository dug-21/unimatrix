# crt-041 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | S1 tick writes `Informs` edges with `source = 'S1'` for active→active pairs sharing ≥3 tags | test | Insert synthetic corpus with ≥1 pair sharing exactly 3 tags, run one tick, assert edge in `graph_edges` with `source='S1'`, `relation_type='Informs'`, correct weight | PENDING |
| AC-02 | S1 is idempotent: running twice produces no duplicate edges | test | Run S1 tick twice on fixed corpus; assert `graph_edges` row count is identical after second run | PENDING |
| AC-03 | S1 excludes edges where either endpoint has `status = Quarantined` (3) | test | Mark one entry in a qualifying pair as Quarantined (status=3), run S1, assert no edge written for any pair containing that entry — test both source_id and target_id positions | PENDING |
| AC-04 | S1 is capped per tick at `max_s1_edges_per_tick`; highest-overlap pairs prioritized (ORDER BY shared_tag_count DESC) | test | Set cap to 5, synthesize 10+ qualifying pairs with distinct overlap counts, run one tick, assert ≤5 edges written and all 5 correspond to highest-overlap pairs | PENDING |
| AC-05 | S1 weight is `min(shared_tag_count * 0.1, 1.0)` | test | Assert weight=0.3 for 3 shared tags, weight=0.5 for 5, weight=1.0 for 10, weight=1.0 for 12 (cap check) | PENDING |
| AC-06 | S2 tick writes `Informs` edges with `source = 'S2'` for active→active pairs sharing ≥2 vocabulary terms | test | Configure vocabulary with 3 terms, insert entries sharing 2 of those terms, run one tick, assert edge present with correct source and relation_type | PENDING |
| AC-07 | S2 is a no-op when `s2_vocabulary` is empty: zero SQL writes, zero edges inserted | test | Set `s2_vocabulary = []`, run S2, assert `graph_edges` row count with `source='S2'` unchanged (remains 0) | PENDING |
| AC-08 | S2 is idempotent: running twice produces no duplicate edges | test | Run S2 tick twice on fixed corpus and vocabulary; assert row count unchanged after second run | PENDING |
| AC-09 | S2 excludes quarantined-endpoint pairs | test | Mark one entry in a vocabulary-matching pair as Quarantined (status=3), run S2, assert no edge written — test both endpoint positions | PENDING |
| AC-10 | S2 vocabulary term matching uses space-padded word boundary (no false-positive substring matches) | test | Include term `"api"` in vocabulary, insert entry whose content contains only `"capabilities"`, run S2, assert no edge written for that entry | PENDING |
| AC-11 | S2 vocabulary terms are bound as sqlx parameters, never interpolated (SQL injection guard) | test | Include a term containing a single quote (e.g., `"it's"`), run S2, assert no SQL error and results match expected pairs | PENDING |
| AC-12 | S2 is capped per tick at `max_s2_edges_per_tick`; highest-overlap pairs prioritized | test | Set cap to 5, synthesize 10+ qualifying pairs, run one tick, assert ≤5 edges written and all 5 are highest-overlap pairs | PENDING |
| AC-13 | S8 tick runs only on ticks where `current_tick % s8_batch_interval_ticks == 0` | test | Set `s8_batch_interval_ticks = 5`; run tick 4, assert zero `graph_edges` rows with `source='S8'`; run tick 5, assert S8 rows appear | PENDING |
| AC-14 | S8 writes `CoAccess` edges with `source = 'S8'` and `weight = 0.25` | test | Insert two `context_search` audit_log rows sharing entry IDs in `target_ids`, run S8, assert edges appear with `source='S8'`, `relation_type='CoAccess'`, `weight=0.25` | PENDING |
| AC-15 | S8 watermark persists: only `event_id > watermark` rows are processed in subsequent runs | test | Run S8 once (processes rows 1..5, watermark=5), insert row 6, run S8 again, assert only row 6's pairs produce new edges | PENDING |
| AC-16 | S8 watermark is updated AFTER edge writes (at-least-once re-processing guarantee) | test | Write edges; do not update watermark (simulated crash); run S8 again; assert no duplicate edges (INSERT OR IGNORE) and watermark now updated correctly | PENDING |
| AC-17 | S8 excludes non-`context_search` operations (e.g., `context_briefing`) | test | Insert a `context_briefing` audit_log row with target_ids, run S8, assert zero edges written for those IDs | PENDING |
| AC-18 | S8 excludes rows with `outcome != 0` (failed or denied searches) | test | Insert a `context_search` row with `outcome = 1`, run S8, assert no edges written for that row's pairs | PENDING |
| AC-19 | S8 excludes quarantined-endpoint pairs | test | Insert audit_log row with target_ids containing a Quarantined entry ID, run S8, assert that pair is not inserted | PENDING |
| AC-20 | S8 handles malformed JSON in `target_ids` without halting; watermark advances past malformed row | test | Insert a row with `target_ids = 'not-json'` between two valid rows, run S8, assert valid rows' pairs are written, warn! logged for malformed row, watermark reflects last valid event_id (not stuck) | PENDING |
| AC-21 | S8 respects `max_s8_pairs_per_batch` (cap on pairs, not on audit_log rows) | test | Configure cap to 3, insert audit_log rows generating 10+ pairs, run S8, assert ≤3 edges written | PENDING |
| AC-22 | Named constants `EDGE_SOURCE_S1 = "S1"`, `EDGE_SOURCE_S2 = "S2"`, `EDGE_SOURCE_S8 = "S8"` are defined in `unimatrix_store::read` and accessible from crate root | test | `use unimatrix_store::{EDGE_SOURCE_S1, EDGE_SOURCE_S2, EDGE_SOURCE_S8}` compiles; assert string values equal `"S1"`, `"S2"`, `"S8"` | PENDING |
| AC-23 | `InferenceConfig` gains all five fields with matching defaults in both `impl Default` and `#[serde(default)]` backing functions | test | `test_inference_config_s1_s2_s8_defaults_match_serde`: assert `InferenceConfig::default().field == toml::from_str("").unwrap().field` for all five fields | PENDING |
| AC-24 | `InferenceConfig::validate()` rejects out-of-range values for all five new fields | test | Set each numeric field to 0 (below minimum), assert `validate()` returns an error naming the field; for `s8_batch_interval_ticks = 0` confirm no panic at tick execution (validate() blocks it) | PENDING |
| AC-25 | All three tick functions emit `tracing::info!` summary logs on completion; on SQL error they log `warn!` and return without panicking (infallible tick) | test | Inject broken pool handle, run each tick, assert no panic and a `warn!` log is emitted | PENDING |
| AC-26 | Tick ordering places `run_graph_enrichment_tick` after `run_graph_inference_tick` | test | Integration test asserting S1 edges written in tick N are visible in `graph_edges` after tick N completes; code review of `background.rs` call order | PENDING |
| AC-27 | Tick ordering invariant comment in `background.rs` updated to include `graph_enrichment_tick` after `structural_graph_tick` | grep | `grep -n "graph_enrichment_tick" crates/unimatrix-server/src/background.rs` returns a match in the tick ordering comment block | PENDING |
| AC-28 | `write_graph_edge(source: &str, ...)` exists in `nli_detection.rs` before any S1/S2/S8 call site is written (pre-flight gate) | shell | `grep -n "pub(crate) async fn write_graph_edge" crates/unimatrix-server/src/services/nli_detection.rs` returns a match | PENDING |
| AC-29 | `GraphCohesionMetrics` already exposes `cross_category_edge_count` and `isolated_entry_count` from col-029; crt-041 adds no new fields | test | Insert synthetic corpus with known cross-category edges and isolated entries, call `compute_graph_cohesion_metrics`, assert both existing fields return expected values; verify no new fields were added | PENDING |
| AC-30 | `inferred_edge_count` in `GraphCohesionMetrics` still counts only `source = 'nli'` edges after delivery; S1/S2/S8 edges not counted | test | Insert edges for all four sources (S1, S2, S8, NLI), run `compute_graph_cohesion_metrics`, assert `inferred_edge_count` equals only the NLI-source count | PENDING |
| AC-31 | `graph_enrichment_tick.rs` is under 500 lines (excluding test file if split) | shell | `wc -l crates/unimatrix-server/src/services/graph_enrichment_tick.rs` output is ≤500 at PR time | PENDING |
| AC-32 | Eval gate: after one complete post-delivery tick, `context_status` shows `cross_category_edge_count > pre-delivery baseline` and `isolated_entry_count < pre-delivery baseline`; MRR ≥ 0.2856 on `product/research/ass-039/harness/scenarios.jsonl` | manual | Record pre-delivery baseline from `context_status`; start server; wait for one full tick; call `context_status` and verify direction of change; run MRR eval harness | PENDING |
