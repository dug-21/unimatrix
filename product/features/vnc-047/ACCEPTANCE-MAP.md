# vnc-047 Acceptance Criteria Map

> Source: product/features/vnc-047/specification/SPECIFICATION.md (AC-01…AC-09, incl. per-path
> sub-items AC-03a–d, AC-05a–d). Regenerated after 2026-07-09 source revision (whole-set-once + R-15
> + ADR-007 ack echo). Risk mapping: RISK-TEST-STRATEGY.md (R-01…R-16).
> Tracks GH #940. `[assembled-path]` ACs (AC-02, AC-05) MUST be proven by a test driving
> MCP→hook→listener→`context_cycle_review`, never a store-only structural test (SR-08/R-03).
> **AC-09 (ack echo) is BEST-EFFORT / NON-GATING — it MUST NOT block any gate.**

| AC-ID | Description | Verification Method | Verification Detail | Gating | Status |
|-------|-------------|--------------------|--------------------|--------|--------|
| AC-01 | Value-opacity: cycle accepts opaque `tags` array; any non-empty value stored/returned verbatim; empty string rejected; no vocabulary/allow-list/length/charset/prefix check. | test | Unit + assembled test supplying mixed tags incl. one empty (rejected), one colon-prefixed and one bare (both stored verbatim). Assert no truncation, no namespace derivation. (R-11) | GATING | PENDING |
| AC-02 | **[assembled-path]** Whole-set-once at start via hook path: tags flow `build_cycle_event_or_fallthrough` → `RecordEvent` → `handle_cycle_event` → `cycle_tags` in the same txn as the cycle_start insert; first tag-bearing start locks the whole set via the FR-6 EXISTS-guard; ignored on non-start events; re-issued start no dup/no error; no post-start mutation. | test | Assembled test: start with tags (assert stored), non-start event with tags (assert absent), duplicate start (assert no dup rows, no error). `proven_by` MUST cite the assembled MCP→review chain test. (R-03, R-05, R-09) | GATING | PENDING |
| AC-02a | Whole-set-once is intended: a re-issued start with same/subset/superset/different set does not overwrite, remove, or accumulate, and does not error — the whole later write is skipped once any `cycle_tags` row exists. Tagless start does not lock (first *tags* win). | test | Assembled, EXACT stored-set equality: (a) `{arm:A}` then `{arm:B}` → exactly `{arm:A}`; (b) `{A,B}` then `{C}` → exactly `{A,B}` (no accumulation); (c) tagless start then tag-bearing start → later tags DO lock. No error on any no-op. (R-08) | GATING | PENDING |
| AC-02b | Concurrency: whole-set-once holds under two concurrent same-`feature_cycle` tag-bearing starts — `BEGIN IMMEDIATE` serializes the EXISTS-guard so exactly one intact whole set locks; the loser is a whole-set no-op, no merge, no partial mix, no error/panic. | test + review | Code/SQL review: `insert_cycle_start_with_tags` opens `BEGIN IMMEDIATE` (not default deferred `pool.begin()`). Concurrency test: fire `{A,B}` and `{C,D}` concurrently → stored set is EXACTLY one of the two whole sets, never `{A,B,C,D}` or a partial mix. (R-15) | GATING | PENDING |
| AC-03 | Storage substrate: `cycle_tags(feature_cycle, tag)` junction (PK `(feature_cycle, tag)`, index `(tag)`) is the source of truth, added by additive migration. Verified as discrete per-path line-items (AC-03a–d, all required). | test | See AC-03a–d. Migration unit tests fresh-create + upgrade-from-v30 + idempotent re-run. (R-01, R-13) | GATING | PENDING |
| AC-03a | `CURRENT_SCHEMA_VERSION` constant advanced 30→31. | grep | `crates/unimatrix-store/src/migration.rs:26` reads `CURRENT_SCHEMA_VERSION: u64 = 31`. | GATING | PENDING |
| AC-03b | Fresh-create path creates `cycle_tags` + `idx_cycle_tags_tag`. | test | Init a v31 DB via `create_tables_if_needed`; assert table exists with PK `(feature_cycle, tag)` and the `(tag)` index present. | GATING | PENDING |
| AC-03c | Migration path creates `cycle_tags` on an existing v30 DB, guarded by `CREATE TABLE/INDEX IF NOT EXISTS`; idempotent re-run safe. | test | Init v30 DB, migrate to v31; assert table + index created and constant reads 31. Re-run on an already-v31 DB → no error. Upgrade from a NON-EMPTY v30 DB → existing data intact. (R-13) | GATING | PENDING |
| AC-03d | Pinned schema-version / migration-hygiene test updated and passes. | test | The pinned schema-version/hygiene test is green at v31. Cross-check: fresh-create and post-migration schemas are structurally identical (guards DDL drift, #376). | GATING | PENDING |
| AC-04 | Durability across GC (protection by OMISSION, ADR-005): `cycle_tags` survives `gc_cycle_activity` AND `gc_unattributed_activity`, unchanged after a full GC pass because it is NOT in any DELETE enumeration; contrast purged `sessions`. | test | Extend `test_gc_protected_tables_regression` (retention.rs:521): seed `cycle_tags`, make cycle purgeable (has `cycle_review_index` row), run full GC across BOTH DELETE surfaces, assert `cycle_tags` rows intact WITH a positive control (`sessions` for same cycle purged). (R-07) | GATING | PENDING |
| AC-05 | **[assembled-path]** Surfaced in review: `context_cycle_review` shows tags in JSON (`RetrospectiveReport.tags`) and markdown (dedicated section), populated via `get_cycle_tags` and folded into `report` so it rides `summary_json`. Verified as discrete per-path line-items (AC-05a–d, all required). | test | Assembled test: start a cycle with tags, run review, assert tags in BOTH JSON and markdown outputs (not just a store getter). `proven_by` MUST cite the assembled-path test. (R-03) | GATING | PENDING |
| AC-05a | `SUMMARY_SCHEMA_VERSION` constant advanced 5→6. | grep | `crates/unimatrix-store/src/cycle_review_index.rs:54` reads `SUMMARY_SCHEMA_VERSION: u32 = 6`. | GATING | PENDING |
| AC-05b | `RetrospectiveReport.tags` round-trips through `summary_json` serialize/deserialize across all three schema paths; `#[serde(default)]` backward-read: a v5 blob (no `tags` key) deserializes to empty vec without error. | test | Serialize populated `tags` → `summary_json` → deserialize with tags intact. Separately: deserialize a v5 blob → empty `tags`, no error (mandatory backward-read case, R-02 scenario 4). | GATING | PENDING |
| AC-05c | Pinned `SUMMARY_SCHEMA_VERSION` test (#4178, #5051) updated and passes. | test | Pinned assertion at `cycle_review_index.rs:~709-716` green at v6; message references vnc-047. | GATING | PENDING |
| AC-05d | JSON output includes `tags`; markdown renders the tag section; a tag-less cycle renders no spurious section. | test | Cycle WITH tags → markdown section + JSON `tags`. Cycle WITHOUT tags → no spurious `## Tags`. Assert against SPEC/`render_goal_section` parity, NOT the ARCHITECTURE illustrative header string (#3337). (R-12) | GATING | PENDING |
| AC-06 | Interface stability + authorization: only external change is additive `tags` param on `context_cycle` (+ additive `tags` field on review output); no new MCP tool; `context_tag`/`context_correct` unchanged; single `Capability::Write` gate; `agent_id` audit-only; NO per-tag audit event. | test | Handler-registry test (no new tool); auth test asserting Write required and agent_id NOT authorizing/scoping; diff review confirming `context_tag`/`context_correct` untouched. (R-06) | GATING | PENDING |
| AC-07 | Prefix convention, not enforced: `namespace` prefix supported by convention only — not required, derived, or validated at the write path. | test | Store a colon-prefixed and a colon-free tag; assert both stored identically with no prefix-based branching / no namespace derivation. (R-11) | GATING | PENDING |
| AC-08 | No back-fill of historical reviews: a cycle whose review was cached at SUMMARY v5 shows no tags; only post-deployment runs carry tags. Documented expectation, not a bug. | manual | Documented in feature docs (NFR-8). Optional confirming test: a v5 cached review surfaces no `tags` without a recompute. (R-14) | GATING (doc only) | PENDING |
| AC-09 | **BEST-EFFORT / NON-GATING.** Ack echo on the EXISTING `context_cycle` ack string: start-with-tags → "N labels accepted for recording… use context_cycle_review to confirm" (accept-for-recording, NOT a durability guarantee); non-start-with-tags → "tags ignored — only recorded at cycle start"; listener tracing distinguishes wrote-set vs frozen-skip. Frozen-skip is operator-visible only (no caller signal without a new interface — not specced). | test (best-effort) | String-assertion on the ack for a start-with-tags and a non-start-with-tags call; log-assert or observe the wrote-set/frozen-skip trace. **A miss here does NOT fail delivery.** No assembled-path proof required for frozen-skip. (R-16) | **NON-GATING** | PENDING |
| AC-EXTRA-1 | No second persistence route: bare MCP `context_cycle` handler persists nothing; `cycle_tags` INSERT reached only from `insert_cycle_start_with_tags` via `handle_cycle_event` step-5 Start branch. | test + grep | Call bare MCP handler with tags → assert nothing persists. Grep: `cycle_tags` INSERT appears only in `insert_cycle_start_with_tags`; no other writer. (R-06/SR-03) | GATING | PENDING |
| AC-EXTRA-2 | Absent/evicted-session persistence on the assembled path: an evicted-session cycle_start carrying `feature_cycle` still persists tags (#519 pre-register); empty/missing `feature_cycle` is the single documented silent drop; DB error → `tracing::warn`, review still succeeds. | test | Assembled start with tags on a session absent from the registry → step-1b fires, tags land for correct `feature_cycle`. Empty `feature_cycle` → gated off, no orphan rows. `get_cycle_tags` read error → `report.tags = []` + warn, review succeeds. (R-04/SR-07) | GATING | PENDING |
| AC-EXTRA-3 | Transaction atomicity: the whole-set EXISTS guard + cycle_start event row + all `cycle_tags` rows share one `BEGIN IMMEDIATE` commit boundary; a mid-op failure leaves no half-written state; duplicate tag within one submitted set does not abort the txn. | test | Assert co-presence of start row + tag rows after one `insert_cycle_start_with_tags` call. Duplicate tag in same submitted set → per-tag ON CONFLICT, no abort. Atomicity-under-failure by fault-injection test or explicit code-review sign-off in coverage report. (R-05) | GATING | PENDING |
| AC-EXTRA-4 | SR-02 version re-verification at implementation start: `CURRENT_SCHEMA_VERSION == 30` and `SUMMARY_SCHEMA_VERSION == 5` confirmed free at HEAD before writing bumps; flag renumber if either taken. | manual | Coverage-report checklist item (NFR-3/R-10). Synthesis re-verified free 2026-07-09; re-confirm at implementation start. | GATING | PENDING |
