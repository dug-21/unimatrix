# vnc-020 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `context_graph(mode="inverse", category="source", missing_edge_types=["Cites"])` returns a JSON array of `EntryRecord` objects for `source` entries with no incoming `Cites` edges | test | infra-001 suite: write source entries with and without incoming Cites edges; assert only entries without are returned | PENDING |
| AC-02 | `inverse` mode with `missing_edge_types` containing an unrecognized string produces a validation error naming the unrecognized value and listing all 16 recognized types | test | Unit test: call `handle_inverse` with `missing_edge_types=["NotAType"]`; assert error message contains "NotAType" and all 16 type names | PENDING |
| AC-03 | `inverse` mode with `missing_edge_types` absent or empty produces a validation error: "inverse mode requires at least one edge type in missing_edge_types" | test | Unit test: call with `missing_edge_types=None` and with `missing_edge_types=[]`; assert exact error text for both | PENDING |
| AC-04 | `inverse` mode with `category` absent produces a validation error: "inverse mode requires category" | test | Unit test: call `handle_inverse` with no category field; assert exact error text | PENDING |
| AC-05 | `inverse` mode `limit` default is 100 when omitted. Valid range [1, 500]; out-of-range values produce a validation error stating the allowed range | test | Unit test: omit limit and assert 100 applied; pass limit=0 and limit=501 and assert validation error with range statement | PENDING |
| AC-06 | `inverse` mode response includes a `total_returned` field with the count of entries returned | test | Integration test: assert `total_returned == entries.len()` in response | PENDING |
| AC-07 | `context_graph(mode="filter", category="goal", min_age_days=30, max_edge_count=0, edge_types=["Advances"])` returns `goal` entries older than 30 days with zero outgoing `Advances` edges (Q10 stale Goal pattern) | test | infra-001: write goal entries with varying ages and outgoing Advances counts; assert only entries meeting both conditions are returned | PENDING |
| AC-08 | `context_graph(mode="filter", category="decision", min_edge_count=2, edge_types=["Advances"])` returns `decision` entries with two or more outgoing `Advances` edges (Q11 multi-Goal advancement pattern) | test | infra-001: write decision entries with 0, 1, 2, 3 outgoing Advances edges; assert only entries with >=2 are returned | PENDING |
| AC-09 | `filter` mode with `min_edge_count` or `max_edge_count` present but `edge_types` absent or empty produces a validation error: "filter mode requires edge_types when edge_count constraints are specified" | test | Unit test: set `min_edge_count=1` with `edge_types=None`; assert exact error text; repeat with `edge_types=[]` | PENDING |
| AC-10 | `filter` mode with `category` absent produces a validation error: "filter mode requires category" | test | Unit test: call `handle_filter` with no category; assert exact error text | PENDING |
| AC-11 | `filter` mode `limit` default is 100 when omitted. Valid range [1, 500] | test | Unit test: omit limit and assert 100 applied; pass limit=0 and limit=501 and assert validation error | PENDING |
| AC-12 | `filter` mode response includes `total_returned` | test | Integration test: assert `total_returned == entries.len()` in response | PENDING |
| AC-13 | `context_graph(mode="path", from_id=A, to_id=B, edge_types=["Supports","Advances"], depth=5)` returns `{ found: true, from_id: A, to_id: B, hops: [...], length: N }` where hops contains N entries (no null relation_types) and `from_id` is NOT in `hops` | test | infra-001: write entries connected by known typed-edge chain; assert response shape, hops content, and that `from_id` is absent from hops array | PENDING |
| AC-14 | `path` mode returns `{ found: false, from_id: A, to_id: B, hops: [], length: 0 }` when no path exists between `from_id` and `to_id` within `depth` hops | test | Integration test: write disconnected entries; assert `found: false` and empty hops | PENDING |
| AC-15 | `path` mode returns `{ found: false, ... }` (not an error) when `from_id` or `to_id` is not found in the current in-memory graph snapshot | test | Unit test: call `handle_path` with a `from_id` not in the `TypedRelationGraph`; assert `PathResponse { found: false }` not an `ErrorData` | PENDING |
| AC-16 | `path` mode with `from_id` absent produces a validation error: "path mode requires from_id" | test | Unit test: call with no `from_id`; assert exact error text | PENDING |
| AC-17 | `path` mode with `to_id` absent produces a validation error: "path mode requires to_id" | test | Unit test: call with no `to_id`; assert exact error text | PENDING |
| AC-18 | `path` mode `depth` default is 5 when omitted. Valid range [1, 10]; out-of-range values produce a validation error | test | Unit test: omit depth and assert 5 applied; pass depth=0 and depth=11 and assert validation error | PENDING |
| AC-19 | `path` mode tool description includes the staleness disclosure (in-memory BFS, tick-window lag) with exact text specified in ARCHITECTURE.md §Staleness Disclosure | manual | Inspect `tools.rs` context_graph tool description string; confirm it contains "cache is rebuilt each tick (typically 30-60 seconds)" and `{ found: false }` not-an-error language | PENDING |
| AC-20 | `path` mode with `resolve_supersessions=true`: if `from_id` or `to_id` is a deprecated entry, the endpoint is resolved to its terminal active successor before BFS begins; `from_id` field in response reflects the resolved ID | test | infra-001: write deprecated entry D superseded by A; call path with from_id=D, resolve_supersessions=true; assert response.from_id == A | PENDING |
| AC-21 | `path` mode with `resolve_supersessions=false` (default): deprecated endpoints and intermediate nodes are used as-is (audit mode) | test | infra-001: same setup as AC-20 with resolve_supersessions=false; assert response.from_id == D (original deprecated ID) | PENDING |
| AC-22 | Passing `from_id` or `to_id` to `chain`, `current`, `neighbors`, `subgraph`, or `filter` modes produces a validation error naming "path" as the correct mode | test | Unit test: one test per affected mode passing from_id; assert error message names "path" mode | PENDING |
| AC-23 | Passing `inverse`-only params (`missing_edge_types`) to non-inverse modes produces a validation error naming "inverse" as the correct mode | test | Unit test: pass `missing_edge_types` with mode="filter"; assert error names "inverse" mode; repeat for chain, current, neighbors, subgraph, path | PENDING |
| AC-24 | Passing `filter`-only params (`min_age_days`, `max_edge_count`, etc.) to non-filter modes produces a validation error naming "filter" as the correct mode | test | Unit test: one test per filter-only param passed to mode="inverse"; assert error names "filter" mode | PENDING |
| AC-25 | Passing `depth` to `chain`, `current`, `subgraph`, `inverse`, or `filter` modes produces a validation error naming the correct mode (corrects prior silent-ignore behavior) | test | Unit test: one test per each of the 5 affected modes passing depth=3; assert validation error returned with message "depth is not supported in {mode} mode — use neighbors or path mode" | PENDING |
| AC-26 | The unrecognized-mode error lists all seven supported modes: "chain, current, neighbors, subgraph, inverse, filter, path" | test | Unit test: call `context_graph(mode="unknown")`; assert error message contains all seven mode names | PENDING |
| AC-27 | An integration test in the infra-001 suite covers `inverse` mode: writes entries of a given category where some have incoming edges of a specified type and others do not; asserts only entries with no incoming edges of that type are returned; also verifies deprecated entries are excluded | test | infra-001 suite test named `test_context_graph_inverse_single_type` or equivalent; include one active entry with no Cites edge, one active entry with a Cites edge, one deprecated entry with no Cites edge — assert only the active no-Cites entry is returned | PENDING |
| AC-28 | An integration test in the infra-001 suite covers `inverse` mode with two `missing_edge_types`: asserts AND semantics — only entries missing ALL specified types are returned | test | infra-001 suite: write 4 entries in states (missing both, missing only first, missing only second, missing neither); assert only entries missing both types are returned | PENDING |
| AC-29 | An integration test in the infra-001 suite covers `filter` mode with `max_edge_count=0`: writes goal entries where some have no outgoing Advances edges and some have one or more; asserts only zero-edge entries are returned; the `= 0` boundary is explicitly validated | test | infra-001 suite test named `test_context_graph_filter_max_edge_count_zero` or equivalent; data fixture: entries with 0, 1, 2, 3 outgoing Advances edges; assert only 0-edge entry returned | PENDING |
| AC-30 | An integration test in the infra-001 suite covers `filter` mode with `min_edge_count >= 2`: asserts only entries with two or more outgoing edges of the specified type are returned | test | infra-001 suite: entries with 0, 1, 2, 3 outgoing edges; assert only 2 and 3 returned | PENDING |
| AC-31 | An integration test in the infra-001 suite covers `path` mode: writes entries connected by a known typed-edge chain; asserts the returned hops match the expected sequence and `from_id` is not in the `hops` array | test | infra-001 suite test named `test_context_graph_path_found` or equivalent; known 2-hop chain A→B→C; assert hops=[{entry_id:B, relation_type:X},{entry_id:C, relation_type:Y}] and from_id=A not in hops | PENDING |
| AC-32 | `path` mode with `from_id == to_id` returns `{ found: false, hops: [], length: 0 }` — a self-path is not a meaningful traversal | test | Unit test: call `handle_path` with from_id == to_id; assert found: false and empty hops | PENDING |
