# crt-037 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `RelationType::from_str("Informs")` returns `Some(RelationType::Informs)` | test | Unit test in `graph.rs`: assert `RelationType::from_str("Informs") == Some(RelationType::Informs)` | PENDING |
| AC-02 | `RelationType::Informs.as_str()` returns `"Informs"` | test | Unit test in `graph.rs`: assert `RelationType::Informs.as_str() == "Informs"` | PENDING |
| AC-03 | `build_typed_relation_graph` with a `GraphEdgeRow` containing `relation_type = "Informs"` succeeds and includes the edge in the output graph | test | Unit test: build graph with one `Informs` row; assert edge is present in the output `TypedRelationGraph` | PENDING |
| AC-04 | `build_typed_relation_graph` does not emit a `warn` for `"Informs"` edge rows (R-10 guard does not fire) | test | Unit test: capture log output during `build_typed_relation_graph` with an `"Informs"` row; assert no `WARN`-level log entries mentioning `"Informs"` | PENDING |
| AC-05 | `personalized_pagerank` propagates mass through `Informs` edges: a graph with seed on a decision node and an `Informs` edge from a lesson node to the decision node results in a non-zero PPR score on the lesson node specifically | test | Unit test in `graph_ppr.rs`: two-node graph (lesson A, decision B), single `Informs` A→B edge, seed PPR at B; assert `scores[A] > 0.0` — the lesson node by index, not any-node non-zero | PENDING |
| AC-06 | `positive_out_degree_weight` includes `Informs` edge weights in the out-degree sum | test | Unit test: node with one `Informs` edge and no other positive edges; assert `positive_out_degree_weight` returns the edge weight, not zero | PENDING |
| AC-07 | `InferenceConfig` deserializes `informs_category_pairs` from TOML; empty TOML yields four default software engineering pairs | test | Unit test in `config.rs`: parse empty TOML string; assert `informs_category_pairs` equals the four canonical SE pairs | PENDING |
| AC-08 | `InferenceConfig` deserializes `nli_informs_cosine_floor` from TOML; empty TOML yields `0.45` | test | Unit test in `config.rs`: parse empty TOML string; assert `nli_informs_cosine_floor == 0.45_f32` | PENDING |
| AC-09 | `InferenceConfig` deserializes `nli_informs_ppr_weight` from TOML; empty TOML yields `0.6` | test | Unit test in `config.rs`: parse empty TOML string; assert `nli_informs_ppr_weight == 0.6_f32` | PENDING |
| AC-10 | `InferenceConfig::validate()` rejects `nli_informs_cosine_floor` outside `(0.0, 1.0)` exclusive | test | Unit test: validate with `0.0` → Err; validate with `1.0` → Err; validate with `0.45` → Ok | PENDING |
| AC-11 | `InferenceConfig::validate()` rejects `nli_informs_ppr_weight` outside `[0.0, 1.0]` inclusive | test | Unit test: validate with `-0.01` → Err; `1.01` → Err; `0.0` → Ok; `1.0` → Ok | PENDING |
| AC-12 | The default `InferenceConfig` passes `validate()` | test | Unit test: `InferenceConfig::default()` then `validate()` returns `Ok(())` | PENDING |
| AC-13 | `run_graph_inference_tick` writes an `Informs` edge when all composite guards pass: category pair in `informs_category_pairs`, cosine ≥ `nli_informs_cosine_floor`, `nli.neutral > 0.5`, `source.created_at < target.created_at`, different `feature_cycle` values | test | Integration test: inject two entries satisfying all guards; run tick; assert `GRAPH_EDGES` contains a row with `relation_type = "Informs"` | PENDING |
| AC-14 | `run_graph_inference_tick` does NOT write an `Informs` edge when `source.created_at >= target.created_at` (temporal ordering guard) | test | Integration test: same pair as AC-13 but with `source.created_at = target.created_at`; assert no `Informs` row written. Also test with `source.created_at > target.created_at`. | PENDING |
| AC-15 | `run_graph_inference_tick` does NOT write an `Informs` edge when `source.feature_cycle == target.feature_cycle` (cross-feature guard) | test | Integration test: pair with identical `feature_cycle` strings; assert no `Informs` row written | PENDING |
| AC-16 | `run_graph_inference_tick` does NOT write an `Informs` edge when the category pair is NOT in `informs_category_pairs` (domain agnosticism guard) | test | Integration test: pair with categories `("decision", "decision")`; assert no `Informs` row written | PENDING |
| AC-17 | `run_graph_inference_tick` does NOT write an `Informs` edge when `cosine < nli_informs_cosine_floor` (cosine floor guard) | test | Integration test: pair with cosine `0.44` against default floor `0.45`; assert no `Informs` row written | PENDING |
| AC-18 | The `Informs` HNSW scan uses `nli_informs_cosine_floor` (default 0.45), distinct from `supports_candidate_threshold` (default 0.50) | test | Unit test or code inspection: assert Phase 4b HNSW scan invocation uses `config.nli_informs_cosine_floor`, not `config.supports_candidate_threshold`; also verify pairs with cosine in [0.45, 0.50) are processed by Phase 4b | PENDING |
| AC-19 | `Informs` edges written to `GRAPH_EDGES` have `source = "nli"` (EDGE_SOURCE_NLI constant) | test | Integration test: after tick writes an `Informs` edge, query `GRAPH_EDGES` and assert `source = "nli"` on that row | PENDING |
| AC-20 | `Informs` edge weight equals `similarity * nli_informs_ppr_weight` (both f32) | test | Unit test: given known similarity `s` and `nli_informs_ppr_weight` `w`, assert written edge weight equals `s * w` within f32 epsilon | PENDING |
| AC-21 | The rayon closure in Phase 7 remains sync-only after the change | shell | `grep -n 'Handle::current' crates/unimatrix-server/src/services/nli_detection_tick.rs` returns empty; enforced as CI gate | PENDING |
| AC-22 | Domain vocabulary strings (`informs_category_pairs` default values) do not appear as string literals in `nli_detection_tick.rs` | shell | `grep -n '"lesson-learned"\|"decision"\|"pattern"\|"convention"' crates/unimatrix-server/src/services/nli_detection_tick.rs` returns empty; enforced as CI gate | PENDING |
| AC-23 | An `Informs` pair already in `GRAPH_EDGES` is not written a second time on subsequent ticks | test | Integration test: run tick twice with the same qualifying pair; assert `GRAPH_EDGES` contains exactly one `Informs` row for that pair | PENDING |
| AC-24 | `graph_penalty` and `find_terminal_active` do NOT traverse `Informs` edges; they continue to filter exclusively to `Supersedes` edges (SR-01 invariant) | test | Unit test: graph with only an `Informs` edge; assert `graph_penalty` returns `FALLBACK_PENALTY` (no penalty contribution from `Informs`); assert `find_terminal_active` returns empty | PENDING |
