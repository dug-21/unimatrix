# crt-045 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | After `EvalServiceLayer::from_profile()` on a snapshot containing graph edges, `TypedGraphState.use_fallback` is `false` and `typed_graph` is non-empty | test | `layer_tests.rs::test_from_profile_typed_graph_rebuilt_after_construction` — assert `!guard.use_fallback` and `guard.all_entries.len() >= 2` via `typed_graph_handle()` accessor | PENDING |
| AC-02 | Running `unimatrix eval run` with `ppr-expander-enabled.toml` and a populated snapshot produces measurably different MRR/P@5 results from `baseline.toml` — the PPR+graph_expand path executes, not the fallback path | manual | Run `unimatrix eval run --profile ppr-expander-enabled.toml` and `unimatrix eval run --profile baseline.toml` against a populated snapshot; confirm metric outputs are not bit-identical | PENDING |
| AC-03 | Running `unimatrix eval run` with `ppr-expander-enabled.toml` does not fail at profile parse time | shell | `unimatrix eval run --profile ppr-expander-enabled.toml` exits without `EvalError::ConfigInvariant`; alternatively `cargo test` parses the TOML via `parse_profile_toml()` and asserts `Ok(profile)` | PENDING |
| AC-04 | Running `unimatrix eval run` with `baseline.toml` (empty inference section) produces the same results as before this fix — no regression in the baseline path | manual | Run `unimatrix eval run --profile baseline.toml` before and after the change; MRR/P@5 values must be identical | PENDING |
| AC-05 | `EvalServiceLayer::from_profile()` does not abort on rebuild failure (cycle detected or store I/O error); it logs a warning, leaves `use_fallback = true`, and returns `Ok(layer)` | test | `layer_tests.rs` — seed a cycle-producing Supersedes edge set (A→B→A), call `from_profile()`, assert `result.is_ok()` and `guard.use_fallback == true` | PENDING |
| AC-06 | Integration test in `layer_tests.rs` asserts `use_fallback = false` and non-empty `typed_graph` after `from_profile()` with a graph-seeded snapshot; test also invokes a live search to confirm `SearchService` observes the rebuilt graph at query time | test | `layer_tests.rs::test_from_profile_typed_graph_rebuilt_after_construction` — three-layer assertion: (1) `use_fallback == false` + `all_entries.len() >= 2`, (2) `find_terminal_active` returns `Some(_)` for seeded entry, (3) `layer.inner.search.search(params).await` returns `Ok(_)` | PENDING |
| AC-07 | `cargo test --workspace` passes with no new failures | shell | `cargo test --workspace` — exit code 0, zero new test failures | PENDING |
| AC-08 | All existing eval profile integration tests (`layer_tests.rs`, `eval/profile/tests.rs`) continue to pass — no behavioral regression in non-graph paths | test | `cargo test -p unimatrix-server eval::profile` — all pre-existing tests pass unchanged | PENDING |
