# vnc-027 Acceptance Criteria Map

AC-01..AC-10 from SCOPE.md; AC-11/AC-12 added by SPECIFICATION.md per scope-risk recommendations (SR-03, SR-08). Where AC-10's "and/or" wording is loose, amended ADR-006 (age-prune-only) is authoritative.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `transport-uds.js` frames byte-identical to `wire.rs`: 4-byte BE u32 length prefix + JSON payload; write path rejects payloads > 1 MiB; read path rejects zero-length and > 1 MiB declared lengths (before allocating) | test | `node --test` byte-compare against committed Rust-generated framing fixtures (parity corpus layer a); boundary cases at exactly 1,048,576 B both directions (R-18) | PENDING |
| AC-02 | Transport selection: remote config → HTTP unchanged; no remote config → UDS at `~/.unimatrix/{projectHash}/unimatrix.sock`; `missing`-config breadcrumb path removed; socket-path hash matches Rust for plain-repo, worktree, and symlink layouts | test | `node --test` on `config.resolve()` mode matrix; TS-vs-Rust hash-fixture corpus (ADR-007 §3: main root, deep subdir, linked worktree, symlinked path, non-git fallback) through the vnc-026 drift mechanism; socketPath-dirname == stateDir-parent invariant | PENDING |
| AC-03 | Round-trip transport/framing parity with the Rust hook over UDS against a live listener: identical HookRequest frames for the post-reduction corpus; sync trio (ContextSearch plain + SubagentStart envelope, CompactPayload, Ping) prints byte-identical stdout; FNF write-then-disconnect without reading. Scope per FR-21 (corpus excludes retired/optional events) and FR-22 (lone-surrogate formally excepted) | test | Integration suite against a live listener; committed stdout goldens (corpus layers b, c); server-side FNF frame-truncation assertion incl. max-size 1 MiB frame (ADR-003 §6); shared-core HTTP-vs-UDS body equivalence check (R-09 s3) | PENDING |
| AC-04 | Fail-open parity on UDS: server absent → FNF enqueued + one stderr line, sync silent, exit 0, no stdout; queued frames replay over whichever transport the next successful spawn selects — cross-transport BOTH directions, both ingest points accepting | test | `node --test` with no listener (stale socket, ENOENT dir, EACCES → `connect` class); bidirectional replay integration (UDS-enqueue → HTTP replay with fresh token; HTTP-enqueue → UDS replay at live listener); session-id split (`http-{sid}` vs raw) pinned; poison-pill immunity; queue bounds on the enqueue-only path (500 files / 5 MiB / 24 h) | PENDING |
| AC-05 | Latency < 20 ms per invocation, p95, server stubbed/local listener, including project-root detection | test | F3 AC-13 measurement protocol run against the UDS path; FNF (pays `'finish'` flush wait) and sync measured separately; 40 ms timeout constants asserted | PENDING |
| AC-06 | TS UDS client never client-side-prepends the PreCompact transcript block; with streamed deltas in the buffer, exactly one server-built block appears in CompactPayload output | test | Integration: stream deltas, fire PreCompact, assert single block in committed golden; one-client-per-project assumption documented (mixed-client row unsupported, R-17) | PENDING |
| AC-07 | `transcript_delta` events stream over UDS as RecordEvent frames and merge into the F2 session buffer | test | End-to-end from TS client through `listener.rs:785,1025` into the F2 buffer — buffer CONTENT asserted, not just frame acceptance | PENDING |
| AC-08 | PreToolUse standalone observation retired: non-`context_cycle` spawns send nothing; cycle interception (`cycle_start`/`phase-end`/`stop`) fully preserved; SubagentStop optional, default-off via `unimatrix.hooks.subagent_stop` settings key; HOOK_EVENTS/matchers updated; sync-injection + PostToolUse/PostToolUseFailure + lifecycle untouched | test | Sentinel matrix on `buildCycleEventOrFallthrough` (null paths + valid cycle frame parity); merge-settings output snapshot (PreToolUse matcher exactly `context_cycle\|mcp__unimatrix__context_cycle`, others unchanged); opt-in key on/off/non-boolean matrix; F-02 `evil_context_cycle_bypass` exact-equality gate; stale-install `*` spawn no-op; no-SubagentStop full-lifecycle test (R-12) | PENDING |
| AC-09 | Size gate: comment-stripped ≤ 100 KB primary + raw ≤ 160 KB backstop; stripper dependency-free and string-literal-safe; gate header documents the human-decision rule. **Merges as the literal first vnc-027 commit (3 bytes of headroom; vnc-030 depends on it)** | test + shell | Embedded self-test corpus on every gate invocation (string `"// not a comment"`, template literals with `${}`, regex with `//` and `/*`, division-vs-regex); synthetic over-limit fixtures trigger both limits independently; merge order audited via `git log` — gate rewrite precedes any `lib/hook-client/` growth | PENDING |
| AC-10 | Offset-file delete no longer fires on per-turn Stop→SessionClose; keyed per amended ADR-006 (authoritative): age-prune is the sole effective mechanism; TaskCompleted branch retained, keyed by canonical event name, unreachable-but-tested; no full re-stream from offset 0 every turn | test | Unit: TaskCompleted spawn deletes after successful carrying send; assertable negative — Stop spawn (also a SessionClose frame) does NOT delete; multi-turn integration: offsets persist across N Stop turns, true deltas after turn 1; `pruneOffsets` 7-day cutoff + fail-open + FNF-path-only (NFR-4) | PENDING |
| AC-11 | Frozen F1 wire contract preserved: `accept` field + `Text` variant additive-only; all pre-existing Rust parity fixtures and ts-rs bindings pass byte-unchanged (including after the mechanical hook.rs `accept: None` edits) | test | Existing Rust fixture suite + ts-rs binding drift check run UNMODIFIED, green; listener units: no-`accept` → typed JSON never `Text`; `accept: "text/plain"` → `Text` only for Entries/BriefingContent (Ack/Error/Pong always JSON); compiled frozen Rust hook end-to-end against the updated daemon, full sync trio unchanged (R-08 s4); queued frames never carry `accept` | PENDING |
| AC-12 | HTTP path regression guard: F3 remote behavior externally unchanged except the intended AC-10 delete-timing effect; existing F3 parity and delta suites pass with at most keying-assertion updates | test + manual | Full F3 test suite on the merged branch; diff of changed assertions human-reviewed against FR-31; offset write cadence, delta format, 1 MiB caps, never-queue-delta rule pinned unchanged | PENDING |

## Post-Merge Obligations (tracked, not gate items)

| Item | Verification Method | Verification Detail | Status |
|------|--------------------|--------------------|--------|
| Dogfooding switchover + drop detector (FR-32) | manual | Switch `.claude/settings.json` to TS client; documented daily procedure (breadcrumbs, queue residue, event counts) with rollback thresholds active from day one | PENDING |
| UDS-path stamp regression test owed to vnc-030 (#699) | manual | Hand off after F4a merge — explicitly NOT an F4a test | PENDING |
| OQ5 residual: live worktree-cwd stderr dump | manual | One SubagentStop/SubagentStart stderr `cwd` capture from a worktree-isolated subagent during the soak; cross-post to #680 | PENDING |
