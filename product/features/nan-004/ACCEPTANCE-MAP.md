# nan-004 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `npm install @dug-21/unimatrix` downloads the platform binary for the current OS/arch into `node_modules` without modifying any project files beyond `node_modules/` and `package.json`/`package-lock.json`. | manual | Install in a clean project, run `git status` to verify no other files changed. Verify `node_modules/@dug-21/unimatrix-linux-x64/bin/unimatrix` exists and is executable. | PENDING |
| AC-02 | The postinstall script pre-downloads the ONNX model (`all-MiniLM-L6-v2`) to `~/.cache/unimatrix-embed/`. If download fails (no network), postinstall succeeds with a warning. | shell | `npm install @dug-21/unimatrix && test -f ~/.cache/unimatrix-embed/sentence-transformers_all-MiniLM-L6-v2/model.onnx && echo PASS`. Test with network blocked: postinstall exit code must be 0. | PENDING |
| AC-03 | `npx unimatrix init` writes `.mcp.json` with the correct absolute path to the `unimatrix` binary. Existing MCP server entries preserved on merge. | test | Unit test: init on clean project creates `.mcp.json` with absolute path. Unit test: init on project with existing `.mcp.json` containing other servers preserves all entries. Grep `.mcp.json` for `node_modules/@dug-21/unimatrix-linux-x64/bin/unimatrix`. | PENDING |
| AC-04 | `npx unimatrix init` merges Unimatrix hook configuration into `.claude/settings.json` for all 7 hook events. Existing hooks and permissions preserved. File created if absent. | test | Unit tests in `merge-settings.js` test suite: empty file, file with permissions only, file with existing non-unimatrix hooks, file with pre-rename `unimatrix-server` hooks. Verify 7 events present after merge. | PENDING |
| AC-05 | `npx unimatrix init` copies all 13 skill files into `.claude/skills/`. Existing same-name skills overwritten; other skills preserved. | test | Integration test: init copies 13 skill directories. Re-run: skills updated. Non-unimatrix skill in `.claude/skills/custom/` preserved. | PENDING |
| AC-06 | `npx unimatrix init` pre-creates `~/.unimatrix/{hash}/` and SQLite database with current schema. Existing database triggers migration. | shell | `npx unimatrix init && test -f ~/.unimatrix/$(npx unimatrix hash)/unimatrix.db && echo PASS` | PENDING |
| AC-07 | `npx unimatrix init` validates the binary by executing `unimatrix --version` and reporting success or diagnostic error. | test | Integration test: init with valid binary prints version. Test with binary removed: init reports diagnostic error. | PENDING |
| AC-08 | `npx unimatrix init` is idempotent -- running twice does not duplicate hooks or corrupt settings. | test | Integration test: run init twice, parse `.claude/settings.json`, assert exactly 7 unimatrix hook entries (one per event). Parse `.mcp.json`, assert exactly one `unimatrix` key. Diff output of second run shows no semantic changes. | PENDING |
| AC-09 | JS shim resolves platform binary and exec's it. Prints clear error on unsupported platform. | test | Unit test: `resolve-binary.js` returns absolute path on linux-x64. Unit test: with platform package removed, throws error listing supported platforms. Integration test: `npx unimatrix version` outputs version string. | PENDING |
| AC-10 | GitHub Actions release workflow triggers on `v*` tags, builds linux-x64 binary, packages it, and publishes both npm packages. | manual | Push a test tag (`v0.5.0-rc.1`), observe workflow execution in GitHub Actions. Verify: Rust 1.89 installed, `patches/anndists` asserted, binary built and stripped, platform package published before root package, GitHub Release created. | PENDING |
| AC-11 | npm package version matches Cargo.toml version. | grep | `grep '"version"' packages/unimatrix/package.json packages/unimatrix-linux-x64/package.json` matches version in root `Cargo.toml` `[workspace.package]`. CI pre-publish step validates this. | PENDING |
| AC-12 | Root package uses `optionalDependencies` with `os`/`cpu` fields so only the matching platform binary downloads. | grep | `grep -A2 '"optionalDependencies"' packages/unimatrix/package.json` shows `@dug-21/unimatrix-linux-x64`. `grep '"os"' packages/unimatrix-linux-x64/package.json` shows `["linux"]`. `grep '"cpu"' packages/unimatrix-linux-x64/package.json` shows `["x64"]`. | PENDING |
| AC-13 | Init prints human-readable summary of all actions and suggests `/unimatrix-init` as next step. | shell | `npx unimatrix init 2>&1 \| grep -q '/unimatrix-init' && echo PASS` | PENDING |
| AC-14 | Init supports `--dry-run` that prints planned actions without modifying files. | shell | `npx unimatrix init --dry-run && test ! -f .mcp.json && echo PASS` (on a clean project where `.mcp.json` does not exist yet) | PENDING |
| AC-15 | All 9 Cargo crates use `version.workspace = true` inheriting from root. Initial version is `0.5.0`. | grep | `grep -r 'version.workspace = true' crates/*/Cargo.toml \| wc -l` equals 9. `grep 'version = "0.5.0"' Cargo.toml` matches in `[workspace.package]` section. | PENDING |
| AC-16 | `/release` skill exists and guides version bump, changelog, commit, tag, and push. | file-check | `test -f .claude/skills/release/SKILL.md && echo PASS`. Manual: invoke `/release` in dry-run to verify steps. | PENDING |
| AC-17 | CHANGELOG.md generated from conventional commits grouped by type. | file-check | `test -f CHANGELOG.md && grep -q '### Features' CHANGELOG.md && grep -q '### Fixes' CHANGELOG.md && echo PASS` (after first release) | PENDING |
