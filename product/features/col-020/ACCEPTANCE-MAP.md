# col-020 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `RetrospectiveReport` includes `session_summaries` field containing one `SessionSummary` per distinct session_id in the observation data | test | Unit test: build report from records with N distinct session_ids, assert N summaries returned | PENDING |
| AC-02 | Each `SessionSummary` contains tool distribution grouped by category (read, write, execute, search, store, spawn, other) | test | Unit test: construct records with known tool names (Read, Edit, Bash, context_search, context_store, SubagentStart, Unknown), verify each category count matches expected | PENDING |
| AC-03 | Each `SessionSummary` contains top 5 file path zones (directory prefixes) by frequency | test | Unit test: construct records with file paths across 7+ directories, verify top 5 returned in descending frequency order | PENDING |
| AC-04 | Each `SessionSummary` contains the list of agents spawned (SubagentStart event tool names) | test | Unit test: include 3 SubagentStart records with distinct agent names, verify agents_spawned contains all 3 | PENDING |
| AC-05 | Each `SessionSummary` contains `knowledge_in` count (context_search + context_lookup + context_get PreToolUse calls) and `knowledge_out` count (context_store PreToolUse calls) | test | Unit test: mix 5 search + 2 lookup + 1 get + 3 store calls, verify knowledge_in=8, knowledge_out=3 | PENDING |
| AC-06 | `knowledge_reuse.tier1_reuse_count` counts distinct entries stored in one session and retrieved (search or injection) in a different session within the same topic | test | Integration test: seed entry in session A, add query_log returning that entry in session B, add injection_log for that entry in session C; verify tier1_reuse_count=1 (deduplicated) | PENDING |
| AC-07 | `knowledge_reuse.by_category` breaks down Tier 1 reuse by entry category | test | Integration test: seed 2 convention entries and 1 pattern entry reused cross-session; verify by_category={"convention": 2, "pattern": 1} | PENDING |
| AC-08 | `knowledge_reuse.category_gaps` lists categories with active entries but zero reuse across the topic's sessions | test | Integration test: seed active entries in "procedure" category with no cross-session retrieval; verify "procedure" appears in category_gaps | PENDING |
| AC-09 | `rework_session_count` counts sessions whose outcome contains `result:rework` or `result:failed` (case-insensitive) | test | Unit test: sessions with outcomes "result:rework", "Result:Failed", "result:pass", None, ""; verify count=2 | PENDING |
| AC-10 | `context_reload_pct` reports percentage of files read in session N+1 that were also read in a prior session | test | Unit test: session 1 reads files A,B,C; session 2 reads B,C,D; verify reload_pct = 2/3 (B and C reloaded out of 3 files in session 2) | PENDING |
| AC-11 | All new report fields use `#[serde(default, skip_serializing_if)]` -- old JSON without new fields deserializes successfully | test | Unit test: deserialize JSON string `{"feature_cycle":"test",...}` (pre-col-020 format) into updated RetrospectiveReport; assert new fields are None | PENDING |
| AC-12 | `context_retrospective` updates `topic_deliveries` aggregate counters after computation | test | Integration test: run retrospective, read topic_deliveries record, verify total_sessions/total_tool_calls/total_duration_secs match computed values; run again, verify values unchanged (idempotent) | PENDING |
| AC-13 | `context_reload_pct` is reported as a raw float (0.0-1.0) without interpretation labels | test | Unit test: verify return type is f64, value in [0.0, 1.0], no string wrapper or label struct | PENDING |
| AC-14 | Empty topic (zero sessions) returns existing cached/empty behavior unchanged -- new fields are None | test | Unit test: call compute_session_summaries with empty records; verify empty vec. Integration test: run retrospective on empty topic; verify new fields absent from JSON output | PENDING |
| AC-15 | All existing retrospective tests continue to pass (no regressions) | shell | `cargo test -p unimatrix-observe -p unimatrix-server` -- all existing tests pass without modification | PENDING |
| AC-16 | Session summaries are ordered by `started_at` ascending (chronological) | test | Unit test: construct 3 sessions with out-of-order timestamps, verify summaries sorted by started_at ascending | PENDING |
