# crt-043 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `cycle_events` table has a `goal_embedding BLOB` column after schema migration (v21) | test | Integration test: open real v20 database via `Store::open()`; query `SELECT COUNT(*) FROM pragma_table_info('cycle_events') WHERE name = 'goal_embedding' AND type = 'BLOB'`; assert result = 1 | PENDING |
| AC-02 | At `context_cycle(type=start)` with non-empty goal, a background task is spawned that calls `EmbedServiceHandle::get_adapter()` and invokes embedding | test | Unit test with stub embed handle that records call count; call `handle_cycle_event(CycleLifecycle::Start, goal="design a query pipeline")`; assert stub records at least one `get_adapter()` call | PENDING |
| AC-03 | On successful embed, `goal_embedding` is written to the `cycle_events` start row via `update_cycle_start_goal_embedding()` | test | Integration test: await background task completion; query `SELECT goal_embedding FROM cycle_events WHERE topic = ? AND event_type = 'cycle_start'`; assert non-NULL blob; decode via `decode_goal_embedding()`; assert `Vec<f32>` of expected embedding dimension (384) | PENDING |
| AC-04 | (a) Embed service unavailable: cycle start returns successfully, `tracing::warn!` emitted, `goal_embedding` NULL. (b) Goal absent or empty: cycle start returns successfully, no warn emitted, `goal_embedding` NULL, no task spawned | test | (a) Configure stub embed handle returning `EmbedNotReady`; call cycle start with goal="text"; assert Ok return, warn captured, DB row has NULL blob. (b) Call cycle start with goal="" and with no goal parameter; assert Ok return, no warn, no stub calls, DB row has NULL blob | PENDING |
| AC-05 | No new crate dependencies introduced | shell | `cargo metadata --no-deps --format-version 1 \| jq '[.packages[].dependencies[].name] \| sort \| unique'`; compare output to pre-crt-043 baseline; assert identical for all workspace members | PENDING |
| AC-06 | `context_cycle` MCP tool response text is unchanged from pre-crt-043 behavior | test | Call `context_cycle(type=start, ...)` through the MCP layer in a test; assert returned string is byte-for-byte identical to the pre-crt-043 expected response text for the same inputs | PENDING |
| AC-07 | `observations` table has a `phase TEXT` column after schema migration (v21) | test | Integration test: open real v20 database via `Store::open()`; query `SELECT COUNT(*) FROM pragma_table_info('observations') WHERE name = 'phase' AND type = 'TEXT'`; assert result = 1 | PENDING |
| AC-08 | `ObservationRow` has `phase: Option<String>` field | grep | `grep -n 'phase: Option<String>' crates/unimatrix-server/src/uds/listener.rs`; assert match found | PENDING |
| AC-09 | `insert_observation()` and `insert_observations_batch()` bind `phase` | test | Round-trip DB test: write observation row with phase="design"; read back via raw SQL `SELECT phase FROM observations WHERE ...`; assert phase = 'design'. Repeat for `insert_observations_batch`. | PENDING |
| AC-10 | (a) Observation written when session has active phase captures that phase. (b) Observation written before any `context_cycle` call produces NULL phase | test | (a) Call `set_current_phase("design")` on session state; write observation; read back row; assert phase = 'design'. (b) Write observation with no prior `context_cycle` call for session_id; read back row; assert phase IS NULL | PENDING |
| AC-11 | Schema migration is idempotent: running `Store::open()` on a v21 database is a no-op | test | Open a v21 database via `Store::open()` a second time; assert no error returned; assert schema_version counter remains 21; assert both `goal_embedding` and `phase` columns still present | PENDING |
| AC-12 | Test suite covers all required scenarios | test | Confirm tests exist for: goal embedding fires and persists blob (embed available); embedding skipped on empty string goal; embedding skipped on absent goal; embedding skipped gracefully when embed service unavailable with warn; observation written with non-NULL phase when session has active phase; observation written with NULL phase when no active cycle; v20 → v21 migration via `Store::open()` on real v20 fixture | PENDING |
| AC-13 | INSERT-before-UPDATE ordering is guaranteed by chosen mechanism; no silent no-op race | test | Integration test: call `handle_cycle_event(CycleLifecycle::Start, goal="test goal")` with operational embed service; await both spawned tasks; assert `goal_embedding` is non-NULL on `cycle_events` start row. Code-review assertion: embed spawn appears strictly after INSERT spawn in `handle_cycle_event`. | PENDING |
| AC-14 | `decode_goal_embedding(blob: &[u8]) -> Result<Vec<f32>>` ships in same module as encode path | test | Round-trip unit test in `unimatrix-store`: `encode_goal_embedding(vec.clone())` → bytes; `decode_goal_embedding(&bytes)` → decoded; assert decoded == vec (exact float equality). Negative test: pass malformed byte slice; assert `DecodeError` returned (not panic). | PENDING |
