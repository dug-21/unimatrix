# vnc-043 Acceptance Criteria Map

context_graph subgraph: Class-1 doc fix + live depth-1 read (GH #903). Every AC from SCOPE.md is listed. Verification detail cites the RISK-TEST-STRATEGY risk scenario where applicable.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | subgraph `max_depth == 1` resolves from live DB; reflects every edge/node write committed before the call — no tick lag | test | Freshness test: write edge, immediately query subgraph depth-1, assert edge present (R-01/R-10 forward) | PENDING |
| AC-02 | subgraph `max_depth > 1` unchanged at the SET level — returned node/edge SET and all filtering/supersession semantics identical: cached BFS over `TypedRelationGraph`, `use_fallback` → live cold-start intact. "Unchanged" is set-level, NOT byte-order (prior depth>1 order was arbitrary/undocumented); FR-9's uniform ordering is presentation-only and set-preserving | test | Depth>1 regression asserts the SET (not order) + cold-start test on empty `TypedRelationGraph` asserts fallback fires (R-02) | PENDING |
| AC-03 | depth-1 issues the same single `query_direct_neighbors` edge query as neighbors depth-1 — no per-edge round-trips | test | Path / query-count assertion; covered via `subgraph_via_db` reuse (integration risk `query_direct_neighbors`) | PENDING |
| AC-04 | depth-1 nodes hydrated via batch pattern (id, title, content, status, kind, tags); identical `EntryRecord` shape to cache path | test | Hydration-field + tag parity assertion on a tagged fixture node vs cache-path hydration (R-05) | PENDING |
| AC-05 | `edge_types` honored at depth-1; only given relation types; absent/`[]` = all except Supersedes | test | Filter test at depth-1: given-type-only + absent-defaults-all; parity both paths (R-03 scenario 2) | PENDING |
| AC-06 | `direction` (incoming/outgoing/both) honored at depth-1; `EdgeRecord`s keep canonical `source→target` with `direction:"outgoing"` label | test | Direction test at depth-1 asserting inclusion changes + label invariant across incoming/outgoing/both (R-11) | PENDING |
| AC-07 | DoD one-shot returns exactly incoming `Advances` capabilities, hydrated, reflecting a write committed immediately before, no client-side filter | test | DoD one-shot integration test (write-then-read): `subgraph, seed_ids:[goal], max_depth:1, edge_types:["Advances"], direction:"incoming"` (R-01/R-03) | PENDING |
| AC-08 | `resolve_supersessions` defaults true and behaves identically on depth-1 live (raw as-stored on explicit false) | test | Supersession test at depth-1: default-true + explicit-false, parity vs cache path (R-03 scenario 3) | PENDING |
| AC-09 | subgraph description staleness text updated: depth-1 = live; depth>1 = tick-cache (30–60s) | grep | Substring assertion — staleness carve-out phrase present in BOTH description literals (`tools.rs`); extend `:6198+` (R-07 scenario 2) | PENDING |
| AC-10 | no `RelationEdge`/hot-path struct change; no `GraphParams` wire-shape change | manual | Wire/struct diff review; confirm no `.read()` on `TypedGraphState` on depth-1 path (R-08); no `.snap`/schema pin exists (FR-5 resolved negative) | PENDING |
| AC-11 | freshness both ways: depth-1 write-then-read edge **appears**; depth>1 within-tick write does **not** appear | test | Two-direction freshness test in one module (mandatory per ADR-005 vnc-018 / R-10) | PENDING |
| AC-12 | chain/current/neighbors filtering + dispatch unchanged | test | Regression on those modes — no new acceptance/rejection wiring (R-01 boundary) | PENDING |
| AC-13 | (DOC) `edge_types` (`:85`) + `direction` (`:82`) schemars docs state subgraph applies; `CONTEXT_GRAPH_DESCRIPTION` + mirror-const state filtering honored; both copies in sync | test + grep | 4 edit points: two literals via `test_graph_tool_attr_description_matches_const` (#869) + extended substrings; two schemars docs via explicit presence check (R-07) | PENDING |
| AC-14 | depth-1 results returned in stable documented order (nodes by `id`, edges by `(source_id, target_id, relation_type)`); DoD one-shot deterministic across runs | test | Ordering test asserting FR-9 keys on both depths; DoD run-twice byte-identical (R-06) | PENDING |
| AC-15 | (no silent truncation) DoD one-shot returns `truncated == false` at realistic fan-in (≥30 incoming `Advances`); `truncated` surfaced/asserted, `true` on >199-neighbor fixture | test | Truncation assertion: ≥30 → `truncated == false`; pathological >199 → `truncated == true` surfaced (R-09) | PENDING |
