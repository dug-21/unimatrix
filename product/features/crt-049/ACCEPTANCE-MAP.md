# crt-049 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `FeatureKnowledgeReuse` has `explicit_read_count: u64` with `#[serde(default)]` | grep | `grep -n "explicit_read_count" crates/unimatrix-observe/src/types.rs` — must show field definition with `#[serde(default)]` on preceding line | PENDING |
| AC-02 [GATE] | `search_exposure_count` carries stacked `#[serde(alias = "delivery_count")]` and `#[serde(alias = "tier1_reuse_count")]`; all three JSON key names deserialize to the same field value without error or data loss | test | Unit test in `types.rs`: (1) JSON key `"search_exposure_count"` → correct value; (2) JSON key `"delivery_count"` → correct value; (3) JSON key `"tier1_reuse_count"` → correct value; (4) serialized output key is `"search_exposure_count"`; (5) full round-trip for each alias name. All five assertions required — any omission is a gate failure | PENDING |
| AC-03 | `extract_explicit_read_ids` returns distinct entry IDs from `PreToolUse` observations where normalized tool is `context_get` or `context_lookup` and `input["id"]` is a non-null integer or parseable string | test | Unit test in `knowledge_reuse.rs` with synthetic `ObservationRecord` slice containing valid `context_get` observations; assert returned `HashSet` contains expected IDs | PENDING |
| AC-04 | Filter-based `context_lookup` calls (no `id` field in input, or `input["id"]` is null) are excluded from `explicit_read_count` | test | Unit test: synthetic observation with `tool = "context_lookup"` and `input = {"query": "some text"}` (no `id` field); assert returned set is empty. Second case: `input = {"id": null}`; assert excluded | PENDING |
| AC-05 | `compute_knowledge_reuse_for_sessions` accepts and uses the `attributed` slice; field is non-zero when agents made explicit reads during the cycle | test | Integration test: run `compute_knowledge_reuse_for_sessions` with a session containing at least one `context_get` observation in the attributed slice; assert `explicit_read_count > 0` in returned struct | PENDING |
| AC-06 [GATE] | Tool names with `mcp__unimatrix__` prefix are correctly handled via `normalize_tool_name` before matching; bare string comparison is not acceptable | test | Unit test AC-12(d): synthetic observation with `tool = "mcp__unimatrix__context_get"` and valid `input["id"]`; assert returned set is non-empty. Also test `mcp__unimatrix__context_lookup`. Non-negotiable (pattern #4211) | PENDING |
| AC-07 | `render_knowledge_reuse` outputs separate labeled lines for "Search exposures" and "Explicit reads" when either is non-zero | test | Golden-output assertion: construct `FeatureKnowledgeReuse` with `search_exposure_count = 10`, `explicit_read_count = 3`, `total_served = 5`; render and assert (a) "Entries served to agents (reads + injections)" appears with value `5`; (b) "Search exposures (distinct)" appears with value `10`; (c) "Explicit reads (distinct)" appears with value `3`; (d) legacy label "Distinct entries served" does NOT appear; (e) section order is correct | PENDING |
| AC-08 | `SUMMARY_SCHEMA_VERSION` is `3`; `CRS-V24-U-01` assertion updated to assert `3` | test | `cargo test CRS-V24-U-01` passes; `grep -n "SUMMARY_SCHEMA_VERSION" crates/unimatrix-store/src/cycle_review_index.rs` shows value `3` | PENDING |
| AC-09 | A cycle with zero search exposures but non-zero explicit reads does not hit the early-return zero-delivery path and returns a populated `FeatureKnowledgeReuse` | test | Unit test: call `compute_knowledge_reuse` (or `compute_knowledge_reuse_for_sessions`) with `search_exposure_count = 0`, `injection_count = 0`, `explicit_read_ids = {5}`; assert returned struct has `explicit_read_count = 1` and is not a default/empty struct | PENDING |
| AC-10 | Existing serialization round-trip tests for `FeatureKnowledgeReuse` continue to pass with the renamed field; no existing test weakened to accommodate the rename | test | `cargo test` green in `unimatrix-observe`; no existing alias test modified to reduce coverage | PENDING |
| AC-11 | `compute_knowledge_reuse_for_sessions` still loads `query_log` and `injection_log` and populates `search_exposure_count`, `cross_session_count`, `by_category`, `cross_feature_reuse`, `intra_cycle_reuse`, and `top_cross_feature_entries` as before | test | Existing knowledge reuse integration tests pass without modification | PENDING |
| AC-12 | New unit tests cover: (a) explicit reads from `context_get` observations; (b) filter `context_lookup` (no `id`) excluded; (c) single-ID `context_lookup` (with `id`) included; (d) prefixed tool name matched after normalization [GATE — see AC-06]; (e) empty observations produces `explicit_read_count = 0` | test | Five dedicated unit tests in `knowledge_reuse.rs` test module, each testing one case. AC-12(d) is a gate item — it must test the `mcp__unimatrix__` prefix path explicitly | PENDING |
| AC-13 [GATE] | `FeatureKnowledgeReuse` has `explicit_read_by_category: HashMap<String, u64>` with `#[serde(default)]`; populated by calling `batch_entry_meta_lookup` once on the full extracted explicit read ID set and tallying counts per category string; field name, type, and join source are frozen as a Group 10 contract | test | Unit test with synthetic ID set producing a known category distribution; assert map contents match expected tallies. Integration test reads actual `entries.category` values. Serde round-trip with absent field deserializes to empty map. Gate: field name, type, and source must not deviate | PENDING |
| AC-14 [GATE] | `total_served` is the count of distinct entry IDs in `explicit_reads ∪ injections` (deduplicated set union); search exposure IDs must NOT contribute to `total_served`; display label is "Entries served to agents (reads + injections)" | test | Unit test: explicit reads `{1, 2}`, injections `{2, 3}`, search exposures `{4, 5, 6}`; assert `total_served = 3` (not 5, not 6). Rendered output must contain the correct label string | PENDING |
| AC-15 [GATE] | Unit test verifies `total_served` is the count of distinct IDs in explicit reads or injections; an entry appearing only in search results does NOT increase `total_served` | test | Unit test: synthetic scenario where entry ID appears only in `query_result_ids`, not in explicit reads or injections; assert `total_served` is unchanged. Also: `explicit_read_ids = {1, 2}`, `injection_ids = {2, 3}` → `total_served = 3` (overlap deduplicated) | PENDING |
| AC-16 [GATE] | `extract_explicit_read_ids` correctly handles string-form IDs (`{"id": "42"}`) in addition to integer-form (`{"id": 42}`); both forms produce the same `u64` value; a unit test covers both forms explicitly | test | Unit test: two synthetic `ObservationRecord` entries — one with `input["id"]` as JSON number `42`, one with `input["id"]` as JSON string `"42"`; assert both IDs (`42`) are present in the returned `HashSet<u64>`. Failure mode: string-form IDs silently dropped with no diagnostic | PENDING |
| AC-17 [GATE] | Injection-only cycle (injections present, zero search exposures, zero explicit reads) does NOT trigger early-return render guard; `render_knowledge_reuse` produces output showing `total_served > 0`; the render guard condition is `total_served == 0 && search_exposure_count == 0`, NOT the old three-condition form | test | Unit test: `FeatureKnowledgeReuse` with `injection_ids` non-empty (so `total_served > 0`), `search_exposure_count = 0`, `explicit_read_count = 0`; assert rendered output is non-empty and `total_served > 0` appears. Code review assertion: old guard form `search_exposure_count == 0 && explicit_read_count == 0 && injection_count == 0` must NOT be present in codebase | PENDING |
