# Agent Report — vnc-026-agent-5-config (config.js)

## Files Created
- `packages/unimatrix/lib/hook-client/config.js` (255 lines)
- `packages/unimatrix/test/hook-client/config.test.js` (382 lines)
- `packages/unimatrix/test/fixtures/parity/project-hash-goldens.json` (Rust-generated, 5 cases)

Committed: `d7eae293 impl(config): hook-client config resolution, root walk, hash parity (#679)`

## Implementation Summary
- ADR-006 resolution: env pair `UNIMATRIX_REMOTE_URL`/`UNIMATRIX_REMOTE_TOKEN` wins outright (file never read on env path — fs-spy verified); exactly one set → `{ok:false, reason:"partial_env"}` (breadcrumb class `auth` mapped by index.js); single read of `{project_root}/.claude/settings.local.json` `unimatrix.remote`; key absent/incomplete → `"missing"`; unparseable file → `"malformed"`. Module never throws, no network surface.
- `walkToProjectRoot`: non-throwing port of init.js `detectProjectRoot`; `.git` dir OR file (worktree) accepted; no `.git` → resolved cwd. Documented divergence (worktree gitdir not chased) carried from pseudocode.
- `computeProjectHash`: first 16 hex of SHA-256 over UTF-8 path bytes — parity with `project.rs::compute_project_hash`, verified against committed Rust-generated goldens.
- Same `projectRoot` string feeds config lookup AND state-dir hash (split-brain assertion in tests).
- State dir `~/.unimatrix/{hash}/hook-client`; `os.homedir()` throw/empty → `stateDir = null` (persistence disabled, resolution still returns ok).
- Timeouts: ADR-005 defaults 750/2000/3000; overrides `unimatrix.remote.timeouts.{connect_ms,sync_ms,fnf_ms}`, finite 1..600000, floored, junk silently ignored; env path always gets defaults.
- Unparseable URL NOT rejected (`urlHost: ""`); transport classifies at send time (fail-open).

## Goldens Provenance
Generated by a temporary Rust integration test calling `unimatrix_engine::project::compute_project_hash` (`cargo test -- --nocapture`), captured into the fixture, temp test deleted. No hand-written hashes. Cases: ASCII, non-ASCII (CJK+umlaut), trailing-slash-normalized, repo root, Windows backslash path.

## Tests
`node --test test/hook-client/config.test.js`: **29 pass / 0 fail / 1 skip** (the skip is `test_root_walk_windows_separators`, gated to the Windows CI runner per R-14).

Covers the full FR-06 matrix (rows 1-8), nested-git nearest-root + split-brain, no-.git resolved-cwd with single-file-read fs spy, `.git`-file worktree, hash parity goldens, trailing-slash normalization, state-dir shape, homedir degradation, timeout override matrix, security posture (static zero-network/zero-output assertion on module source; failure shapes carry no url/token).

The FR-06 "no network + breadcrumb + exit 0 + no stdout" obligations are index.js-level (config.js has no network/breadcrumb code); unit tests prove distinct typed reasons (`partial_env` vs `missing` vs `malformed`) plus the static no-network-module assertion. Integration rows remain for the Stage 3c tester.

## Issues / Notes for Delivery Leader
1. **Pre-existing failures, NOT from this component**: `test/merge-settings.test.js` and `test/init.test.js` fail at branch HEAD — `lib/merge-settings.js:112` hardcodes `LD_LIBRARY_PATH=` into the hook command while tests expect it absent. Tracked files unmodified by me; belongs to the init-remote/merge-settings workstream.
2. Worktree shared with parallel agents (transcript/transform/transport-http files untracked at time of writing); I committed only my three files.
3. An old stash (`stash@{0}: WIP on main: 5caec455`) exists in the repo — untouched, flagging in case it's stale.

## Knowledge Stewardship
- Queried: mcp__unimatrix__context_briefing — surfaced ADR-006 (#4756) and vnc-001 ADR-004 project-isolation-via-path-hash (#80); applied both. context_search for prior parity-golden patterns — no match.
- Stored: entry #4766 "Hash the normalized root string when porting Rust path-hash identity to JS; generate parity goldens from the Rust oracle, never hand-write them" via /uni-store-pattern.
