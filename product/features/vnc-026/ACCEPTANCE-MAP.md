# vnc-026 Acceptance Criteria Map

All ACs from SCOPE.md (binding). Verification details reference SPECIFICATION.md §5 and
RISK-TEST-STRATEGY.md scenarios. Tests are `node:test` under `packages/unimatrix/test/`
unless noted.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Client `HookRequest` JSON-equal to Rust `build_request` for every parity-corpus case (13 events + Gemini aliases + unknown-event passthrough + missing/malformed-stdin defensive cases) | test | Layer 1 parity suite: run `buildRequest` over `test/fixtures/parity/` corpus, structural JSON equality vs goldens after volatile-field normalization (`timestamp`→0, `ppid-\d+`→`ppid-X`); full ADR-001 edge-case inventory (R-01 scenario 1); corpus manifest audit covers every `build_request` arm (R-02) | PENDING |
| AC-02 | Fire-and-forget POSTs to `{url}/observe` with `Authorization: Bearer <token>`; 2xx incl. 204 = success; no stdout | test | Stub-server test asserting method, path, headers, and byte-exact empty stdout per FNF event type | PENDING |
| AC-03 | Sync requests send `Accept: text/plain`; 200 body → stdout per host envelope; 204 → no stdout | test | Stub-server test per sync event (UserPromptSubmit ≥5 words → ContextSearch; PreCompact → CompactPayload; SubagentStart → ContextSearch source="SubagentStart"); header + stdout assertions; raw-JSON-on-stdout canary (#4703 shape) | PENDING |
| AC-04 | SubagentStart `hookSpecificOutput` envelope byte-identical to `write_stdout_subagent_inject` (field order, serialization, trailing newline) | test | Byte-compare `transform.js` output vs golden Rust stdout bytes (`expected-stdout.bin`); literal-template enforcement grep-gate: no `JSON.stringify` of envelope objects in transform.js (R-03) | PENDING |
| AC-05 | Two-layer parity suite passes: Layer 1 byte-identical stdout with deterministic buffer pre-population (incl. PreCompact); Layer 2 content-equivalence modulo elision markers under streamed deltas + injected drops | test | FR-23 + FR-24 runs in CI; PreCompact vs identically populated F2 server buffer; pre-population isolated behind one test helper (SR-11); Layer 2 runs against the **merged** F2 server (vnc-025 merged PR #692 — C-08 satisfied, R-17); Layer-2 helper asserts the pinned post-elision server state (FR-08/ADR-008): hole behind the content at `(last_offset, file_len − bytes.length)`, `high_water == file_len`, `contiguous_tail` crosses the elision seam, no NUL bytes served | PENDING |
| AC-06 | Deltas ship `[last_offset, file_len)` as `transcript_delta` `{offset, bytes}` (non-elided frames declare `offset = last_offset`); offset advances; no delta when file unchanged | test | Integration test growing/holding transcript across spawns; assert POST presence/absence, **declared offset**, AND persisted offset values (not just POST presence — R-04); UTF-8 boundary property test (sum of shipped spans == contiguous prefix) | PENDING |
| AC-07 | >64 KiB delta truncated head 48 KiB + tail 12 KiB with `…[N bytes elided]…` marker in a single **end-anchored** frame: declared `offset = file_len − bytes.length` (frame ends exactly at `file_len`, ADR-008); serialized frame < 1 MiB; offset advances to `file_len` per the uniform ADR-004 rule; elided bytes never re-sent | test | Unit test with oversized + escape-heavy (worst-case inflation) content; assert declared offset is end-anchored (NOT the span start) and `offset + bytes.length == file_len`; post-serialization size assertion (SR-04); truncation-at-multi-byte-boundary cases; Layer 2 elision-mid-session run continuing to correct PreCompact restoration with the pinned ADR-008 server-state assertions (R-06) | PENDING |
| AC-08 | No transcript_delta on sync trio; sync path performs no delta-related transcript I/O (RQ-6 SubagentStart tail read exempt by design — query derivation) | test | Sync events with `transcript_path` present → single POST only; fs-call spy asserting no stat/span-read/offset persistence on sync spawns; zero queue I/O on sync path (R-13) | PENDING |
| AC-09 | Unreachable/timeout/non-2xx → exit 0, no stdout, all event types; delta-send failure never affects carrying event | test | Failure-matrix (ECONNREFUSED, timeout, 401, 413, 500) × event types; delta-failure injection with independent `Promise.allSettled` outcome assertion (ADR-007); breadcrumb class assertions per failure (R-10); timeout-expiry timing test (no hang) | PENDING |
| AC-10 | No mis-attribution by construction; ≥8 interleaved sessions with drops → each server buffer holds only its own session's bytes | test | FR-26 concurrency test against an F2 server (ass-069 PoC method, per-session byte tagging); raw session_id on the wire (no client `http-` prefix); requires vnc-025 merged (C-08) | PENDING |
| AC-11 | `init --remote` writes idempotent node-command hooks for full remote event set (incl. PreCompact, PostToolUseFailure), preserves foreign hooks, recognizes own entries on re-run, Ping-validates, stores token/URL in settings.local.json with env override, no token on argv, skips `.mcp.json` with message | test | Init matrix: fresh config / re-run / foreign hooks (incl. foreign `node` command that must NOT match) / old-style `unimatrix hook` entries (SR-08); spaced-path regex table test (R-11 — resolve WARN 2 first); double-fire check (entries per event == 1 after two runs); argv + file-content + 0600-mode + gitignore-warning assertions; Ping wrong-token loud-failure (R-18) | PENDING |
| AC-12 | CI on Node 18/20/22/24; zero runtime deps; `lib/hook-client/` payload < 100 KB | shell | CI matrix config (plus Linux/macOS/Windows OS runners per R-14 — approved expansion); `package.json` dependency audit; size check step in CI; corpus drift-check job must FAIL not skip when generator absent (R-20) | PENDING |
| AC-13 | Spawn p50 ≤ ~12 ms, p95 ≤ 20 ms (entry + parse + build + transform + state-dir hash, server stubbed) on reference env, recorded in testing artifacts | shell | Benchmark harness ≥50 iterations with warmup (ass-068 Q1 method) incl. hash derivation, root walk, and health.json write (R-13); results committed under `product/features/vnc-026/testing/`; validates RQ-2 state-dir choice (cwd-keying only as measured fallback) | PENDING |
| AC-14 | Client-built request JSON round-trips against committed contract fixtures incl. `transcript_delta_payload.json` | test | Extend `contract.test.mjs` pattern to client-produced frames vs `crates/unimatrix-engine/bindings/fixtures/*.json`; runs in CI | PENDING |
| AC-15 | Send failure → enqueue (non-delta FNF frames only); next reachable spawn → in-order replay (≤32 frames / 256 KiB) before new event, queue drained; queue failures never affect exit/stdout; sync trio never queued; `transcript_delta` never queued (ADR-004 carve-out — **variance pending human approval**) | test | Queue lifecycle test: fail → enqueue → recover → replay-order → drain; queue-failure injection (full disk, corrupt frame poison-pill); sync-trio non-enqueue assertion; delta-failure case asserts offset-non-advance + NO queue file (not queue presence); bounds/eviction/24 h-prune tests (R-08); gate must not run until the AC-15 variance is recorded/SCOPE amended | PENDING |
| AC-16 | Local `HOOK_EVENTS` includes PreCompact + PostToolUseFailure (init.js + merge-settings.js matchers) with regression test: full 9-event local set written and recognized on re-run | test | FR-21 regression over fresh AND pre-existing local configs; back-compat wrapper test (local init flow produces byte-identical settings output post-`commandSource` generalization); diff-to-local-mode confined to list + matchers (SR-07 blast-radius gate, R-12) | PENDING |

## Notes

- **AC-15 variance**: vision guardian flagged the ADR-004 `transcript_delta` carve-out as a
  change to SCOPE AC-15's letter, recommendation Accept, pending human approval at design
  session end. Record approval (or amend SCOPE.md AC-15) before the delivery gate evaluates AC-15.
- **C-08 delivery gate**: SATISFIED — vnc-025 (#670) merged via PR #692; AC-05 and AC-10 run
  against the merged F2 server.
- **R-14 OS coverage**: AC-12's verification includes the OS matrix expansion (Linux/macOS/
  Windows) justified by the alignment report — beyond the AC's literal Node-version wording.
