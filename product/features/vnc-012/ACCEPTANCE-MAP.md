# vnc-012 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `GetParams`, `DeprecateParams`, `QuarantineParams` each deserialize successfully when `id` is a JSON string (`"3770"`), producing `id == 3770i64` | test | `cargo test --workspace` — unit tests in `tools.rs` `#[cfg(test)]`, one per struct, asserting field value equals 3770i64 | PENDING |
| AC-02 | `CorrectParams` deserializes successfully when `original_id` is a JSON string, producing `original_id == 3770i64` | test | `cargo test --workspace` — unit test in `tools.rs` asserting `original_id == 3770i64` from `{"original_id": "3770", "content": "c"}` | PENDING |
| AC-03 | `LookupParams` deserializes `{"id": "42"}` to `id == Some(42i64)` and `{"limit": "10"}` to `limit == Some(10i64)` | test | `cargo test --workspace` — two unit tests in `tools.rs`, one per optional field | PENDING |
| AC-03-ABSENT-ID | `LookupParams` with no `id` key produces `id == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.id.is_none()` on JSON with no `id` key | PENDING |
| AC-03-ABSENT-LIMIT | `LookupParams` with no `limit` key produces `limit == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.limit.is_none()` on JSON with no `limit` key | PENDING |
| AC-03-NULL-ID | `LookupParams` with `{"id": null}` produces `id == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.id.is_none()` with explicit null value | PENDING |
| AC-03-NULL-LIMIT | `LookupParams` with `{"limit": null}` produces `limit == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.limit.is_none()` with explicit null value | PENDING |
| AC-04 | `SearchParams` deserializes `{"query": "q", "k": "5"}` to `k == Some(5i64)` | test | `cargo test --workspace` — unit test in `tools.rs` asserting `k == Some(5i64)` | PENDING |
| AC-04-ABSENT | `SearchParams` with `{"query": "q"}` (no `k`) produces `k == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.k.is_none()` | PENDING |
| AC-04-NULL | `SearchParams` with `{"query": "q", "k": null}` produces `k == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.k.is_none()` with explicit null | PENDING |
| AC-05 | `BriefingParams` deserializes `{"task": "t", "max_tokens": "3000"}` to `max_tokens == Some(3000i64)` | test | `cargo test --workspace` — unit test in `tools.rs` asserting `max_tokens == Some(3000i64)` | PENDING |
| AC-05-ABSENT | `BriefingParams` with `{"task": "t"}` (no `max_tokens`) produces `max_tokens == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.max_tokens.is_none()` | PENDING |
| AC-05-NULL | `BriefingParams` with `{"task": "t", "max_tokens": null}` produces `max_tokens == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.max_tokens.is_none()` with explicit null | PENDING |
| AC-06 | `RetrospectiveParams` deserializes `{"feature_cycle": "col-001", "evidence_limit": "5"}` to `evidence_limit == Some(5usize)` | test | `cargo test --workspace` — unit test in `tools.rs` asserting `evidence_limit == Some(5usize)` | PENDING |
| AC-06-ZERO | `RetrospectiveParams` with `evidence_limit: "0"` produces `evidence_limit == Some(0usize)` | test | `cargo test --workspace` — unit test asserting `evidence_limit == Some(0usize)` | PENDING |
| AC-06-ABSENT | `RetrospectiveParams` with no `evidence_limit` key produces `evidence_limit == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.evidence_limit.is_none()` | PENDING |
| AC-06-NULL | `RetrospectiveParams` with `{"feature_cycle": "col-001", "evidence_limit": null}` produces `evidence_limit == None` | test | `cargo test --workspace` — dedicated unit test asserting `params.evidence_limit.is_none()` with explicit null | PENDING |
| AC-07 | All nine fields continue to deserialize correctly when passed as JSON integers (no regression) | test | `cargo test --workspace` — unit tests per struct asserting both integer and string forms produce equal values | PENDING |
| AC-08 | Non-numeric strings (e.g., `"abc"`) for any required integer field produce a serde deserialization error | test | `cargo test --workspace` — one unit test per required-integer struct (`GetParams`, `DeprecateParams`, `QuarantineParams`, `CorrectParams`) asserting `serde_json::from_str(...).is_err()` | PENDING |
| AC-08-OPT | Non-numeric strings for any optional integer field produce a serde deserialization error | test | `cargo test --workspace` — one unit test per optional-integer field asserting `is_err()` | PENDING |
| AC-09 | Negative strings (e.g., `"-1"`) for `evidence_limit` produce a serde deserialization error | test | `cargo test --workspace` — unit test asserting `serde_json::from_str::<RetrospectiveParams>(r#"{"feature_cycle":"c","evidence_limit":"-1"}"#).is_err()` | PENDING |
| AC-09-FLOAT | Float strings (e.g., `"3.5"`) for any integer field (required and optional) produce a serde deserialization error | test | `cargo test --workspace` — unit tests for at least one required field and one optional field asserting `is_err()` on float string input | PENDING |
| AC-09-FLOAT-NUMBER | Float JSON Numbers (e.g., `{"id": 3.0}` as a Number, not a string) for any integer field produce a serde deserialization error | test | `cargo test --workspace` — unit tests using `serde_json::from_str` with numeric float literals (e.g., `r#"{"id": 3.0}"#`) asserting `is_err()` and confirming `visit_f64` returns error not truncated integer | PENDING |
| AC-10 | JSON Schema advertised by the MCP server for all nine affected fields retains `type: integer` | test | `cargo test --workspace` — schema snapshot test in `tools.rs` that constructs a `UnimatrixServer` via `make_server()`, calls `tool_router.list_all()`, extracts `input_schema` for each affected tool, and asserts `"type": "integer"` for each affected property | PENDING |
| AC-11 | All existing tests in `tools.rs` and `infra/validation.rs` pass without modification | test | `cargo test --workspace` produces no regressions; baseline test count 2169+ is not reduced | PENDING |
| AC-12 | Three deserializer functions in `serde_util.rs` covered by unit tests for: integer input, string input, non-numeric string rejection, null for optional variants, absent for optional variants | test | `cargo test --workspace` — tests in `serde_util.rs` `#[cfg(test)]` block or `tools.rs` test block covering all five cases per helper | PENDING |
| AC-13 | Rust in-process integration test exercises the full rmcp `Parameters<T>` dispatch path for `context_get` with a string-encoded id | test | `cargo test --workspace` — integration test in `crates/unimatrix-server/src/server.rs` or `crates/unimatrix-server/tests/mcp_coercion.rs`; test name must include `coercion` or `string_id`; stores a test entry, calls `context_get` with `{"id": "<id-as-string>", "agent_id": "human"}`, asserts `Ok(_)` with non-empty content | PENDING |
| IT-01 | `context_get` MCP tool called over stdio transport with string-encoded id returns success | test | infra-001 smoke suite: `test_get_with_string_id` in `product/test/infra-001/suites/test_tools.py` marked `@pytest.mark.smoke`; stores entry, retrieves with string id, asserts success and non-empty content | PENDING |
| IT-02 | `context_deprecate` MCP tool called over stdio transport with string-encoded id returns success | test | infra-001 smoke suite: `test_deprecate_with_string_id` in `product/test/infra-001/suites/test_tools.py` marked `@pytest.mark.smoke`; stores entry, deprecates with string id, asserts success | PENDING |
