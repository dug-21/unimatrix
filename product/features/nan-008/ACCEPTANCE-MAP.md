# nan-008 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `cc_at_k` (f64) is present in each profile's result object in the per-scenario JSON produced by `eval run` | shell | Run `eval run` against any profile + scenario set; `cat <results-dir>/*.json \| jq '.[].profiles[].cc_at_k'` — all values must be numeric (not null) | PENDING |
| AC-02 | `icd` (f64) is present in each profile's result object in the per-scenario JSON produced by `eval run` | shell | Same run as AC-01; `cat <results-dir>/*.json \| jq '.[].profiles[].icd'` — all values must be numeric | PENDING |
| AC-03 | `cc_at_k_delta` and `icd_delta` are present in the `comparison` object in the per-scenario JSON when two profiles are present | shell | Run `eval run` with baseline + candidate profiles; `jq '.comparison.cc_at_k_delta, .comparison.icd_delta'` on any result JSON — both must be numeric | PENDING |
| AC-04 | The `eval report` summary table includes CC@k and ICD columns and delta columns alongside P@K and MRR | shell | Run `eval report` on a two-profile result set; `grep -E 'CC@k\|ICD' <report.md>` — column headers must appear in the Summary section | PENDING |
| AC-05 | The `eval report` includes a Distribution Analysis section (section 6) with per-profile CC@k range (min/max/mean) and top-5 improvement/degradation scenario rows (two-profile run) | grep | `grep -c "Distribution Analysis" <report.md>` returns 1; `grep -c "## 6\." <report.md>` returns at least 1; section contains a min/max/mean sub-table and at least one improvement or degradation row | PENDING |
| AC-06 | `configured_categories` is sourced from `profile.config_overrides.knowledge.categories` — no hardcoded category lists in metric computation code | grep | `grep -rn '"decision"\|"convention"\|"lesson-learned"\|"pattern"\|"procedure"\|"duty"' crates/unimatrix-server/src/eval/runner/metrics.rs crates/unimatrix-server/src/eval/runner/replay.rs` — zero matches | PENDING |
| AC-07 | Pre-nan-008 result JSON files (without `cc_at_k`/`icd` fields) deserialize without error; missing fields default to 0.0 | test | `cargo test -p unimatrix-server test_report_backward_compat_pre_nan008_json` — passes; asserts exit 0 and `cc_at_k == 0.0`, `icd == 0.0` from deserialization | PENDING |
| AC-08 | CC@k and ICD documented in "Understanding the metrics" section of `docs/testing/eval-harness.md`, including formulas, range, and interpretation guidance | grep | `grep -c "CC@k\|Category Coverage" docs/testing/eval-harness.md` >= 1; `grep -c "ICD\|Intra-query" docs/testing/eval-harness.md` >= 1; ICD subsection contains "not comparable" or equivalent caveat | PENDING |
| AC-09 | Current baseline CC@k and ICD recorded in `product/test/eval-baselines/log.jsonl` with `feature_cycle: "nan-008"` and non-null non-zero values; `README.md` updated | shell | `grep "nan-008" product/test/eval-baselines/log.jsonl \| python3 -c "import sys,json; e=json.loads(sys.stdin.read()); assert e['cc_at_k']>0 and e['icd']>0"` — passes; `grep "cc_at_k" product/test/eval-baselines/README.md` — match found | PENDING |
| AC-10 | Unit tests cover four required boundary cases: CC@k = 1.0 all categories present; CC@k = 1/n one category; ICD = ln(n) maximum entropy; ICD = 0.0 single category | test | `cargo test -p unimatrix-server test_cc_at_k_all_categories_present test_cc_at_k_one_category_present test_icd_maximum_entropy test_icd_single_category` — all four pass | PENDING |
| AC-11 | `ScoredEntry` in runner output JSON includes `category` field with non-empty string value | shell | Run `eval run` against a snapshot with categorized entries; `jq '.[].profiles[].entries[].category' <any-result.json>` — all values non-empty strings | PENDING |
| AC-12 | Round-trip test: runner output with non-zero `cc_at_k`/`icd`/`cc_at_k_delta`/`icd_delta`/`category` is correctly deserialized by the report module (guards dual type copy divergence) | test | `cargo test -p unimatrix-server test_report_round_trip_cc_at_k_icd_fields_and_section_6` — passes; asserts non-zero values (0.857 / 1.234 / 0.143 / 0.211) appear in rendered markdown | PENDING |
| AC-13 | Full rendered-markdown test asserts section 6 appears after section 5, Summary table contains CC@k and ICD columns, no section duplicated or misordered | test | `cargo test -p unimatrix-server test_report_contains_all_six_sections` — passes; asserts `pos("## 1.") < pos("## 2.") < ... < pos("## 5.") < pos("## 6.")` and `contains("CC@k")` and `contains("ICD")` in Summary | PENDING |
| AC-14 | Rendered Distribution Analysis section contains `ln(` annotation in the ICD column header or caption | grep | `grep "ln(" <report.md>` within the Distribution Analysis section — at least one match; also asserted by `test_report_round_trip_cc_at_k_icd_fields_and_section_6` | PENDING |
