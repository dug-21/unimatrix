# crt-039 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `run_graph_inference_tick` is called unconditionally on every tick regardless of `inference_config.nli_enabled`. The `if inference_config.nli_enabled` guard in `background.rs` is removed with no replacement condition. | grep | `grep -n 'nli_enabled' crates/unimatrix-server/src/background.rs` must not show an `if` guard wrapping the `run_graph_inference_tick` call site | PENDING |
| AC-02 | When `nli_enabled = false` and NLI model not loaded, Phase 4b still executes and can write Informs edges; Phase 8 (Supports) writes zero edges. | test | TC-01 (`test_phase4b_writes_informs_when_nli_not_ready`) and TC-02 (`test_phase8_no_supports_when_nli_not_ready`) — both integration tests with real Store | PENDING |
| AC-03 | `apply_informs_composite_guard` does not reference `nli_scores` in any form. Function signature does not include `nli_scores: &NliScores`. Remaining guards are temporal (guard 2) and cross-feature (guard 3) only. | grep | `grep -n 'nli_scores' crates/unimatrix-server/src/nli_detection_tick.rs` restricted to `apply_informs_composite_guard` function body must return empty | PENDING |
| AC-04 | Default `nli_informs_cosine_floor` is 0.5 in `InferenceConfig::default()` and `default_nli_informs_cosine_floor()`. | test | `test_inference_config_default_nli_informs_cosine_floor` asserts `0.5_f32` | PENDING |
| AC-05 | Candidate pair with cosine 0.499 is excluded by Phase 4b; pair at 0.500 is included (inclusive floor, `>=` semantics). | test | TC-05 (`test_phase4b_cosine_floor_0500_included`) and TC-06 (`test_phase4b_cosine_floor_0499_excluded`) | PENDING |
| AC-06 | Contradiction scan continues to be gated on embed adapter availability and `current_tick.is_multiple_of(CONTRADICTION_SCAN_INTERVAL_TICKS)`. Behavior unchanged. | test | Existing contradiction scan tests pass without modification; `cargo test --workspace` green | PENDING |
| AC-07 | Ordering invariant preserved and documented: compaction → promotion → graph-rebuild → structural_graph_tick (always) → contradiction_scan (if embed adapter ready, every N ticks). | grep | Inspect `run_single_tick` body in `background.rs`; ordering invariant comment present as specified in FR-11 | PENDING |
| AC-08 | All tests that previously asserted `nli_informs_cosine_floor == 0.45` are updated to assert 0.5. | grep | `grep -n '0\.45' crates/unimatrix-server/src/infra/config.rs` in test section returns no assertions against the default value | PENDING |
| AC-09 | Tests `test_phase8b_no_informs_when_neutral_exactly_0_5` and `test_phase8b_writes_informs_when_neutral_just_above_0_5` are removed. They must not be repurposed as vacuous assertions. Replacement tests cover cosine floor boundary at 0.5. | grep | `grep -rn 'test_phase8b_no_informs_when_neutral_exactly_0_5\|test_phase8b_writes_informs_when_neutral_just_above_0_5' crates/` returns empty | PENDING |
| AC-10 | `cargo test --workspace` passes with no regressions after all changes. | shell | `cargo test --workspace` exits 0 | PENDING |
| AC-11 | Eval harness MRR >= 0.2913 (crt-038 baseline). 1,585 scenarios in `product/research/ass-039/harness/scenarios.jsonl`. No regression from behavioral ground truth baseline. | manual | Run eval harness against deployed or test binary; confirm MRR >= 0.2913 | PENDING |
| AC-12 | `MAX_INFORMS_PER_TICK = 25` is enforced as hard write limit via `informs_metadata.truncate(MAX_INFORMS_PER_TICK)` in Phase 5 before any Phase 8b write. Dedup pre-filter (`query_existing_informs_pairs`) applied in Phase 2 before Phase 4b candidate selection. | grep | Inspect `nli_detection_tick.rs`: `truncate(MAX_INFORMS_PER_TICK)` call appears before first `write_nli_edge` for Informs; `query_existing_informs_pairs` call appears in Phase 2 block before Phase 4b loop | PENDING |
| AC-13 | Candidate set separation produces mutual exclusion between Phase 4 Supports candidates and Phase 4b Informs candidates. Phase 4b explicitly excludes pairs already present in the Phase 4 `candidate_pairs` Supports set. | test | TC-07 (`test_phase4b_excludes_supports_candidates`): pair with cosine 0.68 present in `candidate_pairs` is absent from `informs_metadata` | PENDING |
| AC-14 | After Phase 1 guard split, Phase 8 entry requires successful `get_provider()` call. If `get_provider()` returns `Err`, no `score_batch` call is made and zero Supports edges are written. | test | TC-02 (`test_phase8_no_supports_when_nli_not_ready`) — integration test asserts zero Supports edges when NLI not ready | PENDING |
| AC-15 | New test `test_phase4b_writes_informs_when_nli_not_ready` asserts Phase 4b CAN write Informs edges when NLI provider is not ready. Positive assertion — at least one Informs edge written. | test | TC-01 present and passing; `store.query_graph_edges()` contains at least one Informs edge | PENDING |
| AC-16 | New test `test_phase8_no_supports_when_nli_not_ready` asserts Phase 8 does NOT write Supports edges when NLI provider not ready. Separate test from AC-15. Old test `test_run_graph_inference_tick_nli_not_ready_no_op` removed. | test | TC-02 present and passing; `grep -n 'test_run_graph_inference_tick_nli_not_ready_no_op' crates/` returns empty | PENDING |
| AC-17 | `tracing::debug!` call at Phase 4b completion records `informs_candidates_found`, `informs_candidates_after_dedup`, `informs_candidates_after_cap`, `informs_edges_written`. | grep | `grep -n 'informs_candidates_found' crates/unimatrix-server/src/nli_detection_tick.rs` returns at least one match in the Phase 4b / Phase 5 region | PENDING |
| AC-18 | `format_nli_metadata_informs` helper removed; replaced by `format_informs_metadata`. No dead-code warning. Informs edge metadata JSON does not contain `nli_neutral`, `nli_entailment`, `nli_contradiction` fields. | shell | `cargo clippy --workspace -- -D warnings` exits 0; `grep -rn 'format_nli_metadata_informs' crates/` returns empty | PENDING |
