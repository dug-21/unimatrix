# vnc-013 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `normalize_event_name("BeforeTool", None)` returns `("PreToolUse", "gemini-cli")`. `normalize_event_name("AfterTool", None)` returns `("PostToolUse", "gemini-cli")`. `normalize_event_name("SessionEnd", None)` returns `("Stop", "gemini-cli")`. All Claude Code event names with `None` hint map to themselves with `"claude-code"`. Unknown names map to themselves with `"unknown"`. | test | Unit tests in `hook.rs` covering each mapping table row and the unknown fallback; `test_normalize_event_name_gemini_unique_names`, `test_normalize_event_name_claude_code_passthrough`, `test_normalize_event_name_unknown_fallback` | PENDING |
| AC-02 | When Gemini fires `BeforeTool` for `context_cycle` with `type="start"`, `build_request()` produces `HookRequest::RecordEvent { event_type: "cycle_start" }`. Payload contains `feature_cycle`. `cycle_events` is written by `listener.rs`. | test | Integration test: inject synthetic Gemini `BeforeTool`+`context_cycle` event through full hook-to-listener path; assert `cycle_events` row exists with `event_type = "cycle_start"` and `source_domain = "gemini-cli"` | PENDING |
| AC-03 | When Gemini fires `BeforeTool` for any Unimatrix tool other than `context_cycle`, `build_request()` produces `HookRequest::RecordEvent` with `event_type: "PreToolUse"` and `provider: "gemini-cli"`. | test | Unit test with synthetic `BeforeTool` payload where `mcp_context.tool_name` is not `context_cycle` (e.g., `"context_search"`); assert `HookRequest::RecordEvent { event_type: "PreToolUse" }` | PENDING |
| AC-04 | When Gemini fires `AfterTool`, `build_request()` produces `HookRequest::RecordEvent` with `event_type: "PostToolUse"` and `provider: "gemini-cli"`. Rework candidate logic does NOT trigger (gated by `provider != "claude-code"`). | test | Unit test: `build_request()` with canonical `"PostToolUse"` and `provider = "gemini-cli"`; assert correct `HookRequest` variant and that `is_rework_eligible_tool()` is never called | PENDING |
| AC-05 | `ImplantEvent.provider` is non-`None` for all events processed through the normalization layer. | test | Unit test `test_implant_event_provider_set_for_all_variants`: for each `HookRequest` variant produced by `build_request()` with each canonical event name and each provider value, assert `ImplantEvent.provider.is_some()` | PENDING |
| AC-06 | `source_domain` on the written `ObservationRecord` is `"gemini-cli"` for Gemini-originated events and `"claude-code"` for Claude Code-originated events (write path: `listener.rs:1894`). | test | Integration test: inject events with `provider: "gemini-cli"` and `provider: "claude-code"`; assert resulting `ObservationRecord.source_domain` values | PENDING |
| AC-07 | All three `source_domain` hardcodes are replaced: (a) `listener.rs:1894` uses `event.provider.clone().unwrap_or("claude-code")`; (b) `background.rs:1330` uses Approach A registry-with-fallback; (c) `services/observation.rs:585` uses Approach A and `_registry` prefix is removed. The string literal `"claude-code"` does not appear as a hardcoded assignment in any of the three locations. | grep | `grep -n '"claude-code"'` in `listener.rs`, `background.rs`, `services/observation.rs` confirms no literal assignment remains at the three target sites | PENDING |
| AC-08 | All existing unit and integration tests pass without modification. Normalization is additive — no existing Claude Code behavior changes. | shell | `cargo test --workspace` exits 0 | PENDING |
| AC-09 | `context_cycle_review` returns correct results for a Gemini-sourced feature cycle: a test inserts `cycle_start`/`cycle_stop` events via the Gemini `BeforeTool` code path and verifies `context_cycle_review` finds and processes them. This is a mandatory (not stretch) AC. | test | Integration test in `services/` or `tools.rs` using synthetic Gemini events through the full hook-to-listener path; assert `context_cycle_review` finds the feature cycle | PENDING |
| AC-10 | `.gemini/settings.json` reference configuration is written and matches the format required by Gemini CLI v0.31+. The matcher regex `mcp_unimatrix_.*` covers all 12 Unimatrix tools. | file-check | File exists at `.gemini/settings.json`; valid JSON; contains `"mcp_unimatrix_.*"` matcher; covers `BeforeTool`, `AfterTool`, `SessionStart`, `SessionEnd` | PENDING |
| AC-11 | `extract_event_topic_signal()` handles canonical event names correctly for Gemini-sourced events. `"PreToolUse"` (normalized from `"BeforeTool"`) extracts topic signal from `tool_input` at the top-level payload position. | test | Unit test with synthetic Gemini `BeforeTool` payload where `tool_input` is at top level; after normalization to `"PreToolUse"`, call `extract_event_topic_signal()`; assert non-empty signal result | PENDING |
| AC-12 | Rework candidate detection (`is_rework_eligible_tool()`, `is_bash_failure()`) is gated to `provider == "claude-code"`. Gemini `AfterTool` events do not enter the rework tracking path. The gate expression references the `provider` field, not a tool-name predicate. | test | Unit test `test_gemini_after_tool_skips_rework_path`: assert rework path is never entered when `provider == "gemini-cli"`. Code review: gate expression in `"PostToolUse"` arm references `provider` field with comment citing ADR-005. | PENDING |
| AC-13 | `DomainPackRegistry` builtin claude-code pack `event_types` list is unchanged. The registry requires no changes. Gemini event names never reach the registry (normalized at ingest). | grep | No diff in `crates/unimatrix-server/src/domain/mod.rs` `builtin_claude_code_pack()` function; `git diff HEAD -- crates/unimatrix-server/src/domain/mod.rs` shows empty or no relevant changes | PENDING |
| AC-14 | `HookInput.mcp_context` is deserialized from Gemini `BeforeTool`/`AfterTool` payloads. `tool_name` is extracted from it for the `context_cycle` interception test. The bare name `"context_cycle"` satisfies the match condition. Gate prerequisite: this AC's unit tests must be green before other Gemini `BeforeTool` ACs are attempted. | test | Unit test `test_gemini_mcp_context_tool_name_promotion`: deserialize synthetic Gemini `BeforeTool` JSON with `mcp_context: { "tool_name": "context_cycle" }` and `tool_input: { "type": "start" }`; assert `build_request()` produces `HookRequest::RecordEvent { event_type: "cycle_start" }` | PENDING |
| AC-15 | The `unimatrix hook` binary accepts Gemini event names without error. `run("BeforeTool", ...)` completes normally (exit 0) regardless of whether the server is running. | shell | `unimatrix hook BeforeTool` with no server running; verify exit code 0: `unimatrix hook BeforeTool; echo $?` produces `0` | PENDING |
| AC-16 | `extract_observation_fields()` contains a guard (`debug_assert!` or exhaustive match arm) that fires if `"post_tool_use_rework_candidate"` would reach the `hook` column as a raw string. The guard is scoped to rework-candidate strings only. The `PostToolUseFailure` arm is untouched. | test | Unit test `test_rework_candidate_guard_fires_in_debug` under `#[cfg(debug_assertions)]`: attempt to produce an observation with `event_type = "post_tool_use_rework_candidate"`; assert the guard fires. Separate test verifies `PostToolUseFailure` still produces `event_type = "PostToolUseFailure"` unchanged. | PENDING |
| AC-17 | `unimatrix hook PreToolUse --provider codex-cli` processes the event with `provider: "codex-cli"`. `normalize_event_name("PreToolUse", Some("codex-cli"))` returns `("PreToolUse", "codex-cli")`. | test | Unit test: `normalize_event_name("PreToolUse", Some("codex-cli"))` asserts `("PreToolUse", "codex-cli")`; integration/CLI test: simulate `run()` receiving `provider = Some("codex-cli")` and assert resulting `ImplantEvent.provider == Some("codex-cli")` | PENDING |
| AC-18 | `unimatrix hook PreToolUse` (no `--provider` flag) processes the event with `provider: "claude-code"` as the backward-compatible default. `normalize_event_name("PreToolUse", None)` returns `("PreToolUse", "claude-code")`. | test | Unit test: `normalize_event_name("PreToolUse", None)` asserts `("PreToolUse", "claude-code")`; documents the silent Codex mislabel risk explicitly | PENDING |
| AC-19 | The Codex reference config (`.codex/hooks.json`) is written. Each event invokes `unimatrix hook <event> --provider codex-cli`. The config carries an explicit caveat that live MCP hook support is blocked by Codex bug #16732. Unit tests use synthetic Codex events verifying normalization produces `provider: "codex-cli"`. | file-check | File exists at `.codex/hooks.json`; valid JSON or JSON-with-comments; every hook command line contains `--provider codex-cli`; caveat text about Codex bug #16732 is present; unit test with synthetic Codex event and `--provider codex-cli` passes | PENDING |
| AC-20 | `normalize_event_name("SessionStart", Some("claude-code"))` returns `("SessionStart", "claude-code")`. `normalize_event_name("SessionStart", Some("codex-cli"))` returns `("SessionStart", "codex-cli")`. Provider hint takes precedence over inference for all shared event names. | test | Unit tests for each case; also verify `normalize_event_name("SessionStart", Some("gemini-cli"))` returns `("SessionStart", "gemini-cli")` | PENDING |
