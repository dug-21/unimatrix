# vnc-017 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | After `context_correct(A → B)`, no `graph_edges` row with `target_id = A` and a non-Supersedes relation type exists. | test | Python infra-001: seed multiple edge types pointing at A; call `context_correct(A → B)`; assert `SELECT COUNT(*) FROM graph_edges WHERE target_id = A AND relation_type != 'Supersedes'` returns 0 | PENDING |
| AC-02 | After `context_correct(A → B)`, all edges that formerly pointed at A (excluding Supersedes) now have `target_id = B`. | test | Python infra-001: assert `SELECT * FROM graph_edges WHERE target_id = B` contains the expected source/relation rows | PENDING |
| AC-03 | The redirect always targets `new_entry.id` (direct correction result), not a cached chain traversal result. | test | Rust unit test: create two-hop chain (A → B → C); call `context_correct(A → B)`; assert redirected target is B, not C; assert no call to `find_terminal_active` or `TypedGraphState` (structural assertion in code review + behavioral test) | PENDING |
| AC-04 | `context_correct` returns a success response even when one or more redirect calls return `Err`. | test | Rust unit test: inject a failing `redirect_graph_edge` stub; assert handler returns well-formed success response with `deprecated_original` and `corrected_entry` fields present and correct | PENDING |
| AC-05 | `query_incoming_edges(target_id)` is implemented in `read.rs`, returns `Vec<IncomingEdgeRow>` (source_id, relation_type, created_at), uses `read_pool()`. | test | Rust unit test: seed three edge rows with same `target_id` and one with a different `target_id`; call `query_incoming_edges`; assert exactly three tuples returned with correct values; assert `relation_type` values match seeded values | PENDING |
| AC-06 | Python infra-001 integration test: store C, store A, add edge `C → A` (Prerequisite), call `context_correct(A → B)`, assert `graph_edges` contains `C → B` and no row with `target_id = A` for non-Supersedes types. | test | Python infra-001 new test case: full auto-redirect flow end-to-end | PENDING |
| AC-07 | `Contradicts` edges are correctly handled: integration test includes a `Contradicts` edge, and after redirect both forward and reverse rows point at the new target. | test | Python infra-001: extend AC-06 with a Contradicts edge pair; assert both `C → B` and `B → C` (reverse) rows exist post-redirect | PENDING |
| AC-08 | When the source entry of an incoming edge is quarantined or deprecated, that edge is skipped, `tracing::warn!` is emitted, and the failure counter is NOT incremented. | test | Rust unit test: seed incoming edge whose source has status Quarantined; call redirect loop; assert edge row unchanged, `skipped == 1`, `failed == 0`, `tracing::warn!` captured with source_id | PENDING |
| AC-09 | A UNIQUE conflict (new edge already exists — idempotent redirect) returns `Ok(())` and is counted as redirected (success). No warning is logged. | test | Rust unit test: pre-insert `C → B`; call redirect loop for `C → A → B`; assert `failed == 0`, `redirected == 1`, no `tracing::warn!` emitted | PENDING |
| AC-10 | `Supersedes` edges in `graph_edges` are excluded from the redirect loop and remain unchanged after `context_correct`. | test | Rust/integration test: seed a Supersedes row pointing at A; call `context_correct(A → B)`; assert Supersedes row still has `target_id = A` and no Supersedes row with `target_id = B` was inserted | PENDING |
| AC-11 | When no incoming non-Supersedes edges exist, response text is identical to current `context_correct` output (no "Redirected" append) and no summary log is emitted. | test | Rust unit test: call `context_correct` on entry with no incoming edges; assert response text does not contain "Redirected" and no `tracing::info!` summary log emitted | PENDING |
| AC-12 | When all non-Supersedes incoming edges redirect successfully, response text is appended with `"Redirected N incoming edges (0 failed, see logs)"`. | test | Python infra-001: seed two Prerequisite edges; call `context_correct(A → B)`; assert MCP response text contains exactly `"Redirected 2 incoming edges (0 failed, see logs)"` | PENDING |
| AC-13 | When incoming edges exist and some fail (SQL error), response text reflects the correct failure count: `"Redirected N incoming edges (M failed, see logs)"` with M > 0. | test | Rust unit test: stub partial-failure (some edges fail SQL); assert response text contains accurate counts for redirected and failed | PENDING |
| AC-14 | Rust unit test exercises redirect loop end-to-end: seeds an edge row pointing at `original_id`, calls `context_correct`, verifies edge row now points at `new_entry.id`. | test | Rust unit test using in-memory SQLite: assert `graph_edges` state before and after call; verify `target_id` updated | PENDING |
| AC-15 | `context_edge(mode="redirect")` tool remains operational. All existing tests for that tool pass without modification. | test | Run existing `context_edge(mode="redirect")` test suite; assert all pass unchanged | PENDING |
| AC-16 | `DependencyOnDeprecated` detection does not fire for successfully auto-redirected edges after the next graph tick. | test | Python infra-001: after AC-06 flow, trigger graph state tick; assert no DependencyOnDeprecated event raised for the redirected edge source | PENDING |
| AC-17 | When all incoming edges are skipped (all sources Quarantined/Deprecated), response text uses skipped-count variant rather than bare zero count. | test | Rust unit test: seed 3 incoming edges from Quarantined sources; call `context_correct`; assert response text contains `"Redirected 0 incoming edges (3 skipped — invalid source, 0 failed, see logs)"`, `failed == 0` | PENDING |

## Additional Test Coverage (from RISK-TEST-STRATEGY)

| Risk | Scenario | Verification Detail | Status |
|------|----------|--------------------|----|
| R-01 | Compile-time return contract verification | Rust: redirect loop match arms handle `Ok(())` and `Err(EdgeRedirectError)` with no unreachable arms; no `Ok(bool)` branch exists | PENDING |
| R-02 | Structural assertion on `query_incoming_edges` for Supersedes exclusion | Rust: call `query_incoming_edges` on a target with only Supersedes rows; assert empty result (validates SQL-level exclusion, not loop-level) | PENDING |
| R-05 | Fan-in ceiling truncation | Rust unit test: seed 55 incoming edges; assert exactly 50 redirects, `tracing::warn!` with `total_found=55`, response text contains `"(truncated from 55, see logs)"` | PENDING |
| R-05 | Exactly-at-ceiling (50) | Rust unit test: seed exactly 50 edges; assert no truncation warn emitted, all 50 processed | PENDING |
| R-06 | Deprecated source skip | Rust unit test: seed `B → A` (Contradicts) where B is Deprecated; assert edge unchanged, `skipped == 1`, `failed == 0`, warn emitted | PENDING |
| R-06 | Mixed-status Contradicts fan-in | Rust unit test: seed one valid Contradicts (Active source) and one invalid (Quarantined source); assert valid one redirects (both directions), invalid skipped without failure count | PENDING |
| R-08 | AC-16 full-redirect clears DependencyOnDeprecated | Python infra-001: after full redirect, trigger tick and assert detection rule does not fire for redirected edges | PENDING |
| R-10 | Phase B + redirect loop double-write | Rust unit test: seed `C → A`; pass `C → B` in Phase B `params.edges`; assert exactly one `C → B` row exists post-call, `failed == 0` | PENDING |
| R-11 | Response text verified in integration test | Python infra-001: assert returned `CallToolResult` text contains exact format string `"Redirected N incoming edges (0 failed, see logs)"` | PENDING |

## Minimum Gate for Merge

All AC-01 through AC-16 must pass, plus the following risk scenarios: R-01 compile-time structural test, R-02 Supersedes exclusion structural test, R-06 Contradicts mixed-status test.
