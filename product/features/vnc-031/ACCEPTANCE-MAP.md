# vnc-031 Acceptance Criteria Map

Every AC from SCOPE.md (AC-01..AC-10) appears. New tests extend `packages/unimatrix/test/merge-settings.test.js`; harness changes land in `packages/unimatrix/test/dogfood-effect.test.js`. Verification details name the test/command that asserts each criterion.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Legacy `"*"` `PreToolUse` uni hook migrates clean: fresh hook under `PRETOOLUSE_CYCLE_MATCHER`, no uni hook under `"*"`. | test | `test_legacy_star_pretooluse_migrates_clean` in `packages/unimatrix/test/merge-settings.test.js` — seed `PreToolUse:[{matcher:"*",hooks:[{command:"/old/path/unimatrix hook PreToolUse"}]}]`; assert cycle-matcher group with fresh command exists, no uni hook under `"*"`. | PENDING |
| AC-02 | Exactly one uni hook per managed event, in the `EVENT_MATCHERS[event]` group with the fresh command, never zero (fail-loud). | test | Extend `test_each_event_has_exactly_one_unimatrix_entry` to a cross-group seed; assert `count === 1` per event, survivor in managed group with `command === source.commandForEvent(event)`, plus explicit `assert(count !== 0, ...)` self-identifying message. Covers FR-01/02/03/04, SR-01. | PENDING |
| AC-03 | Foreign hooks (incl. foreign `"*"` PreToolUse and uni-looking-but-unclassified near-miss) preserved byte-for-byte; foreign-retaining groups not dropped. | test | `test_cross_group_preserves_foreign_star_hook` + `test_cross_group_preserves_near_miss_foreign_hook` + non-command (`type:"url"`) preservation case in `merge-settings.test.js`. Covers FR-05/13, SR-02. | PENDING |
| AC-04 | Group emptied solely by removing stale uni hooks is dropped; event key retained (managed group holds keep-target). | test | `test_cross_group_drops_emptied_group_keeps_event` — seed `"*"` group whose only entry is a stale uni hook; assert `"*"` group absent, `content.hooks.PreToolUse` present and contains `PRETOOLUSE_CYCLE_MATCHER`. Covers FR-06. | PENDING |
| AC-05 | Idempotent including stale-`"*"`-on-first-run: two runs yield `deepStrictEqual` content. | test | `test_cross_group_migration_idempotent` — seed legacy `"*"`, run twice, `assert.deepStrictEqual(first.content, second.content)`; existing `test_merge_idempotent_round_trip` + `test_subagentstop_optout_idempotent` still pass. Covers FR-12, SR-07. | PENDING |
| AC-06 | Both call arms (string `init` / object `initRemote`) produce a clean cross-group migration on the same legacy input. | test | `test_cross_group_migration_string_arm` + `test_cross_group_migration_object_arm` — same legacy `"*"` seed, run each `commandSource` shape, assert AC-01/AC-02 post-state for each (object-arm fresh command via `buildHookClientCommand`). Covers FR-09, SR-05. | PENDING |
| AC-07 | Opt-out prune for non-registered events (SubagentStop default-off) unchanged; `(opt-out)` action strings preserved. | test | Existing `test_subagentstop_pruned_on_opt_out`, `test_subagentstop_optout_preserves_foreign_hook`, `test_subagentstop_optout_idempotent`, `test_subagentstop_optin_then_optout_round_trip` pass unmodified; new cross-matcher phrase substring-disjoint from `(opt-out)`. Covers FR-10/11, SR-03. | PENDING |
| AC-08 | Action emitted per cross-group removal, `[dry-run]`-prefixed under dry-run. | test | `test_cross_group_emits_action_and_dry_run_prefix` — seed legacy `"*"`; non-dry-run asserts cross-matcher phrase; dry-run asserts same action with `[dry-run]` prefix and no file written. Covers FR-07. | PENDING |
| AC-09 | Dogfood script bespoke prune retired; harness asserts clean migration from `mergeSettings` alone; parity proven on real legacy input before deletion. | test + grep + manual | (a) **grep** `scripts/dogfood-switchover.sh` contains no `PRUNE_FRAGMENT` / `pruneStaleUniHooks` / `commandReferencesTarget` / `shellTokens` / `emitAndWrite`; both arms call `mergeSettings(..., {dryRun})`. (b) **test** `dogfood-effect.test.js` clean-state assertions pass with the simplified script; **negative control preserved** (must `assert.throws` on an unpruned post-state — GATE B / R-13). (c) **manual (GATE C)** P1–P8 parity proven GREEN on REAL legacy-shaped input for promote+rollback **before** the fragment-deletion commit (#4938). Covers FR-14/15/16, SR-04. | PENDING |
| AC-10 | Full `packages/unimatrix` suite green, incl. existing merge-settings cases, new cross-group cases, dogfood tests, and `init`/`init --remote` consumer tests that assert event/group shape; fixture touches justified. | shell + manual | **shell** `node --test packages/unimatrix/test/` passes. **manual** consumer init tests iterating `HOOK_EVENTS` / asserting counts re-run; reviewer confirms each changed fixture is an intended install-surface shape change (#4826, R-12). | PENDING |

## Delivery Gates (cross-cutting — not single ACs)

| Gate | Description | Verification | Blocks |
|------|-------------|-------------|--------|
| GATE A | crt-052 #706 + vnc-027 #4811 present on the delivery base branch (OQ-B / R-14). | manual — `git log` / `git merge-base` check, recorded before the prune lands. | The prune commit (AC-01..AC-08). |
| GATE B | nan-016 harness clean-state attribution repointed to `mergeSettings`-alone; negative control preserved (not deleted). OQ-5 / OQ-C / R-13. | test — negative control `assert.throws` on unpruned post-state still passes. | AC-09 harness change. |
| GATE C | P1–P8 parity proven GREEN on REAL legacy input before deleting `PRUNE_FRAGMENT` (ADR-003 / SR-04 / R-04 / #4938). | manual ordering gate — parity proof commit precedes the fragment-deletion commit. | AC-09 script-retire (`scripts/dogfood-switchover.sh` deletion). |
