# crt-051 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `contradiction_density_score()` signature is `fn contradiction_density_score(contradiction_pair_count: usize, total_active: u64) -> f64`. The `total_quarantined` parameter is removed. | grep | `grep -n "pub fn contradiction_density_score" crates/unimatrix-server/src/infra/coherence.rs` — output must show `contradiction_pair_count: usize` | PENDING |
| AC-02 | `contradiction_density_score(0, N)` returns `1.0` for any `N > 0` (cold-start or no contradictions detected). | test | `cargo test -p unimatrix-server infra::coherence::contradiction_density_no_pairs` | PENDING |
| AC-03 | `contradiction_density_score(0, 0)` returns `1.0` (empty database guard). | test | `cargo test -p unimatrix-server infra::coherence::contradiction_density_zero_active` | PENDING |
| AC-04 | `contradiction_density_score(N, N)` returns `0.0` (all entries contradicted, clamped at zero). | test | `cargo test -p unimatrix-server infra::coherence::contradiction_density_pairs_exceed_active` | PENDING |
| AC-05 | For `pair_count > 0` and `total_active > pair_count`, the score is strictly between 0.0 and 1.0. | test | `cargo test -p unimatrix-server infra::coherence::contradiction_density_partial` — asserts result in (0.0, 1.0) and `(result - 0.95).abs() < 1e-10` | PENDING |
| AC-06 | The call site in `status.rs::compute_report()` passes `report.contradiction_count` (not `report.total_quarantined`) to `contradiction_density_score()`. | grep | `grep -n "contradiction_density_score" crates/unimatrix-server/src/services/status.rs` — output must show `report.contradiction_count` as argument | PENDING |
| AC-07 | `report.contradiction_count` is populated in Phase 2 of `compute_report()` (~lines 583–591) before the Lambda computation in Phase 5 (~lines 747–756). Ordering invariant preserved. | manual | Read `status.rs` lines 583–593 and 747–748; confirm Phase 2 contradiction cache read precedes Phase 5 Lambda call; confirm phase-ordering comment present at Phase 5 call site | PENDING |
| AC-08 | `generate_recommendations()` still receives `total_quarantined` as its parameter; the quarantine recommendation path is not altered. | grep | `grep -n "generate_recommendations" crates/unimatrix-server/src/services/status.rs` — output must show `report.total_quarantined` as argument; `grep -n "fn generate_recommendations" crates/unimatrix-server/src/infra/coherence.rs` — must show `total_quarantined: u64` parameter | PENDING |
| AC-09 | `total_quarantined` is NOT passed to `contradiction_density_score()` anywhere in the codebase. | grep | `grep -rn "contradiction_density_score.*total_quarantined" crates/` must return zero matches; `grep -rn "total_quarantined.*contradiction_density_score" crates/` must return zero matches | PENDING |
| AC-10 | All unit tests in `coherence.rs` pass. Tests for `contradiction_density_score()` assert the new pair-count semantics. | test | `cargo test -p unimatrix-server infra::coherence` exits 0 | PENDING |
| AC-11 | `cargo test --workspace` passes with zero failures. | shell | `cargo test --workspace` exits 0 | PENDING |
| AC-12 | `cargo clippy --workspace -- -D warnings` passes with zero warnings. | shell | `cargo clippy --workspace -- -D warnings` exits 0 | PENDING |
| AC-13 | The doc comment on `contradiction_density_score()` describes new semantics. Old "quarantined-to-active ratio" description is absent. | manual | Read updated doc comment in `crates/unimatrix-server/src/infra/coherence.rs`; confirm it mentions "detected pair count" and "contradiction scan cache"; confirm no mention of "quarantined" or "quarantine" in the doc comment | PENDING |
| AC-14 | Full test rewrite for `coherence.rs` contradiction_density_score tests. Three tests replaced: (a) `usize` first argument, (b) no name contains "quarantined", (c) covers zero-active, zero-pairs-with-active, pairs-exceed-active, and mid-range value. | manual | Read test block in `crates/unimatrix-server/src/infra/coherence.rs`; confirm no test name contains "quarantined"; confirm all four cases are present with correct argument types | PENDING |
| AC-15 | `make_coherence_status_report()` fixture in `response/mod.rs` has `contradiction_count: 15` and `contradiction_density_score: 0.7000`. `coherence: 0.7450` unchanged. | grep | `grep -n "contradiction_count" crates/unimatrix-server/src/mcp/response/mod.rs` at `make_coherence_status_report()` must show `15`; `grep -n "contradiction_density_score" crates/unimatrix-server/src/mcp/response/mod.rs` at fixture must show `0.7000` | PENDING |
| AC-16 | Phase ordering invariant documented in code. Phase 2 contradiction cache read precedes Phase 5 Lambda call; relationship documented by comment or block label. | manual | Read `status.rs` Phase 2 (~lines 583–591) and Phase 5 (~lines 747–748); confirm comment at Phase 5 call site references Phase 2 dependency (e.g., "Phase 5 must not be reordered above Phase 2") | PENDING |
| AC-17 | Cold-start behavior produces score=1.0 with a dedicated unit test. Test named `contradiction_density_cold_start` (or containing "cold") calls `contradiction_density_score(0, N>0)` and asserts 1.0. | test | `cargo test -p unimatrix-server infra::coherence::contradiction_density_cold_start` exits 0; test uses non-zero `total_active` | PENDING |
