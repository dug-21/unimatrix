# crt-022 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `rayon = "1"` is present in `unimatrix-server/Cargo.toml`; no other workspace crate gains a rayon dependency | shell | `cargo tree -p unimatrix-server \| grep rayon` returns an entry; `cargo tree -p unimatrix-core \| grep rayon` returns empty; same for `unimatrix-embed`, `unimatrix-vector`, `unimatrix-store` | PENDING |
| AC-02 | `unimatrix-server/src/infra/rayon_pool.rs` exists and exports `RayonPool` and `RayonError`; `RayonPool::spawn` is an async method accepting `FnOnce() -> T + Send + 'static` and returning `Result<T, RayonError>` | file-check + test | File exists at `crates/unimatrix-server/src/infra/rayon_pool.rs`; unit test dispatches a closure and receives the return value | PENDING |
| AC-03 | A panic inside a `RayonPool::spawn` closure returns `Err(RayonError::Cancelled)` to the awaiting tokio task; the panic does not propagate to the tokio runtime or MCP handler thread; test runtime does not abort | test | `cargo test -p unimatrix-server rayon_pool` — test spawns a closure containing `panic!("test")`, awaits result, asserts `Err(RayonError::Cancelled)` | PENDING |
| AC-04 | Rayon pool initialised at server startup from `[inference] rayon_pool_size` config (default `(num_cpus::get() / 2).max(4).min(8)`, max 8); pool is `Arc<RayonPool>` reachable from all inference consumers via `ServiceLayer` | test | Integration smoke test — server starts with minimal config; search request triggering embedding inference completes successfully | PENDING |
| AC-05 | `AsyncEmbedService` does not exist in `unimatrix-core/src/async_wrappers.rs` or any file reachable from `unimatrix-core/src/lib.rs`; `AsyncVectorStore` is retained and unchanged; `unimatrix-core` gains no new dependencies; `cargo check --workspace` passes | shell + grep | `grep -r "AsyncEmbedService" crates/` returns zero results; `grep -r "AsyncVectorStore" crates/unimatrix-core/` returns the struct definition; `cargo check --workspace` exits 0 | PENDING |
| AC-06 | All 7 ONNX embedding inference call sites in `unimatrix-server` use `ml_inference_pool.spawn(...)` or `ml_inference_pool.spawn_with_timeout(...)` instead of `spawn_blocking` or `spawn_blocking_with_timeout` | grep | At each of the 7 locations (`services/search.rs`, `services/store_ops.rs`, `services/store_correct.rs`, `background.rs` ~543, `background.rs` ~1162, `uds/listener.rs`, `services/status.rs`), the old `spawn_blocking` call is absent and a `ml_inference_pool.spawn` call is present | PENDING |
| AC-07 | `spawn_blocking` is not used for ONNX inference anywhere in the codebase after this feature ships; enforced by a CI grep step on every PR against main | shell | CI step greps `spawn_blocking` in `crates/unimatrix-server/src/services/` and `crates/unimatrix-server/src/background.rs`; step exits non-zero if any inference site remains; step runs on every PR | PENDING |
| AC-08 | `OnnxProvider::new(config)` in `unimatrix-server/src/infra/embed_handle.rs` remains on `spawn_blocking` | grep | `grep -n "spawn_blocking" crates/unimatrix-server/src/infra/embed_handle.rs` returns exactly one result (the `OnnxProvider::new` call) and no rayon-related call | PENDING |
| AC-09 | `InferenceConfig` struct with field `rayon_pool_size: usize` exists in `unimatrix-server/src/infra/config.rs`; included in `UnimatrixConfig` as `inference: InferenceConfig`; derives `serde::Deserialize` with `#[serde(default)]`; `rayon_pool_size` validates `[1, 64]`; out-of-range value aborts startup with a structured error | test | Unit tests for `InferenceConfig::validate()` covering values 0, 1, 8, 64, 65; integration test confirming server refuses to start with `rayon_pool_size = 0` | PENDING |
| AC-10 | Server starts and handles MCP requests successfully with the rayon pool active; embedding inference produces valid embeddings (same dimensionality and L2-normalised output as before migration) after the migration | test | Existing integration test suite passes without modification; embedding output for a known input produces the same vector (within f32 tolerance) before and after migration | PENDING |
| AC-11 | At minimum 8 new unit tests cover: (1) `RayonPool::spawn` successful dispatch, (2) `RayonPool::spawn` panic safety, (3) pool init with `num_threads = 1`, (4) pool init with `num_threads = 8`, (5) `InferenceConfig` valid lower bound, (6) `InferenceConfig` valid upper bound, (7) `InferenceConfig` rejects 0, (8) `InferenceConfig` rejects 65 | test | `cargo test -p unimatrix-server` passes including all 8 new tests | PENDING |
