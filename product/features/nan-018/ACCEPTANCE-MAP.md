# nan-018 Acceptance Criteria Map

Every AC from SCOPE.md is listed. Verification detail draws on the spec's verification table and the Risk-Test Strategy's gating scenarios. Wave assignment in the Description column. Status PENDING until verified in delivery.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | [Wave 1] Penalty levers are `UnimatrixConfig` fields threaded through `graph_penalty`; TOML steepness sweep yields expected A/B delta; **defaults reproduce current behavior bit-for-bit**. | test + grep | (a) Default-equivalence test: `graph_penalty_with(.., &GraphPenaltyParams::default()) == graph_penalty(..) ==` named const for every status-shape branch + clamp. (b) Two-profile TOML sweep of one steepness lever produces predicted penalty delta in report. (c) **Enumerated-site grep guard (R-01/WARN-1, SOURCE OF TRUTH)**: every `graph_penalty` reference — the two `search.rs` penalty sites (:727 fallback branch, :729 `graph_penalty`) + `with_rate_config` resolution — routes through `graph_penalty_with`/resolved field. (`background.rs:583` is a `tracing::error!` log string, NOT a penalty site — excluded.) `cargo test -p unimatrix-server eval::` + engine default-equivalence test. | PENDING |
| AC-02 | [Wave 1] Forbidden-ID-set absence assertion, surfaced in `report`, counted in regression check. | test | Fixture scenario with forbidden set: assert `forbidden ∩ top_k == ∅` (pass when absent, fail when present); outcome appears in report; newly-introduced violation registers in Section 5 regression list. Exit-code semantics unchanged. | PENDING |
| AC-03 | [Wave 1] Relative-rank (A below B) assertion, surfaced and gated likewise. | test | rank-below truth table: present/present rank(A)>rank(B) ⇒ pass; rank(A)<rank(B) ⇒ fail; **A-absent ⇒ pass**; **B-absent ⇒ FAIL** (asymmetric — R-11); surfaced + regression-counted. | PENDING |
| AC-04 | [Wave 1] Steepness sweep reports trust **alongside** P@5/MRR in one run. | test | One `eval run` over ≥2 steepness profiles on the fixture corpus; report contains trust outcomes AND P@5/MRR for the same scenarios in a single correlated section. | PENDING |
| AC-05 | [Wave 1] Fixture entry-graph authored/loaded/searched; `expected` property-based. | test | Author fixture graph of the five shapes; load + run search replay; property assertions (redirect-to-head/absence/rank-below) resolve and evaluate; **loader rejects null `expected` and literal-ID `expected`** in primary corpus (SR-07). | PENDING |
| AC-06 | [Wave 1] Canonical fixture corpus ships in-repo covering ≥ the four shapes. | file-check + test | In-repo version-controlled corpus under `crates/unimatrix-server/src/eval/corpus/fixtures/`; test asserts presence of multi-correction chain, dangling chain, superseded-Active, deprecated-connected (optional 5th dead-end chain); each loads and searches. | PENDING |
| AC-07 | [Wave 1 plumbing; Wave 2 docs] Two-corpus model: snapshot still yields realistic P@5/MRR; docs state primary/durable vs realism/ephemeral. | test + manual | Snapshot path → P@5/MRR still produced unchanged (existing tests green); doc-review confirms the two roles stated (Band-2). | PENDING |
| AC-08 | [Wave 1] Retrieval-shape hash stamp over enumerated inputs; fails-loud/warns on mismatch; spec states durability branch. | test | (a) Hash over ordered, versioned manifest (entry columns, edge types, confidence dims, **embedding model-id/dimension**). (b) Deliberate-mismatch test: mutate one manifest input → guard fails-loud on primary / warns on snapshot, message names diverged dimension. (c) Determinism (NFR-03): hash identical across N≥100 runs, permuted-order, and separate processes. (d) Spec states **branch (b)** (embed-model-id/dim in hash; no sidecar) — R-05 live-source assertion. | PENDING |
| AC-09 | [Wave 1] Token-weighted cost = Σ per-result token-proxy, k secondary; surfaced in `report`; proxy-narrowing only as explicit in-design call, never a deferral. | test | Unit test of `cost = Σ token_proxy` on a known set; two sets with **same k but different token loads yield different cost** (R-07); cost + k both surfaced in report; `token_proxy` deterministic (R-08); ADR-003 records token-weighted default (precision+k NOT taken). | PENDING |
| AC-10 | [Wave 2] `docs/testing/eval-harness.md` updated; ADRs stored. | file-check + manual | Doc diff covers all new capabilities; Unimatrix ADR entries exist (#4889-#4893). | PENDING |
| AC-11 | [Wave 2] Band-2 docs sufficient for author/migrate/sweep. | file-check + manual | Authoring guide, migration runbook, two-corpus doc, config-knob reference (incl. cost-proxy fidelity caveat) exist; doc-review confirms a dev could author/migrate/sweep from docs alone. | PENDING |
| AC-12 | [Wave 2] (a) Protocol recommendation doc, **no `.claude/protocols/` edits**; (b) `convention` couples shape-change⇒migration; (c) `procedure` entries. | file-check + shell + manual | (a) `product/features/nan-018/RECOMMENDATION-band3-protocol.md` exists; `git diff` shows **zero** `.claude/protocols/` changes. (b) Unimatrix `convention` entry surfacable in briefing. (c) `procedure` entries exist. | PENDING |
| AC-13 | [Wave 2 — HARD GATE] No protocol/workflow changes at all; deferred-design boundary documented; recommended trigger asset-maintenance only. | shell + manual | **Hard gate**: `git diff --name-only` confirms **no `.claude/protocols/` file edited** and no eval-execution-as-gate wiring added; docs state the deferred-separate-design boundary and asset-maintenance-only trigger. `git diff --name-only origin/main -- .claude/protocols/` must be empty. | PENDING |
| AC-14 | [Wave 1 EXIT GATE] **Proof-by-use**: a correlated steepness sweep runs end-to-end on the fixture corpus reporting trust + P@5/MRR + cost in one run — **non-vacuously**. | test | **End-to-end Wave-1 exit (R-15 gating scenario — NOT satisfied by mere execution)**: one `eval run` steepness sweep on the fixture corpus where (1) report contains trust outcomes (AC-02/03) AND P@5/MRR (AC-04) AND token-weighted cost (AC-09) for the same scenarios, with **≥1 trust assertion non-vacuously evaluated against a non-empty result set**; (2) each of the 4 required shapes yields ≥1 evaluated assertion; (3) the two profiles differ in a penalty lever with an **observable non-zero penalty/ranking delta** (lever proven live); (4) swept baseline reproduces default behavior bit-for-bit (AC-01); (5) corpus guarded by a deterministic, actually-firing drift guard (AC-08). A sweep that executes but fails any of 1-5 does NOT pass. | PENDING |
