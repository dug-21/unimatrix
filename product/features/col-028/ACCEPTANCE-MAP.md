# col-028 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `context_search` passes `current_phase: Some(phase)` in `UsageContext` when active session has a phase set; passes `None` when no session or no phase | test | Unit test with mock `SessionRegistry`: register session with phase, call `context_search` handler, assert `UsageContext.current_phase == Some(phase)`; repeat with no session and assert `None` | PENDING |
| AC-02 | `context_lookup` passes `current_phase: Some(phase)` in `UsageContext` when active session has a phase set; passes `None` when no session or no phase | test | Unit test with mock `SessionRegistry`: register session with phase, call `context_lookup` handler, assert `UsageContext.current_phase == Some(phase)`; repeat with no session and assert `None` | PENDING |
| AC-03 | `context_get` passes `current_phase: Some(phase)` in `UsageContext` when active session has a phase set; passes `None` when no session or no phase | test | Unit test with mock `SessionRegistry`: register session with phase, call `context_get` handler, assert `UsageContext.current_phase == Some(phase)`; repeat with no session and assert `None` | PENDING |
| AC-04 | `context_briefing` passes `current_phase: Some(phase)` in `UsageContext` when active session has a phase set; passes `None` when no session or no phase | test | Unit test with mock `SessionRegistry`: register session with phase, call `context_briefing` handler, assert `UsageContext.current_phase == Some(phase)`; repeat with no session and assert `None` | PENDING |
| AC-05 | `context_get` uses `access_weight: 2` (changed from 1); an entry fetched via `context_get` produces `access_count = 2` on first access | test | Unit test: insert entry X with `access_count=0`, call `context_get` for entry X in fresh session (no prior dedup entry), assert `access_count` increments to 2 | PENDING |
| AC-06 | `context_briefing` uses `access_weight: 0`; briefing does not increment `access_count` for any returned entry | test | Unit test: insert entries [X, Y] with `access_count=0`, call `context_briefing` returning [X, Y], assert `access_count` for both X and Y remains 0 | PENDING |
| AC-07 | Briefing on entry X followed by `context_get` on entry X increments `access_count` by 2; briefing call does not consume the dedup slot (D-01 validation) | test | Integration test: register session, call `record_briefing_usage` with entry X and weight=0, assert `UsageDedup.access_counted` does NOT contain `(agent_id, X)`, then call `record_mcp_usage` with entry X weight=2, assert `access_count` increments by 2 (not 0); must use real `UsageService` and inspect dedup state | PENDING |
| AC-08 | `SessionState` has `confirmed_entries: HashSet<u64>` initialised empty on `register_session` | test | Unit test: call `register_session`, retrieve state, assert `confirmed_entries.is_empty()` | PENDING |
| AC-09 | After `context_get` for entry ID X, `confirmed_entries` for that session contains X | test | Unit test: call `context_get` handler for entry X, retrieve session state, assert `confirmed_entries.contains(X)` | PENDING |
| AC-10 | After `context_lookup` with a single target ID X, `confirmed_entries` contains X; after multi-target lookup (two or more IDs), `confirmed_entries` is not updated | test | Two unit tests: (1) call `context_lookup` with `target_ids=[X]`, assert `confirmed_entries.contains(X)`; (2) call `context_lookup` with `target_ids=[X, Y]`, assert `confirmed_entries` does NOT contain X or Y | PENDING |
| AC-11 | `context_lookup` access_weight remains 2 (unchanged from pre-feature state) | test | Existing lookup tests pass; code review confirms `access_weight: 2` in the `context_lookup` `UsageContext` literal | PENDING |
| AC-12 | Phase snapshot is taken synchronously before any `await` in each of the four read-side handlers; the call uses shared free function `current_phase_for_session` | manual | Code review: inspect each of the four handler bodies in `mcp/tools.rs`, confirm `current_phase_for_session` is the first statement before any `.await` (delivery gate checklist item) | PENDING |
| AC-13 | `CURRENT_SCHEMA_VERSION = 17` | test | `#[test] fn test_current_schema_version_is_17()` asserts `CURRENT_SCHEMA_VERSION == 17` in `migration.rs` | PENDING |
| AC-14 | After opening a v17 database (fresh or migrated), `query_log` has a `phase TEXT` column and `idx_query_log_phase` index | test | `migration_v16_to_v17.rs` tests T-V17-01 (fresh DB) and T-V17-03 (index present) | PENDING |
| AC-15 | The v16→v17 migration is idempotent; running it on an already-v17 database does not fail | test | `migration_v16_to_v17.rs` test T-V17-04: run migration function twice, assert no error returned and schema_version remains 17 | PENDING |
| AC-16 | A `context_search` call in a session with active phase "delivery" writes `phase = "delivery"` to the query_log row; a call with no active session writes `phase = NULL` | test | Integration test: open session with phase "delivery", issue `context_search`, flush analytics drain, query `query_log`, assert `phase = "delivery"`; repeat with no session and assert `phase = NULL` (must use real analytics drain per pattern #3004) | PENDING |
| AC-17 | `QueryLogRecord` has `phase: Option<String>`; `scan_query_log_by_session` returns records with correct phase value; `row_to_query_log` correctly deserializes column index 9 | test | Integration test: write row via `insert_query_log` with `phase = Some("design")`, read back via `scan_query_log_by_session`, assert `record.phase == Some("design")`; write with `phase = None`, assert read-back is `None` (primary SR-01 guard against positional drift) | PENDING |
| AC-18 | Pre-existing `query_log` rows read back with `phase = None` after v16→v17 migration | test | `migration_v16_to_v17.rs` test T-V17-05: insert row in v16 database, run migration, read back row via `scan_query_log_by_session`, assert `phase = None` | PENDING |
| AC-19 | `migration_v16_to_v17.rs` covers: T-V17-01 (fresh DB at v17), T-V17-02 (v16→v17 from fixture), T-V17-03 (index present), T-V17-04 (idempotency), T-V17-05 (pre-existing rows get phase=None), T-V17-06 (schema_version=17) | test | `cargo test -p unimatrix-store --test migration_v16_to_v17` passes all six sub-tests | PENDING |
| AC-20 | `make_state_with_rework` and all related test helpers updated for `confirmed_entries: HashSet::new()`; all existing tests pass | test | `cargo test --workspace` passes with no new failures; no compile errors from missing field initializer in `SessionState` struct literals | PENDING |
| AC-21 | analytics.rs INSERT (`?9` = phase), `scan_query_log_by_sessions` SELECT (tenth column), `scan_query_log_by_session` SELECT (tenth column), and `row_to_query_log` deserializer (index 9) are updated in the same commit; no partial state permitted | manual | Code review: confirm all four sites modified in the same commit; AC-17 round-trip test provides runtime enforcement — if any site diverges, AC-17 fails with column-index error | PENDING |
| AC-22 | All migration test files with `schema_version == 16` assertions updated to 17; `migration_v15_to_v16.rs` and `server.rs` (lines 2059, 2084) updated | shell | `grep -r 'schema_version.*== 16' crates/` returns zero matches | PENDING |
| AC-23 | `uds/listener.rs:1324` passes `None` for `phase` in `QueryLogRecord::new(...)`; workspace compiles | shell | `cargo build --workspace` completes without error | PENDING |
| AC-24 | `confirmed_entries` field in `SessionState` carries doc comment stating: populated by `context_get` (always) and `context_lookup` (single-ID requests only, request-side cardinality); not populated by briefing/search/write/mutation; in-memory only; first consumer is Thompson Sampling | grep | `grep -A 8 'confirmed_entries' crates/unimatrix-server/src/infra/session.rs` shows the full doc comment as specified in SPECIFICATION.md §Exact Signatures | PENDING |
