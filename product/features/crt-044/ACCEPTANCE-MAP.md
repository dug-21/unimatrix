# crt-044 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | After v19→v20 migration, every forward S1/S2 Informs edge has a reverse partner — count of paired Informs edges equals total S1/S2 Informs edge count | test | Integration test: query `SELECT COUNT(*) FROM GRAPH_EDGES g1 WHERE g1.relation_type='Informs' AND g1.source IN ('S1','S2') AND EXISTS (SELECT 1 FROM GRAPH_EDGES g2 WHERE g2.source_id=g1.target_id AND g2.target_id=g1.source_id AND g2.relation_type='Informs')` returns `COUNT(*) > 0` and equals `SELECT COUNT(*) FROM GRAPH_EDGES WHERE relation_type='Informs' AND source IN ('S1','S2')`. Note: scope both sides to `source IN ('S1','S2')` to avoid false failures from nli/cosine_supports edges. | PENDING |
| AC-02 | After v19→v20 migration, every forward S8 CoAccess edge has a reverse partner — count of paired S8 CoAccess edges equals total S8 CoAccess edge count | test | Integration test: equivalent query to AC-01 scoped to `relation_type='CoAccess' AND source='S8'` on both `g1` and `g2`. Returns `COUNT(*) > 0` and equals `SELECT COUNT(*) FROM GRAPH_EDGES WHERE relation_type='CoAccess' AND source='S8'`. | PENDING |
| AC-03 | `run_s1_tick` writes two rows per qualifying pair going forward: `(lower_id, higher_id, 'Informs', ...)` and `(higher_id, lower_id, 'Informs', ...)` | test | Integration test with two-entry fixture with shared tags. After one tick run, assert both `(a→b, source='S1', relation_type='Informs')` and `(b→a, source='S1', relation_type='Informs')` exist in GRAPH_EDGES via direct table query. | PENDING |
| AC-04 | `run_s2_tick` writes two rows per qualifying pair going forward (same pattern as AC-03) | test | Integration test with two-entry fixture with structural vocabulary overlap. After one tick run, assert both directions with `source='S2'` and `relation_type='Informs'` exist in GRAPH_EDGES. | PENDING |
| AC-05 | `run_s8_tick` writes two rows per qualifying pair going forward: `(*a, *b, 'CoAccess', ...)` and `(*b, *a, 'CoAccess', ...)` | test | Integration test with two-entry co-access fixture. After one tick run, assert both `(a→b, source='S8', relation_type='CoAccess')` and `(b→a, source='S8', relation_type='CoAccess')` exist in GRAPH_EDGES. For a new pair, assert `pairs_written = 2`. | PENDING |
| AC-06 | `CURRENT_SCHEMA_VERSION` is incremented to 20 in `migration.rs` | grep | `grep 'CURRENT_SCHEMA_VERSION' crates/unimatrix-store/src/migration.rs` contains `= 20`. Pre-merge note: if crt-043 has shipped v20 first, this value must be 21 instead. | PENDING |
| AC-07 | The v19→v20 migration block uses `INSERT OR IGNORE` and is idempotent: running it twice produces no duplicate rows and no errors | test | Explicit migration test (not just an assertion comment): apply v19→v20 block twice in sequence; assert GRAPH_EDGES row count is identical after the first and second run, and no SQL error is returned on the second run. | PENDING |
| AC-08 | The `pub fn graph_expand(` line in `graph_expand.rs` carries a `// SECURITY:` comment stating the caller quarantine obligation | grep | `grep '// SECURITY:' <path>/graph_expand.rs` is non-empty. Comment appears immediately before `pub fn graph_expand(`. Verify actual file path before edit (see Delivery Note 1). | PENDING |
| AC-09 | All existing migration tests pass. New migration block has at least one test asserting a forward-only S1 Informs edge and a forward-only S8 CoAccess edge each gain a reverse partner after migration | test | Migration test creates a v19-state fixture with one forward-only S1 Informs edge `(1→2)` and one forward-only S8 CoAccess edge `(5→6)`. After `migrate_if_needed`, asserts `(2→1, source='S1', relation_type='Informs')` and `(6→5, source='S8', relation_type='CoAccess')` both exist. All pre-existing migration tests exit 0. | PENDING |
| AC-10 | All existing `graph_enrichment_tick` tests pass. New tests for `run_s1_tick`, `run_s2_tick`, and `run_s8_tick` assert both `(a→b)` and `(b→a)` edges exist after the tick runs on a two-entry fixture | test | Three independent test cases (one per tick function). Each creates a minimal two-entry store, runs the relevant tick, queries GRAPH_EDGES directly for both edge directions by `source` field. These are per-source regression guards: a future removal of the second call in any one tick fails exactly one test. | PENDING |
| AC-11 | `cargo test --workspace` passes with no regressions | shell | `cargo test --workspace` exits with code 0. | PENDING |
| AC-12 | PR description documents that `run_s8_tick`'s `pairs_written` counter now counts per-edge, not per logical pair; 2× values for new pairs are correct and expected | manual | Reviewer confirms PR description contains explicit statement of the `pairs_written` semantic change (per-pair → per-edge) before merge is approved. Implementation test for AC-05 also asserts `pairs_written = 2` for a single new pair. | PENDING |
| AC-13 | `write_graph_edge` returning `false` on the second direction call MUST NOT trigger a warning, log message at warn/error level, or increment any error counter | test | Run tick on two-entry fixture whose reverse edge already exists (simulate post-migration state). Assert: (a) no warn/error log entries, (b) `edges_written` / `pairs_written` increments by 1 total (first call returns true, second returns false via UNIQUE conflict), (c) no error counter increments. Tick completes successfully. | PENDING |
| AC-14 | Migration test suite includes a two-run idempotency test with pre-existing reverse edges: applying v19→v20 to a DB that already has some bidirectional pairs produces the same row count as a single-run migration | test | Test creates a DB with one pre-existing reverse edge alongside one forward-only edge. Runs v19→v20 migration twice. Asserts GRAPH_EDGES row count is identical after the first and second run, and no SQL error is raised on the second run. Distinct from AC-07 (which tests clean state); this test exercises partial-bidirectionality input state. | PENDING |
