# crt-026 Acceptance Criteria Map

GH Issue: #341

Note: AC-07 is explicitly dropped per SPECIFICATION.md §AC-07 (OQ-03 resolved — explicit phase
affinity boost deferred to W3-1; `w_phase_explicit=0.0` is always a no-op in crt-026).

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `SessionState` has a `category_counts: HashMap<String, u32>` field, initialized to `HashMap::new()` in `register_session` | test | Unit test: call `register_session`; assert `session_state.category_counts.is_empty()` | PENDING |
| AC-02 | A successful `context_store` increments `category_counts[category]` by 1; a duplicate store (`insert_result.duplicate_of.is_some()`) does NOT increment the histogram | test | Unit test: store same entry twice; assert histogram count = 1 after second store (`test_duplicate_store_does_not_increment_histogram`) | PENDING |
| AC-03 | `record_category_store` is a silent no-op when `session_id` is `None` or the session is not registered — no panic, no side effect | test | Unit test: call `record_category_store("nonexistent-session", "decision")` on a fresh registry; assert no panic and histogram unchanged (`test_record_category_store_unregistered_session_is_noop`) | PENDING |
| AC-04 | `ServiceSearchParams` has a `session_id: Option<String>` field | grep | `grep -n "session_id: Option<String>" crates/unimatrix-server/src/services/search.rs` — assert match in `ServiceSearchParams` struct definition | PENDING |
| AC-05 | The `context_search` handler passes `ctx.audit_ctx.session_id.clone()` into `ServiceSearchParams.session_id` and passes the pre-resolved histogram into `ServiceSearchParams.category_histogram` | test | Unit test on handler: search with a session that has prior stores; assert `ServiceSearchParams.session_id` and `.category_histogram` are populated correctly | PENDING |
| AC-06 | When `session_id` is present and session histogram is non-empty, `SearchService` applies `p(category) * w_phase_histogram` to each result entry's fused score via `compute_fused_score` | test | Verified transitively via AC-12 (score delta test at p=1.0 confirms the term is applied inside `compute_fused_score`) | PENDING |
| AC-07 | — DROPPED — | — | Explicit phase affinity boost (`phase_category_weight * w_phase_explicit`) deferred to W3-1. `w_phase_explicit=0.0` in crt-026; `phase_explicit_norm=0.0` at all call sites. See SPECIFICATION.md §AC-07 and ADR-003. | N/A |
| AC-08 | When the session histogram is empty (cold start), the affinity boost is `0.0` for all entries; `SearchService` output is identical to the pre-crt-026 pipeline | test | Unit test: search with no prior stores; assert all `phase_histogram_norm = 0.0` and final scores equal scores produced without session context (`test_cold_start_search_produces_identical_scores`) | PENDING |
| AC-09 | Both `w_phase_explicit` (default `0.0`) and `w_phase_histogram` (default `0.005`) are configurable in `[inference]` config using the `default_w_*` serde pattern | test | Unit test: `InferenceConfig::default()` returns `w_phase_explicit=0.0, w_phase_histogram=0.005`; config round-trip test confirms both fields serialize/deserialize correctly (`test_phase_explicit_norm_placeholder_fields_present`) | PENDING |
| AC-10 | The histogram term is applied INSIDE `compute_fused_score` as a first-class dimension, before `status_penalty` is applied. `final_score = compute_fused_score(&inputs, &weights) * status_penalty` | test | Unit test: construct a penalized candidate (`status_penalty=0.5`) with a histogram match; assert `final_score = (fused_including_boost) * 0.5` — boost participates in pre-penalty fused score (`test_status_penalty_applied_after_histogram_boost`) | PENDING |
| AC-11 | The `CompactPayload` synthesis output includes a `Recent session activity: ...` block when the session has a non-empty histogram; the block is omitted when the histogram is empty | test | Unit test on `format_compaction_payload`: (1) call with non-empty histogram `{"decision": 3, "pattern": 2}` → assert output contains `"Recent session activity: decision × 3, pattern × 2"`; (2) call with empty histogram → assert output does NOT contain `"Recent session activity"` (`test_compact_payload_histogram_block_present_and_absent`) | PENDING |
| AC-12 | A result whose category appears in the session histogram at `p=1.0` ranks higher than an otherwise equal result whose category is absent; score delta ≥ `w_phase_histogram * 1.0 = 0.005` | test | Unit test: two synthetic `ScoredEntry` values equal on all dimensions except category; histogram = `{"decision": 5}`, total=5; assert `score(decision) - score(other) >= 0.005` (`test_histogram_boost_score_delta_at_p1_equals_weight`) | PENDING |
| AC-13 | A result whose category is NOT in the session histogram receives `phase_histogram_norm = 0.0` and boost `= 0.0` from the histogram term | test | Unit test: populate histogram with category `"decision"`; search result has category `"lesson-learned"`; assert `phase_histogram_norm = 0.0` (`test_absent_category_phase_histogram_norm_is_zero`) | PENDING |
| AC-14 | The WA-2 extension stubs in `search.rs` (`FusedScoreInputs` line 55, `FusionWeights` line 89, `compute_fused_score` line 179) are resolved; no `WA-2 extension:` stub comment remains | grep | `grep -n "WA-2 extension" crates/unimatrix-server/src/services/search.rs` — assert zero matches | PENDING |
