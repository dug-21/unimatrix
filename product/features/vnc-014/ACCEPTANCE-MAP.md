# VNC-014 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | HTTP session: `initialize` with `clientInfo.name = "codex-mcp-client"` produces audit rows with `agent_attribution = "codex-mcp-client"` and `metadata` JSON containing `"client_type":"codex-mcp-client"` | test | Integration test: mock HTTP session with `Mcp-Session-Id` UUID; call a tool; assert `audit_log` row fields | PENDING |
| AC-02 | Empty `clientInfo.name` produces no `client_type_map` entry; audit rows have `agent_attribution = ""` and `metadata = "{}"` with no `client_type` key | test | Unit test on `initialize` handler: pass empty `client_info.name`; assert map is not populated; assert audit fields | PENDING |
| AC-03 | No session context (stdio with no prior initialize, or tool call with no map entry): `agent_attribution = ""`, `metadata = "{}"`, `credential_type = "none"`, tool call succeeds without error | test | Existing test suite passes unmodified (except infrastructure changes for append-only triggers); verify all 12 tools functional | PENDING |
| AC-04 | After v24→v25 migration: `pragma_table_info('audit_log')` returns `credential_type` (NOT NULL DEFAULT 'none'), `capability_used` (NOT NULL DEFAULT ''), `agent_attribution` (NOT NULL DEFAULT ''), `metadata` (NOT NULL DEFAULT '{}'); existing rows unaffected | test | Migration integration test: start from v24 DB, run `migrate_if_needed`, run `pragma_table_info`, assert 12 columns with correct defaults | PENDING |
| AC-05 | `AuditEvent` round-trip: `log_audit_event` then `read_audit_event` correctly stores and retrieves all four new fields | test | Unit/integration test in `audit.rs`: insert row with `credential_type="none"`, `capability_used="write"`, `agent_attribution="codex-mcp-client"`, `metadata="{\"client_type\":\"codex-mcp-client\"}"`, then read and assert each field | PENDING |
| AC-05b | Append-only DDL triggers installed and enforced: `DELETE FROM audit_log` raises `Err` containing `"audit_log is append-only: DELETE not permitted"`; `UPDATE audit_log` raises `Err` containing `"audit_log is append-only: UPDATE not permitted"`; `gc_audit_log` updated; `import::drop_all_data` updated | test | Integration test: insert a row, then attempt raw DELETE and UPDATE via `sqlx::query`, assert error messages; separately verify `gc_audit_log` no longer issues DELETE | PENDING |
| AC-06 | `initialize` override returns `InitializeResult` bit-identical to `self.get_info()` — same capabilities, protocol version, instructions | test | Unit test: compare `server.initialize(request, ctx)` return value field-by-field to `server.get_info()` | PENDING |
| AC-07 | Concurrent HTTP session isolation: two simultaneous sessions with distinct `Mcp-Session-Id` UUIDs and distinct `clientInfo.name` values produce correctly attributed audit rows with no cross-session bleed | test | Concurrent integration test: session A (`"codex-mcp-client"`) and session B (`"gemini-cli-mcp-client"`) run tool calls; assert A rows carry `"codex-mcp-client"` and B rows carry `"gemini-cli-mcp-client"` with no cross-contamination | PENDING |
| AC-08 | Stdio session: `initialize` with non-empty `clientInfo.name` stores value under key `""` in `client_type_map`; subsequent tool calls produce `agent_attribution` set to that value | test | Integration test using stdio-mode server construction: call `initialize`, then a tool, assert `agent_attribution` in audit row | PENDING |
| AC-09 | Schema version bumped to 25, no data loss: `SELECT value FROM counters WHERE name = 'schema_version'` returns 25; existing `audit_log` row count unchanged; pre-existing rows have valid defaults in four new columns | test | Migration test on v24 DB with pre-existing rows: run migration, assert `schema_version = 25`, assert row count unchanged, assert new column values equal their defaults | PENDING |
| AC-10 | `clientInfo.name` of 300 chars is truncated to exactly 256 chars in `client_type_map` and `agent_attribution`; WARN log emitted during `initialize` handling | test | Unit test: call `initialize` with 300-char name; assert stored value is 256 chars; assert WARN was emitted; additionally test exactly-256-char name is NOT truncated and no WARN emitted | PENDING |
| AC-11 | `capability_used` values are canonical: each of the 12 tool handlers writes the lowercase `Capability::as_audit_str()` value for its capability gate; no ad-hoc or free-form strings used | test | Unit test per tool + code review: `Capability::Read.as_audit_str()` = `"read"`, `Write` = `"write"`, `Search` = `"search"`, `Admin` = `"admin"`; for each tool, call it and assert `capability_used` in audit row | PENDING |
| AC-12 | `build_context()` fully superseded: original function is either absent (preferred) or `#[deprecated]`; no production call site invokes the old path | shell | `cargo build --workspace 2>&1 \| grep -c "build_context[^_]"` should be 0 (removal); or build produces no warnings on tool-handler call sites (deprecation) | PENDING |
