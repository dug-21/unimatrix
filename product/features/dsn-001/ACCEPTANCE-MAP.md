# dsn-001 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | When no `~/.unimatrix/config.toml` or per-project `config.toml` exists, server starts with all compiled default values and all existing tests pass without modification | test | `cargo test --workspace` with no config files present; all tests pass | PENDING |
| AC-02 | When `[knowledge] categories` is set, the loaded list replaces the default 8-category allowlist and `CategoryAllowlist` reflects the new list for all validation calls | test | Unit test: construct `CategoryAllowlist::from_categories(["custom"])`, assert `"custom"` passes and a default-only category (e.g. `"outcome"`) fails | PENDING |
| AC-03 | When `[knowledge] boosted_categories` is set, those categories receive the provenance boost in search re-ranking; no literal `"lesson-learned"` string comparison remains in `search.rs` | test + grep | Unit test: configure `boosted_categories = ["custom-boost"]`; assert boost applied to `"custom-boost"` and not to `"lesson-learned"`. Grep: `grep -n '"lesson-learned"' crates/unimatrix-server/src/services/search.rs` returns zero matches in comparison position | PENDING |
| AC-04 | When `[knowledge] freshness_half_life_hours` is set, `freshness_score()` uses that value instead of the compiled `168.0` constant | test | Unit test: call `freshness_score()` with `ConfidenceParams { freshness_half_life_hours: 336.0, ..Default::default() }`; assert a 336-hour-old entry scores 0.5 | PENDING |
| AC-05 | When `[server] instructions` is set, that string appears in `ServerInfo.instructions` returned during the MCP initialize handshake | test | Unit test: construct `UnimatrixServer` with a config containing custom instructions string; assert `server_info.instructions == Some(custom_string)` | PENDING |
| AC-06 | When `[agents] default_trust` and `session_capabilities` are set, unknown agents auto-enroll with the configured capabilities | test | Unit test (strict): configure `default_trust = "strict"`, enroll unknown agent, assert caps are `[Read, Search]` and not `[Write]`. Separate test: configure `session_capabilities = ["Read"]`, enroll, assert caps are exactly `[Read]` | PENDING |
| AC-07 | A per-project config at `{data_dir}/config.toml` overrides the global `~/.unimatrix/config.toml` for all keys it specifies; unspecified keys fall through to global or compiled defaults | test | Unit test 1: global `freshness_half_life_hours = 500.0`, per-project `freshness_half_life_hours = 24.0`; merged reads `24.0`. Unit test 2: global `categories = ["a"]`, per-project omits `categories`; merged reads `["a"]` | PENDING |
| AC-08 | A config file with world-writable permissions (`mode & 0o002 != 0`) causes startup to abort with an error message identifying the file path and violation | test | Unit test (Unix only, `#[cfg(unix)]`): write temp config file, `chmod 777`; assert `load_config()` returns `Err` containing the file path | PENDING |
| AC-09 | A config file with group-writable permissions (`mode & 0o020 != 0`) logs a warning but does not abort startup | test | Unit test (Unix only, `#[cfg(unix)]`): write temp config file, `chmod 664`; assert `load_config()` returns `Ok` and a `tracing::warn!` is emitted | PENDING |
| AC-10 | A `[knowledge] categories` entry with characters outside `[a-z0-9_-]`, length > 64, or a total category count > 64 causes startup to abort with a descriptive error | test | Unit tests: (a) uppercase in category → `Err`; (b) space in category → `Err`; (c) 65-character category name → `Err`; (d) list of 65 categories → `Err` | PENDING |
| AC-11 | A `[knowledge] boosted_categories` value not present in `[knowledge] categories` causes startup to abort with an error naming the invalid value | test | Unit test: `categories = ["a","b"]`, `boosted_categories = ["c"]`; assert `Err` message contains `"c"` | PENDING |
| AC-12 | A `[server] instructions` value matching any injection pattern in `ContentScanner` causes startup to abort with an error identifying the triggering pattern category | test | Unit test: set `instructions` to a known injection-triggering string from the `ContentScanner` test corpus; assert `Err` | PENDING |
| AC-13 | The MCP tool formerly named `context_retrospective` is now named `context_cycle_review`; all callers in protocols, skills, tests, and other files are updated; no occurrence of `context_retrospective` as a tool-name or caller reference remains in the codebase | grep + test | `grep -r "context_retrospective" . --include="*.rs" --include="*.py" --include="*.md" --include="*.toml"` returns zero results (excluding archival feature docs). Integration test in `test_protocol.py` asserts `"context_cycle_review"` is in the tool list and `"context_retrospective"` is absent | PENDING |
| AC-14 | The `CycleParams.topic` field doc no longer references "feature" as the primary example; the doc communicates the domain-agnostic concept of a bounded work unit | manual | Code review of `CycleParams.topic` doc comment in `crates/unimatrix-server/src/mcp/tools.rs`; confirm the comment references multiple domain examples (e.g. incident, campaign, case) and does not use software-delivery vocabulary as the sole illustration | PENDING |
| AC-15 | A config file exceeding 64 KB causes startup to abort with an error before TOML parsing begins | test | Unit test: write a temp file of 65537 bytes; assert `load_config()` returns `Err` before any TOML parse call (verify by ensuring the oversized file contains invalid TOML — if TOML parse errors appear, size check did not fire first) | PENDING |
| AC-16 | A `[knowledge] freshness_half_life_hours` value of `0.0`, negative, `NaN`, or `Infinity` causes startup to abort with a descriptive error | test | Unit tests: one for each invalid value (`0.0`, `-1.0`, `f64::NAN`, `f64::INFINITY`); each returns `Err` | PENDING |
| AC-17 | A `[knowledge] freshness_half_life_hours` value greater than `87600.0` causes startup to abort with a descriptive error | test | Unit test: value `87600.1` → `Err`. Boundary test: value `87600.0` → `Ok` | PENDING |
| AC-18 | A `[agents] default_trust` value other than `"permissive"` or `"strict"` causes startup to abort with an error listing valid values | test | Unit test: `default_trust = "open"` → `Err` with message containing both `"permissive"` and `"strict"` | PENDING |
| AC-19 | A `[agents] session_capabilities` list containing any value other than `"Read"`, `"Write"`, or `"Search"` (including `"Admin"`) causes startup to abort with an error | test | Unit test: `session_capabilities = ["Read","Admin"]` → `Err` | PENDING |
| AC-20 | A `[server] instructions` value exceeding 8 KB causes startup to abort with an error before `ContentScanner` runs | test | Unit test: `instructions` of 8193 bytes → `Err`; verify error message identifies length (not injection pattern), confirming size check fired before scanner | PENDING |
| AC-21 | All new validation paths have unit tests. All existing unit and integration tests continue to pass | test | `cargo test --workspace` passes with zero failures; new tests cover every new `Err` path in `validate_config()` | PENDING |
