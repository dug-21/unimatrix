# dsn-001 Acceptance Criteria Map

> Revised: 2026-03-18 (post-preset-system design re-run)
> Source ACs: product/features/dsn-001/SCOPE.md (AC-01 through AC-27) +
>             product/features/dsn-001/specification/SPECIFICATION.md

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | When no `~/.unimatrix/config.toml` and no per-project `config.toml` exist, server starts with all existing default values and all existing tests pass without modification. | test | `cargo test --all` with no config files present; assert zero test changes and zero test failures. | PENDING |
| AC-02 | When `[knowledge] categories` is set to a valid list, `CategoryAllowlist` reflects that list instead of the compiled 8-category default. | test | Construct `CategoryAllowlist::from_categories(vec!["custom-cat".into()])` and assert `is_allowed("custom-cat")` returns true and `is_allowed("outcome")` returns false (outcome not in list). | PENDING |
| AC-03 | When `[knowledge] boosted_categories` is set, those categories receive the provenance boost in search re-ranking; the hardcoded `entry.category == "lesson-learned"` comparison no longer exists in `search.rs`. | grep + test | `grep -n '"lesson-learned"' crates/unimatrix-server/src/services/search.rs` must return zero results. Integration test with `boosted_categories = ["decision"]` confirming "decision" entries receive the provenance boost and "lesson-learned" entries do not when absent from the list. | PENDING |
| AC-04 | When `[knowledge] freshness_half_life_hours` is set to a valid value, `freshness_score()` uses that value instead of the compiled 168.0 constant. | test | Unit test calling `freshness_score(last, created, now, &params)` with `params.freshness_half_life_hours = 24.0` vs `168.0` at a fixed known age; assert the ratio between the two results matches the expected exponential decay ratio. A compiled-constant implementation would return the same value for both. | PENDING |
| AC-05 | When `[server] instructions` is set, that string appears in `ServerInfo.instructions` returned during the MCP `initialize` handshake. | test | Integration test with config `instructions = "Test domain guidance."` asserting the exact string appears in the `initialize` response `ServerInfo.instructions` field. | PENDING |
| AC-06 | When `[agents] default_trust = "strict"` and `session_capabilities` are set, auto-enrolled unknown agents receive the configured capability set rather than the default permissive set. | test | Integration test enrolling an unknown agent with `default_trust = "strict"`, `session_capabilities = ["Read", "Search"]`; assert enrolled capabilities are exactly `[Read, Search]` and do not contain `Write`. | PENDING |
| AC-07 | A per-project config at `{data_dir}/config.toml` overrides the global `~/.unimatrix/config.toml` for all fields it specifies; unspecified fields fall through to the global value or compiled defaults. | test | Integration test with global `categories = ["a", "b"]` and per-project `categories = ["c"]`; assert effective categories are `["c"]` only, confirming replace semantics (not append). | PENDING |
| AC-08 | A config file with world-writable permissions (`mode & 0o002 != 0`) causes server startup to abort with an error message identifying the file path and the violation. | test | Unit test (unix only, `#[cfg(unix)]`): create a temp file with mode 0o666, call `check_permissions(path)`, assert `Err(ConfigError::WorldWritable(...))` containing the file path. | PENDING |
| AC-09 | A config file with group-writable permissions (`mode & 0o020 != 0`) logs a tracing warning and does not abort startup. | test | Unit test (unix only, `#[cfg(unix)]`): create a temp file with mode 0o664, call `check_permissions(path)`, assert `Ok(())` return and warning emission. | PENDING |
| AC-10 | A `[knowledge] categories` entry containing characters outside `[a-z0-9_-]`, length > 64, or a total category count > 64 causes startup to abort with a descriptive error. | test | Three unit tests: (a) category with uppercase `"Cat!"` → `Err(InvalidCategoryChar)`; (b) category name of length 65 → `Err(InvalidCategoryLength)`; (c) list of 65 categories → `Err(TooManyCategories)`. All error messages must be descriptive. | PENDING |
| AC-11 | A `[knowledge] boosted_categories` value not present in the validated `categories` set causes startup to abort with an error naming the invalid value. | test | Unit test with `categories = ["a"]` and `boosted_categories = ["b"]`; assert `Err(BoostedCategoryNotInAllowlist(...))` and that the error message contains `"b"`. | PENDING |
| AC-12 | A `[server] instructions` value matching any injection pattern in `ContentScanner` causes startup to abort with an error identifying the triggering pattern category. | test | Unit test with a known injection-triggering string from `ContentScanner`'s 26-regex set; assert `Err(ConfigError::InstructionsInjection(...))`. | PENDING |
| AC-13 | The MCP tool formerly named `context_retrospective` is now named `context_cycle_review`; all callers in protocols, skills, and tests are updated; no reference to `context_retrospective` remains outside the historically-excluded directories listed in SPECIFICATION.md §SR-05. | grep + test | `grep -r "context_retrospective" .` at repo root returns zero matches outside excluded historical directories. Integration test in `test_protocol.py` asserts `"context_cycle_review"` is in the tool list and `"context_retrospective"` is absent. Integration test calling `context_cycle_review(feature_cycle: "col-022")` via Python harness returns a valid structured response. | PENDING |
| AC-14 | The `CycleParams.topic` field doc no longer references "feature" as the canonical example; the doc communicates the domain-agnostic concept of a bounded work unit tracked by any domain. | manual | Read `CycleParams.topic` doc comment in `crates/unimatrix-server/src/mcp/tools.rs`; confirm the comment references multiple domain examples (e.g. feature, incident, campaign, case, experiment) and does not use software-delivery vocabulary as the sole or primary illustration. | PENDING |
| AC-15 | A config file exceeding 64 KB causes startup to abort with an error before TOML parsing begins. | test | Unit test writing a 65537-byte temp file; assert `Err(ConfigError::FileTooLarge(...))`. Confirm size check fires before TOML parse by making the oversized content invalid TOML — if a TOML parse error is returned, the size check did not fire first. Also test a 65536-byte file passes (inclusive boundary). | PENDING |
| AC-16 | A `[knowledge] freshness_half_life_hours` value of `0.0`, negative, `NaN`, or `Infinity` causes startup to abort with a descriptive error. | test | Four named unit tests, one per value: `0.0` → `Err(InvalidHalfLifeValue)`, `-1.0` → `Err`, `f64::NAN` → `Err`, `f64::INFINITY` → `Err`. Also test `-0.0` (IEEE negative zero). | PENDING |
| AC-17 | A `[knowledge] freshness_half_life_hours` value greater than `87600.0` (10 years in hours) causes startup to abort with a descriptive error. | test | Unit test with `87600.001` → `Err(HalfLifeOutOfRange)`. Boundary test with `87600.0` → `Ok` (inclusive upper bound). | PENDING |
| AC-18 | A `[agents] default_trust` value other than `"permissive"` or `"strict"` causes startup to abort with an error listing valid values. | test | Unit test with `default_trust = "admin"` → `Err(ConfigError::InvalidDefaultTrust(...))`. Error message must list both `"permissive"` and `"strict"`. | PENDING |
| AC-19 | A `[agents] session_capabilities` list containing any value other than `"Read"`, `"Write"`, or `"Search"` (including `"Admin"`) causes startup to abort with an error. | test | Unit test with `session_capabilities = ["Admin"]` → `Err(InvalidSessionCapability)`. Unit test with `["Read", "Admin"]` → `Err`. Unit test with `["admin"]` (lowercase) → behavior must be deterministic and tested. Unit test with `["Read", "Write", "Search"]` → `Ok`. | PENDING |
| AC-20 | A `[server] instructions` value exceeding 8 KB (8192 bytes) causes startup to abort with an error before `ContentScanner` runs. | test | Unit test with 8193-byte string → `Err(ConfigError::InstructionsTooLong)`. Confirm length check precedes scanner by asserting a 9000-byte injection string returns `InstructionsTooLong` (not `InstructionsInjection`). Unit test with 8192-byte string → `Ok`. | PENDING |
| AC-21 | All new validation paths have unit tests. The mandatory SR-10 test is present in `unimatrix-server` with the exact comment text "SR-10: If this test fails, fix the weight table, not the test." All existing unit and integration tests continue to pass. | test | `cargo test --all` is green. Named test `collaborative_preset_equals_default_confidence_params` asserting `assert_eq!(confidence_params_from_preset(Preset::Collaborative), ConfidenceParams::default())` is present with the required comment verbatim. | PENDING |
| AC-22 | When no `[profile]` section is present, the server uses the `collaborative` preset (current compiled defaults). The effective `ConfidenceParams` equals `ConfidenceParams::default()`. | test | Unit test: `resolve_confidence_params(&UnimatrixConfig::default())` returns `Ok(params)` where `params == ConfidenceParams::default()`. This is the AC-21 SR-10 test expressed at the config resolution level. | PENDING |
| AC-23 | When `preset` is `authoritative`, `operational`, `empirical`, or `collaborative`, the corresponding weight vector and `freshness_half_life_hours` from the ADR-005 table are used; `[confidence]` section is ignored even if present; a warning is logged if `[confidence] weights` is present with a named preset. | test | Unit tests asserting exact field values from the ADR-005 table for each named preset: e.g. `confidence_params_from_preset(Preset::Authoritative).w_trust == 0.22`, `freshness_half_life_hours == 8760.0`. Confirm `[confidence] weights` values are NOT reflected in resolved params when a named preset is active. Confirm warning emitted. | PENDING |
| AC-24 | When `preset = "custom"`, all six `[confidence] weights` keys are required; startup aborts with a descriptive error naming the missing field if any are absent. | test | Unit test with `preset = custom` and `weights` entirely absent → `Err(CustomPresetMissingWeights)`. Unit test with exactly five of the six weight keys → `Err` message names the missing key. | PENDING |
| AC-25 | `freshness_half_life_hours` precedence for all four combinations: (1) named preset + absent → preset's built-in; (2) named preset + present → `[knowledge]` value; (3) `custom` + absent → startup abort; (4) `custom` + present → `[knowledge]` value. | test | Four named unit tests, one per row, calling `resolve_confidence_params()` with the corresponding `UnimatrixConfig` built directly. Row 1: `Preset::Operational`, no `[knowledge]` override → `params.freshness_half_life_hours == 720.0`. Row 2: `Preset::Operational`, `[knowledge] = Some(336.0)` → `336.0`. Row 3: `Preset::Custom` + valid weights + no `[knowledge]` → `Err(CustomPresetMissingHalfLife)`. Row 4: `Preset::Custom` + valid weights + `[knowledge] = Some(24.0)` → `24.0`. | PENDING |
| AC-26 | An unrecognised `preset` value causes startup to abort with an error listing the five valid values. | test | TOML deserialization of `preset = "unknown"` returns a serde deserialization error (fails before `validate_config` runs because `Preset` uses `#[serde(rename_all = "lowercase")]` with no catch-all variant). Confirm error message lists the five valid values. | PENDING |
| AC-27 | The `ConfidenceParams` struct carries the effective weight vector (from the active preset or `custom` values) at startup. W3-1 reads this struct for GNN cold-start without any additional config parsing. | test | Assert `ConfidenceParams` has exactly nine public fields (`w_base`, `w_usage`, `w_fresh`, `w_help`, `w_corr`, `w_trust`, `freshness_half_life_hours`, `alpha0`, `beta0`). Assert `resolve_confidence_params` returns `Ok(params)` with all nine fields non-zero for each of the five preset variants (using valid custom weights for `Preset::Custom`). | PENDING |
