# crt-030 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `personalized_pagerank(graph: &TypedRelationGraph, seed_scores: &HashMap<u64, f64>, alpha: f64, iterations: usize) -> HashMap<u64, f64>` is implemented in `unimatrix-engine/src/graph_ppr.rs` and re-exported from `graph.rs` | grep | `grep -r "pub fn personalized_pagerank" crates/unimatrix-engine/src/graph_ppr.rs` returns a result; `grep "pub use graph_ppr::personalized_pagerank" crates/unimatrix-engine/src/graph.rs` returns a result | PENDING |
| AC-02 | All graph traversal in `graph_ppr.rs` uses `edges_of_type()` exclusively — no direct `.edges_directed()` calls | grep | `grep "edges_directed" crates/unimatrix-engine/src/graph_ppr.rs` returns no results | PENDING |
| AC-03 | PPR traverses only `Supports`, `CoAccess`, and `Prerequisite` edges. `Supersedes` and `Contradicts` are excluded by construction. The `personalized_pagerank` doc-comment states SR-01 non-applicability | test | Unit test T-PPR-08: node connected to seed only via `Supersedes` edge receives zero PPR mass; same for `Contradicts` edge | PENDING |
| AC-04 | The `personalized_pagerank` function doc-comment explicitly states: "SR-01 constrains `graph_penalty` and `find_terminal_active` to Supersedes-only traversal; it does not restrict new retrieval functions from using other edge types." | manual | Code review of doc-comment in `graph_ppr.rs` | PENDING |
| AC-05 | Power iteration runs exactly `iterations` steps (no early exit). Node-ID accumulation is sorted ascending each iteration | test | Unit test T-PPR-09: two calls with identical inputs return identical output maps (exact equality). Code review confirms no convergence guard and sort is outside the iteration loop | PENDING |
| AC-06 | Personalization vector is `hnsw_score × phase_affinity_score`, normalized to sum 1.0 before iteration. Cold-start: `phase_affinity_score = 1.0`. Zero-sum guard: if all values are 0.0, return empty map without calling PPR. No `use_fallback` guard wraps the `phase_affinity_score` read in Step 6d | test | Unit test with non-uniform `phase_snapshot` produces different `seed_scores` than uniform baseline. Unit test with `phase_snapshot = None` produces `seed_scores = hnsw_scores`. Unit test with all-zero HNSW scores skips PPR and returns unchanged pool | PENDING |
| AC-07 | Out-degree for propagation normalization uses only positive-edge out-degree (`Supports` + `CoAccess` + `Prerequisite`). Nodes with zero positive out-degree do not propagate forward | test | Unit test T-PPR-05: a node with only `Supersedes` out-edges propagates zero mass to any neighbor | PENDING |
| AC-08 | `Supports` and `Prerequisite` edges traverse `Direction::Incoming`. `CoAccess` edges traverse `Direction::Incoming` (symmetric due to bidirectional storage) | test | Unit test T-PPR-03: `Supports` edge `A→B` with B as seed produces `result[A] > 0.0`. Unit test T-PPR-06: `CoAccess` edge `N→S` with S as seed produces `result[N] > 0.0`. Unit test for `Prerequisite` edge `A→B` with B as seed produces `result[A] > 0.0` | PENDING |
| AC-09 | `ppr_alpha`, `ppr_iterations`, `ppr_inclusion_threshold`, `ppr_blend_weight`, and `ppr_max_expand` are present in `InferenceConfig` with `#[serde(default)]`, validated in `validate()`, and present in `Default::default()`. `SearchService` constructor receives all five values | test | TOML round-trip test (following InferenceConfig pattern from entry #3662). `InferenceConfig::default()` produces: `ppr_alpha=0.85`, `ppr_iterations=20`, `ppr_inclusion_threshold=0.05`, `ppr_blend_weight=0.15`, `ppr_max_expand=50` | PENDING |
| AC-10 | All five PPR fields reject out-of-range values at startup with a `ConfigError` naming the specific field. Rejection occurs in `validate()` before the server starts | test | Unit tests for each field: `ppr_alpha = 0.0` → `Err(ConfigError)`, `ppr_alpha = 1.0` → `Err(ConfigError)`, `ppr_iterations = 0` → `Err(ConfigError)`, `ppr_iterations = 101` → `Err(ConfigError)`, `ppr_inclusion_threshold = 0.0` → `Err(ConfigError)`, `ppr_inclusion_threshold = 1.0` → `Err(ConfigError)`, `ppr_blend_weight = -0.001` → `Err(ConfigError)`, `ppr_blend_weight = 1.001` → `Err(ConfigError)`, `ppr_max_expand = 0` → `Err(ConfigError)`, `ppr_max_expand = 501` → `Err(ConfigError)` | PENDING |
| AC-11 | PPR is Step 6d in `search.rs` — inserted after Step 6b (supersession injection), before Step 6c (co-access prefetch), before NLI (Step 7). Co-access prefetch runs over the full expanded pool (HNSW + PPR entrants) | test | Code review of `search.rs` step comment ordering. Integration test T-PPR-IT-01 verifies a PPR-surfaced entry receives a co-access boost (only possible if PPR runs before Step 6c) | PENDING |
| AC-12 | When `use_fallback == true`, Step 6d is skipped entirely — zero allocation, zero cost. The candidate pool, scores, and ordering are bit-for-bit identical to pre-crt-030 | test | Unit test: construct search context with `use_fallback = true` and non-empty HNSW pool. Assert `results_with_scores` Vec (entry IDs and scores by value) is identical before and after Step 6d | PENDING |
| AC-13 | Entries with PPR score `> ppr_inclusion_threshold` not already in pool are sorted by PPR score descending, capped at `ppr_max_expand`, fetched from store, and appended to `results_with_scores`. Quarantined entries and fetch errors are silently skipped | test | Integration test T-PPR-IT-01. Unit test: `entry_store.get()` returns quarantined entry → not appended. Unit test: `entry_store.get()` returns `Err` → not appended. Unit test: entry with PPR score exactly equal to threshold (`==`) is NOT included; entry with `ppr_score + f64::EPSILON` IS included | PENDING |
| AC-14 | PPR-only entries (not in HNSW pool) have initial similarity `= ppr_blend_weight × ppr_score` | test | Unit/integration test: assert `injected_entry.similarity == ppr_blend_weight * ppr_score` with tolerance `1e-9` for a known PPR-only entry | PENDING |
| AC-15 | Entries already in the pool whose IDs appear in PPR output have their similarity score updated: `new_sim = (1.0 - ppr_blend_weight) * current_sim + ppr_blend_weight * ppr_score` | test | Unit test: known inputs `sim=0.8`, `ppr_score=0.4`, `ppr_blend_weight=0.15` → `new_sim = 0.85 * 0.8 + 0.15 * 0.4 = 0.74`. Assert with tolerance `1e-9` | PENDING |
| AC-16 | When #414 is merged and `PhaseFreqTable` has non-uniform phase data for the current phase, the PPR personalization vector differs from the pure-HNSW-seeded baseline | test | Unit test with synthetic `phase_snapshot` containing non-uniform scores for two entries: assert resulting `seed_scores` differ from `{id: hnsw_score}` for those entries | PENDING |
| AC-17 | Integration test in `search.rs` (inline unit test module or `search_tests.rs`) constructs a `TypedRelationGraph` where entry B is an HNSW candidate and entry A has a `Supports` edge `A→B`. After Step 6d, entry A appears in `results_with_scores` with PPR score exceeding `ppr_inclusion_threshold` | test | `cargo test` — test asserts `results_with_scores.iter().any(\|e\| e.entry.id == A_id)` after Step 6d runs with appropriate config | PENDING |
| AC-18 | A node connected to a seed exclusively via `CoAccess` edges receives non-zero PPR mass from that seed | test | Unit test in `graph_ppr.rs`: two-node graph (seed S, neighbor N) with `CoAccess` edge `N→S`. Seeds PPR with `{S: 1.0}`. Assert `result[N] > 0.0` | PENDING |
