# vnc-045 Acceptance Criteria Map

Every AC from SCOPE.md §Acceptance Criteria (AC-01..AC-07 — scope REDUCED 2026-07-07; the old AC-08/AC-09 `protected_tags` criteria are removed with the deferral). Verification detail draws from SPECIFICATION §7 and RISK-TEST-STRATEGY. Status is PENDING until Stage-3c execution.

**Test-seam constraint (#5468):** the `context_tag` `#[tool]` handler is NOT unit-constructible — orchestration + audit proofs live at the `StoreTagService` + store-primitive + `audit_log` read-back seams; route/format proofs in Stage-3c integration.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `context_tag(id, action, tag)` with `action ∈ {add, remove, replace}` is a registered MCP op gated on `Capability::Write`, writes `entry_tags` directly (single-row INSERT/DELETE), never invokes `update()` or `context_correct`; a mutation leaves `content_hash`/`previous_hash` and all edges unchanged | test | Integration (route seam): (a) tool registered/callable; (b) agent lacking `Write` rejected. Service/primitive seam: (c) `content_hash`, `previous_hash`, full edge set byte-identical pre/post; (d) behavioral proof no `update()`/`correct()` — no new entry id, no supersession version. Handler not unit-constructible (#5468). Maps FR-01..04, R-01 | PENDING |
| AC-02 | After a mutation the five learning columns (`confidence`, `access_count`, `last_accessed_at`, `helpful_count`, `unhelpful_count`) are unchanged (contrast: `context_correct` zeroes them); the mutated tag is visible to the next tag-filtered read with no invalidation step | test | Seed entry with non-zero `confidence`/`access_count`/`helpful_count`; mutate a tag at the service seam; assert all five learning columns byte-identical; issue tag-filtered query immediately after — entry present (add) / absent (remove), no stale window (live SQL, ADR-002). Maps FR-06, NFR-01, NFR-04, R-01 | PENDING |
| AC-03 | `replace` removes the prior value + adds the new in ONE transaction; a partial failure leaves the entry's tags unchanged | test | Store-primitive seam: `add delivery:partial`, then `replace delivery:proven` → exactly one `delivery:*` tag remains (`proven`), prior gone, in one observable step (`replace_tag` returns the evicted prior). Inject a mid-transaction INSERT failure → DELETE rolls back, prior survives, never a zero-tag window (0 observable intermediate states). Colon-less/null-namespace tag → degrades to `add` (pure insert, no prior removed), never hard-errors. Maps FR-05, NFR-05, R-02 | PENDING |
| AC-04 | Every mutation emits `operation="context_tag"`, `target_ids=[id]`, `agent_id`, `capability_used`, `timestamp`, `metadata={action, namespace, tag, prior_value, new_value}`; `namespace` derived from the tag prefix (before first `:`, else null) and recorded but NEVER validated; `prior_value` present and non-null on every remove/replace; record reconstructs what/when/who; a future `protected_tags` adds no field | test | `audit_log` read-back seam (with ~50ms fire-and-forget settle delay): assert full field set; `namespace` derives to `delivery` for `delivery:x` and `null` for a colon-less tag; `prior_value` non-null on remove and replace-with-prior, null/absent on plain add; metadata is well-formed JSON, **never `"{}"`** (#5468 — serialize error → warn + skip, not sentinel); exactly ONE event per mutation (replace = one carrying prior+new together); `Outcome`/enums serialize as variant strings not integers (#4366); `session_id` captured before `tokio::spawn` (#4388/#4389). End-to-end integration confirms the route emits the same shape. Maps FR-10, R-03 | PENDING |
| AC-05 | The handler writes any tag value without interpreting it — `delivery:proven`, `delivery:anythingelse`, and free-form `foo` all succeed on bare `Capability::Write`; no allow-list; exactly one pre-write point marked (ADR + code note) as the future hygiene-validator seam; no stub or config type introduced | test + grep | Table test at the service seam: (a) `delivery:proven` accepted; (b) `delivery:anythingelse` accepted (no rejection path exists); (c) free-form `foo` accepted; (d) same `Write` agent throughout, no `TrustLevel` difference changes outcome. Static/review: the marked seam exists as a single interception point; `context_tag` does NOT invoke `validate_outcome_tags` (`tools.rs:895-898`); grep confirms no `ProtectedTagsConfig`/allow-list/`evaluate_protected_tag` type shipped. **Do NOT write a rejection-path test — none ships.** Maps FR-07, R-04 | PENDING |
| AC-06 | The op calls `check_write_rate` (subject to the live per-caller limit) and is counted by `audit_write_count_since` (`'context_tag'` added to its op-list) | test | (a) exceed the per-caller `check_write_rate` limit → op throttled with `ServiceError::RateLimited` (route/service seam, respecting the `UdsSession` exemption, `gateway.rs:60/166`); (b) after N `context_tag` mutations assert `audit_write_count_since` reflects them (`'context_tag'` in the op-list, `audit.rs:84`) — latent signal, NOT a live throttle. Maps FR-08, FR-09, R-07 | PENDING |
| AC-07 | `context_tag` on a quarantined entry is refused; tagging a deprecated entry is allowed (no protected-tag concept ships) | test | Service/store seam: (a) quarantine an entry → any `context_tag` (add/remove/replace) refused with a lifecycle `invalid_params`; (b) deprecate an entry → an arbitrary/free-form tag is ALLOWED and written (over-restriction fails here — no "refuse protected tag on deprecated" rule ships); (c) active entry → valid mutations proceed. Guards re-implemented in-op (FR-11), not inherited. Maps FR-11, R-05 | PENDING |
