# vnc-039 Acceptance Criteria Map

> Every AC from SCOPE.md: AC-01..AC-11, AC-08b, AC-08c, AC-08d, AC-12, AC-13 — plus the AC-04b per-socket re-pin row carried from RISK-TEST-STRATEGY (R-02). Verification methods from SPECIFICATION.md (AC table) and RISK-TEST-STRATEGY.md (R-01..R-17).
>
> **Validation tier:** **[no-cloud]** = Scope B, validatable without a reachable cloud. **[stub/local]** = Scope A fast first tier, pinned to a captured rmcp `initialize` response. **+ live** = **#774 MERGED (PR #778, commit 913b78cb, 2026-06-18)** so live cloud validation against the real `/v1/{slug}` endpoint is **in scope and a delivery gate** — green-on-stub does NOT stand in for live. The earlier "not-validated-live (#774)" caveat is **retired**.
>
> **Sequencing:** Scope B (no-cloud) lands FIRST by risk/independence; Scope A follows, live-validatable.
> **Live-validation checklist FIRST item:** validate the rmcp 1.7.0 session-id handshake live (server-minted+returned vs client-minted) before AC-03/AC-04/AC-12 are trusted as validated-live.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | After `init --bundle`, `.mcp.json` has a `unimatrix` **stdio** entry invoking the JS bridge (not the Rust binary) configured against the bundle's `mcp_url`/`fp`; the "Skipped .mcp.json" remote behavior is gone. | test | Run `init --bundle` against a fixture bundle; assert `.mcp.json` has a `stdio` `unimatrix` entry with the bridge command; assert no "Skipped .mcp.json" line. **[no-cloud]** | PENDING |
| AC-02 | The bridge is pure Node stdlib; no runtime `dependencies` added, no MCP SDK pulled in (by decision, ass-080). | test + grep | Assert `package.json` `dependencies` absent/empty; grep the bridge module for `@modelcontextprotocol/sdk` / `mcp-remote` imports → absent. **[no-cloud]** | PENDING |
| AC-03 | Bridge proxies a full MCP lifecycle: `initialize` → valid result; session id captured + replayed; `tools/list` returns `context_*`; `context_search`/`context_get` `tools/call` round-trips. | test | Drive bridge stdin (`initialize`, `tools/list`, `tools/call`) against a stub mirroring rmcp behavior; assert stdout results and `Mcp-Session-Id` replay; **then validate LIVE against the real cloud (#774 merged)** — session-id handshake first. **[stub/local] + live** | PENDING |
| AC-04 | Bridge owns TLS and pins `fp`: token flushed **only after** leaf fingerprint matches; on mismatch socket destroyed before token sent, fails loud with expected-vs-presented message. | test | Live self-signed handshake (R-01): (a) good-pin → connects + round-trips; (b) wrong-pin → socket destroyed, capturing server received **no** `Authorization` header (token never on wire), loud diagnosable error. **Route to fresh-context security review even if gates are green.** **[stub/local] + live** | PENDING |
| AC-04b | Per-socket re-pin on the persistent connection: every TLS socket the bridge opens re-runs `verifyPeerFingerprint` before its first body byte; no connection-pool agent flushes on an unverified socket. | test | R-02: drive `initialize` then `tools/call`; assert each opened socket pinned before first body byte; force a new socket mid-session and confirm re-pin; mid-session cert-swap on socket #2 → rejected, no token-bearing body flushed. **[stub/local] + live** | PENDING |
| AC-05 | Bridge posts to `mcp_url` **verbatim** — composes no path, derives no slug, appends nothing. | test | Assert the request URL equals `mcp_url` exactly for every proxied request. **[stub/local] + live** | PENDING |
| AC-06 | Bridge handles `application/json` (always required) and, **only if the live SSE-skip probe fails**, `text/event-stream` (SSE) framing — surfacing the JSON-RPC result on stdout for each. | test + manual (probe) | Run the SSE-skip probe **LIVE** across the full lifecycle with `Accept: application/json` (in-repo rmcp `/observe` content-negotiation, vnc-024/PR #686, as fast pre-check): if JSON-only, assert `sse-parse` absent/unused. If probe fails, stub both content types (multi-line SSE `data:` framing, chunk-split-invariant) and assert correct JSON-RPC on stdout for both. **[stub/local] + live probe** | PENDING |
| AC-07 | The remote `.mcp.json` write is idempotent + merge-preserving and honors `--dry-run`. | test | Pre-seed `.mcp.json` with a co-resident server; run `init` twice; assert `unimatrix` not duplicated, co-resident preserved; `--dry-run` → no write; malformed existing `.mcp.json` → throws. **[no-cloud]** | PENDING |
| AC-08 | Bearer token written **only** to the out-of-tree store (`~/.unimatrix/<projectHash>/remote.json`, mode 0600, keyed by `projectHash`), **not** to any path in the repo tree; `git status`/`git add -A` surfaces no token-bearing file; no `unimatrix.remote` credential in `.claude/settings.local.json`. | test + file-check + shell | Run remote `init`; assert store file exists out-of-tree at mode 0600 with the token; `git status --porcelain` / `git add -A` dry-run lists no token-bearing path; grep `.claude/settings.local.json` for the token → absent. **[no-cloud]** | PENDING |
| AC-08b | Single per-`projectHash` store: two projects → two distinct `~/.unimatrix/<projectHash>/remote.json` files; re-`init` for a project updates its entry idempotently without clobbering others. | test | Run `init` for project A then B; assert two distinct hash-dir files; re-run `init` for A; assert A updated, B untouched. **[no-cloud]** | PENDING |
| AC-08c | Both consumers resolve from the new store: hook/observe `resolve()` loads token + `observe_url` (+ `fp`/timeouts) and POSTs to `observe_url`; bridge loads `mcp_url`/token/`fp`. Relocation breaks neither. | test | Seed the store; assert hook file-mode `resolve()` returns token + `observe_url` + **populated `pinnedFp`** (reconciled-mismatch regression, FR-23); assert bridge resolves `mcp_url`/token/`fp`. **[no-cloud]** | PENDING |
| AC-08d | File-mode remote observe **actually runs over pinned HTTPS** post-fix (current-break fix). With the store populated (incl. `fingerprint`), file-mode resolves, sets `pinnedFp`, and POSTs to `observe_url` over a pinned HTTPS connection — **no** UDS fall-through, **not** unpinned. **Formal wire test for the file-mode remote-observe error boundary (ARCHITECTURE §9 cross-refs AC-08d here).** | test | Stand up a local pinned `https.createServer` (self-signed, known leaf fp); seed store with its `observe_url`/`fingerprint`/token; drive a hook event through file-mode `resolve()`; assert POST lands on the HTTPS server (not UDS), connection pinned (good-pin delivered; wrong-pin fail-open exit 0, no token on wire), `pinnedFp` populated. **Field-presence is necessary but NOT sufficient — prove wire behavior (R-06).** **[no-cloud]** | PENDING |
| AC-09 | Token never appears in `printSummary()`, stdout, stderr, `.mcp.json`, or any other remote-path log; read by the bridge from the store at spawn time, never on the command line or in `.mcp.json`. | test + grep | Capture all output surfaces during remote `init` and a bridge run (incl. pin-mismatch path); assert the token string appears in none; assert `.mcp.json` has no token/`mcp_url`/`fp` — only `command`/`args:[<bridge>,<projectHash>]`/`env:{}`. **[stub/local] + [no-cloud]** | PENDING |
| AC-10 | Legacy `--remote`/`--token` path is **explicitly unsupported** for cloud MCP: no bridge wired, and a **loud, deterministic** message states cloud MCP requires a `v:2` bundle (not a silent skip). | test | Run `init --remote/--token`; assert `.mcp.json` has no `unimatrix` MCP entry; assert the **exact** unsupported-message text and the command exit behavior (wording + exit testable, SR-06); assert the legacy observe path still works. **[no-cloud]** | PENDING |
| AC-11 | Scope B lands independently of Scope A with no #774 dependency — validatable without a reachable cloud (store written on `init`; nothing token-bearing in the repo tree; both consumers resolve from the store). | test | Run the full Scope B test set with no cloud reachable and the bridge module un-spawned; all pass. Coverage report states Scope B is mergeable independently of Scope A. **[no-cloud]** | PENDING |
| AC-12 | Bridge sends a **stable** `Mcp-Session-Id` and `clientInfo.name` for a given attached project — same identity across bridge restarts/sessions, derived deterministically (not per-process random). Server keys audit attribution on these (vnc-014/#4708). | test | Drive multiple requests through one bridge session against a capturing stub; assert `Mcp-Session-Id` and `clientInfo.name` are byte-identical across all post-`initialize` requests (server-minted id replayed verbatim; `clientInfo.name` constant); two successive spawns for the same project present the same `clientInfo.name`; distinct projects → distinct identity (R-17). Validated live first via the session-id handshake checklist item — ARCHITECTURE §3.3 labels this handshake validation a **DELIVERY CHECKPOINT** (live-validation checklist item #1). **[stub/local] + live** | PENDING |
| AC-13 | `unimatrix mcp-bridge` subcommand is routed to the JS bridge in `bin/unimatrix.js` and does **not** `execFileSync` the Rust binary — invoking it on a host with no Rust binary runs the bridge rather than throwing (OQ-4). | test | Invoke `unimatrix mcp-bridge <projectHash>` in an environment without the Rust platform binary; assert it runs the JS bridge (no missing-binary throw); assert `bin/unimatrix.js` early-returns to JS before the Rust `execFileSync` block. **[no-cloud]** | PENDING |
