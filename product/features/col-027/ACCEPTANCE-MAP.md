# col-027 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `PostToolUseFailure` is registered in `.claude/settings.json` with the same command pattern as `PreToolUse` and `PostToolUse` | grep | `grep -A3 '"PostToolUseFailure"' .claude/settings.json` — assert key present with `matcher: "*"` and command containing `unimatrix hook PostToolUseFailure`; assert command path format matches `PreToolUse`/`PostToolUse` entries | PASS |
| AC-02 | `hook_type::POSTTOOLUSEFAILURE` constant exists in `unimatrix-core/src/observation.rs` with value `"PostToolUseFailure"` | test | Unit test in `unimatrix-core`: `assert_eq!(hook_type::POSTTOOLUSEFAILURE, "PostToolUseFailure")` | PASS |
| AC-03 | A failed tool call that fires `PostToolUseFailure` produces a stored observation record with `event_type = "PostToolUseFailure"`, the correct `tool` name, and a non-empty `response_snippet` containing the error content | test | Unit test in `listener.rs`: construct `ImplantEvent` with `event_type = "PostToolUseFailure"` and `payload["error"] = "some error message"`, call `extract_observation_fields()`; assert `obs.hook == "PostToolUseFailure"`, `obs.tool.is_some()`, `obs.response_snippet == Some("some error message")`, `obs.response_size == None` | PASS |
| AC-04 | The stored `hook` column value for a `PostToolUseFailure` event is `"PostToolUseFailure"` (not normalized to `"PostToolUse"`) | test | Same unit test as AC-03; add explicit assertion `assert_eq!(obs.hook, "PostToolUseFailure")` — verify the constant `hook_type::POSTTOOLUSEFAILURE` is used, not `hook_type::POSTTOOLUSE` | PASS |
| AC-05 | `PermissionRetriesRule` no longer fires for sessions where tool failures (Pre with corresponding `PostToolUseFailure`) are the sole source of Pre-Post imbalance | test | Unit test in `friction.rs`: construct 5 `make_pre` + 0 `make_post` + 5 `make_failure` records for the same tool; call `PermissionRetriesRule::detect()`; assert `findings.is_empty()`. [Coupled with AC-07: both must pass together] | PASS |
| AC-06 | `PermissionRetriesRule` still fires correctly when genuine Pre-with-no-terminal imbalance exceeds threshold; all existing tests pass unchanged | test | (a) All pre-existing `PermissionRetriesRule` unit tests pass without modification. (b) New test: 5 `make_pre` + 2 `make_post` + 0 `make_failure`; assert one finding fires with `measured == 3.0`. [Coupled with AC-07] | PASS |
| AC-07 | `permission_friction_events` metric is computed by subtracting both `PostToolUse` and `PostToolUseFailure` terminal records from `pre_count` per tool | test | Unit test in `metrics.rs`: construct 4 `PreToolUse` + 2 `PostToolUse` + 2 `PostToolUseFailure` for the same tool; call `compute_universal()`; assert `permission_friction_events == 0`. Also assert value is never negative when `failure_count > pre_count` (saturating_sub). [Coupled with AC-05/AC-06: same-commit delivery required] | PASS |
| AC-08 | A new `ToolFailureRule` detection rule fires a `HotspotFinding` when a single tool accumulates more than 3 `PostToolUseFailure` records | test | Unit test in `friction.rs`: construct 4 `make_failure` records for tool `"Bash"` (source_domain `"claude-code"`); call `ToolFailureRule::detect()`; assert exactly 1 finding, `rule_name == "tool_failure_hotspot"`, `measured == 4.0`, `threshold == 3.0`, `claim == "Tool 'Bash' failed 4 times"` | PASS |
| AC-09 | `ToolFailureRule` produces no finding when all tools have 3 or fewer failure records | test | Unit test in `friction.rs`: construct exactly 3 `make_failure` records for tool `"Read"` (source_domain `"claude-code"`); call `ToolFailureRule::detect()`; assert `findings.is_empty()` (boundary: 3 is at-threshold, threshold fires at strictly greater than 3) | PASS |
| AC-10 | Unit tests exist for all changed/new detection rule logic, following the `make_pre` / `make_post` test helper pattern established in `friction.rs` | grep | Inspect `friction.rs` test module: `grep -n 'fn make_failure' crates/unimatrix-observe/src/detection/friction.rs` — assert helper exists; verify AC-05, AC-06, AC-08, AC-09 tests use `make_failure` not inline struct construction | PASS |
| AC-11 | `build_request()` in `hook.rs` handles `"PostToolUseFailure"` with an explicit match arm (not falling through to the `_` wildcard) | test | (a) Code inspection: `grep -n '"PostToolUseFailure"' crates/unimatrix-server/src/uds/hook.rs` — assert a match arm exists. (b) Unit test: call `build_request("PostToolUseFailure", mock_input)` with `extra["tool_name"] = "Bash"` and `extra["error"] = "permission denied"`; assert the returned `HookRequest` is `RecordEvent` with `event_type == "PostToolUseFailure"` and `tool_name` propagated | PASS |
| AC-12 | The hook binary exits 0 for a `PostToolUseFailure` event (hook never fails, per FR-03.7) | shell | (a) Unit tests for `build_request("PostToolUseFailure", ...)` with missing/malformed payload fields assert no panic and a valid `RecordEvent` is returned. (b) Integration: `echo '{}' \| unimatrix hook PostToolUseFailure; echo $?` — assert output is `0`. (c) Integration: `echo 'not-json' \| unimatrix hook PostToolUseFailure; echo $?` — assert output is `0` | PASS |
