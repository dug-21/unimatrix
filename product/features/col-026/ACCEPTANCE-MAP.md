# col-026 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | Markdown report header reads `# Unimatrix Cycle Review — {feature_cycle}` (not `# Retrospective:`) | test | `test_header_rebrand` in `retrospective.rs`: assert rendered output starts with `# Unimatrix Cycle Review —` and does not contain `# Retrospective:` | PENDING |
| AC-02 | Header shows `Goal: {goal_text}` when a goal is stored in `cycle_events`; Goal line entirely absent (no blank line) when no goal is recorded | test | `test_header_goal_present` and `test_header_goal_absent` in `retrospective.rs`: verify Goal line presence/absence in rendered markdown | PENDING |
| AC-03 | Header shows `Cycle type: {type}` inferred from goal keywords when goal is present; shows `Cycle type: Unknown` when goal is absent | test | `test_cycle_type_classification` in `retrospective.rs`: exercise all 5 keyword groups (Design/Delivery/Bugfix/Refactor/Unknown) plus absent-goal path | PENDING |
| AC-04 | Header shows correct `Attribution:` label for each of the three paths: `cycle_events-first (primary)`, `sessions.feature_cycle (legacy)`, `content-scan (fallback)` | test | `test_attribution_path_labels` in `tools.rs` handler integration tests: verify each path label assigned for each branch of the three-path conditional | PENDING |
| AC-05 | When no `cycle_stop` event exists in `cycle_events`, report header includes `Status: IN PROGRESS` | test | `test_is_in_progress_three_states`: events=[], events with cycle_start only, events with cycle_start+cycle_stop — verify formatter output for each state; verify `Some(true)` renders `IN PROGRESS`, `Some(false)` and `None` omit Status line | PENDING |
| AC-06 | When `cycle_events` records exist, report contains Phase Timeline table with one row per phase showing: phase name, duration, pass count (>1 = rework), record count, agents spawned, knowledge served + stored, gate outcome | test | `test_phase_timeline_table` in `retrospective.rs`: seed known `cycle_events` and `ObservationRecord` fixtures, assert table rows match expected values for each column | PENDING |
| AC-07 | Phase Timeline table includes a rework annotation below the table for any phase with `pass_count > 1`, showing pass-level duration and record count | test | `test_phase_timeline_rework_annotation`: provide events where the same phase name appears twice; assert footnote line containing `Rework:`, `pass 1`, and per-pass duration/records | PENDING |
| AC-08 | Each finding in Findings section includes a `phase: {phase_name}` annotation when `cycle_events` data is available | test | `test_finding_phase_annotation` in `retrospective.rs`: provide PhaseStats with hotspot_ids populated; assert each finding header contains `— phase:` string | PENDING |
| AC-09 | Per-finding evidence rendered as relative-time burst notation `Timeline: +0m(N) +12m(N) ...` with `Peak:` line; raw `ts=` epoch values absent | test | `test_burst_notation_rendering` in `retrospective.rs`: assert output contains `Timeline:` and `Peak:` lines, does not contain `ts=` | PENDING |
| AC-10 | "What Went Well" section appears when at least one metric shows a non-outlier favorable signal; section omitted entirely when no favorable signals exist | test | `test_what_went_well_present` and `test_what_went_well_absent`: supply `baseline_comparison` fixtures with known favorable/unfavorable metrics; verify section presence/absence | PENDING |
| AC-11 | Recommendations section appears immediately after the header block, before Findings | test | `test_section_order` in `retrospective.rs`: render a full report with all sections populated; scan rendered string for all section headers in order: Recommendations before Phase Timeline before What Went Well before Sessions before Findings | PENDING |
| AC-12 | Knowledge Reuse section shows: total entries served, total stored, cross-feature count, intra-cycle count, by-category breakdown, top 3–5 cross-feature entries; `category_gaps` not rendered; `cross_session_count` not rendered | test | `test_knowledge_reuse_section` in `retrospective.rs`: supply `FeatureKnowledgeReuse` with all new fields populated; assert rendered output contains expected field labels and values, does not contain `category_gaps` or `cross_session_count` | PENDING |
| AC-13 | No finding or claim string in markdown output contains "threshold" paired with a numeric value; no `compile_cycles` finding or recommendation contains "allowlist" | test | `test_no_threshold_language` and `test_no_allowlist_in_compile_cycles`: render findings from each of the 9 enumerated detection sites; assert no rendered output matches `threshold[\s:]+[\d.]+`; assert no output contains "allowlist" for compile_cycles | PENDING |
| AC-14 | Session profile table includes tool distribution column (`NR NE NW NS`) and Agents column listing agent types spawned | test | `test_session_table_enhancement` in `retrospective.rs`: assert session table headers include tool distribution and Agents columns; assert row values render correctly for known fixture data | PENDING |
| AC-15 | Session profile section includes "Top file zones:" line showing top 3–5 directory paths by touch count | test | `test_top_file_zones` in `retrospective.rs`: supply session summaries with known file touch counts; assert rendered section contains `Top file zones:` line with expected paths | PENDING |
| AC-16 | JSON format response includes new `goal`, `cycle_type`, `attribution_path`, `is_in_progress`, `phase_stats` fields on `RetrospectiveReport` | test | `test_json_format_new_fields`: serialize `RetrospectiveReport` with all new fields set to `Some`; deserialize; assert all fields present with correct values. Serialize with all `None`; assert keys absent (not null) | PENDING |
| AC-17 | All existing `context_cycle_review` tests pass without modification; no test file deleted or renamed | test | `cargo test -p unimatrix-server -- context_cycle_review`: zero failures on pre-existing tests; verify file list unchanged via `git status` | PENDING |
| AC-18 | `FeatureKnowledgeReuse` struct change is backward-compatible: JSON deserializers using `#[serde(default)]` on new fields continue to work | test | `test_knowledge_reuse_serde_backward_compat`: deserialize JSON payload that lacks all four new fields; assert struct fields default to `0` / empty vec without error | PENDING |
| AC-19 | `compile_cycles` recommendation text describes iterative compilation errors with no mention of allowlists or permission prompts; `compile_cycles` and `permission_friction_events` recommendation templates are confirmed independent | test | `test_recommendation_compile_cycles_above_threshold` (updated assertion): assert recommendation text contains "batch" or "iterative" and does not contain "allowlist"; `test_permission_friction_recommendation_independence`: verify `permission_friction_events` recommendation template does not reference compile cycles or allowlists | PENDING |
