# col-031 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `PhaseFreqTable::new()` returns `use_fallback = true`, empty table. | test | Unit test asserting `use_fallback == true` and `table.is_empty()`. | PENDING |
| AC-02 | `PhaseFreqTable::rebuild(store, lookback_days)` queries `query_log` rows where `phase IS NOT NULL`, `result_entry_ids IS NOT NULL`, and `ts > strftime('%s', 'now') - lookback_days * 86400`. Result is `HashMap<(String, String), Vec<(u64, f32)>>` keyed by `(phase, category)`, each Vec sorted descending by rank score. | test | Covered by AC-08 and AC-14 integration/unit tests; SQL form inspected in code review. | PENDING |
| AC-03 | `PhaseFreqTable::new_handle()` returns `Arc<RwLock<PhaseFreqTable>>` in cold-start state. All lock acquisitions use `.unwrap_or_else(|e| e.into_inner())`. | test | Code review confirms no bare `.unwrap()` on lock acquisitions; unit test asserts handle wraps cold-start state. | PENDING |
| AC-04 | Background tick calls `PhaseFreqTable::rebuild` once per cycle. On success, handle swapped under write lock. On failure, existing state retained and `tracing::error!` emitted. | test | Integration test injecting a failing store confirms: (a) success path swaps handle; (b) error path retains pre-tick table (non-cold-start) and emits error. | PENDING |
| AC-05 | `ServiceLayer` creates `PhaseFreqTableHandle` and threads it to `SearchService` and background tick via `Arc::clone`. Handle is a required non-optional constructor parameter. | test | Code review confirms no `Option<PhaseFreqTableHandle>` at any site. `grep crates/unimatrix-server/ -r "SearchService::new"` output reviewed to confirm all sites receive the handle. | PENDING |
| AC-06 | In fused scoring loop, `FusedScoreInputs.phase_explicit_norm` is `0.0` when `current_phase = None` or `use_fallback = true`; otherwise computed from `phase_affinity_score`. The `use_fallback` check fires before `phase_affinity_score` is called. Lock released before scoring loop begins. | test | AC-11 unit tests (Tests 1 and 2); code review confirms lock-release ordering before loop iteration. | PENDING |
| AC-07 | `phase_affinity_score(entry_id: u64, entry_category: &str, phase: &str) -> f32` is public. Returns `f32 ∈ [0.0, 1.0]`. Returns `1.0` when `use_fallback = true`, phase absent from table, or entry absent from bucket. | test | Unit tests covering each `1.0` return path (cold-start, absent-phase, absent-entry), each asserting `== 1.0f32`. | PENDING |
| AC-08 | Integration test using `TestDb`: seed `query_log` with 10 rows, `phase="delivery"`, `result_entry_ids=[42]`, `ts` within lookback window. After `rebuild`, assert `phase_affinity_score(42, "decision", "delivery") > 0.0` and `phase_affinity_score(99, "decision", "delivery") == 1.0`. | test | `cargo test` — named integration test passes in CI; confirms `CAST(je.value AS INTEGER)` returns correct `entry_id` values and absent-entry returns `1.0`. | PENDING |
| AC-09 | `InferenceConfig.w_phase_explicit` default is `0.05`. Config TOML with no `w_phase_explicit` key deserializes to `0.05`. Existing test `test_inference_config_default_phase_weights` updated to assert `0.05`. | test | Updated `test_inference_config_default_phase_weights` passes; deserialization test with empty TOML asserts `0.05`. | PENDING |
| AC-10 | `InferenceConfig.query_log_lookback_days` exists with type `u32` and default `30`. Config TOML with no `query_log_lookback_days` key deserializes to `30`. | test | Unit test asserting default deserialization to `30u32`. | PENDING |
| AC-11 | Cold-start invariants — three separate unit tests: (1) `current_phase = None`, populated table → `phase_explicit_norm = 0.0`, scores bit-for-bit identical to pre-col-031; (2) `current_phase = Some(phase)`, `use_fallback = true` → `phase_explicit_norm = 0.0` via `use_fallback` guard, guard fires before `phase_affinity_score` is called; (3) `phase_affinity_score` called directly on `use_fallback = true` table → returns `1.0` (PPR neutral multiplier contract). | test | Three named unit tests, all passing; Test 2 confirms guard fires before method call (no mock call observed); Test 3 asserts `== 1.0f32`. | PENDING |
| AC-12 | Eval regression gate (non-vacuous; requires AC-16 complete): MRR ≥ 0.35, CC@5 ≥ 0.2659, ICD ≥ 0.5340 (col-030 baselines). AC-12 must not be declared PASS without AC-16 complete and verified. | shell | `cargo run --bin eval -- --scenarios <path>` with AC-16 fix applied; scenario output file inspected to confirm non-null `current_phase` values; reported metrics compared against thresholds. Gate 3b must reject submission lacking this evidence. | PENDING |
| AC-13 | Rank-based normalization formula: within each `(phase, category)` bucket, `score = 1.0 - ((rank - 1) / N)` (1-indexed, N = bucket size). Top entry (rank 1) → `1.0`. Last entry (rank N) → `(N-1)/N`. Single-entry bucket (N=1) → `1.0`. Absent entry → `1.0`. | test | AC-14 normalization test with exact score assertions; single-entry bucket unit test asserting `1.0`; code review confirms correct 1-indexed formula (not `1 - rank/N`). | PENDING |
| AC-14 | Unit test using existing test fixtures: `PhaseFreqTable::rebuild` from synthetic `query_log` produces correct `(phase, category)` keying, correct descending rank order, and correct normalization values. | test | Named unit test seeding frequencies 10, 5, 1 in a 3-entry bucket; asserts rank-1 → `1.0`, rank-2 → `0.666...`, rank-3 → `0.333...`; asserts descending sort order. | PENDING |
| AC-15 | `services/phase_freq_table.rs` ≤ 500 lines. SQL aggregation in `unimatrix-store/src/query_log.rs`. | shell | `wc -l crates/unimatrix-server/src/services/phase_freq_table.rs` reports ≤ 500 at merge time. | PENDING |
| AC-16 | `eval/scenarios/replay.rs` forwards `current_phase` from scenario data into `ServiceSearchParams`. No other change to `replay.rs` is in scope. (`extract.rs` and `output.rs` already handle phase — no changes needed there.) | test | Eval scenario output file contains non-null `current_phase` values for rows with `query_log.phase` set. Code diff limited to `replay.rs` propagation and `ServiceSearchParams` field assignment. | PENDING |
| AC-17 | `phase_affinity_score` carries a doc comment naming both callers and their respective cold-start contracts: PPR (#398) calls directly and receives `1.0` on cold-start; fused scoring must guard on `use_fallback` before calling and returns `0.0` via the guard path, not via this method. | manual | Code review confirms doc comment present, naming both callers explicitly with their cold-start semantics. | PENDING |
