# col-031 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `PhaseFreqTable::new()` returns `use_fallback = true` and an empty `table`. `phase_affinity_score(any_entry_id, any_category, any_phase)` returns `1.0` when `use_fallback = true`. | test | Unit test: instantiate `PhaseFreqTable::new()`, assert `use_fallback == true`, assert `table.is_empty()`, assert `phase_affinity_score(0, "decision", "delivery") == 1.0`. | PENDING |
| AC-02 | `PhaseFreqTable::rebuild(store, retention_cycles)` queries `query_log` rows where `phase IS NOT NULL` and feature_cycle is within the last `retention_cycles` completed cycles or is an open cycle. Result is `HashMap<(String, String), Vec<(u64, f32)>>` keyed by `(phase, category)`, each Vec sorted descending by frequency score. | test | Integration test (see AC-08 and AC-14): synthetic `query_log` with known rows; assert correct keying, correct descending ordering, `use_fallback == false` on returned struct. | PENDING |
| AC-03 | `PhaseFreqTable::new_handle()` returns `Arc<RwLock<PhaseFreqTable>>` initialized to cold-start. All lock acquisitions use `.unwrap_or_else(|e| e.into_inner())` for poison recovery. | test | Unit test: call `new_handle()`, assert handle contains cold-start state (`use_fallback == true`). Code review confirms poison recovery pattern on all lock sites in `phase_freq_table.rs`. | PENDING |
| AC-04 | Background tick calls `PhaseFreqTable::rebuild` once per tick cycle. On success, handle is swapped under write lock. On failure, existing state is retained and error is logged via `tracing::error!`. | test | Code review confirms tick integration. Error path test: inject `StoreError` into rebuild, assert handle retains pre-failure state, assert `tracing::error!` was emitted. `cargo test --workspace` passes. | PENDING |
| AC-05 | `ServiceLayer` creates `PhaseFreqTableHandle` and threads it to both `SearchService` and the background tick via `Arc::clone`. | test | Code review confirms handle initialization in `ServiceLayer::with_rate_config()` and `Arc::clone` into both consumers. `cargo test --workspace` passes. | PENDING |
| AC-06 | In the fused scoring loop, `FusedScoreInputs.phase_explicit_norm` is computed from `PhaseFreqTable::phase_affinity_score` when `params.current_phase` is `Some`. When `params.current_phase` is `None`, `phase_explicit_norm = 0.0`. | test | Code review confirms replacement of hardcoded `phase_explicit_norm: 0.0` in `search.rs`. Unit test with `current_phase = None`: assert `phase_explicit_norm == 0.0` for all candidates. Unit test with `current_phase = Some("delivery")` and populated table: assert `phase_explicit_norm != 0.0` for a known top-ranked entry. | PENDING |
| AC-07 | `PhaseFreqTable::phase_affinity_score` is a public method with signature `(entry_id: u64, entry_category: &str, phase: &str) -> f32`. Returns `f32 ∈ [0.0, 1.0]` for all inputs. Returns `1.0` when cold-start, phase absent, or entry absent in the `(phase, category)` bucket. | test | Unit tests per FR-14: cold-start case (any input → 1.0), absent-phase case (→ 1.0), absent-entry case (→ 1.0), single-entry bucket (→ 1.0), multi-entry bucket (rank-based scores). Code review confirms `pub` visibility and return type. | PENDING |
| AC-08 | Integration test: synthetic `query_log` with 10 rows for `phase="delivery"`, `category="decision"`, all pointing to `entry_id=42`. After `PhaseFreqTable::rebuild`: `phase_affinity_score(42, "decision", "delivery") > 0.0` and `phase_affinity_score(99, "decision", "delivery") == 1.0`. | test | Integration test using existing test fixtures against real SQLite `TestDb` (not a mock). Assertions: `phase_affinity_score(42, "decision", "delivery") > 0.0` and `== 1.0` for absent entry 99. | PENDING |
| AC-09 | `InferenceConfig.w_phase_explicit` default is `0.05`. Config TOML with no `w_phase_explicit` key deserializes to `0.05`. Existing test `test_inference_config_default_phase_weights` updated to assert `0.05`. | test | Updated test `test_inference_config_default_phase_weights` passes with assertion `w_phase_explicit == 0.05`. TOML round-trip test: deserialize empty TOML section, assert `w_phase_explicit == 0.05`. | PENDING |
| AC-10 | `InferenceConfig.query_log_retention_cycles` field exists with default `20`. Config TOML with no `query_log_retention_cycles` key deserializes to `20`. | test | Unit test: deserialize empty TOML `[inference]` section, assert `query_log_retention_cycles == 20`. | PENDING |
| AC-11 | Cold-start ranking-preservation invariant: when `params.current_phase` is `None`, all `phase_explicit_norm` values in the scoring loop are `0.0` and `compute_fused_score` output is numerically identical to pre-col-031 behavior. When `current_phase = Some(...)` and table is cold-start, scores shift uniformly (ranking-preserving, not bit-for-bit identical). | test | Unit test — `None` path: run scoring with `current_phase = None`, assert `phase_explicit_norm == 0.0` for all candidates, assert final scores match pre-col-031 reference values. Unit test — cold-start + `Some("delivery")` path: assert `phase_affinity_score` returns `1.0` for all entries, assert relative ordering of fused scores is unchanged from pre-col-031. | PENDING |
| AC-12 | Eval regression gate (requires AC-16 complete): MRR ≥ 0.35 (floor), CC@5 ≥ 0.2659 (col-030 baseline), ICD ≥ 0.5340 (col-030 baseline). Gate is non-vacuous only when AC-16 is confirmed complete and eval scenarios carry `current_phase` values. | manual | Pre-check: confirm generated scenario JSONL contains at least one non-null `current_phase` value (if all null, AC-16 is incomplete; do NOT declare AC-12 passing). Run eval harness; assert `render_distribution_gate` report shows MRR ≥ 0.35, CC@5 ≥ 0.2659, ICD ≥ 0.5340. | PENDING |
| AC-13 | `phase_affinity_score` normalization: rank-based formula `score = 1.0 - (rank / N)`, 0-indexed within `(phase, category)` bucket. Top entry (rank 0) → `1.0`. Bottom entry (rank N-1) → `1/N`. Absent entry → `1.0`. | test | Unit test with a 5-entry bucket: assert rank-0 entry returns `1.0`, rank-1 returns `0.8`, rank-2 returns `0.6`, rank-3 returns `0.4`, rank-4 returns `0.2`. Assert entry not in bucket returns `1.0`. Test with non-monotonic entry IDs to expose any mistaken binary-search assumption. | PENDING |
| AC-14 | Unit test: frequency table rebuild from a synthetic `query_log` (via test support fixtures, not isolated scaffolding) produces correct `(phase, category)` keying, correct entry ranking (most-frequent entry at index 0), correct rank-based normalization. | test | Test seeded with known frequency counts across multiple `(phase, category)` buckets; assertions on `table` structure, `phase_affinity_score` return values, and bucket ordering. | PENDING |
| AC-15 | `services/phase_freq_table.rs` does not exceed 500 lines. The store query method is in `unimatrix-store` at the appropriate query layer, not in the service module. | shell | `wc -l crates/unimatrix-server/src/services/phase_freq_table.rs` outputs ≤ 500. Code review confirms SQL query in `unimatrix-store/src/query_log.rs`, not in `phase_freq_table.rs`. | PENDING |
| AC-16 | `eval/scenarios/extract.rs` emits `current_phase` in extracted scenarios: `query_log.phase` is selected and populated in scenario output. AC-12 regression gate is non-vacuous as a result. | grep | `grep -n 'current_phase' crates/unimatrix-server/src/eval/scenarios/extract.rs` returns at least one match. Inspect generated scenario JSONL: confirm `current_phase` field is present and non-null for at least one post-col-028 row. Prerequisite to AC-12 being declared passing. | PENDING |
