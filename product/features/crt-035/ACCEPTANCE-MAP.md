# crt-035 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `run_co_access_promotion_tick` writes BOTH `(entry_id_a, entry_id_b, 'CoAccess')` AND `(entry_id_b, entry_id_a, 'CoAccess')` for each qualifying pair in a single tick run | test | T-NEW-01 `test_bidirectional_edges_inserted_same_weight` in `co_access_promotion_tick_tests.rs`: seed one pair, run tick, assert `count_co_access_edges == 2`, both `fetch_co_access_edge(a,b)` and `fetch_co_access_edge(b,a)` return Some | PENDING |
| AC-02 | Both forward and reverse edges carry the same normalized weight (`count / max_count`) from the same `co_access` row | test | T-NEW-01: assert `edge_ab.weight == edge_ba.weight` within 1e-9 | PENDING |
| AC-03 | Both forward and reverse edges are subject to weight update logic: delta > 0.1 triggers update; delta == 0.1 does not | test | T-NEW-02 `test_bidirectional_both_directions_updated_when_drift_exceeds_delta`: pre-seed both at 0.5, run tick with `new_weight=1.0`; assert both updated. Add boundary case: pre-seed at 0.6, `new_weight=0.5` (delta exactly 0.1); assert neither updated | PENDING |
| AC-04 | When one direction already exists, `INSERT OR IGNORE` skips it silently; no duplicate rows created | test | Pre-seed forward edge; run tick; assert `count_co_access_edges == 2` (not 3+); assert `SELECT COUNT(*) ... WHERE source_id=a AND target_id=b AND relation_type='CoAccess' == 1` | PENDING |
| AC-05 | `tracing::info!` summary includes fields `promoted_pairs`, `edges_inserted`, `edges_updated`; for a fully-fresh single-pair tick: `promoted_pairs=1, edges_inserted=2, edges_updated=0` | test | T-NEW-03 `test_log_format_promoted_pairs_and_edges_inserted` with `#[tracing_test::traced_test]`: seed two pairs, run tick, assert log fields `promoted_pairs: 2, edges_inserted: 4, edges_updated: 0` | PENDING |
| AC-06 | v18→v19 migration back-fills the reverse edge for every `GRAPH_EDGES` row where `relation_type = 'CoAccess'` AND `source = 'co_access'` AND no reverse already exists; covers both `created_by = 'bootstrap'` and `created_by = 'tick'` | test | MIG-U-03: build v18 DB with bootstrap-era forward edges; open; assert each forward edge has a corresponding reverse row. MIG-U-04: same for tick-era (`created_by='tick'`) forward edges. Both in `tests/migration_v18_to_v19.rs` | PENDING |
| AC-07 | Back-fill uses `INSERT OR IGNORE` and is idempotent — re-running the migration produces no duplicates and no errors | test | MIG-U-06 in `tests/migration_v18_to_v19.rs`: open once (back-fill runs), close, open again (migration skipped by version guard), assert edge count is unchanged | PENDING |
| AC-08 | v18→v19 migration does NOT modify `Supersedes`, `Contradicts`, or `Supports` edges | test | MIG-U-05 in `tests/migration_v18_to_v19.rs`: pre-seed non-CoAccess edges (`relation_type` = 'Supersedes', 'Contradicts', 'Supports'); after migration assert they are unmodified in count and content | PENDING |
| AC-09 | `CURRENT_SCHEMA_VERSION` is 19 in `migration.rs` | test | MIG-U-01 in `tests/migration_v18_to_v19.rs`: `assert_eq!(unimatrix_store::migration::CURRENT_SCHEMA_VERSION, 19)` | PENDING |
| AC-10 | `tests/migration_v18_to_v19.rs` covers all 7 MIG-U cases | test | `cargo test -p unimatrix-store --test migration_v18_to_v19` must show 7 passing tests: MIG-U-01 (version), MIG-U-02 (fresh DB), MIG-U-03 (bootstrap back-fill), MIG-U-04 (tick back-fill), MIG-U-05 (non-CoAccess untouched), MIG-U-06 (idempotency), MIG-U-07 (empty GRAPH_EDGES no-op) | PENDING |
| AC-11 | All tests in `co_access_promotion_tick_tests.rs` that assert one-directional behavior or odd edge counts are updated to reflect bidirectional semantics | grep | `grep -n "count_co_access_edges" crates/unimatrix-server/src/services/co_access_promotion_tick_tests.rs` — every asserted value must be even (0, 2, 4, 6, 10...); also `grep "no duplicate\|reverse edge must not" ...` must return no matches | PENDING |
| AC-12 | PPR seeding the higher-ID entry surfaces the lower-ID entry via the reverse CoAccess edge; test uses real SQLite-backed `SqlxStore`, not an in-memory fixture | test | `test_ppr_reverse_coaccess_edge_seeds_lower_id_entry` in `#[cfg(test)] mod tests` in `crates/unimatrix-server/src/services/typed_graph.rs`: open real `SqlxStore` (tempfile), insert two entries (A < B), insert `CoAccess` edge `(B→A)` into GRAPH_EDGES, call `TypedGraphState::rebuild()`, run PPR seeded at B, assert entry A has non-zero score | PENDING |
| AC-13 | Cycle detection behavior unchanged — no false-positive cycles from bidirectional CoAccess edges | test | `cargo test -p unimatrix-server -- test_build_typed_relation_graph_cycle_detection` and all existing cycle detection tests pass with no new failures | PENDING |
| AC-14 | Unimatrix entry #3830 (ADR-006) is updated to confirm: follow-up contract fulfilled by crt-035, bidirectional writes now default, v1 forward-only layout was intentional, back-fill bounded by `source = 'co_access'` | manual | `context_correct` call on entry #3830 at delivery time; verify the entry text reflects crt-035 delivery confirmation | PENDING |
