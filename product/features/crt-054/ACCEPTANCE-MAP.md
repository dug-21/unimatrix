# crt-054 Acceptance Criteria Map

**Date**: 2026-06-16 — regenerated after the four open questions were RESOLVED (rework).
**Source**: `specification/SPECIFICATION.md` §Acceptance Criteria (AC-01..AC-16). SCOPE.md defines the binding constraints; the spec's AC set is the canonical, testable enumeration and every constraint traces into it.
**Risk traceability**: `RISK-TEST-STRATEGY.md` (R-01..R-15). Each AC's anchor risks noted in Verification Detail.

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `compaction_events` table created with the exact contract columns/types (`id` PK, `session_id` TEXT NOT NULL, `compacted_at` INTEGER NOT NULL, `high_water` INTEGER NOT NULL DEFAULT 0) + index on `session_id`; present in fresh-create AND migration upgrade path. | test | Schema/migration test: fresh DB and upgraded-from-v28 DB both expose the table via `pragma_table_info`; index present; cascade-file existence asserted (#4484). (R-04) | PENDING |
| AC-01a | `compaction_events.compacted_at` is documented as Unix **SECONDS** in the DDL/migration (schema comment naming "Unix SECONDS") in both fresh-create and upgrade block. | grep | Inspect DDL/migration source: assert `compacted_at` carries an explicit "Unix SECONDS" comment in both paths. The gate-side `ts/1000` is crt-055's — out of scope here. (Q3 resolved; R-11) | PENDING |
| AC-02 | Exactly one row per compaction event at `handle_compact_payload`, `compacted_at` in seconds, `high_water` = buffer's `high_water` at compaction; no UPDATE/DELETE path. | test | Integration test: drive a compaction; assert one row with correct `session_id`, `compacted_at` (seconds, within tolerance of now) and `high_water`; a second compaction adds a second row (0..N). (R-11, R-13, R-14) | PENDING |
| AC-03 | Row written even when the session has **no** `feature_cycle` declared; carries no `feature_cycle` column and no content. | test | Integration test: compact an undeclared session; assert a session-keyed row is written; assert schema has no `feature_cycle`/content column. (R-08) | PENDING |
| AC-04 | The Surface A INSERT does not deadlock or stall against locks held at `handle_compact_payload`; the compaction ACK completes. | test + manual | Documented lock-ordering review in ARCHITECTURE/ADR-007 (manual) + concurrency test driving compaction under registry/session lock contention without deadlock/timeout. (Q1 resolved; R-03, R-09) | PENDING |
| AC-04a | A failed `compaction_events` INSERT emits a **named counter** (`compaction_events_insert_failed`), not a generic log line, and the compaction proceeds **non-blocking**. | test | Fault-injection test: force an INSERT failure; assert the named counter increments (not merely a log) and the compaction response still completes; assert no content is logged. (Q1 resolved; R-15) | PENDING |
| AC-05 | The fold accumulates `bytes_total`, `delta_count`, `class_counts` at `apply_delta` on the **registered** route. | test | Unit/integration test: apply deltas on a registered session; assert counters advance as specified. | PENDING |
| AC-06 | The fold accumulates on the **held** route: a representative TS-client cycle through drain→hold→re-adopt yields `bytes_total > 0`, `delta_count > 0` at review; a held-route miss fails red. | test | **Mandatory held-route regression guard** (integration, on cumulative crt-052/vnc-025 fixtures) with a negative-mutation check. A registered-only or unit-only test does NOT satisfy this (pattern #3624). (R-01, Critical) | PENDING |
| AC-07 | The fold counter stays accurate/readable until crt-055 reads `activity_snapshot()`; crt-054 does not zero/drop before the crt-052 purge; buffers zeroed only **after** the read. | test | Read-before-purge integration test: assert `activity_snapshot()` returns non-zero, then assert `purge_cycle_transcripts` zeroes the buffer (read provably first). (R-02, Critical) | PENDING |
| AC-08 | `activity_snapshot()` returns `Copy` `ActivitySnapshot { bytes_total: u64, delta_count: u32, class_counts: [u32; MAX_SIGNAL_CLASSES] }` with no content field; `Debug` prints only scalars; no `Display`. | test | Type/structural test + content-opacity test asserting no byte-bearing field and metadata-only `Debug` (mirrors `test_candidates_structurally_absent`). (R-05) | PENDING |
| AC-09 | One shared `RegexSet`/Aho-Corasick scan per delta (not one pass per pattern); a delta matching multiple classes increments multiple `class_counts`. | test | Unit test: a crafted delta matching both `error` and `refusal` increments `class_counts[0]` and `class_counts[1]`; scan invoked once per delta. | PENDING |
| AC-10 | `[transcript_signals]` parses with `#[serde(default)]`, ships the domain-neutral `error`/`refusal` default set only (no SDLC literals, no `reread`/`compaction` class), maps `error→0`, `refusal→1`. | test | Config test: default config yields exactly the two v1 classes at fixed indices; assert no SDLC literal patterns and no `reread`/`compaction` class. (Q2 resolved; R-10) | PENDING |
| AC-10a | The default `error`/`refusal` patterns are calibrated against real transcripts before locking (high-precision, anchored); counts documented as directional, not precise. | manual | Delivery-time calibration: a sample of real transcript deltas exercises each default pattern; precision/false-positive observations recorded in the delivery artifact; pattern set finalized before merge; doc review asserts counts surfaced as directional. (Q2 resolved; Coordination item 3) | PENDING |
| AC-11 | `MAX_SIGNAL_CLASSES == 16` exactly (matching crt-055); `validate()` rejects, loudly at load, a config exceeding the cap or with an invalid regex. | test | Constant assertion (`MAX_SIGNAL_CLASSES == 16`); negative config tests: a >16-class set and an unparseable regex each fail `validate()` with a clear error; no silent fallback. (Q2 resolved; R-10) | PENDING |
| AC-12 | For an undeclared/purged session whose fold died, crt-054 emits no fabricated zero; absence is distinguishable from a measured zero at the producer boundary. | test | Integration test: an undeclared session contributes no entry to the activity read set; assert its bytes do not appear and no zero is fabricated. (R-08) | PENDING |
| AC-13 | No crt-054 output depends on the retired `PreToolUse` event or any single hook-event presence; both outputs derive from the delta stream (B) or the compaction seam (A). | manual + test | Design/code review asserting neither surface reads `PreToolUse`/single-hook presence; covered transitively by AC-06's route coverage. (R-06) | PENDING |
| AC-14 | `activity_snapshot()` returns native `u64`/`u32` widths **cast-free on the producer side** — no `as i64`/narrowing cast in crt-054's code; the checked/saturating `→ i64` conversion is crt-055's at persist. | grep + test | Producer-side structural/grep test asserting no narrowing cast of `bytes_total`/`delta_count`/`class_counts`; value-level assertion a near-`u64::MAX` `bytes_total` round-trips un-narrowed. (Q3 resolved; R-07) | PENDING |
| AC-15 | crt-054 does NOT bump `SUMMARY_SCHEMA_VERSION`, does NOT ALTER `cycle_review_index`, and produces no token-named field or `reread`/`compaction` class. | grep | Negative/structural grep test that crt-054's diff touches neither `cycle_review_index` nor `SUMMARY_SCHEMA_VERSION` and introduces no `token_*` symbol nor `reread`/`compaction` class. (Q4 resolved; R-12) | PENDING |
| AC-16 (co-owned with crt-055) | Across the producer→consumer seam, the seconds-gate semantics hold: a PostToolUse read just **after** a compaction boundary counts as post-compaction, just **before** as pre-compaction, when crt-055 compares the normalized read `ts` against crt-054's `compaction_events.compacted_at` (seconds). | test | **Co-owned cross-gate integration test**: crt-054 produces rows in seconds; crt-055 owns the `ts/1000` normalization and the `read_ts_secs > compacted_at` test. Physical ownership of the test file resolved by the SM at the test-plan handoff (Coordination item 1); neither side lands its half as a unit-only test. (Q3 resolved; R-11) | PENDING |

## Coverage Notes

- **Every binding SCOPE constraint (1–8 in SPEC §Constraints) traces into an AC**: Constraint 1 → AC-08; Constraint 2 → AC-06/AC-13; Constraint 3 → AC-07; Constraint 4 → AC-03/AC-12; Constraint 5 → AC-04; Constraint 6 → AC-13; Constraint 7 → AC-15; Constraint 8 → AC-01.
- **The four resolved open questions are each test-anchored**: Q1 (lock/transaction shape + failure counter) → AC-04/AC-04a; Q2 (`MAX_SIGNAL_CLASSES = 16` + default catalog) → AC-10/AC-10a/AC-11; Q3 (`compacted_at` seconds + cast-free widths) → AC-01a/AC-14/AC-16; Q4 (schema-version ownership) → AC-01/AC-15.
- **Critical risks anchor the believable-zero family**: R-01 → AC-06 (held route), R-02 → AC-07 (read-before-purge), R-03 → AC-04 (lock graph), R-04 → AC-01 (schema version). AC-06 and AC-07 MUST be held-route, drain→hold→review integration tests; a registered-only or unit-only test does NOT satisfy them.
