# vnc-030 Acceptance Criteria Map

GH Issue: #699 · Source: product/features/vnc-030/SCOPE.md (AC-01..10) · FR/verification detail: SPECIFICATION.md (FR-01..29), RISK-TEST-STRATEGY.md (R-01..23) · Regenerated 2026-06-08 (rev2 — AC-10 added; AC-06 canary rescoped per ADR-006 rev2)

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | Cycle tracker `cycles/{session_key}.json` — created on intercepted `context_cycle(start)` post-validation, phase updated on `phase-end`, deleted on `stop`; never touched by SessionStart/SessionClose/Stop; survives crash + `--resume`; 7-day prune; atomic writes | test | `cycles.js` unit tests per lifecycle event (FR-01..05); multi-turn sim: cycle_start → 3×(Stop + RecordEvent), assert file byte-unchanged and stamp still attaches (R-02); crash+`--resume` sim asserts file persists and same session_key re-stamps (R-08); prune sweep removes only stale (>7d). `cd packages/unimatrix && npm test -- cycles` | PENDING |
| AC-02 | Additive `ImplantEvent.cycle_stamp: Option<CycleStampPayload{topic, phase}>` — serde field + 7th ts-rs binding; all pre-existing wire fixtures pass unmodified; old/new client+server combos tolerated | test | `cargo test -p unimatrix-engine` serde trio (None-absent/Some-present/null-tolerant) + tolerance both directions (FR-11/12); fixture suite byte-unchanged (no `deny_unknown_fields`); ts-rs export sentinel recounted to 7; `git diff --exit-code crates/unimatrix-engine/bindings/` clean except additive `ImplantEvent.ts` + new `CycleStampPayload.ts` (R-16) | PENDING |
| AC-03 | Client topic extraction emits no `topic_signal` while a stamp file exists for the session; extraction unchanged when none does | test | `index.js` decoration unit: same prompt content with tracker file → frame carries `cycle_stamp`, no `topic_signal`; without → carries `topic_signal`, no stamp (FR-08). CYCLE_* frame keeps `topic_signal=topic` (R-05). `npm test -- index-decoration` | PENDING |
| AC-04 | Server precedence presence-gated stamp → marker → vote-on-NULL: stamped events attribute from stamp; `enrich_topic_signal` NULL-fill retained; declared wins over majority vote at session close AND `sweep_stale_sessions`; `FeatureSource::{Declared,Inferred}`; vote consulted only when no declared feature | test | `cargo test -p unimatrix-server`: integration — stamped event lands declared attribution regardless of registry state (FR-14); unit — sweep (session.rs:628) + close (listener.rs:1950-1978) inversion flips, declared beats contradicting vote (FR-16/17); enrich FeatureSource decision-tree per branch (FR-15, R-04); never-declare floor regression (FR-19). MARKER tier NOT implemented — deferred to #700 ("marker when present" is normative wording, not shipped code) | PENDING |
| AC-05 | Additive `observations.topic_source` column recording attribution source (`declared`/`extracted`/`registry-fill`/`vote`/NULL) per row | test | `cargo test -p unimatrix-store` migration idempotence (fresh / already-migrated / pre-migration v27→v28, FR-20); one integration case per value asserting the column matches the writing code path (FR-21); grep-audit `INSERT INTO observations` confirms both record-path sites (:3015,:3055) extended, non-record-path sites stay NULL (R-12). `CURRENT_SCHEMA_VERSION = 28` | PENDING |
| AC-06 | Root-session-id contract + **subagent-gated zero-tolerance** `stamp_miss` canary (rescoped, ADR-006 rev2): (a) subagent-context event with stdin session_id=S joins S and is stamped from `cycles/{S}.json`; (b) the canary increments **only** on a depth≥1 subagent-context event whose inherited root tracker is missing (inheritance drift); a depth-0 never-declare session with no tracker is **NOT** counted | test | Integration (parity layer): subagent event with root session_id=S joins S, stamped from `cycles/{S}.json` (FR-22a); an unknown-id depth-0 event produces an unstamped row and does NOT increment (FR-22b). Canary unit fixtures (FR-09, ADR-006 §5, R-19): depth-0 never-declare → no increment; depth≥1 subagent with inherited root tracker present → no increment; depth≥1 subagent carrying a non-inherited id while the root tracker exists → exactly one increment; depth>1 grandchild id with no tracker → lands in `stamp_miss`, not silent loss. Healthy single-declared-session-with-subagent fixture asserts `stamp_miss == 0`. No 0.20 threshold, no `fnf_record_send_count` denominator, no `anyOtherCycleFile` rule, no baseline measurement. Test docs + IMPLEMENTATION-BRIEF pin claude 2.1.167 (FR-10). `npm test -- state-canary index-decoration` | PENDING |
| AC-07 | Attribution accuracy on declared-protocol-session sample ≥ current heuristic baseline; heuristic fallback still attributes marker-less ad-hoc sessions | manual | Accuracy denominator = declared protocol sessions only (declaration = ground truth); fallback regression sample includes multiple never-declare shapes — ≥1 uni-zero, ≥1 research-spike, ≥1 ad-hoc (SR-06); before/after `topic_source` distribution comparison on live DB windowed on post-migration rows (R-20). Canary is **decoupled** from this baseline step (ADR-006 rev2) — no noise-baseline measurement is performed here | PENDING |
| AC-08 | Stamping works from a worktree checkout: `cycle_start` intercepted in a git worktree writes the tracker under the main-root hash; subsequent worktree events are stamped | test | `npm test` worktree regression over the F3 gitdir port: `.git` *file* pointing at main gitdir; cycle_start writes tracker under main-root hash via `walkToProjectRoot`/`resolveGitFile`; subsequent worktree event stamped (FR-23). Grep/assert no stamp path hashes raw cwd — all route through `config.resolve(cwd).stateDir` (C-11, R-15) | PENDING |
| AC-09 | All three protocols contain the restart re-declaration line: on re-entering a broken session, the leader's first action is to re-issue `context_cycle(type:"start", topic:"{feature-id}")` | grep | `grep -l 'context_cycle(type:"start"' .claude/protocols/uni/uni-design-protocol.md .claude/protocols/uni/uni-delivery-protocol.md .claude/protocols/uni/uni-bugfix-protocol.md` — line present in all three (FR-24) | PENDING |
| AC-10 | UDS-path stamp regression — the stamp decoration is proven byte-equivalent over the **merged vnc-027 UDS transport**, not just HTTP (vnc-027's post-merge obligation owed to #699) | test | Regression test (FR-29) at the seam `transport-uds.encodeFrame` (`transport-uds.js:55-62`): drive one stamped FNF `RecordEvent` (tracker present) through `index.js` `runFireAndForget` with `config.mode = "uds"`; assert the bytes from `transport-uds.encodeFrame` decode to a JSON payload containing `cycle_stamp`, byte-equivalent to the `transport-http.post` body for the same input (decoration is upstream of `selectTransport` at `index.js:410`); queued/replayed frame over UDS carries the same stamp (R-23). `npm test -- uds-stamp-regression` | PENDING |

## Cross-Feature / Seam Verification (not numbered ACs; gate-blocking per ADR-007)

| Item | Verification Method | Verification Detail | Status |
|------|---------------------|---------------------|--------|
| Post-rebase interception-seam survival (R-07, FR-28, ADR-007 §1) | test | Runs against the rebased merged tree BEFORE any vnc-030 server work is validated: `context_cycle(start)` PreToolUse through rebased `index.js` creates tracker AND sends CYCLE_START frame with `cycle_stamp` (not the `build-request-tools.js:326` null sentinel); non-cycle PreToolUse → sentinel, `index.js:366` exits 0 with no tracker touch / canary bump (`state.bumpStampMiss` not called) / network. Anchors now real `file:line` (matcher `merge-settings.js:49`, sentinel `build-request-tools.js:326`) | PENDING |
| End-to-end round-trip at all 3 server read sites (#3486, R-01, FR-13) | test | Stamped event through each of single-record path (~:719), second single site (~:861), batch (~:1042) lands row with `topic_signal=stamp.topic`, `topic_source='declared'` — asserted independently per site; shared `apply_stamp_to_row` helper exercised by all three; batch of N → N declared rows | PENDING |
| UDS-path stamp regression discharges #699 post-merge obligation (AC-10, FR-29, R-23, ADR-002 §7) | test | Covered by AC-10 above; called out here because it is vnc-027's explicit post-merge obligation owed to #699 and folds into the wire/round-trip gate family (R-06 batch/replay, R-16 wire tolerance) | PENDING |
| OQ-E canary-independence delivery probe (ADR-006 §7, R-19) | manual/test | Delivery probes whether subagent-context detection survives a simulated/aged CLI where root-id inheritance is absent. Branch A → ship production canary; Branch B → test-time invariant only, drop production signal. Test-time `stamp_miss == 0` invariant ships either way | PENDING |
| #574 no-race assumption expiry (R-21, ADR-007 §3) | manual | At delivery: if #574 merged, re-verify cycle_events rows carry windowing timestamps AND client-side `context_cycle` PreToolUse interception still fires | PENDING |
| Drive-by docstring fix (FR-25) | grep | `attribution.rs` + `topic-signal.js` `{alpha}-{digits}` docstrings corrected to describe actual filter (hyphen required, `[A-Za-z0-9-_.]`, no digit requirement); comment-only, no behavior change | PENDING |
| #588 disposition (FR-26) | manual | PR description carries the resolved/residue list; close #588 via this PR or convert residue to named follow-up | PENDING |
