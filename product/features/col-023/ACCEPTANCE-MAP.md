# col-023 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `ObservationRecord.hook: HookType` is replaced with `event_type: String` and `source_domain: String`. `HookType` as a field type no longer appears anywhere in the retrospective pipeline hot path. | grep + shell | `grep -r "hook: HookType" crates/` returns no matches outside the constants module; `cargo check --workspace` exits 0 | PENDING |
| AC-02 | All 21 detection rules compile and produce identical findings for Claude Code event streams after the type change. No existing rule test is deleted or weakened. The test count for `unimatrix-observe` does not decrease. | test | `cargo test -p unimatrix-observe` passes; assert test count >= pre-feature baseline count | PENDING |
| AC-03 | A server started with a config that has no `[observation]` section loads the "claude-code" default domain pack automatically. Events are processed correctly with no config required. | test | Integration test: start server without `[observation]` config; call `parse_observation_rows` with `event_type = "PreToolUse"`; assert `source_domain = "claude-code"` is returned and the event is not dropped | PENDING |
| AC-04 | An empty config produces identical `RetrospectiveReport` output for a fixed Claude Code session fixture as the current codebase. Zero behavioral regression. | test | Snapshot test: run a fixed claude-code session fixture through the pipeline before and after; assert field-for-field equivalence on findings count, finding types, and all 21 `UniversalMetrics` field values | PENDING |
| AC-05 | A synthetic event stream with `source_domain = "sre"` and `event_type = "incident_opened"` can be ingested without the server rejecting it or panicking. A domain-specific detection rule registered for "sre" fires on "sre" events. "claude-code" rules do not fire on "sre" events. | test | Integration test: register synthetic "sre" pack; ingest "sre" session; run `detect_hotspots`; assert (a) sre rule fires, (b) zero claude-code rule findings, (c) no panic | PENDING |
| AC-06 | Payloads exceeding 64 KB are rejected at ingest with `PayloadTooLarge`. JSON nesting deeper than 10 levels is rejected with `NestingTooDeep`. Boundary values pass. | test | Unit tests: 65,536-byte payload passes; 65,537-byte payload returns `PayloadTooLarge`; depth-10 payload passes; depth-11 payload returns `NestingTooDeep` | PENDING |
| AC-07 | `source_domain` values not matching `^[a-z0-9_-]{1,64}$` are rejected at both domain pack registration and event ingest with `InvalidSourceDomain`. Test cases: empty string, uppercase letters, spaces, >64 characters, special characters outside the allowed set. | test | Unit tests: each invalid case returns `InvalidSourceDomain`; valid boundary cases (exactly 64 chars matching regex) pass | PENDING |
| AC-09 | The `OBSERVATION_METRICS` table has a `domain_metrics_json TEXT` column after startup with a fresh database (schema v14). Rows from schema v13 (column absent) read back with `domain_metrics_json = NULL`, deserializing as `HashMap::new()`. | test | Migration test in `unimatrix-store/src/migration.rs` test suite: fresh DB has 22 columns; v13 rows read back with `domain_metrics` empty; `PRAGMA user_version` returns 14 | PENDING |
| AC-10 | The structural test (R-03/C-06) enforcing column-field alignment is updated to expect 22 entries (21 original + `domain_metrics_json`). It continues to verify each of the 21 original column names in declaration order. `domain_metrics_json` is verified separately by name. | test | `cargo test` passes; `UNIVERSAL_METRICS_FIELDS.len() == 22`; temporarily removing one of the 21 entries from the const causes the structural test to fail (negative test) | PENDING |
| AC-11 | `parse_observation_rows` no longer drops unknown event types. Records with `event_type` values not in any registered pack pass through with `source_domain = "unknown"`. | test | Integration test: insert a record with an unregistered `event_type` string; assert it appears in the pipeline output with `source_domain = "unknown"` and is stored in the `observations` table with the raw `event_type` intact | PENDING |
