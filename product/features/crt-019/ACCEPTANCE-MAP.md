# crt-019 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | After crt-019, the p95–p5 confidence spread of the active entry population (non-zero-confidence entries) is >= 0.20 when measured in the live database following at least one completed refresh tick | manual + test | Manual: export active entry confidence values and compute `p95 - p5`; verify >= 0.20. Automated: `cargo test --test pipeline_calibration T-CAL-SPREAD-01` with synthetic 50-entry population spanning signal space | PENDING |
| AC-02 | `MINIMUM_SAMPLE_SIZE` removed from `confidence.rs`. Bayesian Beta-Binomial posterior replaces Wilson score. Exact assertions: `bayesian_helpfulness(0, 0, 3.0, 3.0) == 0.5` (cold-start neutral); `bayesian_helpfulness(0, 2, 3.0, 3.0) == 0.375` (two unhelpful votes lower score); `bayesian_helpfulness(2, 2, 3.0, 3.0) == 0.5` (balanced votes return neutral); `bayesian_helpfulness(2, 0, 3.0, 3.0) > 0.5` (two helpful votes raise score) | test | `cargo test -p unimatrix-engine bayesian_helpfulness` — unit tests replacing T-05 helpfulness tests in `confidence.rs`. Note: `bayesian_helpfulness(2, 2, 3.0, 3.0) == 0.5` exactly (not > 0.5 as stated in original SCOPE — SPEC AC-02 corrects this; R-14 confirms) | PENDING |
| AC-03 | `W_BASE + W_USAGE + W_FRESH + W_HELP + W_CORR + W_TRUST == 0.92` with exact f64 equality | test | `cargo test -p unimatrix-engine weight_sum_invariant_f64` — existing test updated to verify new constants `{0.16, 0.16, 0.18, 0.12, 0.14, 0.16}` | PENDING |
| AC-04 | All six signal ablation tests T-ABL-01 through T-ABL-06 in `pipeline_calibration.rs` pass with the new weight vector | test | `cargo test --test pipeline_calibration` — verify T-ABL-01, T-ABL-02, T-ABL-03, T-ABL-04, T-ABL-05, T-ABL-06 all green; and T-CAL-04 (Kendall's tau > 0.6 under 10% perturbation) passes | PENDING |
| AC-05 | `base_score(Status::Active, "auto") < 0.5` (returns 0.35); `base_score(Status::Active, "agent")`, `base_score(Status::Active, "human")`, and `base_score(Status::Active, "system")` all return 0.5; `base_score(Status::Proposed, "auto") == 0.5` (no differentiation for non-Active) | test | `cargo test -p unimatrix-engine base_score` — unit tests in `confidence.rs` extending T-02; including explicit `auto_proposed_base_score_unchanged` test asserting `base_score(Status::Proposed, "auto") == 0.5` | PENDING |
| AC-06 | `SEARCH_SIMILARITY_WEIGHT` compile-time constant replaced by runtime adaptive blend. Assertions: `adaptive_confidence_weight(0.20) == 0.25`; `adaptive_confidence_weight(0.10) == 0.15` (floor); `adaptive_confidence_weight(0.30) == 0.25` (cap); `observed_spread` computed and cached during refresh tick (not per-query) | test | `cargo test -p unimatrix-engine adaptive_confidence_weight` — unit tests for formula; integration test confirming `rerank_score` receives cached value after a refresh tick; grep confirms `SEARCH_SIMILARITY_WEIGHT` is absent from codebase | PENDING |
| AC-07 | `MAX_CONFIDENCE_REFRESH_BATCH == 500` in `coherence.rs`; wall-clock guard using `std::time::Instant` present in the refresh loop, breaking early when `elapsed > 200ms` and logging partial count; guard is checked before each `update_confidence()` call | test + grep | `grep -n "MAX_CONFIDENCE_REFRESH_BATCH" crates/unimatrix-server/src/infra/coherence.rs` asserts value 500; `cargo test` unit test for duration guard early exit; code review confirms guard is pre-iteration (not post) | PENDING |
| AC-08a | When `context_get` is called with `params.helpful.is_none()`, the retrieved entry's `helpful_count` is incremented by 1. When called with `params.helpful = Some(false)`, `helpful_count` is not incremented. No additional `spawn_blocking` task is spawned | test | `cargo test -p unimatrix-server test_context_get_implicit_helpful_vote` — unit/integration test: call `context_get` handler with `helpful: None`, wait for spawn_blocking, assert `entry.helpful_count == 1`; second call with `helpful: false` asserts `helpful_count` unchanged; diff review confirms zero new `spawn_blocking` calls in handler | PENDING |
| AC-08b | When `context_lookup` is called, each returned entry's `access_count` is incremented by 2 (not 1); `helpful_count` is unchanged; if UsageDedup has already recorded access for this agent-entry pair, the increment is 0 (not 2) on subsequent call | test | `cargo test -p unimatrix-server test_context_lookup_doubled_access` — three scenarios: (1) new agent, new entry -> assert `access_count == 2`, `helpful_count == 0`; (2) same agent second call -> assert `access_count` remains 2; (3) two different agents -> assert `access_count == 4`. Requires store-layer verification per R-11 | PENDING |
| AC-09 | `.claude/skills/uni-knowledge-search/SKILL.md` includes `helpful: true` in at least one example invocation and guidance on when to pass `helpful: false`; `.claude/skills/uni-knowledge-lookup/SKILL.md` includes the same | manual | Manual review of git diff for both skill files; confirm `helpful: true` present in primary example and `helpful: false` guidance is present | PENDING |
| AC-10 | All pipeline test suites pass: `pipeline_calibration.rs` (T-ABL-01..06, T-CAL-04 tau > 0.6, T-RET-01 verified under adaptive blend); `pipeline_regression.rs` (T-REG-01 ordering preserved, T-REG-02 updated to new weight constants) | test | `cargo test --test pipeline_calibration --test pipeline_regression` green; T-REG-02 assertions show new values `{W_BASE: 0.16, W_USAGE: 0.16, W_HELP: 0.12, W_TRUST: 0.16}`; T-REG-01 asserts `expert > good > auto > stale > quarantined` with new formula | PENDING |
| AC-11 | `weight_sum_invariant_f64` test asserts `stored_sum == 0.92_f64` with exact equality (not tolerance) | test | `cargo test -p unimatrix-engine weight_sum_invariant_f64` passes; verify the test uses `assert_eq!` not `assert!((sum - 0.92).abs() < eps)` | PENDING |
| AC-12 | Calibration scenario `auto_vs_agent_spread` in `pipeline_calibration.rs` confirms that an Active entry with `trust_source = "agent"` scores strictly higher in `compute_confidence` than an identically-signaled Active entry with `trust_source = "auto"`, across at least three signal levels (zero-signal, mid-signal, high-signal) | test | `cargo test --test pipeline_calibration auto_vs_agent_spread` passes; scenario uses `Status::Active` entries (not Proposed); at all three signal levels, agent confidence > auto confidence | PENDING |
