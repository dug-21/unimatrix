# nan-011 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | README and PRODUCT-VISION.md opening section uses the approved vision statement verbatim | grep + manual | Diff README.md opening block character-by-character against the approved text in SCOPE.md §Proposed Approach. Repeat for PRODUCT-VISION.md. Both diffs must be empty. | PENDING |
| AC-02 | README contains zero mentions of NLI re-ranking, NLI cross-encoder, NLI contradiction, or any variant used to describe an active or shipped feature | shell | `grep -i "nli re-rank\|nli cross-encoder\|nli contradiction\|nli re-ranker\|nli sort" README.md` — must return zero matches | PENDING |
| AC-03 | README contains a "Graph-Enhanced Retrieval" section (or equivalent heading) covering PPR expansion, phase-conditioned category affinity, and co-access ranking; plus at least one paragraph each for behavioral signal delivery and domain-agnostic observation pipeline | manual | Read README.md and confirm: (1) Graph-Enhanced Retrieval section exists with PPR/phase-affinity/co-access content; (2) behavioral signal delivery paragraph present; (3) domain-agnostic observation pipeline paragraph present | PENDING |
| AC-04 | All binary name references in README use `unimatrix`; build path shown as `target/release/unimatrix` | grep | `grep -n "unimatrix-server" README.md` — must return zero matches | PENDING |
| AC-05 | PRODUCT-VISION.md correctly marks W1-5 as COMPLETE with col-023/PR #332/GH #331 reference, and the HookType domain coupling gap row as Fixed | manual | Read W1-5 row: confirm "COMPLETE", "col-023", "PR #332", "GH #331" in same block. Read Domain Coupling table HookType row: confirm Status = "Fixed" with col-023/W1-5/PR #332. | PENDING |
| AC-06 | `config.toml` covers all 7 sections beyond `[retention]`: `[profile]`, `[knowledge]`, `[server]`, `[agents]`, `[observation]`, and the advanced block with `[confidence]` and `[inference]`; every user-facing field has a comment | manual | Read config.toml and confirm all 8 section headers present; every uncommented field has an explanatory comment above it | PENDING |
| AC-07 | `config.toml` contains a complete commented `[[observation.domain_packs]]` example showing `source_domain`, `event_types`, `categories`, and `rule_file` with explanatory comments on each field | grep + manual | `grep -n "observation.domain_packs\|source_domain\|event_types\|rule_file" config.toml` — confirm all four fields appear in a commented example block | PENDING |
| AC-08 | `config.toml` is valid TOML; every uncommented field parses without error; all shown default values match compiled defaults in `config.rs` | shell | `python3 -c "import tomllib; tomllib.load(open('config.toml','rb'))"` — must produce no errors. Cross-check every uncommented field value against ADR-002 verified defaults table. | PENDING |
| AC-09 | The NLI sub-block in `[inference]` is present but fully commented out with a note that it requires an external model file not bundled with Unimatrix | grep + manual | `grep -n "nli_enabled\|nli_model_path" config.toml` — confirm lines exist and every one is prefixed with `#` | PENDING |
| AC-10 | Zero bare Unimatrix tool name invocations across all 14 skill SKILL.md files | shell | Run both passes from ADR-004: (1) `grep -rn '`context_[a-z_]*(' .claude/skills/*/SKILL.md` (2) `grep -rn 'context_[a-z_]*(' .claude/skills/*/SKILL.md \| grep -v 'mcp__unimatrix__'` — manually review each match; zero confirmed invocations without prefix. Apply same to `skills/uni-retro/SKILL.md` at repo root. | PENDING |
| AC-11 | `uni-init` SKILL.md lists all 14 current skills by name, accurately and completely | manual | Read `.claude/skills/uni-init/SKILL.md` CLAUDE.md append block. Cross-reference against canonical list: uni-git, uni-release, uni-review-pr, uni-init, uni-seed, uni-store-lesson, uni-store-adr, uni-store-pattern, uni-store-procedure, uni-knowledge-lookup, uni-knowledge-search, uni-query-patterns, uni-zero, uni-retro. Exactly 14, no duplicates, no phantom entries. | PENDING |
| AC-12 | `uni-retro` SKILL.md contains no references to `HookType`, closed-enum event type matching, or any col-023 predecessor concept | shell | `grep -rn "HookType\|closed.enum\|UserPromptSubmit\|SubagentStart\|PreCompact\|PreToolUse\|PostToolUse\|Stop hook" .claude/skills/uni-retro/SKILL.md` — must return zero matches | PENDING |
| AC-13 | `uni-release` SKILL.md includes steps to copy `protocols/` and include `uni-retro` in the npm package; `package.json` `files` array includes both artifacts; `npm pack --dry-run` output confirms both appear | shell | (1) Read `.claude/skills/uni-release/SKILL.md` and confirm Steps 7a and 7b are present. (2) Read `packages/unimatrix/package.json` and confirm `"protocols/"` in `files` array. (3) Run `npm pack --dry-run` from `packages/unimatrix/`: confirm `protocols/README.md` (or any protocol file) and `skills/uni-retro/SKILL.md` appear; confirm `uni-release/SKILL.md` does NOT appear. | PENDING |
| AC-14 | `protocols/` directory exists at repo root containing all 4 protocol files and a `README.md`; the README includes a `context_cycle` usage example | file-check + grep | `ls protocols/` — confirm 5 files: uni-design-protocol.md, uni-delivery-protocol.md, uni-bugfix-protocol.md, uni-agent-routing.md, README.md. `grep -n "context_cycle" protocols/README.md` — must return matches showing type: "start", type: "phase", type: "stop". | PENDING |
| AC-15 | All 4 protocols contain zero references to removed features (NLI, MicroLoRA, `unimatrix-server`); `context_cycle` call signatures in protocols match the current MCP tool | shell | `grep -rn "NLI\|MicroLoRA\|unimatrix-server\|HookType" protocols/` — must return zero matches. Run same against `.claude/protocols/uni/` — must return zero matches. Diff each corresponding file pair: must be identical. | PENDING |
| AC-16 | `uni-seed` SKILL.md uses `mcp__unimatrix__context_store` for all tool calls, describes the blank-installation use case, and warns against re-running on an established installation | grep + manual | (1) `grep -n "context_store\|context_status" .claude/skills/uni-seed/SKILL.md \| grep -v "mcp__unimatrix__"` — zero matches. (2) Confirm idempotency warning text appears before the first `mcp__unimatrix__context_store` call. (3) Confirm skill description states blank-install use case. | PENDING |
| AC-17 | All seed entries in `uni-seed` use categories present in the current `INITIAL_CATEGORIES` list in `crates/unimatrix-server/src/infra/categories/mod.rs` | manual | Read `categories/mod.rs` INITIAL_CATEGORIES array. Read `uni-seed` SKILL.md and extract every `category:` value from tool call arguments. Confirm each is in ["lesson-learned", "decision", "convention", "pattern", "procedure"]. | PENDING |
