# col-010 Acceptance Criteria Map

| AC-ID | Description | Priority | Verification Method | Verification Detail | Status |
|-------|-------------|----------|--------------------|--------------------|--------|
| AC-01 | Schema v5 migration runs on Store::open() when schema version is 4 — creates SESSIONS and INJECTION_LOG tables, writes next_log_id = 0 to COUNTERS. Schema version increments to 5. All existing entries and signal records survive intact. | P0 | test | Integration test: open v4 store, run Store::open(), verify schema_version=5, SESSIONS and INJECTION_LOG tables exist, COUNTERS["next_log_id"]=0, all prior ENTRIES/SIGNAL_QUEUE records intact. Also verify migration idempotency: call migrate_v4_to_v5 twice on same open transaction — next_log_id not reset. | PENDING |
| AC-02 | SessionRegister dispatch writes a SessionRecord with status = Active and started_at set to current unix timestamp. The record is readable via get_session(session_id). | P0 | test | Integration test: send HookRequest::SessionRegister with a valid session_id, await spawn_blocking completion, call store.get_session(session_id) — returns Some(SessionRecord) with status=Active, started_at within 2s of now, ended_at=None, total_injections=0. | PENDING |
| AC-03 | SessionClose dispatch with final_outcome = Success updates the SessionRecord to status = Completed, ended_at = now, outcome = "success", total_injections = N. | P0 | test | Integration test: register session, inject 3 entries (ContextSearch), close with Success, await all spawn_blocking tasks, get_session — status=Completed, outcome="success", ended_at set, total_injections=3. | PENDING |
| AC-04 | SessionClose dispatch with final_outcome = Rework updates the SessionRecord to status = Completed, outcome = "rework". | P0 | test | Integration test: register session, inject entries, trigger rework signal, close — get_session returns status=Completed, outcome="rework". | PENDING |
| AC-05 | SessionClose dispatch with final_outcome = Abandoned writes status = Abandoned, outcome = "abandoned". No auto-outcome entry is created. | P0 | test | Integration test: register session, close with Abandoned — get_session returns status=Abandoned, outcome="abandoned". Verify context_lookup(category:"outcome", tags:["type:session"]) returns no entries for this session. | PENDING |
| AC-06 | Every injected entry in a ContextSearch response produces an InjectionLogRecord in INJECTION_LOG with correct session_id, entry_id, confidence, and timestamp. | P0 | test | Integration test: register session, ContextSearch returning 3 entries, await spawn_blocking, scan_injection_log_by_session — returns exactly 3 records with session_id, entry_id, confidence, and timestamp all correct. | PENDING |
| AC-07 | scan_injection_log_by_session(session_id) returns exactly the records written for that session (full scan + filter). | P0 | test | Unit test: insert_injection_log_batch for session-A (3 records) and session-B (2 records) — scan for session-A returns exactly 3; scan for session-B returns exactly 2; no cross-contamination. | PENDING |
| AC-08 | Maintenance sweep marks Active sessions with started_at > 24h as TimedOut (without deleting them). | P0 | test | Integration test: insert Active SessionRecord with started_at = now - 25*3600, call gc_sessions(TIMED_OUT_THRESHOLD_SECS, DELETE_THRESHOLD_SECS) — status=TimedOut, record still present, INJECTION_LOG records for session intact. | PENDING |
| AC-09 | Maintenance sweep deletes sessions (any status) with started_at > 30 days. | P0 | test | Integration test: insert session with started_at = now - 31*24*3600 (any status), call gc_sessions — get_session returns None; corresponding INJECTION_LOG records deleted; GcStats.deleted_session_count=1, GcStats.deleted_injection_log_count=N. | PENDING |
| AC-10 | VALID_TYPES in outcome_tags.rs includes "session". validate_outcome_tags(["type:session"]) returns Ok(()). | P0 | test | Unit test: assert VALID_TYPES.contains(&"session"); assert validate_outcome_tags(&["type:session"]) == Ok(()). | PENDING |
| AC-11 | Auto-outcome entry is written on SessionClose with outcome Success or Rework when total_injections > 0. Entry has category = "outcome", tags contains "type:session" and "result:pass" or "result:rework", embedding_dim = 0 (no vector embedding). | P0 | test | Integration test: close session Success with 3 injections, await spawn_blocking, context_lookup(category:"outcome", tags:["type:session"]) — returns entry with embedding_dim=0, tag "result:pass", source="hook", trust_source="system". Verify no entry for Abandoned session or zero-injection session. | PENDING |
| AC-12 | from_structured_events(store, feature_cycle) returns a RetrospectiveReport with session_count equal to the number of non-abandoned SESSIONS records for that feature_cycle. | P0 | test | Integration test: insert 5 sessions for "fc-test" (3 Completed, 1 Abandoned, 1 TimedOut), with injection records, call from_structured_events("fc-test") — report.session_count=3 (Abandoned and TimedOut excluded). | PENDING |
| AC-13 | context_retrospective tool prefers the structured path when SESSIONS data exists for the feature_cycle; falls back to JSONL path otherwise. | P0 | test | Integration test (a): SESSIONS populated for feature_cycle — verify tracing::debug log indicates "structured" path; (b): SESSIONS empty AND no JSONL data — returns empty report; (c): SESSIONS empty AND JSONL files exist — JSONL path used. | PENDING |
| AC-14 | A server restart (simulated by clearing in-memory SessionRegistry) does not lose SESSIONS or INJECTION_LOG data from prior sessions. | P0 | test | Integration test: write sessions and injection records, drop and reopen Store — get_session returns records; scan_injection_log_by_session returns records. | PENDING |
| AC-15 | context_retrospective with default evidence_limit = 3 returns ≤ 3 evidence items per hotspot; total payload ≤ 10KB for a feature cycle with 13 hotspots. | P1 | test | Integration test: build synthetic report with 13 hotspots, call context_retrospective without evidence_limit (defaults to 3), serialize JSON response — assert len(bytes) <= 10240; assert each hotspot.evidence.len() <= 3. | PENDING |
| AC-16 | context_retrospective with evidence_limit = 0 returns complete evidence arrays — output is backward-compatible with pre-col-010 callers. | P1 | test | Integration test: snapshot pre-col-010 context_retrospective response for a known feature_cycle; after col-010, call with evidence_limit=0 — field-by-field parity on hotspot evidence arrays. All pre-col-010 integration tests pass when updated to pass evidence_limit=0. | PENDING |
| AC-17 | context_retrospective with default evidence_limit = 3 returns narratives alongside capped evidence when the structured-events path is used. | P1 | test | Integration test: populate SESSIONS + INJECTION_LOG for a feature cycle with hotspots, call context_retrospective without evidence_limit — report.narratives is Some, each narrative.summary is non-empty string, each hotspot.evidence.len() <= 3. When JSONL fallback used, narratives is None. | PENDING |
| AC-18 | For a sleep_workarounds hotspot with escalating sleep intervals (30 to 60 to 90 to 120), HotspotNarrative.sequence_pattern contains the sequence string. | P1 | test | Unit test: construct ObservationRecord stream with sleep events at 30s/60s/90s/120s monotone-increasing, call synthesize_narratives, assert narrative.sequence_pattern == Some("30s->60s->90s->120s"). Non-monotone pattern returns None. | PENDING |
| AC-19 | Recommendation templates: a RetrospectiveReport with a permission_retries hotspot includes a Recommendation with non-empty action. A report with no recognized hotspot types includes an empty recommendations list. | P1 | test | Unit test: recommendations_for_hotspots with permission_retries hotspot — Vec has 1 Recommendation with non-empty action. recommendations_for_hotspots with unrecognized hotspot type only — returns empty Vec. All 4 template types verified. | PENDING |
| AC-20 | After context_retrospective completes with >= 1 hotspot or recommendation, a category:lesson-learned entry exists in Unimatrix with topic = "retrospective/{feature_cycle}", non-empty content, trust_source = "system", and a valid embedding (embedding_dim > 0). | P1 | test | Integration test: context_retrospective with >= 1 hotspot, wait for fire-and-forget embed task to complete, context_lookup(category:"lesson-learned", topic:"retrospective/{fc}") — returns entry with trust_source="system", embedding_dim>0, non-empty content. | PENDING |
| AC-21 | Calling context_retrospective twice for the same feature_cycle produces exactly one active lesson-learned entry — the second call supersedes the first (prior entry status = Deprecated, superseded_by set). | P1 | test | Integration test: call context_retrospective twice for same feature_cycle, wait for both embed tasks — exactly 1 entry with status=Active for topic "retrospective/{fc}"; prior entry has status=Deprecated and superseded_by set to new entry id. | PENDING |
| AC-22 | context_search with a query related to a prior retrospective finding (e.g., "permission retry patterns") returns the lesson-learned entry in results. | P1 | test | Integration test: context_retrospective produces lesson-learned entry with "permission retry" in content, wait for embedding, context_search("permission retry patterns") — lesson-learned entry appears in results (within top 5). | PENDING |
| AC-23 | A lesson-learned entry and a generic convention entry with identical similarity and confidence scores: the lesson-learned entry ranks higher in search results by exactly PROVENANCE_BOOST = 0.02. | P1 | test | Unit test: compute rerank_score(sim=0.8, conf=0.6, co_access=0.01, category="lesson-learned") and rerank_score(sim=0.8, conf=0.6, co_access=0.01, category="convention") — difference is exactly 0.02. Integration test via both MCP context_search and ContextSearch hook path verify lesson-learned ranks first. | PENDING |
| AC-24 | All existing tests pass without modification after schema v5 migration, new write paths, RetrospectiveReport type changes, and provenance boost addition. | P0+P1 | shell | cargo test --workspace 2>&1 | grep -E "^(test result|FAILED)" — all test suites show "ok", zero failures. | PENDING |
