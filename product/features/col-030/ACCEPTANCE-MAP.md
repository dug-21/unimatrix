# col-030 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `suppress_contradicts(result_ids, graph)` returns a `Vec<bool>` of the same length as `result_ids`. For an empty graph or result set with no `Contradicts` edges, all values are `true`. | test | Unit test in `graph_suppression.rs` `#[cfg(test)]`: empty graph case asserts `result.len() == result_ids.len()` and `result.iter().all(\|&v\| v)`. No-Contradicts case asserts same. | PENDING |
| AC-02 | For two entries connected by a `Contradicts` edge in the Outgoing direction, the lower-ranked entry (higher index in sorted order) is suppressed (`false`); the higher-ranked entry is retained (`true`). | test | Unit test: construct `TypedRelationGraph` with `Contradicts` edge rank-0 → rank-1; assert `keep[0] == true`, `keep[1] == false`. | PENDING |
| AC-03 | For two entries connected by a `Contradicts` edge in the Incoming direction (edge written from lower-ranked toward higher-ranked), the lower-ranked entry is still suppressed. | test | Unit test: edge written as rank-1 → rank-0 (Incoming to rank-0); assert `keep[0] == true`, `keep[1] == false`. This is the mandatory bidirectional coverage test (R-05). | PENDING |
| AC-04 | Non-`Contradicts` edges (`CoAccess`, `Supports`, `Supersedes`, `Prerequisite`) between result entries do not cause any suppression. | test | Unit test: entries with `Supports` and `CoAccess` edges between them; assert all values `true`. | PENDING |
| AC-05 | When `use_fallback = true`, the suppression step in `SearchService::search` is skipped; all results pass through unchanged. | test | Existing cold-start tests in `search.rs` continue to pass unmodified. Code review confirms `if !use_fallback` guard form is present and non-inverted. | PENDING |
| AC-06 | Zero-regression eval gate passes: MRR, P@K, and score distribution are unchanged across all existing eval scenarios. | shell | `cargo run -p eval-runner -- --distribution_change false` produces no regressions in `render_zero_regression` report. | PENDING |
| AC-07 | When a conflicting pair is present, the lower-ranked entry per pair is absent from returned results. The result set length is reduced; no padding to `k` is applied. | test | Positive integration test in `search.rs` (FR-14): entries A, B, C with `Contradicts` edge A↔B (A ranks higher); assert A in results, B absent, result count = k-1. Uses `build_typed_relation_graph` with `bootstrap_only=false` fixture (not `create_graph_edges_table`). | PENDING |
| AC-08 | `suppress_contradicts` is defined in `unimatrix-engine/src/graph_suppression.rs` and re-exported from `graph.rs` via `pub use graph_suppression::suppress_contradicts`. Unit tests are in `graph_suppression.rs` `#[cfg(test)]` covering all FR-13 cases. | manual | Code review: confirm file exists at `crates/unimatrix-engine/src/graph_suppression.rs`; `graph.rs` contains `mod graph_suppression; pub use graph_suppression::suppress_contradicts;`; `graph_tests.rs` has no new tests added. | PENDING |
| AC-09 | All existing tests in `graph_tests.rs` and `search.rs` pass without modification. | test | `cargo test --workspace` green; no test removals, no `#[ignore]` additions. | PENDING |
| AC-10 | `edges_of_type` is the sole call site for querying `Contradicts` neighbors inside `suppress_contradicts`. No direct `.edges_directed()` or `.neighbors_directed()` calls exist in the suppression function. | grep | `grep -n "edges_directed\|neighbors_directed" crates/unimatrix-engine/src/graph_suppression.rs` returns no matches. | PENDING |
| AC-11 | A `tracing::debug!` log line is emitted for each suppressed entry, recording both the suppressed entry ID and the contradicting (retaining) entry ID. | manual | Code review confirms `debug!` call includes both `suppressed_entry_id` and `contradicting_entry_id` fields. Manual test: `RUST_LOG=debug cargo run` with a known Contradicts pair confirms both IDs appear in log output. | PENDING |
| AC-12 | `results_with_scores` and `final_scores` are filtered by the suppression mask in a single indexed pass. The two Vecs remain strictly parallel after suppression. `final_scores` is shadowed (`let final_scores = new_fs`) not mutated via `let mut` at line 893. | manual | Code review: confirm single `enumerate()` loop over `zip(results_with_scores, final_scores[..aligned_len])`; no separate `retain` calls; `aligned_len = results_with_scores.len()` (not `final_scores.len()`); `let final_scores = new_fs` shadow present. | PENDING |
