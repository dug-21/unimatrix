# nan-009 Acceptance Criteria Map

GH Issue: #400

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `context.phase` is present in JSONL output of `eval scenarios` when the source `query_log` row has a non-null `phase` value | test | Integration test in `eval/scenarios/tests.rs`: insert row with `phase = "delivery"`, run extraction, assert `context.phase == "delivery"` in output JSONL | PENDING |
| AC-02 | `context.phase` key is absent from JSONL output (not emitted as `"phase":null`) when the source `query_log` row has `phase = NULL` | test | Integration test in `eval/scenarios/tests.rs`: insert row with `phase = NULL`, run extraction, assert JSONL for that scenario contains no `"phase"` key | PENDING |
| AC-03 | `phase` is present on `ScenarioResult` in per-scenario JSON produced by `eval run`, carrying the value from `context.phase`; null-phase results emit explicit `"phase":null` | test | Unit test on `replay_scenario` output struct asserting `phase` field is set; round-trip test in AC-11 | PENDING |
| AC-04 | `eval report` section 6 "Phase-Stratified Metrics" is rendered when at least one scenario result has a non-null phase; section is omitted entirely when all phase values are null | test | Golden-output render test: one run with mixed-phase data asserts `## 6. Phase-Stratified Metrics` present; one run with all-null phases asserts section 6 heading absent (`!content.contains("## 6. Phase-Stratified Metrics")`) | PENDING |
| AC-05 | Section 6 shows one row per distinct phase value with: phase name, scenario count, mean P@K, mean MRR, mean CC@k, mean ICD; `"(unset)"` bucket row appears last | test | Unit test on `compute_phase_stats` with controlled input asserting correct grouping, counts, means, and `"(unset)"` sort position; golden-output render test | PENDING |
| AC-06 | Pre-nan-009 result JSON files (without `phase` field) are deserialized by `eval report` without error; missing `phase` defaults to `None` | test | Unit test deserializing a JSON string without any `"phase"` key into the report module's `ScenarioResult`; assert no error and `phase == None` | PENDING |
| AC-07 | `docs/testing/eval-harness.md` documents: `context.phase` field; known vocabulary (`design`, `delivery`, `bugfix`) as a snapshot not an allowlist; section 6 Phase-Stratified Metrics in the report reference; phase population requires MCP-sourced sessions with `context_cycle`; migration-based governance model | manual | Documentation review: confirm all five items are present in `docs/testing/eval-harness.md` | PENDING |
| AC-08 | Section 2 "Notable Ranking Changes" in `eval report` includes the phase label alongside each scenario header line when phase is non-null; null-phase scenario header lines are unchanged | test | Golden-output render test with mixed-phase results: assert phase label present for non-null scenario lines and absent for null-phase scenario lines in section 2 output | PENDING |
| AC-09 | Unit tests cover five named cases: (1) `ScenarioContext` with non-null phase serializes `phase` key present; (2) `ScenarioContext` with null phase serializes `phase` key absent; (3) `compute_phase_stats` groups results correctly for mixed phase values; (4) `compute_phase_stats` returns empty vec when all phases are null; (5) section 6 is omitted from rendered output when no non-null phases are present | test | Five named unit tests exist, each covering exactly the listed case | PENDING |
| AC-10 | SQL extraction integration test: insert row with `phase = "delivery"`, verify extracted JSONL contains `context.phase = "delivery"`; insert row with `phase = NULL`, verify `phase` key absent from JSONL | test | Integration test in `eval/scenarios/tests.rs` using the `insert_query_log_row` helper extended to accept `phase: Option<&str>`; two named tests — `test_scenarios_extract_phase_non_null` and `test_scenarios_extract_phase_null` | PENDING |
| AC-11 | End-to-end round-trip integration test: scenario extraction with non-null phase → `eval run` replay → `eval report` rendering; test asserts (1) rendered section 6 contains a row for the non-null phase; (2) `phase` is non-null in `ScenarioResult` JSON from `eval run`; a partial update to only one `ScenarioResult` copy must cause this test to fail | test | Integration test `test_report_round_trip_phase_section_7_distribution` in `eval/report/tests.rs`: write scenario JSONL with `phase = "delivery"`, run replay, check result JSON, run report, assert `## 6. Phase-Stratified Metrics` present, `"delivery"` present in section 6, `## 7. Distribution Analysis` present, `!content.contains("## 6. Distribution Analysis")`, `pos("## 6.") < pos("## 7.")` | PENDING |
| AC-12 | Golden-output render test for section order exists and is updated before delivery; asserts section 6 = "Phase-Stratified Metrics", section 7 = "Distribution Analysis"; a section-order regression must cause this test to fail | test | Test `test_report_round_trip_phase_section_7_distribution` (AC-11) includes `!content.contains("## 6. Distribution Analysis")` and `pos("## 6.") < pos("## 7.")` assertions; existing `test_report_contains_all_five_sections` updated to assert seven sections | PENDING |
