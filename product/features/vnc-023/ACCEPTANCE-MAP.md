# vnc-023 Acceptance Criteria Map

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|--------------------|--------------------|--------|
| AC-01 | `Cargo.toml` specifies `rmcp = { version = "=1.7.0", ... }` and `cargo build --workspace` succeeds with zero errors | grep + shell | `grep 'version = "=1.7.0"' crates/unimatrix-server/Cargo.toml` confirms version string; `cargo build --workspace` exits 0; verify all 6 feature flags present in the rmcp dependency line | PENDING |
| AC-02 | `ServerInfo` and `Implementation` construction in `server.rs` uses constructors or builders (no struct literal construction of `#[non_exhaustive]` types) | grep + shell | `grep -n 'Implementation {' crates/unimatrix-server/src/server.rs` returns zero matches in production code; `cargo build -p unimatrix-server` exits 0 | PENDING |
| AC-03 | Test `ClientInfo` construction in `server.rs` test module compiles using builder or constructor | grep + shell | `grep -n 'ClientInfo {' crates/unimatrix-server/src/server.rs` returns zero matches; `cargo test -p unimatrix-server --no-run` exits 0 | PENDING |
| AC-04 | All 8 `ErrorData::invalid_params` call sites in `tools.rs` compile without modification | shell + grep | `cargo build -p unimatrix-server` exits 0; `git diff crates/unimatrix-server/src/mcp/tools.rs` shows zero changes to `ErrorData::invalid_params` call sites | PENDING |
| AC-05 | `LocalSessionManager::default()` compiles and session `keep_alive` defaults to 5 minutes | shell + test | `cargo build -p unimatrix-server` exits 0; existing integration tests pass without session timeout failures | PENDING |
| AC-06 | UDS transport via `(OwnedReadHalf, OwnedWriteHalf)` tuple compiles and `IntoTransport` blanket impl resolves | shell | `cargo build -p unimatrix-server` exits 0; no explicit `transport-async-rw` feature added to Cargo.toml | PENDING |
| AC-07 | Extension propagation validated — `ResolvedIdentity` accessible in `RequestContext.extensions.get::<http::request::Parts>()` at tool call time | test | Integration test exercises full HTTP->rmcp->tool chain with bearer token; tool handler extracts and asserts `ResolvedIdentity` presence; test must fail if propagation breaks | PENDING |
| AC-08 | `Implementation` includes `.with_description("Self-learning knowledge engine for agentic workflows")` in production `ServerInfo` construction | grep + test | `grep 'Self-learning knowledge engine' crates/unimatrix-server/src/server.rs` returns match; unit/integration test asserts description field present in `get_info()` result | PENDING |
| AC-09 | `HttpConfig` gains `allowed_origins: Vec<String>` field (default empty), wired to `StreamableHttpServerConfig.allowed_origins` in `McpAdapter::new()` | grep + test | `grep 'allowed_origins' crates/unimatrix-server/src/infra/config.rs` returns field definition with `#[serde(default)]`; config deserialization test: TOML without `allowed_origins` parses to empty vec; config with `allowed_origins` propagates values to `StreamableHttpServerConfig` | PENDING |
| AC-10 | `cargo clippy --workspace -- -D warnings` passes with zero warnings | shell | `cargo clippy --workspace -- -D warnings` exits 0 | PENDING |
| AC-11 | Full test suite (`cargo test --workspace`) passes | shell | `cargo test --workspace` exits 0 | PENDING |
| AC-12 | `ServerHandler::initialize` override in `server.rs` compiles with unchanged logic | shell + grep | `cargo build -p unimatrix-server` exits 0; diff shows no logic changes to initialize body (signature adaptation permitted); `client_type_map` population and session key extraction code unchanged | PENDING |
