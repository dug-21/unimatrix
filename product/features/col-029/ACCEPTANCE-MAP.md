# col-029 Acceptance Criteria Map

GH Issue: #413

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | `StatusReport` struct contains all six new fields with correct types: `graph_connectivity_rate: f64`, `isolated_entry_count: u64`, `cross_category_edge_count: u64`, `supports_edge_count: u64`, `mean_entry_degree: f64`, `inferred_edge_count: u64` | grep | `grep -E "graph_connectivity_rate\|isolated_entry_count\|cross_category_edge_count\|supports_edge_count\|mean_entry_degree\|inferred_edge_count" crates/unimatrix-server/src/mcp/response/status.rs` — all six names must appear in the struct definition | PENDING |
| AC-02 | All-isolated store (no non-bootstrap edges): `connectivity_rate = 0.0`, `isolated_entry_count = total_active`, `cross_category_edge_count = 0` | test | `cargo test -p unimatrix-store test_graph_cohesion_all_isolated` | PENDING |
| AC-03 | All-connected store (every active entry has ≥1 non-bootstrap edge): `connectivity_rate = 1.0`, `isolated_entry_count = 0`; `mean_entry_degree` equals `(2 * edge_count) / active_count` | test | `cargo test -p unimatrix-store test_graph_cohesion_all_connected` | PENDING |
| AC-04 | `cross_category_edge_count` counts only edges where both endpoints are Active (status=0) and have different `category` values; same-category edges and edges with deprecated endpoints are not counted | test | `cargo test -p unimatrix-store test_graph_cohesion_cross_category` and `cargo test -p unimatrix-store test_graph_cohesion_same_category_only` | PENDING |
| AC-05 | `inferred_edge_count` equals exactly the count of `GRAPH_EDGES` rows with `source = 'nli'` and `bootstrap_only = 0`; rows with `bootstrap_only = 1` are excluded even if `source = 'nli'` | test | `cargo test -p unimatrix-store test_graph_cohesion_nli_source` | PENDING |
| AC-06 | `supports_edge_count` equals exactly the count of `GRAPH_EDGES` rows with `relation_type = 'Supports'` and `bootstrap_only = 0` | test | `cargo test -p unimatrix-store test_graph_cohesion_all_connected` (includes `supports_edge_count` assertion) or dedicated test within AC-13 coverage | PENDING |
| AC-07 | `mean_entry_degree = 0.0` when no non-bootstrap edges exist; for a non-empty connected graph equals `(2 * non_bootstrap_edge_count) / active_entry_count` | test | `cargo test -p unimatrix-store test_graph_cohesion_all_isolated` (zero case) and `cargo test -p unimatrix-store test_graph_cohesion_all_connected` (non-zero case) — both must include explicit `mean_entry_degree` assertion | PENDING |
| AC-08 | Deprecated and quarantined entries are excluded from `isolated_entry_count` and `graph_connectivity_rate`; an edge whose endpoint is deprecated does not count as connecting an active entry | test | `cargo test -p unimatrix-store test_graph_cohesion_mixed_connectivity` — must include a deprecated-endpoint edge case asserting `connected_entry_count` excludes the deprecated entry | PENDING |
| AC-09 | `context_status` Summary format response includes `isolated_entry_count` and `cross_category_edge_count` values numerically (not just field names) when at least one active entry with edges exists | manual | Invoke `context_status` with a non-empty store (format: summary or default); assert response substring contains the numeric values of `isolated_entry_count` and `cross_category_edge_count` | PENDING |
| AC-10 | `context_status` Markdown format response includes a `#### Graph Cohesion` sub-section with all six metric values individually labeled | manual | Invoke `context_status` with markdown format on a non-empty store; assert all six labels present: "Connectivity:", "Isolated entries:", "Cross-category edges:", "Supports edges:", "Mean entry degree:", "Inferred (NLI) edges:" | PENDING |
| AC-11 | `compute_graph_cohesion_metrics()` is called in `compute_report()` Phase 5 and all six fields are assigned to `StatusReport` before `format_status_report` is called | grep | `grep -n "compute_graph_cohesion_metrics" crates/unimatrix-server/src/services/status.rs` — must show call in Phase 5 block; `grep -n "graph_connectivity_rate\|isolated_entry_count\|cross_category_edge_count\|supports_edge_count\|mean_entry_degree\|inferred_edge_count" crates/unimatrix-server/src/services/status.rs` — all six assignments must appear | PENDING |
| AC-12 | All six fields default to `0` / `0.0` in `StatusReport::default()` — no field omitted from the `default()` block | shell | `cargo check -p unimatrix-server` must pass (missing field in `default()` is a compile error); additionally `grep` for all six field names in the `default()` block of `status.rs` | PENDING |
| AC-13 | Unit tests cover all seven scenarios: all-isolated, all-connected, mixed connectivity, cross-category edges, same-category-only edges, NLI-source edges, bootstrap-only edges excluded | test | `cargo test -p unimatrix-store 2>&1 \| grep "test_graph_cohesion_"` — must list seven test functions: `test_graph_cohesion_all_isolated`, `test_graph_cohesion_all_connected`, `test_graph_cohesion_mixed_connectivity`, `test_graph_cohesion_cross_category`, `test_graph_cohesion_same_category_only`, `test_graph_cohesion_nli_source`, `test_graph_cohesion_bootstrap_excluded` | PENDING |
| AC-14 | `cross_category_edge_count` SQL joins `entries` twice (once on `source_id`, once on `target_id`) with `status = 0` on both aliases; edge where one endpoint is deprecated/quarantined is excluded | grep | `grep -A 10 "LEFT JOIN entries src_e" crates/unimatrix-store/src/read.rs` — both `src_e` and `tgt_e` aliases must appear with `status = 0` in the ON clause; confirmed by AC-08 test | PENDING |
| AC-15 | `compute_graph_cohesion_metrics()` is NOT called from the background maintenance tick (`maintenance_tick`) or written into `MaintenanceDataSnapshot` | grep | `grep -rn "compute_graph_cohesion_metrics" crates/` — must return exactly one result in `services/status.rs`; must not appear in `load_maintenance_snapshot` or `maintenance_tick` | PENDING |
| AC-16 | Bootstrap-only edges (`bootstrap_only=1`) are excluded from all six metrics, including `inferred_edge_count` for any `source='nli'` rows that have `bootstrap_only=1` | test | `cargo test -p unimatrix-store test_graph_cohesion_bootstrap_excluded` — must explicitly insert a `source='nli', bootstrap_only=1` edge and assert `inferred_edge_count = 0` | PENDING |
