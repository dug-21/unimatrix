# nan-016 Acceptance Criteria Map

> Every AC from SCOPE.md (AC-01..AC-06). Verification methods are effect-based per SR-04 —
> AC-02/AC-03 require a real re-fired hook + negative control, not a string-diff.
> Component / FR / R-ID columns map each AC to the artifacts and risk scenarios that verify it.

| AC-ID | Description | Component | FRs | R-IDs | Verification Method | Verification Detail | Status |
|-------|-------------|-----------|-----|-------|--------------------|--------------------|--------|
| AC-01 | Reproducible idempotent copy-install of `packages/unimatrix` to `~/.unimatrix/dogfood-client/` via `npm pack`+extract (never `npm link`); re-run is idempotent clean-replace; named F6 soak-reset point. | `scripts/dogfood-install.sh` | FR-1, FR-2, FR-3, FR-4 | R-02, R-11, R-12 | test + shell | Run `scripts/dogfood-install.sh` twice. Assert (a) install root is NOT a symlink into the working tree; (b) `~/.unimatrix/dogfood-client/lib/hook-client/index.js` exists and the full `files`-array tree (`bin/ lib/ skills/ postinstall.js protocols/`) is present, platform binary ABSENT; (c) mutate/add a stray installed file, re-install, assert tree byte-identical to fresh install and stray file GONE (clean-replace, NFR-1); (d) staged-then-`mv` (no extract-in-place); (e) postinstall proven inert (no ONNX download / host mutation). | PENDING |
| AC-02 | Switchover repoints hooks to `node <installed-path>/lib/hook-client/index.js <EVENT>` — verified by the script's effect on a scratch fixture, NOT by flipping live settings. | `scripts/dogfood-switchover.sh`, `packages/unimatrix/test/dogfood-effect.test.js` | FR-5, FR-6, FR-8, FR-10, FR-11, FR-13 | R-01, R-03, R-07, R-08, R-09, R-10 | test | In the effect harness (scratch project root with real `.git/` dir under `os.tmpdir()` + scratch `settings.json` seeded with `"*"` PreToolUse + a foreign hook): run real `promote --settings <scratch> --client <installed>`. Assert (a) every Unimatrix command points at the installed entrypoint; (b) PreToolUse matcher equals the **imported** `PRETOOLUSE_CYCLE_MATCHER` (SR-05 delta); (c) foreign hook survives, no duplicates; (d) actual event-set asserted against opt-in state (8 without `SubagentStop` opt-in); (e) **re-fire** `execFileSync("node",[installedIndexJs,"SessionStart"],{cwd:scratchRoot})` asserts exit 0 / empty stdout (daemon-absent, scratch hash distinct from live hash); (f) negative control — broken install path makes the re-fire assertion FAIL. No assertion touches live settings. | PENDING |
| AC-03 | Copy-install isolation (code-freezing): after install, editing in-repo `packages/unimatrix/lib/hook-client/` does NOT alter the installed copy's bytes/behavior. NOT state-dir separation. | `packages/unimatrix/test/dogfood-effect.test.js` | FR-12 (NFR-3) | R-04, R-13 | test | Install (test-scoped temp `--target`). Capture content hash of installed `lib/hook-client/index.js`. Make a **behavior-changing** in-repo edit (e.g. `process.stderr.write` marker) in a **throwaway copy / git stash** of the tree (never the live working tree, R-13). Assert (a) installed-path hash unchanged; (b) re-fired installed-path behavior unchanged (marker absent); (c) `fs.lstatSync(installedIndexJs).isSymbolicLink() === false` (anti-`npm link`, C-6); (d) explicit negative assertion that the test does NOT require separate `{hash}` state dirs (#4923, SR-07). Working tree provably clean after the test incl. on failure paths. | PENDING |
| AC-04 | Runbook documents promotion = re-run build+install (= F6 reset); rollback = revert hooks to Rust `hook.rs`; flip deferred to a no-active-feature window; PreToolUse matcher-narrowing as intended delta; daemon assumed running / fail-open if absent. | `product/features/nan-016/RUNBOOK.md`, `scripts/dogfood-switchover.sh` (rollback) | FR-7, FR-9, FR-14 | R-06 | file-check + grep + test | (1) `RUNBOOK.md` exists and contains each of the five FR-14 items (a–e). (2) `grep` confirms it documents: promotion=re-run install, rollback=revert to Rust `hook.rs`, deferred flip / no-active-feature window, matcher-narrowing delta, daemon-fail-open. (3) Effect harness `rollback` test: on a scratch settings seeded with the real Rust-hook shape, promote then rollback; assert every command equals `LD_LIBRARY_PATH=<repo>/target/release <repo>/target/release/unimatrix hook <EVENT>` over the correct event set, idempotent, foreign hooks preserved, emitted by the shipped legacy arm (no bespoke revert string). | PENDING |
| AC-05 | `npx @dug-21/unimatrix init` (Linux local) produces byte-identical wiring to pre-F5 behavior (no regression). | (frozen surfaces) | FR-15 (SR-09) | R-14 | test + grep | Run existing `test/init.test.js`, `test/init-integration.test.js`, `test/merge-settings.test.js`; assert green. `git diff` shows zero behavioral change to `lib/init.js`, `lib/merge-settings.js`, `lib/hook-client/config.js`, `package.json` runtime. | PENDING |
| AC-06 | C-04 hook-client size gate (stripped ≤ 100 KB / raw ≤ 160 KB) still passes; zero-deps preserved. | (frozen surfaces) | NFR-5, NFR-6 | R-14 | shell | Run `node packages/unimatrix/test/check-hook-client-size.js`; assert exit 0. Run `node packages/unimatrix/test/check-zero-deps.js`; assert exit 0 (NFR-6 / C-9). | PENDING |
