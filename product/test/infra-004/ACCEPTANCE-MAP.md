# infra-004 Acceptance Criteria Map

> Every AC from SCOPE.md / SPECIFICATION.md (AC-01..AC-15) appears below with a
> concrete verification method. Status PENDING until proven in Session 2.
> Critical Gate 3c blockers: AC-01/AC-04 (R-01) and AC-08/AC-12/AC-13 (R-05).

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | Bounded warmup barrier runs before C3/C4 load-bearing writes; confirms model loaded + both per-slug stores live; bound derived from #767, not guessed. | grep + manual | `git diff` of `multi-tenant-isolation-smoke.sh` shows the barrier between `assert_routes_live` and `run_isolation_matrix`; `WARMUP_DEADLINE_SECS` default 180 documented as a #767 (`READY_TIMEOUT_SECS`) derivation with headroom. | PENDING |
| AC-02 | Barrier reuses existing infra-001 idioms (`store_size`/`wait_for_http_active`/`write_then_barrier` deadline-poll) — no new readiness mechanism. | grep | `git diff` shows only calls to existing helpers; no new readiness primitive defined. | PENDING |
| AC-03 | Genuine not-ready past deadline → INFRA (exit 2), never RED/GREEN; barrier never converts not-ready to false pass. | test | Stub-seam truth-table: force barrier timeout → assert exit 2 (INFRA), assert not 0 and not 1. | PENDING |
| AC-04 | Deterministic GREEN on a COLD-model container; no INFRA flap from model warmup before load-bearing writes. (CRITICAL — R-01) | test | Proven by AC-11: the cold-model dispatch run completes GREEN with zero warmup-attributable INFRA flap (NFR-1). Barrier PRESENT must be load-bearing (real durable write, not liveness-only). | PENDING |
| AC-05 | Barrier remains compatible with the off-Docker `SMOKE_*_CMD` stub seam. | test | Run the pre-merge gate-logic stub test post-barrier; full verdict truth-table (exit 0/1/2/3) still drives without Docker. | PENDING |
| AC-06 | New `release.yml` job runs the smoke on every `push: tags:['v*']` and `workflow_dispatch`; independent job status. | file-check + manual | Inspect `release.yml`: new job present with trigger inheritance and its own independent job id/status. | PENDING |
| AC-07 | Job runs against pushed per-arch GHCR bytes via `resolve_image` (push→`:v<ver>-<arch>` UN-stripped; dispatch→`:latest-<arch>`), `IMAGE` exported; no local rebuild. | grep + manual | YAML review: job calls `resolve_image`, exports `IMAGE`, no docker build step; assert NO `${GITHUB_REF_NAME#v}` anywhere in the lane. | PENDING |
| AC-08 | Tri-state discriminated in `release-gate-lib.sh` (D-1): GREEN passes, RED fails, INFRA non-failing-but-VISIBLE (`::warning::` + greppable marker), SKIP-on-Docker hard-fails; no non-GREEN rounded to pass. (CRITICAL — R-05) | test | Stub-seam truth-table through the REAL sourced `release-gate-lib.sh`: assert exit→outcome for 0+marker/0-no-marker/1/2/3/other; grep job output for the canonical INFRA marker + `::warning::` on the exit-2 cell; assert no-pipe + return-not-exit capture shape. | PENDING |
| AC-09 | GREEN credited only on `[infra003-smoke] ALL GATES PASSED` (anchored `-qxE` full-line, runtime line). | test | Stub: exit 0 WITHOUT marker → NOT credited (return 1); WITH runtime-prefixed marker → credited; substring-inside-longer-line → NOT credited. | PENDING |
| AC-10 | Lane provisions `node` AND `sqlite3` (#849, self-contained); absence of either → INFRA, not empty-pass. | grep + test | YAML review of setup step (node + `apt-get install sqlite3`); stub/preflight: missing `sqlite3` → exit 2 (INFRA), not 0. | PENDING |
| AC-11 | Cold-model fresh-build GREEN demonstrated in-feature via `workflow_dispatch` on the feature branch (D-2) before the flip; real first-boot HF download path; evidence recorded. (CRITICAL — R-13) | manual | Dispatch workflow run URL + log: confirm GREEN verdict; confirm cold-download path (HF download log lines, not warm cache / not `:783-smoke`); confirm branch rebased on `main` HEAD (record branch SHA == `main` HEAD, SR-06). Recorded in feature folder. | PENDING |
| AC-12 | Isolation lane IS in `create-container-manifest.needs:`; RED fails the manifest (does not assemble). (CRITICAL — R-05/R-08) | test + grep | `needs:`-graph assertion: lane id ∈ `create-container-manifest.needs`; stub-seam forcing a RED cell → assert the lane job returns 1 (gates the `needs:` edge). | PENDING |
| AC-13 | INFRA (exit 2) does NOT fail the manifest, and emits visible `::warning::` + greppable marker (N4 preserved). | test | Stub-seam forcing INFRA → assert lane returns success (non-blocking) AND log contains `::warning::` + canonical INFRA marker `[infra004-gate] INFRA — ISOLATION NOT VERIFIED THIS RUN`. | PENDING |
| AC-14 | Delivery sets N3 (#5161) `status: proven`, `proven_by =` blocking gate + AC-11 run; note rewritten (last-blocker-closed; C5/#5190 caveat resolved); proven as-of observe + MCP-write surfaces. | manual | Inspect N3 entry post-merge via `context_correct`: `status` literal `proven`; `proven_by` references the gate + AC-11 run; FULL note rewritten (not just status flip); surface-boundary text present. | PENDING |
| AC-15 | No `crates/` change; only gate-script edit is the warmup barrier; changes confined to the three named files. | shell | `git diff --stat`: exactly `multi-tenant-isolation-smoke.sh`, `.github/workflows/release.yml`, `release-gate-lib.sh` (plus feature docs); zero `crates/` paths; the smoke diff contains only the barrier. | PENDING |
