# infra-004 Acceptance Criteria Map

> Every AC from SCOPE.md / SPECIFICATION.md (AC-01..AC-15) appears below with a
> concrete verification method. Status PENDING until proven in Session 2.
> Critical Gate 3c blockers: AC-01/AC-04 (R-01) and AC-08/AC-12/AC-13 (R-05).

| AC-ID | Description | Verification Method | Verification Detail | Status |
|-------|-------------|---------------------|---------------------|--------|
| AC-01 | Bounded warmup barrier runs before C3/C4 load-bearing writes; confirms model loaded + both per-slug stores live; bound derived from #767, not guessed. | grep + manual | `git diff` of `multi-tenant-isolation-smoke.sh` shows the barrier between `assert_routes_live` and `run_isolation_matrix`; `WARMUP_DEADLINE_SECS` default 180 documented as a #767 (`READY_TIMEOUT_SECS`) derivation with headroom (now corroborated by AC-11 measurement: cold-ready ~5s). Warmup probe is the MCP `context_store` round trip. | **PASS** |
| AC-02 | Barrier reuses existing infra-001 idioms (`store_size`/`wait_for_http_active`/`write_then_barrier` deadline-poll) — no new readiness mechanism. | grep | `git diff` shows only calls to existing helpers; no new readiness primitive defined (reuses `write_then_barrier`). | **PASS** |
| AC-03 | Genuine not-ready past deadline → INFRA (exit 2), never RED/GREEN; barrier never converts not-ready to false pass. | test | Stub-seam truth-table: force barrier timeout → assert exit 2 (INFRA), assert not 0 and not 1 (`test_warmup_timeout_is_infra_not_pass`). | **PASS** |
| AC-04 | Deterministic GREEN on a COLD-model container; no INFRA flap from model warmup before load-bearing writes. (CRITICAL — R-01) | test | Proven by AC-11: cold-model dispatch run 28298217877 completes GREEN with zero warmup-attributable INFRA flap (NFR-1); warmup PRESENT via the load-bearing MCP `context_store` round trip. | **PASS** |
| AC-05 | Barrier remains compatible with the off-Docker `SMOKE_*_CMD` stub seam. | test | Run the pre-merge gate-logic stub test post-barrier; full verdict truth-table (exit 0/1/2/3) still drives without Docker (`test_post_barrier_{green,red,infra}_still_drives`). | **PASS** |
| AC-06 | New `release.yml` job runs the smoke on every `push: tags:['v*']` and `workflow_dispatch`; independent job status. | file-check + manual | Inspect `release.yml`: new job present with trigger inheritance and its own independent job id/status (`test_lane_job_exists`, `test_workflow_triggers_tags_and_dispatch`). | **PASS** |
| AC-07 | Job runs against pushed per-arch GHCR bytes via `resolve_image` (push→`:v<ver>-<arch>` UN-stripped; dispatch→`:latest-<arch>`), `IMAGE` exported; no local rebuild. | grep + manual | YAML review: job calls `resolve_image`, exports `IMAGE`, no docker build step; assert NO `${GITHUB_REF_NAME#v}` anywhere in the lane (`test_lane_calls_resolve_image`, `test_lane_no_ref_strip`, `test_lane_no_docker_build`). | **PASS** |
| AC-08 | Tri-state discriminated in `release-gate-lib.sh` (D-1): GREEN passes, RED fails, INFRA non-failing-but-VISIBLE (`::warning::` + greppable marker), SKIP-on-Docker hard-fails; no non-GREEN rounded to pass. (CRITICAL — R-05) | test | Stub-seam truth-table through the REAL sourced `release-gate-lib.sh`: assert exit→outcome for 0+marker/0-no-marker/1/2/3/other; grep job output for the canonical INFRA marker + `::warning::` on the exit-2 cell; assert no-pipe + return-not-exit capture shape (tristate truth table 19/19; CRITICAL R-05). | **PASS** |
| AC-09 | GREEN credited only on `[infra003-smoke] ALL GATES PASSED` (anchored `-qxE` full-line, runtime line). | test | Stub: exit 0 WITHOUT marker → NOT credited (return 1); WITH runtime-prefixed marker → credited; substring-inside-longer-line → NOT credited (`test_tristate_marker_anchored_substring`, `..._byte_identical`). | **PASS** |
| AC-10 | Lane provisions `node` AND `sqlite3` (#849, self-contained); absence of either → INFRA, not empty-pass. | grep + test | YAML review of setup step (node + `apt-get install sqlite3`); stub/preflight: missing `sqlite3` → exit 2 (INFRA), not 0 (`test_lane_provisions_node`, `test_lane_provisions_sqlite3`). | **PASS** |
| AC-11 | Cold-model fresh-build GREEN demonstrated in-feature via `workflow_dispatch` on the feature branch (D-2) before the flip; real first-boot HF download path; evidence recorded. (CRITICAL — R-13) | manual | Run https://github.com/dug-21/unimatrix/actions/runs/28298217877 (`feature/infra-004`), job `multi-tenant-isolation-amd64` = success; `[infra003-smoke] ALL GATES PASSED — bidirectional 2x2 isolation holds on both surfaces`; cold first-boot path; warmup via MCP `context_store` round trip PRESENT; observe GREEN + mcp GREEN; zero INFRA / zero `::warning::`; marker `infra003-warmup-1-22axthb07f` (both-filter-safe). | **PASS** |
| AC-12 | Isolation lane IS in `create-container-manifest.needs:`; RED fails the manifest (does not assemble). (CRITICAL — R-05/R-08) | test + grep | `needs:`-graph assertion: lane id ∈ `create-container-manifest.needs`; stub-seam forcing a RED cell → assert the lane job returns 1 (gates the `needs:` edge). Proven-by-stub-seam (`test_lane_in_manifest_needs` + forced-RED tristate cell → return 1); the blocking edge's tag-push leg is the budgeted post-merge tag round (non-blocking-on-INFRA, safe). | **PASS** |
| AC-13 | INFRA (exit 2) does NOT fail the manifest, and emits visible `::warning::` + greppable marker (N4 preserved). | test | Stub-seam forcing INFRA → assert lane returns success (non-blocking) AND log contains `::warning::` + canonical INFRA marker `[infra004-gate] INFRA — ISOLATION NOT VERIFIED THIS RUN`. Proven-by-stub-seam (`test_tristate_infra_exit2_nonblocking_visible`, `..._canonical_marker_pinned`). | **PASS** |
| AC-14 | Delivery sets N3 (#5161) `status: proven`, `proven_by =` blocking gate + AC-11 run; note rewritten (last-blocker-closed; C5/#5190 caveat resolved); proven as-of observe + MCP-write surfaces. | manual | Inspect N3 entry post-merge via `context_correct`: `status` literal `proven`; `proven_by` references the gate + AC-11 run; FULL note rewritten (not just status flip); surface-boundary text present. AC-11 GREEN evidence (run 28298217877) + the blocking gate now available; final flip gated on the human's chronic-INFRA VARIANCE decision (OQ-3 / R-15). | PENDING (human gate) |
| AC-15 | No `crates/` change; only gate-script edit is the warmup barrier; changes confined to the three named files. | shell | `git diff --stat`: exactly `multi-tenant-isolation-smoke.sh`, `.github/workflows/release.yml`, `release-gate-lib.sh` (plus feature docs); zero `crates/` paths; the smoke diff is warmup-barrier + #859 marker-contract scoped. **Amended:** no `crates/` PRODUCTION change; the sole `crates/` delta is one test-only scanner anchor (`#[cfg(test)]` in `scanning.rs`). | **PASS (amended)** |

## Final Tally (AC-01..AC-15): 14 PASS, 1 deferred (human gate)

- **PASS (14):** AC-01, AC-02, AC-03, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10,
  AC-12, AC-13, AC-15 (12 pre-merge: stub-seam logic + static YAML/`needs:`-graph)
  plus AC-11 and AC-04 (cold-model dispatch run **28298217877** GREEN, zero INFRA).
- **Deferred to human (1):** AC-14 (N3 #5161 `proven`) — GREEN evidence + blocking
  gate now available; final flip gated on the human's chronic-INFRA VARIANCE
  decision (OQ-3 / R-15).
- **Post-merge (not an AC gap):** R-10 `:v<ver>-amd64` tag-push leg — budgeted one
  post-merge tag round (C-10), non-blocking-on-INFRA (safe).
