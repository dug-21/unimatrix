# Retrieval-shape manifest stamp for the primary fixture corpus (nan-018, ADR-002 #4895).
#
# manifest_version : bumped ONLY when the HASH INPUT SET changes (a declared
#                    column / edge type / confidence dim / embedding field). NOT
#                    per-corpus. Distinct from migration_number.
# migration_number : human legibility ONLY — NOT a hash input.
# shape_hash       : SHA-256 lowercase hex (64 chars) of the ordered, versioned,
#                    enumerated retrieval-shape manifest computed by the Wave-2
#                    shape module (compute_shape_hash) over the LIVE schema this
#                    corpus targets. The drift guard recomputes the running hash
#                    and HARD-ERRORS on mismatch for this primary corpus.
manifest_version = 2
migration_number = 47
shape_hash = "3ec3fa759436b818c464b09d061e18474a796e2c2299dda440ec18cac8e0443d"
